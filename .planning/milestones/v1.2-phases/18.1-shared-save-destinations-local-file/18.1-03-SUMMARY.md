---
phase: 18.1-shared-save-destinations-local-file
plan: 03
subsystem: ui
tags: [settings, swiftui, form, sections, destinations]

requires:
  - phase: 18.1
    provides: AppSettings v1.2 destination keys (Plan 18.1-01) consumed by the new section bindings

provides:
  - SettingsView Form with three top-level destination sections (Local File / Obsidian / Notion)
  - Behavior-only Dictation section (hotkey + behavior + restore delay), no destination configuration
  - Single-folder Obsidian section (replaces v1.0 two-folder shape)
  - Source-grep test suite SettingsView18_1Tests covering 11 structural assertions

affects:
  - 18.1-04-dictation-coordinator  # No direct UI dependency; coordinator uses AppSettings keys directly
  - 18.1-05-content-view  # ContentView meeting/memo flow likewise reads AppSettings keys directly

tech-stack:
  added: []
  patterns:
    - "Three-destination shared sections at the top of Settings (Local File / Obsidian / Notion); features below them never own a folder picker"

key-files:
  modified:
    - PSTranscribe/Sources/PSTranscribe/Views/SettingsView.swift  # full restructure
    - PSTranscribe/Tests/PSTranscribeTests/Phase18/SettingsViewDictationSectionTests.swift  # pruned 2 tests
  created:
    - PSTranscribe/Tests/PSTranscribeTests/Phase18.1/SettingsView18_1Tests.swift

key-decisions:
  - "Local File section is inserted directly after Audio Input (top of destinations group) because it's the default-on destination"
  - "Obsidian section keeps its vault-detection green/orange/gray status dot, but it now reads a single obsidianFolderPath instead of OR-ing two paths"
  - "Dictation section helper text updated to point users to Local File / Obsidian / Notion sections for storage configuration -- closes the discoverability loop"

patterns-established:
  - "Settings UI structure: shared destination sections own folder pickers; feature sections are behavior-only"

requirements-completed: [FOLDER-01, FOLDER-05]

duration: ~10min
completed: 2026-04-29
---

# Plan 18.1-03 Summary

**SettingsView restructured into three shared destination sections (Local File / Obsidian / Notion) with a behavior-only Dictation section -- closes the architectural rule that destinations are top-level and features fan out into them.**

## Performance

- **Tasks:** 2
- **Files modified:** 2
- **Files created:** 1
- **Lines:** -94 / +83 in SettingsView (8 sections vs 7 before, but the Obsidian section collapsed from 100+ lines to a single folder block)
- **Tests added:** 11 in SettingsView18_1Tests; **tests pruned:** 2 in SettingsViewDictationSectionTests

## Accomplishments

- Section order is now Audio Input -> Local File -> Obsidian -> Notion -> Privacy -> Updates -> Speech Model -> Dictation; all eight sections present in the correct sequence.
- Local File section UI: enable Toggle, root folder picker, helper text describing the three hardcoded subfolders.
- Obsidian section UI: enable Toggle, single folder picker, vault-detection status dot, "Remove" affordance when a folder is set.
- Dictation section UI is behavior-only: hotkey recorder, hotkey mode picker, clipboard restore Stepper. Output mode picker and folder picker removed.
- 27 SettingsView build errors from the 18.1-01 baseline are now resolved.

## Task Commits

1. **Task 1: SettingsView restructure** -- `13adc61` (feat)
2. **Task 2: SettingsView18_1Tests + prune legacy** -- `b0b5d00` (test)

**Plan metadata:** this file (docs)

## Decisions Made

- **Followed plan as specified.** Section ordering, Local File / Obsidian section bodies, and Dictation section simplification all match the plan code blocks exactly.

## Deviations from Plan

None.

## Issues Encountered

- Build still fails in `ContentView.swift` (11 errors) and `App/DictationCoordinator.swift` (3 errors) -- expected; those are work for plans 18.1-04 and 18.1-05.
- SourceKit reports "No such module 'Testing'" for the new test file. This is a stale-index artifact in the IDE, not a real build error -- the project is on Swift 6.2 and uses Swift Testing throughout.

## Next Phase Readiness

- After plans 04 and 05 finish, the build will compile and the 11 SettingsView18_1Tests should run green alongside the 17 writer/fan-out tests added in 18.1-02.
- Plan 06 (Wave 3) can then run the full test suite migration.

---
*Plan: 18.1-03 -- SettingsView destination sections*
*Completed: 2026-04-29*
