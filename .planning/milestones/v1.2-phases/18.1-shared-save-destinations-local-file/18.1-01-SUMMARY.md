---
phase: 18.1-shared-save-destinations-local-file
plan: "01"
subsystem: settings
tags: [swift, appsettings, userdefaults, macos]

requires:
  - phase: 16-foundation
    provides: "AppSettings class with v1.2 dictation keys, DictationOutputMode enum, DictationHotkeyMode enum"

provides:
  - "Four new shared destination keys on AppSettings: localFileEnabled, localFileRoot, obsidianEnabled, obsidianFolderPath"
  - "DictationOutputMode enum removed from Models.swift"
  - "Four legacy keys removed: vaultMeetingsPath, vaultVoicePath, dictationOutputMode, dictationFolderPath"
  - "vaultMeetingsURL, vaultVoiceURL computed properties removed"
  - "AppSettingsDestinationPersistenceTests: 8 round-trip + default tests for new keys"

affects:
  - "18.1-02 (SaveDestinations + writers read these new keys)"
  - "18.1-03 (SettingsView replaces vault section with new destination sections)"
  - "18.1-04 (ContentView routing replaces vaultMeetingsPath/vaultVoicePath)"
  - "18.1-05 (DictationCoordinator replaces dictationOutputMode/dictationFolderPath)"

tech-stack:
  added: []
  patterns:
    - "Default-true Bool init pattern: object(forKey:) == nil branch sets true (matches existing hideFromScreenShare pattern)"
    - "Default-false Bool init pattern: object(forKey:) == nil branch sets false (consistent)"
    - "NSString tilde expansion at init time for path defaults (matches existing vaultMeetingsPath/dictationFolderPath pattern)"

key-files:
  created:
    - PSTranscribe/Tests/PSTranscribeTests/Phase18.1/AppSettingsDestinationPersistenceTests.swift
  modified:
    - PSTranscribe/Sources/PSTranscribe/Models/Models.swift
    - PSTranscribe/Sources/PSTranscribe/Settings/AppSettings.swift
  deleted:
    - PSTranscribe/Tests/PSTranscribeTests/Phase18/AppSettingsDictationPersistenceTests.swift

key-decisions:
  - "No migration: rip-and-replace; app not yet released to users (D-17). Stale keys orphaned in developer UserDefaults; no end-user data loss."
  - "localFileRoot default is ~/Documents/PSTranscribe (parent); Meeting/, Memo/, Dictation/ subfolders created lazily on first write by Plan 02 writers."
  - "obsidianFolderPath replaces both vaultMeetingsPath + vaultVoicePath with a single folder; content-type distinction moves to YAML session-type frontmatter."
  - "dictationHotkeyMode regression coverage NOT added to new suite -- AppSettingsTests.swift lines 42 and 99 already cover it."

requirements-completed: [FOLDER-05, DICT-05]

duration: 25min
completed: "2026-04-29"
---

# Phase 18.1 Plan 01: AppSettings Destination Key Rip-and-Replace Summary

**Four shared destination keys (localFileEnabled, localFileRoot, obsidianEnabled, obsidianFolderPath) replace four legacy per-feature keys and DictationOutputMode enum -- foundation for Plans 02-06**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-04-29
- **Completed:** 2026-04-29
- **Tasks:** 2 (combined TDD: RED + GREEN for both tasks in one commit)
- **Files modified:** 4 (2 source, 1 test created, 1 test deleted)

## Accomplishments

- Deleted `DictationOutputMode` enum from `Models.swift`; `SessionType.dictation` and `DictationHotkeyMode` preserved intact
- Removed `vaultMeetingsPath`, `vaultVoicePath`, `dictationOutputMode`, `dictationFolderPath` stored properties and their `init()` reads from `AppSettings`
- Removed `vaultMeetingsURL`, `vaultVoiceURL` computed properties
- Added `localFileEnabled` (default true, D-06), `localFileRoot` (default `~/Documents/PSTranscribe` expanded, D-06), `obsidianEnabled` (default false, D-10), `obsidianFolderPath` (default "", D-10) with correct `didSet` plumbing
- Created `AppSettingsDestinationPersistenceTests` with 8 round-trip and default-value tests
- Deleted `AppSettingsDictationPersistenceTests` (tested now-deleted keys; `dictationHotkeyMode` already covered in AppSettingsTests.swift)

## Task Commits

1. **Task 1 + Task 2: Rip-and-replace AppSettings + tests** - `289d7a5` (feat)

## Files Created/Modified

- `PSTranscribe/Sources/PSTranscribe/Models/Models.swift` -- DictationOutputMode enum deleted; DictationHotkeyMode and SessionType.dictation intact
- `PSTranscribe/Sources/PSTranscribe/Settings/AppSettings.swift` -- 4 legacy keys out, 4 new destination keys in with init() defaults
- `PSTranscribe/Tests/PSTranscribeTests/Phase18.1/AppSettingsDestinationPersistenceTests.swift` -- 8 tests created (new)
- `PSTranscribe/Tests/PSTranscribeTests/Phase18/AppSettingsDictationPersistenceTests.swift` -- deleted (tested deleted keys)

## Decisions Made

- No migration code: pre-release rip-and-replace per D-17. Documented in AppSettings.init() comment.
- `localFileRoot` default is the parent `~/Documents/PSTranscribe`; subfolders created lazily by writers (Plan 02).
- `obsidianFolderPath` is a single folder (not per-content-type). Session type tagging via YAML frontmatter replaces folder separation.
- Kept doc comment in `obsidianFolderPath` that references old `vaultMeetingsPath`/`vaultVoicePath` to explain the consolidation -- this is intentional, not a leaked key reference.

## Deviations from Plan

**1. [Rule 3 - Blocking] `swift test --filter AppSettingsDestinationPersistenceTests` deferred**

- **Found during:** Task 2 verification
- **Issue:** Swift Package Manager requires the full source module to compile before running any test. With 41 errors in SettingsView.swift (27), ContentView.swift (11), and DictationCoordinator.swift (3) -- all referencing deleted properties -- the test target cannot link.
- **Fix:** Documented error counts (see below). Tests are structurally correct and will pass once Plans 03/04/05 repair the consumers. The plan explicitly anticipated this: "Project does NOT yet build (consumers still reference deleted properties)."
- **Files modified:** None -- per plan Step D, fixing consumers is out of scope here.
- **Impact:** Deferred verification; not a defect in this plan's implementation.

---

**Total deviations:** 1 (test execution deferred per plan's own success criterion)
**Impact on plan:** No scope creep. The plan's success criteria explicitly state the project does not yet build.

## Compile Errors Introduced (Work Items for Plans 03/04/05)

These are the downstream consumer errors that Plans 03/04/05 must resolve:

| File | Error count | Root cause |
|------|------------|------------|
| `Views/SettingsView.swift` | 27 | References to `vaultMeetingsPath`, `vaultVoicePath`, `dictationOutputMode` |
| `Views/ContentView.swift` | 11 | References to `vaultMeetingsPath`, `vaultVoicePath` |
| `App/DictationCoordinator.swift` | 3 | References to `dictationOutputMode`, `dictationFolderPath` |
| **Total** | **41** | All "has no member" / "cannot find in scope" errors |

Error categories (from `swift build`):
- `value of type 'AppSettings' has no dynamic member 'vaultMeetingsPath'` -- 8 occurrences
- `value of type 'AppSettings' has no dynamic member 'vaultVoicePath'` -- 5 occurrences
- `referencing subscript 'subscript(dynamicMember:)' requires wrapper 'Bindable<AppSettings>'` -- 13 occurrences (cascade from above)
- `cannot find 'DictationOutputMode' in scope` -- 3 occurrences
- `value of type 'AppSettings' has no member 'dictationOutputMode'` -- 2 occurrences
- `value of type 'AppSettings' has no member 'dictationFolderPath'` -- 1 occurrence
- Type-check cascade errors -- 9 occurrences

## Issues Encountered

None -- the cascade of compile errors is expected behavior documented in the plan.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- AppSettings shape is now correct for Plans 02-06 to build on
- Plan 02 (SaveDestinations + LocalFileWriter/ObsidianWriter) can read `localFileEnabled`, `localFileRoot`, `obsidianEnabled`, `obsidianFolderPath`
- Plan 03 (SettingsView) must replace Obsidian vault section with two new sections; consumers of deleted keys are known (see error table above)
- Plan 04 (ContentView) must replace `vaultMeetingsPath`/`vaultVoicePath` routing with new destination fan-out
- Plan 05 (DictationCoordinator) must replace `dictationOutputMode`/`dictationFolderPath` branches with new destination API
- `swift test --filter AppSettingsDestinationPersistenceTests` will be a valid gate check once Plans 03/04/05 restore compile

---
*Phase: 18.1-shared-save-destinations-local-file*
*Completed: 2026-04-29*
