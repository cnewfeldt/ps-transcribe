---
phase: 20-dark-mode-parity
plan: 03
type: execute
wave: 3
depends_on: [20-02]
files_modified:
  - PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift
  - PSTranscribe/Sources/PSTranscribe/Views/NotionTagSheet.swift
  - .planning/phases/20-dark-mode-parity/20-VERIFICATION.md
autonomous: false
requirements: [REQ-20.1, REQ-20.4, REQ-20.5, REQ-20.6]
requirements_addressed: [REQ-20.1, REQ-20.4, REQ-20.5, REQ-20.6]

must_haves:
  truths:
    - ".preferredColorScheme(.light) at ContentView.swift:251 deleted"
    - ".preferredColorScheme(.dark) at NotionTagSheet.swift:130 deleted"
    - "grep -rn preferredColorScheme PSTranscribe/Sources returns zero hits outside #Preview blocks"
    - "swift build clean, zero new warnings"
    - "Manual UAT confirms System Settings > Appearance Light->Dark->Light toggle re-renders all 10 surfaces without app restart"
    - "Manual UAT confirms Auto / sunset transition propagates appearance correctly — VERIFICATION.md contains either 'Auto-transition observed at [timestamp]' or 'Auto-transition scripted via [command]' string"
    - "DictationHUD partial-text legible against .hudWindow vibrancy in both modes"
    - "Light-mode pixel-stable vs Wave-2 (= baseline) post-override-removal"
    - "Any discovered deviations documented in 20-VERIFICATION.md and resolved"
  artifacts:
    - path: ".planning/phases/20-dark-mode-parity/screenshots/wave-3/"
      provides: "Final light AND dark screenshots for every surface"
    - path: ".planning/phases/20-dark-mode-parity/20-VERIFICATION.md"
      provides: "UAT log + any deviation tickets + Auto-transition evidence (timestamp or scripted-command record)"
  key_links:
    - from: "ContentView root view"
      to: "system colorScheme environment"
      via: "no .preferredColorScheme override; SwiftUI auto-observes"
      pattern: "@Environment\\(\\\\.colorScheme\\)"
    - from: "NotionTagSheet root"
      to: "system colorScheme"
      via: "no override"
      pattern: "preferredColorScheme"  # negative pattern — should NOT match
---

<objective>
Wave 3 of the 3-wave dark-mode-parity migration. The moment dark mode goes live. Delete the two `.preferredColorScheme` overrides, run the full UAT script per D-10 (Light → Dark → Light → Auto/sunset toggle re-render of every surface), capture light + dark screenshots for every surface, document any deviation in `20-VERIFICATION.md`, and resolve discovered drift.

Purpose: After Waves 1 and 2 built the unified token system without changing visible behavior (overrides still pinned), Wave 3 removes the pins and verifies the full adaptive palette behaves correctly under live system-appearance changes. Any remaining issues — frozen light backgrounds on dark, missing tokens, illegible vibrancy text — surface here and get resolved.

Output: Both `.preferredColorScheme` calls deleted; verified UAT log in `20-VERIFICATION.md` with concrete evidence of Auto-transition propagation; full set of light + dark screenshots in `screenshots/wave-3/`; any deviation discovered during UAT documented and fixed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-dark-mode-parity/20-CONTEXT.md
@.planning/phases/20-dark-mode-parity/20-SPEC.md
@.planning/phases/20-dark-mode-parity/20-01-SUMMARY.md
@.planning/phases/20-dark-mode-parity/20-02-SUMMARY.md
@PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift
@PSTranscribe/Sources/PSTranscribe/Views/NotionTagSheet.swift
</context>

<truths>
**Locked decisions (D-10 UAT script — executor MUST follow exact order):**

1. Launch app while macOS is in **Light** appearance.
2. Open every surface in this order: main window (ContentView with library + transcript + details), capture dock (idle + recording), ControlBar, SettingsView (every tab), NotionTagSheet (trigger via Notion send flow), OnboardingView (force first-run reset), DictationHUD (trigger via dictation hotkey).
3. Open System Settings > Appearance and switch to **Dark**. Verify each surface re-renders correctly without app restart. No frozen light backgrounds, no clashing dark sheets, no missing tokens.
4. Switch back to **Light**. Verify all surfaces revert and remain pixel-stable vs. the Wave-2 light screenshots.
5. Switch to **Auto** appearance and **observe a real Light↔Dark mid-day transition propagate to the running app** — either by waiting for natural sunrise/sunset OR by scripting the macOS appearance flip while the app is running. Toggling Auto and only verifying its initial mode pick is INSUFFICIENT — the gate requires evidence of an actual mid-session flip.
6. Document evidence of the Auto-transition in `20-VERIFICATION.md` (exact format below) and document any surface that fails as a deviation, then re-run the loop.

**Override removal — exact lines:**

- `PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift:251` — delete the line `.preferredColorScheme(.light)`. Read 5 lines above and below to confirm it is on its own line as a SwiftUI modifier (chain attached to a view body). If indentation/chaining context demands the preceding modifier's `.` chain merge with the next modifier, do that cleanly so swift build stays valid.
- `PSTranscribe/Sources/PSTranscribe/Views/NotionTagSheet.swift:130` — delete the line `.preferredColorScheme(.dark)`. Same chaining note.

**#Preview blocks are out-of-scope per SPEC.md constraints:**
- Any `.preferredColorScheme(.light)` or `.preferredColorScheme(.dark)` calls inside `#Preview { ... }` blocks remain. They are dev-tool-only.
- Verification grep MUST exclude `#Preview` blocks. Use multi-line awareness: `grep -rn "preferredColorScheme(" PSTranscribe/Sources` returns matches; THEN executor manually inspects each match's line context to confirm it is inside a `#Preview { ... }` brace pair. If any match is outside #Preview, gate fails.

**Reject any output that:**
- Re-introduces a `.preferredColorScheme` override on a non-preview view (defeats the entire phase)
- Modifies any token def in DesignTokens.swift (Wave 1 work — locked at this stage; deviations get a *new token* added if needed, not modifications to existing)
- Skips any of the 10 surfaces during UAT
- Skips the Auto/sunset transition step (D-10 step 5 — explicitly part of the UAT contract). **Verifying only Auto's initial mode pick does NOT satisfy step 5.** Evidence of an actual mid-session flip is required.
- Marks UAT complete without screenshots in `screenshots/wave-3/`
- Marks UAT complete without an "Auto-transition observed at [timestamp]" OR "Auto-transition scripted via [command]" string in 20-VERIFICATION.md
</truths>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete the two .preferredColorScheme overrides</name>
  <files>
    PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift,
    PSTranscribe/Sources/PSTranscribe/Views/NotionTagSheet.swift
  </files>
  <read_first>
    - PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift lines 245-260 (modifier chain context around line 251)
    - PSTranscribe/Sources/PSTranscribe/Views/NotionTagSheet.swift lines 125-135 (modifier chain context around line 130)
    - 20-SPEC.md req 1 (acceptance: grep returns zero hits outside #Preview blocks)
  </read_first>
  <action>
    **Part A — ContentView.swift:251**

    Open `PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift`. Locate line 251 containing `.preferredColorScheme(.light)`. Delete the entire line.

    Verify the surrounding modifier chain stays syntactically valid:
    - If line 250 ends with another modifier (e.g. `.someOtherModifier()`) and line 252 is the next modifier or the closing brace — deletion is clean.
    - If line 251 was the last modifier in a chain and removing it leaves a trailing `.` on line 250 — also remove the trailing `.`.
    - Inspect carefully and adjust whitespace as needed.

    **Part B — NotionTagSheet.swift:130**

    Open `PSTranscribe/Sources/PSTranscribe/Views/NotionTagSheet.swift`. Locate line 130 containing `.preferredColorScheme(.dark)`. Delete the entire line. Apply same chain-validation logic as Part A.

    **Part C — Verify build:**

    Run `cd PSTranscribe && swift build`. Confirm clean.

    **Part D — Verify grep gate:**

    Run:
    ```bash
    grep -rn "preferredColorScheme(" PSTranscribe/Sources
    ```

    Inspect each match. For every match, confirm by reading the surrounding lines that it is inside a `#Preview { ... }` block. The pattern is:
    ```swift
    #Preview {
        SomeView()
            .preferredColorScheme(.dark)  // ← inside #Preview, OK to remain
    }
    ```

    If ANY match is outside a `#Preview` block, the gate fails — find the offender and delete it.
  </action>
  <verify>
    <automated>grep -rn "preferredColorScheme(" PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift PSTranscribe/Sources/PSTranscribe/Views/NotionTagSheet.swift; cd PSTranscribe && swift build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "preferredColorScheme(" PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift` returns no match (or only matches inside `#Preview {}` — manual inspect)
    - `grep -n "preferredColorScheme(" PSTranscribe/Sources/PSTranscribe/Views/NotionTagSheet.swift` returns no match (or only inside `#Preview {}`)
    - `grep -rn "preferredColorScheme(" PSTranscribe/Sources` — every match (if any) is inside a `#Preview { ... }` block (verified by reading surrounding lines)
    - `cd PSTranscribe && swift build` exits 0 with zero new warnings
  </acceptance_criteria>
  <done>Both forced overrides deleted; build clean; grep gate passes (zero hits outside #Preview).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run full D-10 UAT script + capture wave-3 light AND dark screenshots</name>
  <what-built>
    - `.preferredColorScheme(.light)` deleted from ContentView:251
    - `.preferredColorScheme(.dark)` deleted from NotionTagSheet:130
    - Grep gate passes (zero non-Preview hits)
    - `swift build` clean
    - App now respects system appearance for the first time
  </what-built>
  <how-to-verify>
    **Step 1 — Pre-UAT setup:**

    - Set macOS to **Light** appearance: System Settings > Appearance > Light
    - Build and launch the app: `cd PSTranscribe && swift build && swift run` (or via Xcode)
    - Have these directories ready:
      - `.planning/phases/20-dark-mode-parity/screenshots/wave-3/light/`
      - `.planning/phases/20-dark-mode-parity/screenshots/wave-3/dark/`
      - Create them: `mkdir -p .planning/phases/20-dark-mode-parity/screenshots/wave-3/{light,dark}`

    **Step 2 — Light-mode UAT (per D-10 step 1-2):**

    Open every surface in this order. Capture each as a PNG in `screenshots/wave-3/light/` with the same filenames as the baseline set:

    1. Main window — open the app, ensure library sidebar visible + transcript + details pane → `main-window-idle.png`
    2. Capture dock idle → `capture-dock-idle.png`
    3. Capture dock recording (start a test recording) → `capture-dock-recording.png`
    4. ControlBar (visible somewhere in the main window — capture if not already in main-window-idle)
    5. SettingsView every tab → `settings-{tab-name}.png` for each tab. Specifically include `settings-notion.png` so the warning chip is captured.
    6. NotionTagSheet (trigger via Notion send flow) → `notion-tag-sheet.png`
    7. OnboardingView (force first-run reset: `defaults delete com.psnow.transcribe firstRunCompleted` or equivalent first-run flag — check `OnboardingView.swift` for the actual UserDefaults key and the relaunch instruction; or use a debug menu if one exists) → `onboarding.png`
    8. DictationHUD (trigger via dictation hotkey — read README or `DictationHUD.swift` for the trigger combo) → `dictation-hud.png`

    **Step 3 — Diff against Wave-2 baseline:**

    Compare `screenshots/wave-3/light/` to `screenshots/wave-2/`. Look for ANY perceptible color drift. Expected: zero drift (because light-side hexes are byte-identical to baseline AND the override removal does not change rendering when system is Light).

    Note: NotionTagSheet light-mode appearance NOW DIFFERS from baseline — pre-Wave-3 it was forced .dark; post-Wave-3 it adapts to system Light. This is INTENTIONAL and expected. Compare against baseline expectation: NotionTagSheet light = paper/ink Chronicle palette in light appearance. If the sheet renders correctly in light mode (warm paper bg, ink text, accent for buttons), pass. If it renders broken (frozen hexes, illegible contrast), fail and document.

    **Step 4 — Switch to Dark and re-render every surface (per D-10 step 3):**

    Without quitting the app, switch System Settings > Appearance to **Dark**. Watch every visible surface re-render. Walk through all 10 surfaces again:

    1. Main window → should show `paper.dark` (#1A1818) backgrounds, `ink.dark` (#F0EDE8) cream foreground text, `accentInk.dark` (#C4A0FF) lavender accents → capture as `screenshots/wave-3/dark/main-window-idle.png`
    2. Capture dock idle + recording → `capture-dock-{idle,recording}.png`
    3. ControlBar (re-capture if independent surface)
    4. SettingsView every tab → `settings-{tab-name}.png` for each. Specifically verify the warning chip in `settings-notion.png` shows `warningTint.dark` (#3D2E1A@0.6 warm amber tint, NOT orange@0.15).
    5. NotionTagSheet → `notion-tag-sheet.png`. Should show warm-dark paper, cream ink, lavender accent.
    6. OnboardingView → `onboarding.png`. Same Chronicle dark palette.
    7. DictationHUD → `dictation-hud.png`. Background uses `.hudWindow` system vibrancy (auto-adapts); partial-text foreground is `Color.ink` (auto-adapts to cream in dark) — should be legible against dark vibrancy.

    **Verify NO surface shows:**
    - Frozen light backgrounds (e.g. white paper while system is dark — would indicate a missed hex literal)
    - Clashing dark sheets (e.g. a dark sheet floating over a still-light parent — would indicate the override removal didn't propagate)
    - Missing tokens (e.g. text invisible because foreground hex didn't flip — token def issue)
    - Slate-blue or pure-black backgrounds (would indicate dark hex was wrong; D-04 strictly mandates `#1A1818` family)

    **Step 5 — Switch back to Light (per D-10 step 4):**

    Switch System Settings > Appearance back to **Light**. Walk through all surfaces. Verify they revert to light-mode rendering. Spot-check that the wave-3/light screenshots taken in Step 2 still match the current rendering (no drift introduced by the dark→light flip).

    **Step 6 — Auto / sunset transition test (per D-10 step 5) — TIGHTENED ACCEPTANCE:**

    Switch System Settings > Appearance to **Auto**. The gate requires evidence of an actual mid-session Light↔Dark flip propagating to the running app. Toggling to Auto and verifying only the initial mode pick does **NOT** satisfy this step.

    Choose one of two acceptable paths:

    **Path A — Observe a real sunrise/sunset transition:**
    - Leave the app running through a natural sunrise or sunset on macOS Auto.
    - When the system flips, observe the app re-render every surface without restart.
    - Note the wall-clock timestamp of the observed flip.

    **Path B — Script the macOS appearance flip while the app is running:**
    - With the app running and System Settings > Appearance set to Auto, run the following from a terminal:
      ```bash
      defaults write -g AppleInterfaceStyle Dark; killall -HUP SystemUIServer; sleep 2; \
      defaults delete -g AppleInterfaceStyle; killall -HUP SystemUIServer
      ```
    - **Executor must confirm this exact incantation triggers a live appearance flip on macOS 26.** If the command above does not flip the visible appearance on macOS 26, executor researches the correct macOS-26-compatible command (e.g. AppleScript via `osascript -e 'tell application "System Events" to tell appearance preferences to set dark mode to true'`, or the `swiftc`-built helper Apple recommends, or any equivalent that produces an actual Light→Dark→Light cycle on the running session). Document the actual command used.
    - Observe the app re-render on each flip. The two-flip cycle (Dark on, then Dark off) is the test.

    **Either path is acceptable. The gate fails if neither is performed.**

    **Step 7 — Document deviations AND Auto-transition evidence:**

    Create `.planning/phases/20-dark-mode-parity/20-VERIFICATION.md` (or append to it if it exists). Use this template — the **Auto-Transition Evidence** section is REQUIRED and must contain one of the two specified strings verbatim:

    ```markdown
    # Phase 20 Wave 3 — UAT Verification Log

    **Date:** [YYYY-MM-DD]
    **Tester:** [user]

    ## D-10 UAT Script Results

    | Step | Surface | Light → Dark | Dark → Light | Auto/Sunset | Notes |
    |------|---------|--------------|--------------|-------------|-------|
    | 2    | ContentView main window | PASS / FAIL | PASS / FAIL | PASS / FAIL | ... |
    | 2    | CaptureDock idle | PASS / FAIL | ... | ... | ... |
    | 2    | CaptureDock recording | ... | ... | ... | ... |
    | 2    | ControlBar | ... | ... | ... | ... |
    | 2    | SettingsView (per tab) | ... | ... | ... | ... |
    | 2    | NotionTagSheet | ... | ... | ... | ... |
    | 2    | OnboardingView | ... | ... | ... | ... |
    | 2    | DictationHUD | ... | ... | ... | ... |

    ## Auto-Transition Evidence (D-10 step 5)

    **REQUIRED:** Exactly one of the following two lines must be present, filled in with the actual timestamp or command used:

    - `Auto-transition observed at [YYYY-MM-DD HH:MM:SS local]` — for Path A (natural sunrise/sunset)
    - `Auto-transition scripted via [exact command]` — for Path B (scripted flip)

    Example fill-in for Path A:
    > Auto-transition observed at 2026-04-30 19:34:12 local — sunset triggered system flip; app re-rendered all 10 surfaces without restart.

    Example fill-in for Path B:
    > Auto-transition scripted via `defaults write -g AppleInterfaceStyle Dark; killall -HUP SystemUIServer; sleep 2; defaults delete -g AppleInterfaceStyle; killall -HUP SystemUIServer` — confirmed working on macOS 26; app re-rendered all 10 surfaces on each flip without restart.

    ## Deviations Found

    [None / list each]

    ## Resolution

    [If deviations: describe fix applied, link to follow-up commit; if none: "no deviations, phase 20 complete"]
    ```

    Take a final pass: every row PASS for all 3 columns, the Auto-Transition Evidence section contains one of the two required strings filled in, AND every deviation has a tracked resolution.
  </how-to-verify>
  <resume-signal>
    Type "approved" if every surface in every transition behaves correctly, `20-VERIFICATION.md` is filled out with PASS rows or resolved deviations, AND the Auto-Transition Evidence section contains either "Auto-transition observed at [timestamp]" or "Auto-transition scripted via [command]" with a real value. OR describe each failure with: which surface, which transition (Light→Dark / Dark→Light / Auto), what visible drift, expected vs actual. Executor will diagnose (token def vs missed call-site) and fix before phase closes.
  </resume-signal>
</task>

</tasks>

<verification>
**Phase 20 final gate (must all pass before phase closes):**

1. `grep -rn "preferredColorScheme(" PSTranscribe/Sources` → every match (if any) is inside a `#Preview { ... }` block — manual verify each
2. `grep -n "extension Color" PSTranscribe/Sources/PSTranscribe/Views/TranscriptView.swift` → 0 matches (Wave 1 carry-over verify)
3. `grep -rn "Color(red:" PSTranscribe/Sources/PSTranscribe/Views/` → 0 matches (Wave 2 carry-over verify)
4. `cd PSTranscribe && swift build` → exits 0, zero new warnings
5. `screenshots/wave-3/light/` and `screenshots/wave-3/dark/` populated with full surface set
6. `20-VERIFICATION.md` contains UAT log with all surfaces marked PASS (or deviations resolved)
7. `20-VERIFICATION.md` contains either the literal string `Auto-transition observed at` followed by a timestamp, OR the literal string `Auto-transition scripted via` followed by the command used. Verify with `grep -E "Auto-transition (observed at|scripted via)" .planning/phases/20-dark-mode-parity/20-VERIFICATION.md` → returns ≥1 match.
8. Manual UAT confirms: Light → Dark → Light → Auto/sunset all propagate to all 10 surfaces without app restart
9. NotionTagSheet renders correctly in BOTH light and dark (no longer frozen)
10. SettingsView warning chip uses `warningTint` tokens (light = orange@0.15, dark = #3D2E1A@0.6)
11. DictationHUD partial-text legible against `.hudWindow` vibrancy in both modes
</verification>

<success_criteria>
- [ ] `.preferredColorScheme(.light)` deleted from ContentView.swift:251
- [ ] `.preferredColorScheme(.dark)` deleted from NotionTagSheet.swift:130
- [ ] Grep gate passes: zero non-Preview hits in `PSTranscribe/Sources`
- [ ] `swift build` clean, zero new warnings
- [ ] Wave-3 light screenshots captured for all 10 surfaces
- [ ] Wave-3 dark screenshots captured for all 10 surfaces
- [ ] D-10 UAT script run: Light → Dark → Light → Auto/sunset all confirmed re-rendering correctly
- [ ] All 10 surfaces marked PASS in 20-VERIFICATION.md, OR every deviation has a resolution entry
- [ ] 20-VERIFICATION.md contains either "Auto-transition observed at [timestamp]" OR "Auto-transition scripted via [command]" with a real value (grep check passes)
- [ ] DictationHUD partial-text legibility confirmed in both modes
- [ ] Light-mode pixel-stable vs Wave-2 (= baseline) spot-checked
</success_criteria>

<output>
After completion, create `.planning/phases/20-dark-mode-parity/20-03-SUMMARY.md` documenting:
- Confirmation both `.preferredColorScheme` overrides deleted
- D-10 UAT script results table (copied from 20-VERIFICATION.md)
- Auto-transition evidence path taken (Path A observed timestamp OR Path B scripted command); record the exact line from VERIFICATION.md
- Any deviations found during UAT and how they were resolved (token additions, call-site fixes, follow-up commits)
- Final phase 20 gate status — PASS or list of open issues
- Recommendation for closing phase or rolling forward

After 20-03-SUMMARY.md is written, the phase is ready for `/gsd-uat-phase 20` (if used) and final phase close.
</output>
</content>
</invoke>