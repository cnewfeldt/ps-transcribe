---
phase: 20-dark-mode-parity
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - PSTranscribe/Sources/PSTranscribe/Design/DesignTokens.swift
  - PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift
  - PSTranscribe/Sources/PSTranscribe/Views/LibrarySidebar.swift
  - PSTranscribe/Sources/PSTranscribe/Views/LibraryEntryRow.swift
  - PSTranscribe/Sources/PSTranscribe/Views/TranscriptView.swift
  - PSTranscribe/Sources/PSTranscribe/Views/DetailsPane.swift
  - PSTranscribe/Sources/PSTranscribe/Views/CaptureDock.swift
  - PSTranscribe/Sources/PSTranscribe/Views/ControlBar.swift
  - PSTranscribe/Sources/PSTranscribe/Views/SettingsView.swift
  - PSTranscribe/Sources/PSTranscribe/Views/NotionTagSheet.swift
  - PSTranscribe/Sources/PSTranscribe/Views/OnboardingView.swift
  - PSTranscribe/Sources/PSTranscribe/Views/DictationHUD.swift
autonomous: true
requirements: [REQ-20.4, REQ-20.5, REQ-20.6]
requirements_addressed: [REQ-20.4, REQ-20.5, REQ-20.6]

must_haves:
  truths:
    - "Zero non-token hex Color literals (Color(red:green:blue:)) in any of the 10 audited view files"
    - "Color.orange.opacity(0.15) at SettingsView.swift:527 replaced with adaptive warningTint token"
    - "Color.black.opacity(0.4) overlay at ContentView.swift:268 replaced with Color.overlayDim adaptive token (no .opacity modifier — opacity baked into token def)"
    - "Color.white.opacity(0.06) border at ControlBar.swift:213 replaced with Color.glassRule adaptive token"
    - "Color.red.opacity(0.1) at NotionTagSheet.swift:106 replaced with adaptive errorTint token"
    - "warningTint and warningInk tokens added to DesignTokens.swift via Color(light:dark:)"
    - "errorTint token added to DesignTokens.swift via Color(light:dark:)"
    - "overlayDim token added to DesignTokens.swift: light=Color.black.opacity(0.4), dark=Color.black.opacity(0.5) — preserves dim-modal semantics in both modes (always darkens the backdrop)"
    - "glassRule token added to DesignTokens.swift: light=Color.white.opacity(0.06), dark=Color.white.opacity(0.06) — both sides identical because ControlBar glass surface is warm-dark in both modes (sits on bg1 = #2E2B29)"
    - "Color(red:30/255,green:30/255,blue:28/255).opacity(...) literals at LibrarySidebar:62 and LibraryEntryRow:55 replaced with rule/ruleStrong/listSelection-shadow tokens"
    - "DictationHUD partial-text foreground uses Color.ink (auto-adaptive) — confirmed via grep"
    - "swift build clean with zero new warnings"
    - "Light-mode pixel-stable vs Wave-1 baseline (manual screenshot diff)"
    - ".preferredColorScheme overrides STILL IN PLACE — removal is Wave 3"
  artifacts:
    - path: "PSTranscribe/Sources/PSTranscribe/Design/DesignTokens.swift"
      provides: "warningTint, warningInk, errorTint, overlayDim, glassRule adaptive tokens added"
      contains: "warningTint"
    - path: ".planning/phases/20-dark-mode-parity/screenshots/wave-2/"
      provides: "Post-Wave-2 light-mode screenshots for pixel-stability gate"
  key_links:
    - from: "SettingsView.swift:527 warning chip"
      to: "Color.warningTint"
      via: ".background(Color.warningTint)"
      pattern: "warningTint"
    - from: "ContentView.swift:268 dim overlay"
      to: "Color.overlayDim"
      via: "adaptive dim-modal overlay (opacity baked in)"
      pattern: "overlayDim"
    - from: "ControlBar.swift:213 border"
      to: "Color.glassRule"
      via: ".stroke(Color.glassRule)"
      pattern: "glassRule"
---

<objective>
Wave 2 of the 3-wave dark-mode-parity migration. Walk every consumer view (10 surfaces) and tokenize remaining inline color literals. Add new adaptive tokens (`warningTint`, `warningInk`, `errorTint`, `overlayDim`, `glassRule`) for the warning/error chips, modal dim overlay, and ControlBar glass border. Resolve "Claude's Discretion" items: rewrite legacy `bg0`/`bg1` call-sites to Chronicle equivalents (`paper`/`paperSoft`) where naming clarity wins, otherwise keep legacy tokens (which are now adaptive). Confirm DictationHUD partial-text foreground uses adaptive tokens.

Purpose: Eliminate the last hex literals scattered through view bodies so DesignTokens.swift becomes the single source of truth for the app palette. After Wave 2, every view reads only from the unified palette and the app is ready for `.preferredColorScheme` removal in Wave 3 — which will be the moment dark mode actually goes live.

Output: 10 view files audited and cleaned of inline hex literals, 5 new adaptive tokens (`warningTint`, `warningInk`, `errorTint`, `overlayDim`, `glassRule`), `swift build` clean, post-Wave-2 light-mode screenshots demonstrating zero pixel drift vs Wave-1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-dark-mode-parity/20-CONTEXT.md
@.planning/phases/20-dark-mode-parity/20-SPEC.md
@.planning/phases/20-dark-mode-parity/20-01-SUMMARY.md
@PSTranscribe/Sources/PSTranscribe/Design/DesignTokens.swift
@PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift
@PSTranscribe/Sources/PSTranscribe/Views/LibrarySidebar.swift
@PSTranscribe/Sources/PSTranscribe/Views/LibraryEntryRow.swift
@PSTranscribe/Sources/PSTranscribe/Views/TranscriptView.swift
@PSTranscribe/Sources/PSTranscribe/Views/DetailsPane.swift
@PSTranscribe/Sources/PSTranscribe/Views/CaptureDock.swift
@PSTranscribe/Sources/PSTranscribe/Views/ControlBar.swift
@PSTranscribe/Sources/PSTranscribe/Views/SettingsView.swift
@PSTranscribe/Sources/PSTranscribe/Views/NotionTagSheet.swift
@PSTranscribe/Sources/PSTranscribe/Views/OnboardingView.swift
@PSTranscribe/Sources/PSTranscribe/Views/DictationHUD.swift

<interfaces>
<!-- Pre-known inline literals from grep audit (executor verifies + finds any additional during walk) -->

Known offenders to fix:
- ContentView.swift:268 — `Color.black.opacity(0.4)` dim overlay
- LibrarySidebar.swift:62 — `Color(red: 30/255, green: 30/255, blue: 28/255).opacity(0.05)` background fill
- LibraryEntryRow.swift:55 — `Color(red: 30/255, green: 30/255, blue: 28/255).opacity(0.08)` shadow color
- ControlBar.swift:213 — `Color.white.opacity(0.06)` stroke (active branch already uses `Color.accent1.opacity(0.12)` which is a token call, OK)
- NotionTagSheet.swift:106 — `Color.red.opacity(0.1)` error background
- SettingsView.swift:527 — `Color.orange.opacity(0.15)` warning chip background

These are the only `Color(red:`/`Color.orange`/`Color.black.opacity`/`Color.white.opacity`/`Color.red.opacity` hits found in views via grep. Executor must re-grep at task start to catch any new additions and audit each view for OTHER patterns (e.g. `.foregroundColor(.gray)`, `.foregroundStyle(.secondary)` where token is more semantic).
</interfaces>
</context>

<truths>
**New adaptive tokens to add to DesignTokens.swift in Wave 2 (5 total):**

- `warningTint` (background for warning chips):
  - `light` = `Color.orange.opacity(0.15)` (preserves current SettingsView:527 visible appearance byte-exact)
  - `dark` = `Color(red: 0x3D/255, green: 0x2E/255, blue: 0x1A/255).opacity(0.6)` (#3D2E1A warm amber tint per D-09 recommendation, slightly higher opacity to read against #1A1818 paper)
  - Note: keep light side as `.orange.opacity(0.15)` literal so it renders bit-identically. Do NOT switch the light side to a hex `Color(red:green:blue:)` literal even if it appears equivalent — system `.orange` may shift with future macOS updates and we want bit-stability today.

- `warningInk` (foreground/text on warning chip — currently the chip's surrounding text uses default ink color, but adding for completeness so future warning chip designs stay token-consistent):
  - `light` = `Color.ink` (token-of-token, auto-resolves to current ink light side #1A1A17)
  - `dark` = `Color(red: 0xE8/255, green: 0xC4/255, blue: 0x80/255)` (warm amber-cream for warning legibility against #1A1818, complements warningTint.dark)

- `errorTint` (background for error chips, replaces `Color.red.opacity(0.1)` at NotionTagSheet:106):
  - `light` = `Color.red.opacity(0.1)` (preserves current NotionTagSheet:106 visible appearance byte-exact)
  - `dark` = `Color(red: 0x3D/255, green: 0x1A/255, blue: 0x1A/255).opacity(0.6)` (#3D1A1A warm dark-red tint, harmonizes with paper.dark family)

- `overlayDim` (modal/sheet dim-backdrop, replaces `Color.black.opacity(0.4)` at ContentView:268):
  - `light` = `Color.black.opacity(0.4)` (preserves current ContentView:268 visible appearance byte-exact)
  - `dark` = `Color.black.opacity(0.5)` (slightly higher alpha — dark mode still dims TOWARD darker, not toward cream; this is the corrected semantic vs the prior plan's `Color.ink.opacity(0.4)` proposal which would have inverted in dark mode and lightened the backdrop)
  - **Critical:** opacity is baked into the token def. Call-site uses `Color.overlayDim` directly with NO `.opacity(0.4)` modifier.

- `glassRule` (subtle hairline border on dark glass surfaces, replaces `Color.white.opacity(0.06)` at ControlBar:213):
  - `light` = `Color.white.opacity(0.06)` (preserves original byte-exact)
  - `dark` = `Color.white.opacity(0.06)` (identical — ControlBar glass surface sits on bg1 = #2E2B29 in BOTH modes because legacy bg1 is warm-dark regardless of system appearance per D-04; the original treatment was always light-on-glass)
  - **Critical:** both sides identical white@0.06. Do NOT use `Color.rule` here — `Color.rule` flips white↔black across modes which is wrong for this glass-on-dark surface (mode flip would invert the border color from white-faint to black-faint, which is visible drift).

**D-09 license:** D-09 explicitly permits new tokens for the warning chip. The new `overlayDim` and `glassRule` tokens are analogous additions inside that license — they fix the same class of problem (preserving correct semantics across modes for an inline literal that doesn't map to an existing adaptive token).

**Replacement map (locked):**

| Location | Before | After |
|----------|--------|-------|
| `SettingsView.swift:527` | `.background(Color.orange.opacity(0.15))` | `.background(Color.warningTint)` |
| `ContentView.swift:268` | `Color.black.opacity(0.4)` | `Color.overlayDim` (no `.opacity` modifier — alpha baked into token def) |
| `ControlBar.swift:213` | `Color.white.opacity(0.06)` | `Color.glassRule` (no `.opacity` modifier — alpha baked in) |
| `LibrarySidebar.swift:62` | `Color(red:30/255,green:30/255,blue:28/255).opacity(0.05)` | `Color.rule.opacity(0.625)` (rule = 0.08 alpha; 0.05/0.08 = 0.625 to preserve current alpha) OR add a dedicated `surfaceTint` token if cleaner — executor picks |
| `LibraryEntryRow.swift:55` | `Color(red:30/255,green:30/255,blue:28/255).opacity(0.08)` | `Color.rule` (rule.light = black@0.08, exact match for original light appearance; rule.dark = white@0.08 which adapts) |
| `NotionTagSheet.swift:106` | `Color.red.opacity(0.1)` | `Color.errorTint` |

**Discretion items (D-09 explicitly permits planner choice; bias minimal churn):**

For each call-site that currently writes `Color.bg0` / `Color.bg1` / `Color.bg2` (now adaptive after Wave 1), executor inspects the call-site and decides whether to rewrite to `paper` / `paperSoft` / `paperWarm` for naming clarity. **Rule: rewrite ONLY if the call-site name is misleading.** Examples:
- `ControlBar` writing `.fill(Color.bg1)` for its rounded glass surface — this is semantically a "soft surface", rewrite to `Color.paperSoft` (paper.dark = #1A1818 != bg1.dark = #2E2B29; they DO differ in dark mode, so the rewrite changes appearance). PRESERVE `.fill(Color.bg1)` if the dark-mode glass-base appearance is desired (which it is — bg1 = #2E2B29 is the legacy "glass base" per the D-04 comment). **Default: do NOT rewrite legacy bg0/bg1/bg2 in ControlBar/OnboardingView/NotionTagSheet/CaptureDock — keep them on legacy tokens.**
- `OnboardingView` writing `Color.accent1` — keep as `accent1` (lavender is the deliberate identity).
- Only rewrite if a name is actively confusing AND the swap is bit-equivalent (e.g. if a view writes `Color.bg0` to mean "primary surface" but really wants paper, the dark-mode change matters — flag it but bias toward keeping legacy tokens to minimize Wave 2 visual risk).

**The default disposition for Wave 2 is: leave legacy bg0/bg1/bg2/fg1/fg2/fg3/accent1/accent2/recordRed/speakerTeal/speakerAmber call-sites alone.** Wave 2 is about removing INLINE HEX LITERALS, not relabeling token call-sites. The relabel is permitted but not required.

**DictationHUD partial-text confirmation:**

- Read `DictationHUD.swift` and locate the partial-text overlay foreground color
- If it uses `Color.ink` (or any Chronicle token) → already adaptive post-Wave-1, no change needed; document in summary
- If it uses a hex literal or system color (`.primary`, `.gray`) → replace with `Color.ink` so it auto-adapts against `.hudWindow` vibrancy in both modes
- Per D-03 from Phase 18, do NOT redesign the HUD; only confirm the partial-text token is adaptive

**.preferredColorScheme overrides — DO NOT TOUCH IN WAVE 2:**
- `ContentView.swift:251` `.preferredColorScheme(.light)` REMAINS
- `NotionTagSheet.swift:130` `.preferredColorScheme(.dark)` REMAINS
- Removal happens in Wave 3 only.

**Reject any output that:**
- Removes either `.preferredColorScheme` call in this wave
- Modifies the dark-side hex of any token added in Wave 1 (those are locked)
- Introduces a new file under `Sources/PSTranscribe/Design/`
- Changes the light-side appearance of warningTint/errorTint/overlayDim/glassRule (must match current `.orange.opacity(0.15)`, `.red.opacity(0.1)`, `.black.opacity(0.4)`, `.white.opacity(0.06)` byte-exact)
- Rewrites legacy bg0/bg1/bg2 call-sites in NotionTagSheet (which is forced .dark in Wave 2 — visible behavior must stay identical)
- Uses `Color.ink.opacity(0.4)` at ContentView:268 (would invert dim semantics in dark mode — superseded by overlayDim token)
- Uses `Color.rule` at ControlBar:213 (would flip white↔black across modes — superseded by glassRule token)
</truths>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add warningTint, warningInk, errorTint, overlayDim, glassRule tokens to DesignTokens.swift</name>
  <files>PSTranscribe/Sources/PSTranscribe/Design/DesignTokens.swift</files>
  <read_first>
    - PSTranscribe/Sources/PSTranscribe/Design/DesignTokens.swift (post-Wave-1 state — confirm Color(light:dark:) helper exists and 28 tokens are defined)
    - 20-CONTEXT.md D-09 (warning chip dark-side recommendation: #3D2E1A; D-09 license also covers analogous overlayDim and glassRule additions)
    - This plan's `<truths>` block (locked hexes for the 5 new tokens)
  </read_first>
  <action>
    Open `PSTranscribe/Sources/PSTranscribe/Design/DesignTokens.swift`. Add a new `// MARK: - Warning / error chips + overlay + glass rule (adaptive)` section after the Status group (where `recRed` and `liveGreen` are defined) and before the legacy palette section. Add five new tokens using exact values from `<truths>`:

    ```swift
    // MARK: - Warning / error chips + overlay + glass rule (adaptive)

    extension Color {
        /// Background for warning chips (e.g. SettingsView Notion mismatch warning).
        static let warningTint = Color(
            light: Color.orange.opacity(0.15),
            dark:  Color(red: 0x3D/255, green: 0x2E/255, blue: 0x1A/255).opacity(0.6)
        ) // light: orange@0.15 / dark: warm amber #3D2E1A@0.6

        /// Foreground/text on warning chips (auto-adaptive via ink in light, warm-amber in dark).
        static let warningInk = Color(
            light: Color(red: 0x1A/255, green: 0x1A/255, blue: 0x17/255), // = ink.light
            dark:  Color(red: 0xE8/255, green: 0xC4/255, blue: 0x80/255)
        ) // light: ink / dark: warm amber-cream

        /// Background for error chips (e.g. NotionTagSheet error states).
        static let errorTint = Color(
            light: Color.red.opacity(0.1),
            dark:  Color(red: 0x3D/255, green: 0x1A/255, blue: 0x1A/255).opacity(0.6)
        ) // light: red@0.1 / dark: warm dark-red #3D1A1A@0.6

        /// Modal/sheet dim-backdrop overlay. Opacity baked in — call-site uses Color.overlayDim with NO .opacity modifier.
        /// Both modes darken toward black; dark mode uses slightly higher alpha to maintain perceived contrast.
        static let overlayDim = Color(
            light: Color.black.opacity(0.4),
            dark:  Color.black.opacity(0.5)
        ) // light: black@0.4 / dark: black@0.5 — dim semantics preserved across modes

        /// Subtle hairline border on dark-glass surfaces (ControlBar, etc.). Both sides identical white@0.06
        /// because the underlying glass surface (bg1 = #2E2B29) is warm-dark in BOTH modes per D-04.
        /// Opacity baked in — call-site uses Color.glassRule with NO .opacity modifier.
        static let glassRule = Color(
            light: Color.white.opacity(0.06),
            dark:  Color.white.opacity(0.06)
        ) // light: white@0.06 / dark: white@0.06 — light-on-glass regardless of mode
    }
    ```

    Note on `warningInk.light`: cannot reference `Color.ink` directly inside `Color(light:dark:)` because that would introduce a token-of-token chain THROUGH the adaptive helper, which complicates resolution. Inline the hex (#1A1A17) for the light side; comment marks it as "= ink.light" so future maintainers see the equivalence.

    Run `cd PSTranscribe && swift build`. Confirm clean.
  </action>
  <verify>
    <automated>grep -c "static let warningTint\|static let warningInk\|static let errorTint\|static let overlayDim\|static let glassRule" PSTranscribe/Sources/PSTranscribe/Design/DesignTokens.swift; cd PSTranscribe && swift build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "static let warningTint" PSTranscribe/Sources/PSTranscribe/Design/DesignTokens.swift` returns 1
    - `grep -c "static let warningInk" PSTranscribe/Sources/PSTranscribe/Design/DesignTokens.swift` returns 1
    - `grep -c "static let errorTint" PSTranscribe/Sources/PSTranscribe/Design/DesignTokens.swift` returns 1
    - `grep -c "static let overlayDim" PSTranscribe/Sources/PSTranscribe/Design/DesignTokens.swift` returns 1
    - `grep -c "static let glassRule" PSTranscribe/Sources/PSTranscribe/Design/DesignTokens.swift` returns 1
    - Combined: `grep -c "static let \(warningTint\|warningInk\|errorTint\|overlayDim\|glassRule\)" PSTranscribe/Sources/PSTranscribe/Design/DesignTokens.swift` returns 5
    - `cd PSTranscribe && swift build` exits 0 with zero new warnings
  </acceptance_criteria>
  <done>5 new adaptive tokens added (warningTint, warningInk, errorTint, overlayDim, glassRule), file compiles clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace 6 inline hex literals across 6 view files</name>
  <files>
    PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift,
    PSTranscribe/Sources/PSTranscribe/Views/LibrarySidebar.swift,
    PSTranscribe/Sources/PSTranscribe/Views/LibraryEntryRow.swift,
    PSTranscribe/Sources/PSTranscribe/Views/ControlBar.swift,
    PSTranscribe/Sources/PSTranscribe/Views/SettingsView.swift,
    PSTranscribe/Sources/PSTranscribe/Views/NotionTagSheet.swift
  </files>
  <read_first>
    - This plan's `<truths>` block (replacement map)
    - Each view file at the line indicated below for context (3 lines above and below)
    - 20-SPEC.md req 4 (forced-dark and dark-only-token call-sites adapt)
  </read_first>
  <action>
    Apply the following exact replacements:

    **1. SettingsView.swift:527** — Warning chip background (REQ-20.4 explicit acceptance criterion)

    Find: `.background(Color.orange.opacity(0.15))`
    Replace with: `.background(Color.warningTint)`

    Verify the surrounding text block uses `Color.ink` or chronicleBody() (which uses ink) — if it still uses default text color, it will auto-adapt; if it explicitly references a hex, also replace with `Color.warningInk`. Read 5 lines above and below 527 to confirm context.

    **2. ContentView.swift:268** — Dim overlay

    Find: `Color.black.opacity(0.4)` (the dim-modal-background overlay; verify by reading 5 lines above and below 268 to confirm it's the modal/sheet darkening overlay)
    Replace with: `Color.overlayDim`

    **CRITICAL:** Do NOT add `.opacity(0.4)` modifier after `Color.overlayDim`. The opacity is already baked into the token def (light=black@0.4, dark=black@0.5). Adding `.opacity(0.4)` would compound the alpha to 0.16 (light) / 0.20 (dark) — visibly wrong.

    **Rationale:** The prior plan revision proposed `Color.ink.opacity(0.4)` which would have inverted dim semantics in dark mode (cream-toned overlay would lighten the backdrop instead of darken it). The new `overlayDim` token preserves "darken-the-modal-backdrop" semantics in both modes by anchoring on black@N alpha across the board. Light side is byte-identical to the original `Color.black.opacity(0.4)`. Dark side bumps to 0.5 alpha to compensate for the warm-dark paper surface (perceived contrast equivalence).

    **3. ControlBar.swift:213** — Stroke border (inactive branch)

    Find: `Color.white.opacity(0.06)`
    Replace with: `Color.glassRule`

    **CRITICAL:** Do NOT add `.opacity(0.06)` modifier after `Color.glassRule`. The opacity is already baked into the token def (both sides white@0.06). Adding `.opacity(0.06)` would compound alpha to ~0.0036 — invisible.

    **Rationale:** The prior plan revision proposed `Color.rule` which flips white↔black across modes. ControlBar's glass surface sits on `bg1` = #2E2B29 in BOTH modes (per D-04 — bg1 is warm-dark regardless of system appearance), so the border treatment must stay light-on-dark always. `Color.rule` would render the border as black@0.08 in light mode — visible drift on dark glass. The new `glassRule` token keeps both sides at white@0.06 — pixel-stable.

    **4. LibrarySidebar.swift:62** — Background fill

    Find: `Color(red: 30/255, green: 30/255, blue: 28/255).opacity(0.05)` (in the `.fill(...)` call)
    Replace with: `Color.rule.opacity(0.625)` (rule alpha = 0.08; original alpha = 0.05; ratio = 0.625 preserves the visible alpha)

    Alternative: introduce `surfaceTint` token if executor judges the math unwieldy:
    ```swift
    static let surfaceTint = Color(
        light: Color(red: 30/255, green: 30/255, blue: 28/255).opacity(0.05),
        dark:  Color.white.opacity(0.05)
    )
    ```
    Executor picks whichever reads cleaner; document the choice in the Wave 2 SUMMARY.

    **5. LibraryEntryRow.swift:55** — Shadow color (selected entry lift)

    Find: `Color(red: 30/255, green: 30/255, blue: 28/255).opacity(0.08)` (in the shadow color expression)
    Replace with: `Color.rule` (rule.light = black@0.08, exact match for original light appearance; rule.dark = white@0.08 which adapts).

    Note: there is already a `Shadows.listSelection` definition in DesignTokens.swift (lines 76-79 of original) using exactly this color/alpha. Executor verifies whether LibraryEntryRow:55 should reference `Shadows.listSelection` instead of inlining a shadow with `Color.rule`. If the call-site is a manual `.shadow(color:radius:x:y:)` inline, prefer rewriting to `.shadow(Shadows.listSelection)` (which uses the existing helper). If the call-site is a custom shadow with different radius/offset, use `Color.rule` for the color only.

    **6. NotionTagSheet.swift:106** — Error background

    Find: `.background(Color.red.opacity(0.1))`
    Replace with: `.background(Color.errorTint)`

    Run `cd PSTranscribe && swift build` after all 6 replacements. Confirm clean.
  </action>
  <verify>
    <automated>grep -rn "Color\.orange\.opacity\|Color\.black\.opacity(0\.4)\|Color\.white\.opacity(0\.06)\|Color\.red\.opacity(0\.1)\|Color(red: 30/255" PSTranscribe/Sources/PSTranscribe/Views/ | grep -v '^[^:]*:[^:]*:[[:space:]]*//' | grep -c .; cd PSTranscribe && swift build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Color.orange.opacity" PSTranscribe/Sources/PSTranscribe/Views/SettingsView.swift` returns no match
    - `grep -n "Color.black.opacity(0.4)" PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift` returns no match
    - `grep -n "overlayDim" PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift` returns 1 match (the replacement at line 268)
    - `grep -n "Color.white.opacity(0.06)" PSTranscribe/Sources/PSTranscribe/Views/ControlBar.swift` returns no match
    - `grep -n "glassRule" PSTranscribe/Sources/PSTranscribe/Views/ControlBar.swift` returns 1 match (the replacement at line 213)
    - `grep -n "Color(red: 30/255" PSTranscribe/Sources/PSTranscribe/Views/LibrarySidebar.swift PSTranscribe/Sources/PSTranscribe/Views/LibraryEntryRow.swift` returns no match (or returns matches only inside `Shadows.listSelection` reference)
    - `grep -n "Color.red.opacity(0.1)" PSTranscribe/Sources/PSTranscribe/Views/NotionTagSheet.swift` returns no match
    - `grep -n "warningTint\|errorTint" PSTranscribe/Sources/PSTranscribe/Views/SettingsView.swift PSTranscribe/Sources/PSTranscribe/Views/NotionTagSheet.swift` returns matches at the replacement sites
    - `cd PSTranscribe && swift build` exits 0 with zero new warnings
  </acceptance_criteria>
  <done>All 6 inline hex literals tokenized using the 5 new tokens (overlayDim and glassRule used directly without compounding opacity); swift build clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Audit 10 surfaces for any remaining inline literals + DictationHUD partial-text confirm</name>
  <files>
    PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift,
    PSTranscribe/Sources/PSTranscribe/Views/LibrarySidebar.swift,
    PSTranscribe/Sources/PSTranscribe/Views/TranscriptView.swift,
    PSTranscribe/Sources/PSTranscribe/Views/DetailsPane.swift,
    PSTranscribe/Sources/PSTranscribe/Views/CaptureDock.swift,
    PSTranscribe/Sources/PSTranscribe/Views/ControlBar.swift,
    PSTranscribe/Sources/PSTranscribe/Views/SettingsView.swift,
    PSTranscribe/Sources/PSTranscribe/Views/NotionTagSheet.swift,
    PSTranscribe/Sources/PSTranscribe/Views/OnboardingView.swift,
    PSTranscribe/Sources/PSTranscribe/Views/DictationHUD.swift
  </files>
  <read_first>
    - All 10 view files (full read — these are the audit targets)
    - 20-CONTEXT.md `<canonical_refs>` (10 surfaces requiring audit)
    - 18-CONTEXT.md D-03 (HUD locked as native vibrancy — partial-text only is in scope for tokenization)
  </read_first>
  <action>
    **Part A — Comprehensive grep audit:**

    Run these 4 grep commands and address every match in the 10 audited files:

    ```bash
    # 1. Find any remaining Color(red:green:blue:) hex literals in views
    grep -rn "Color(red:" PSTranscribe/Sources/PSTranscribe/Views/

    # 2. Find any system color literals that should be tokens
    grep -rn "Color\.\(orange\|black\|white\|red\|gray\|blue\|green\|yellow\|purple\|pink\|brown\|cyan\|mint\|teal\|indigo\)" PSTranscribe/Sources/PSTranscribe/Views/ | grep -v "Color\.bg\|Color\.fg\|Color\.ink\|Color\.paper\|Color\.accent\|Color\.spk\|Color\.you\|Color\.rec\|Color\.live\|Color\.rule\|Color\.warning\|Color\.error\|Color\.speaker\|Color\.record\|Color\.clear"

    # 3. Find foreground/background calls with system semantic colors
    grep -rn "\.foregroundColor(\.\|\.foregroundStyle(\.\|\.background(\." PSTranscribe/Sources/PSTranscribe/Views/ | grep -v "ink\|paper\|accent\|warning\|error\|rule\|spk\|you\|rec\|live\|chronicle\|primary\|secondary"

    # 4. Find UInt32 / NSColor / hex string literals
    grep -rn "0x[0-9A-F]\{6\}\|NSColor(" PSTranscribe/Sources/PSTranscribe/Views/
    ```

    For each match found:
    - If it's a system semantic color (`.primary`, `.secondary`) — usually OK; system semantics ARE adaptive, document in summary
    - If it's `Color.clear` — OK, leave it
    - If it's a hex literal — tokenize using existing token if one fits, otherwise add a new token to DesignTokens.swift following the same pattern as Task 1
    - If it's a system color (.orange / .red / .blue / etc.) — tokenize via warningTint / errorTint / accentInk depending on semantic; if no fit, add a new token

    **Part B — DictationHUD partial-text confirmation:**

    Read `DictationHUD.swift` end-to-end. Locate the partial-text overlay (the SwiftUI Text view that shows in-progress dictation). Identify its foreground color expression. Verify:

    - If `Color.ink` or any Chronicle adaptive token → no change, document in summary as "already adaptive post-Wave-1, no action"
    - If `.primary` or `.secondary` (SwiftUI semantic) → no change, document as "uses SwiftUI semantic color which auto-adapts; intentional given .hudWindow vibrancy"
    - If a hex literal or system color (.gray etc.) → replace with `Color.ink` so it adapts

    Per Phase 18 D-03, do NOT redesign the HUD. Only verify partial-text foreground tokens are adaptive.

    **Part C — Discretion items (legacy token rewrites):**

    For each call-site writing `Color.bg0` / `Color.bg1` / `Color.bg2` / `Color.fg1` / `Color.fg2` / `Color.fg3` / `Color.accent1` / `Color.accent2` / `Color.recordRed` / `Color.speakerTeal` / `Color.speakerAmber`:

    - Default: LEAVE AS-IS. These tokens are now adaptive (light=dark=legacy-hex post-Wave-1) and rewriting them risks visual drift in Wave 2.
    - Exception: rewrite ONLY if the executor can prove the call-site name is misleading AND the swap is bit-equivalent in BOTH light and dark modes.

    Bias: minimum churn. Document any rewrites in the SUMMARY with before/after and justification.

    Run `cd PSTranscribe && swift build` after all changes. Confirm clean.
  </action>
  <verify>
    <automated>grep -rn "Color(red:" PSTranscribe/Sources/PSTranscribe/Views/ | wc -l; grep -rn "Color\.\(orange\|black\.opacity\|white\.opacity(0\.06)\|red\.opacity(0\.1)\)" PSTranscribe/Sources/PSTranscribe/Views/ | wc -l; cd PSTranscribe && swift build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "Color(red:" PSTranscribe/Sources/PSTranscribe/Views/` returns 0 matches (every hex literal in views is now in a token def in DesignTokens.swift instead)
    - `grep -rn "Color\.orange\b" PSTranscribe/Sources/PSTranscribe/Views/` returns 0 matches
    - `grep -rn "Color\.black\.opacity" PSTranscribe/Sources/PSTranscribe/Views/` returns 0 matches
    - `grep -rn "Color\.red\.opacity" PSTranscribe/Sources/PSTranscribe/Views/` returns 0 matches outside token replacements (errorTint usage is OK)
    - DictationHUD partial-text foreground confirmed adaptive (documented in summary)
    - `cd PSTranscribe && swift build` exits 0 with zero new warnings
  </acceptance_criteria>
  <done>10 surfaces audited; zero inline hex literals remaining in views; DictationHUD partial-text confirmed adaptive.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Wave 2 light-mode pixel-stability gate</name>
  <what-built>
    - 5 new adaptive tokens in DesignTokens.swift: `warningTint`, `warningInk`, `errorTint`, `overlayDim`, `glassRule`
    - 6 known inline hex literals replaced with token references (SettingsView orange chip → warningTint, ContentView dim overlay → overlayDim, ControlBar border → glassRule, LibrarySidebar fill → rule.opacity, LibraryEntryRow shadow → rule, NotionTagSheet error bg → errorTint)
    - 10 surfaces audited; any additional inline hex literals discovered during audit also tokenized
    - DictationHUD partial-text foreground confirmed adaptive
    - `swift build` clean, no new warnings
    - `.preferredColorScheme(.light)` at ContentView:251 and `.preferredColorScheme(.dark)` at NotionTagSheet:130 INTENTIONALLY UNCHANGED — Wave 3 work
  </what-built>
  <how-to-verify>
    Build and launch the app: `cd PSTranscribe && swift build && swift run` (or via Xcode).

    With macOS in **Light** appearance, capture post-Wave-2 screenshots into `.planning/phases/20-dark-mode-parity/screenshots/wave-2/` mirroring the wave-1 set:
    1. `main-window-idle.png`
    2. `capture-dock-idle.png`
    3. `capture-dock-recording.png`
    4. `settings-general.png`
    5. `settings-notion.png` (the warning chip should now show `warningTint.light` = `Color.orange.opacity(0.15)` — visually IDENTICAL to baseline)
    6. `settings-other-tabs.png`
    7. `notion-tag-sheet.png` (still forced .dark; error chip if visible should match baseline)
    8. `onboarding.png`
    9. `dictation-hud.png`

    Side-by-side visual diff `screenshots/wave-1/` vs `screenshots/wave-2/`:
    - Special attention to: warning chip in settings-notion.png (should match baseline exactly), modal overlay in main-window-idle.png if any modal is open, ControlBar borders, sidebar background tint, library entry shadows
    - The SettingsView warning chip is the canonical visual test — its light side should be byte-identical to baseline because `Color.orange.opacity(0.15)` literal is preserved in `warningTint.light`
    - The ContentView dim overlay should be byte-identical because `overlayDim.light` = `Color.black.opacity(0.4)` (same as before)
    - The ControlBar border should be byte-identical because `glassRule.light` = `Color.white.opacity(0.06)` (same as before)

    Expected outcome: ZERO perceptible drift in light mode. The 5 new tokens were intentionally designed with byte-stable light sides specifically to pass this gate.

    Sanity check: app should still render in forced-light mode (because `.preferredColorScheme(.light)` is still pinned). NotionTagSheet still forced dark.
  </how-to-verify>
  <resume-signal>
    Type "approved" if pixel-stable, OR describe the drift (which surface, which color/border, baseline vs wave-2). Executor will adjust the token def or introduce additional bit-stable tokens before Wave 3 starts.
  </resume-signal>
</task>

</tasks>

<verification>
**Wave 2 gate (must all pass before Wave 3 starts):**

1. `grep -rn "Color(red:" PSTranscribe/Sources/PSTranscribe/Views/` → 0 matches
2. `grep -rn "Color\.orange\b\|Color\.black\.opacity\|Color\.red\.opacity\(0\.1\)" PSTranscribe/Sources/PSTranscribe/Views/` → 0 matches
3. `grep -c "warningTint\|errorTint\|overlayDim\|glassRule" PSTranscribe/Sources/PSTranscribe/Views/` → ≥4 (at least one of each at the replacement sites)
4. `grep -c "static let \(warningTint\|warningInk\|errorTint\|overlayDim\|glassRule\)" PSTranscribe/Sources/PSTranscribe/Design/DesignTokens.swift` → 5
5. `cd PSTranscribe && swift build` → exits 0, zero new warnings
6. Manual screenshot diff `screenshots/wave-1/` vs `screenshots/wave-2/` → no perceptible drift
7. `grep -n "preferredColorScheme" PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift PSTranscribe/Sources/PSTranscribe/Views/NotionTagSheet.swift` → still 2 hits
</verification>

<success_criteria>
- [ ] 5 new adaptive tokens defined in DesignTokens.swift (warningTint / warningInk / errorTint / overlayDim / glassRule)
- [ ] All 6 known inline hex literals replaced with token references using the correct token (no `.opacity` modifier compounded onto overlayDim or glassRule)
- [ ] 10 surfaces audited via grep — zero remaining `Color(red:`, `Color.orange`, `Color.black.opacity(0.4)`, `Color.red.opacity(0.1)`, `Color.white.opacity(0.06)` in views
- [ ] DictationHUD partial-text foreground documented as adaptive
- [ ] Discretion items (legacy bg0/bg1 rewrites) handled per default-leave-as-is policy
- [ ] `swift build` exits 0 with zero new warnings
- [ ] Post-Wave-2 screenshots captured in `screenshots/wave-2/`
- [ ] Manual screenshot diff vs Wave-1 shows zero perceptible light-mode drift
- [ ] `.preferredColorScheme` overrides still in place at ContentView:251 and NotionTagSheet:130
</success_criteria>

<output>
After completion, create `.planning/phases/20-dark-mode-parity/20-02-SUMMARY.md` documenting:
- Final list of inline literal replacements (file:line, before, after) — confirm all 6 use the 5 new tokens correctly
- Any new tokens added beyond the locked-in 5 (warningTint/warningInk/errorTint/overlayDim/glassRule) — e.g. surfaceTint if executor chose that path for LibrarySidebar
- Discretion-item decisions: which legacy token call-sites were rewritten (if any), with justification
- DictationHUD partial-text foreground state (already adaptive vs newly tokenized)
- Result of pixel-stability gate
- Path forward into Wave 3
</output>
</content>
</invoke>