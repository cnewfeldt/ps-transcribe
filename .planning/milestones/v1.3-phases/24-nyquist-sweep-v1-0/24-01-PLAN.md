---
phase: 24-nyquist-sweep-v1-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - PSTranscribe/Tests/PSTranscribeTests/RebrandInfoPlistTests.swift
  - .planning/milestones/v1.0-phases/01-rebrand/01-VALIDATION.md
autonomous: true
requirements: [NYQUIST-01]
tags: [nyquist, validation, rebrand, info-plist]

must_haves:
  truths:
    - "Honors D-01 (Phase 1 re-audit posture): re-audits 01-VALIDATION.md instead of skipping; bumps last_audited to 2026-05-05; preserves the prior 2026-04-27 audit block as a second audit entry"
    - "Honors D-02 (one @Test per testable requirement ID): 5 testable REBR rows → 5 @Test methods at requirement granularity (not phase-level, not behavior-cluster)"
    - "Honors D-04 (flat test files named by behavior): RebrandInfoPlistTests.swift placed at PSTranscribe/Tests/PSTranscribeTests/ (no Phase24/ subdir)"
    - "RebrandInfoPlistTests.swift exists with 5 @Test methods covering REBR-01, REBR-02, REBR-04, REBR-06, REBR-07"
    - "swift test --filter RebrandInfoPlistTests passes (exit 0) locally"
    - "Full swift test suite passes (exit 0) locally"
    - "01-VALIDATION.md frontmatter shows status: approved AND last_audited: 2026-05-05 (Phase 24 completion date)"
    - "01-VALIDATION.md REBR-08 row has Test Type: WITHDRAWN with Source: 01-VERIFICATION.md REBR-08"
    - "01-VALIDATION.md Per-Task Map rows for REBR-01..07 list automated swift test commands (no manual rows)"
    - "01-VALIDATION.md no longer contains a Manual-Only Verifications section (D-03 lenient policy)"
    - "build-check.yml CI green on the PR (swift test step exits 0 on macos-26)"
  artifacts:
    - path: "PSTranscribe/Tests/PSTranscribeTests/RebrandInfoPlistTests.swift"
      provides: "Swift Testing assertions for Info.plist bundle ID, app name, exec name, SUFeedURL, mic-usage description"
      contains: "@Suite(\"RebrandInfoPlistTests\")"
      min_lines: 60
    - path: ".planning/milestones/v1.0-phases/01-rebrand/01-VALIDATION.md"
      provides: "Re-audited validation contract (D-01: bumps last_audited; preserves status: approved)"
      contains: "last_audited: 2026-05-05"
  key_links:
    - from: "PSTranscribe/Tests/PSTranscribeTests/RebrandInfoPlistTests.swift"
      to: "PSTranscribe/Sources/PSTranscribe/Info.plist"
      via: "URL(fileURLWithPath: \"Sources/PSTranscribe/Info.plist\") + PropertyListSerialization (Risk #2: NOT Bundle.main; test bundle is the runner, not the app)"
      pattern: "PropertyListSerialization\\.propertyList"
    - from: ".planning/milestones/v1.0-phases/01-rebrand/01-VALIDATION.md"
      to: "PSTranscribe/Tests/PSTranscribeTests/RebrandInfoPlistTests.swift"
      via: "Per-Task Verification Map row Automated Command column citing swift test --filter RebrandInfoPlistTests"
      pattern: "swift test --filter RebrandInfoPlistTests"
---

<objective>
Write the first new Phase 24 test file (`RebrandInfoPlistTests.swift`) and re-audit `01-VALIDATION.md` per D-01.

Purpose: Phase 1 was retroactively approved on 2026-04-27 via `swift build` + grep alone (no test target existed). D-01 brings Phase 1 into Phase 24's uniform sweep so the audit basis becomes real Swift Testing assertions, not just compile-time + grep. This plan establishes the direct-file-IO Info.plist read pattern used by no other test in the suite (per Risk #2: `Bundle.main` is the test runner, not the app — keys are unreliable).

Output:
- 1 new test file: `RebrandInfoPlistTests.swift` with 5 `@Test` methods
- 1 re-audited VALIDATION.md: `01-VALIDATION.md` with bumped `last_audited`, REBR-08 row migrated from `manual` to `WITHDRAWN`, Manual-Only Verifications section removed per D-03
- 1 SUMMARY.md (created at plan close, includes `requirements-completed: [NYQUIST-01]`)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-nyquist-sweep-v1-0/24-CONTEXT.md
@.planning/phases/24-nyquist-sweep-v1-0/24-RESEARCH.md
@.planning/phases/24-nyquist-sweep-v1-0/24-PATTERNS.md
@.planning/milestones/v1.0-phases/01-rebrand/01-VALIDATION.md
@.planning/milestones/v1.0-phases/01-rebrand/01-VERIFICATION.md
@PSTranscribe/Tests/PSTranscribeTests/DictationLoggerTests.swift
@PSTranscribe/Sources/PSTranscribe/Info.plist

<interfaces>
<!-- Key Info.plist keys this plan asserts on. Values verified via direct file read 2026-05-05. -->
<!-- Source: PSTranscribe/Sources/PSTranscribe/Info.plist -->

```
CFBundleName            = "PS Transcribe"            (line ~6)
CFBundleIdentifier      = "com.pstranscribe.app"     (line ~10)
CFBundleExecutable      = "PSTranscribe"             (line ~16)
SUFeedURL               = "https://raw.githubusercontent.com/cnewfeldt/ps-transcribe-releases/main/appcast.xml"  (line ~30)
NSMicrophoneUsageDescription = mentions "PS Transcribe"
CFBundleDisplayName     = "PS Transcribe"
```

Test bundle CWD when `swift test` runs: `PSTranscribe/` (the SwiftPM package root). Therefore the relative path to Info.plist is: `Sources/PSTranscribe/Info.plist`.

Read pattern (from 24-PATTERNS.md, sourced from `ObsidianURLTests.swift::16` `try #require` shape):
```swift
let url = URL(fileURLWithPath: "Sources/PSTranscribe/Info.plist")
let data = try Data(contentsOf: url)
let plist = try PropertyListSerialization.propertyList(from: data, format: nil) as? [String: Any]
let name = try #require(plist?["CFBundleName"] as? String)
#expect(name == "PS Transcribe")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RebrandInfoPlistTests.swift with 5 @Test methods (REBR-01/02/04/06/07)</name>
  <files>PSTranscribe/Tests/PSTranscribeTests/RebrandInfoPlistTests.swift</files>
  <read_first>
    - PSTranscribe/Tests/PSTranscribeTests/DictationLoggerTests.swift (canonical analog: `@Suite` + `@Test` + `try #require` shape; lines 1-13 imports + tempDir, lines 121-126 throws-assertion form not needed here but read for `await` pattern)
    - PSTranscribe/Tests/PSTranscribeTests/ObsidianURLTests.swift (pure-function test analog; `try #require` use at line 16; `URLComponents` query assertion shape)
    - PSTranscribe/Sources/PSTranscribe/Info.plist (source of truth — verify CFBundleName/CFBundleIdentifier/CFBundleExecutable/SUFeedURL/NSMicrophoneUsageDescription/CFBundleDisplayName values BEFORE writing assertions)
    - .planning/phases/24-nyquist-sweep-v1-0/24-PATTERNS.md (lines 49-87 — Pattern Assignments § RebrandInfoPlistTests; provides exact `@Suite` + `@Test` shape to copy)
    - .planning/phases/24-nyquist-sweep-v1-0/24-RESEARCH.md (lines 121-130 — Per-Requirement API Audit Phase 01 row REBR-01..08 confirms which keys live where; lines 218-219 — Proposed Test File Roster row for `RebrandInfoPlistTests.swift`; Risk #2 at line 401 — "Bundle.main is the test runner, not the app"; Risk #3 at line 402 — cwd assumption smoke test)
  </read_first>
  <behavior>
    Test 1 (REBR-01): `bundleNameIsPSTranscribe()` — read Info.plist via direct file IO, parse via PropertyListSerialization, assert `CFBundleName == "PS Transcribe"`.
    Test 2 (REBR-02): `bundleIdentifierIsPSTranscribe()` — assert `CFBundleIdentifier == "com.pstranscribe.app"`. (Logger subsystem grep at StreamingTranscriber.swift is covered by `ErrorPathLoggingTests.swift` in Plan 24-05; this test asserts the Info.plist source-of-truth only.)
    Test 3 (REBR-04 / REBR-07a): `executableNameIsPSTranscribe()` — assert `CFBundleExecutable == "PSTranscribe"` (no spaces, exact string).
    Test 4 (REBR-06): `sparkleFeedURLPointsAtPSTranscribe()` — assert `SUFeedURL` contains `"ps-transcribe"` AND `"appcast.xml"`, AND does NOT contain `"Tome"` / `"Gremble"` / `"OWNER"` placeholder.
    Test 5 (REBR-07): `displayNameAndMicUsageMentionPSTranscribe()` — assert `CFBundleDisplayName == "PS Transcribe"` AND `NSMicrophoneUsageDescription` contains `"PS Transcribe"` (string-contains, since the description is a sentence).
    Smoke pre-condition (REBR-04 indirect / Risk #3 mitigation): each `@Test` MUST `try #require(!data.isEmpty)` before parsing — produces a clear failure message if the cwd assumption is wrong, instead of a silent nil-coalesce-to-empty plist that hides the real error.
  </behavior>
  <action>
    Create `PSTranscribe/Tests/PSTranscribeTests/RebrandInfoPlistTests.swift`. Use Write tool (file does not exist).

    Header doc-comment (mandatory per 24-PATTERNS.md "No Analog Found" — first test in this target to read Info.plist via file IO):
    ```swift
    // RebrandInfoPlistTests
    // Phase 24 (NYQUIST-01) — re-audit of v1.0 Phase 1 Rebrand requirements REBR-01/02/04/06/07.
    //
    // Reads Info.plist via direct file IO from the package root (`Sources/PSTranscribe/Info.plist`).
    // Bundle.main is intentionally NOT used: in a Swift Testing target, Bundle.main is the test
    // runner's bundle, not the app bundle, and the app's CFBundleName/CFBundleIdentifier may not
    // be present. Working-directory assumption: `swift test` runs from `PSTranscribe/` (the SwiftPM
    // package root). The first `try #require(!data.isEmpty)` per test fails fast with a clear
    // message if that assumption breaks.
    //
    // REBR-08 (UserDefaults migration from io.gremble.tome) is WITHDRAWN per Phase 24 D-03 —
    // code deleted in commit 4ef30e0 post-v1.0 ship; no surviving API. See 01-VALIDATION.md.
    ```

    Then exactly the structure (from 24-PATTERNS.md §RebrandInfoPlistTests):
    ```swift
    import Testing
    import Foundation

    @Suite("RebrandInfoPlistTests")
    struct RebrandInfoPlistTests {

        private func loadInfoPlist() throws -> [String: Any] {
            let url = URL(fileURLWithPath: "Sources/PSTranscribe/Info.plist")
            let data = try Data(contentsOf: url)
            try #require(!data.isEmpty)  // Risk #3: fail fast if cwd is not PSTranscribe/
            let plist = try PropertyListSerialization.propertyList(from: data, format: nil) as? [String: Any]
            return try #require(plist)
        }

        @Test func bundleNameIsPSTranscribe() throws {
            let plist = try loadInfoPlist()
            let name = try #require(plist["CFBundleName"] as? String)
            #expect(name == "PS Transcribe")
        }

        @Test func bundleIdentifierIsPSTranscribe() throws {
            let plist = try loadInfoPlist()
            let id = try #require(plist["CFBundleIdentifier"] as? String)
            #expect(id == "com.pstranscribe.app")
        }

        @Test func executableNameIsPSTranscribe() throws {
            let plist = try loadInfoPlist()
            let exec = try #require(plist["CFBundleExecutable"] as? String)
            #expect(exec == "PSTranscribe")  // exact, no spaces
        }

        @Test func sparkleFeedURLPointsAtPSTranscribe() throws {
            let plist = try loadInfoPlist()
            let feed = try #require(plist["SUFeedURL"] as? String)
            #expect(feed.contains("ps-transcribe"))
            #expect(feed.contains("appcast.xml"))
            #expect(!feed.contains("Tome"))
            #expect(!feed.contains("Gremble"))
            #expect(!feed.contains("OWNER"))  // resolved tech debt — no placeholder
        }

        @Test func displayNameAndMicUsageMentionPSTranscribe() throws {
            let plist = try loadInfoPlist()
            let displayName = try #require(plist["CFBundleDisplayName"] as? String)
            #expect(displayName == "PS Transcribe")
            let micUsage = try #require(plist["NSMicrophoneUsageDescription"] as? String)
            #expect(micUsage.contains("PS Transcribe"))
        }
    }
    ```

    No `.serialized` (pure file reads, no shared state). No `@MainActor` (no SwiftUI). No `@testable import` (we read a file, not internal types — but DO NOT add `@testable import PSTranscribe` since this test exercises only Foundation + the Info.plist file).

    After writing the file, verify by running the focused suite:
    ```
    cd PSTranscribe && swift test --filter RebrandInfoPlistTests
    ```
    Must exit 0 with 5 tests passing. If REBR-06's SUFeedURL contains an unexpected token (e.g., the placeholder is back), STOP and escalate — do not modify the test to accept it. The Info.plist is the source of truth.
  </action>
  <verify>
    <automated>cd PSTranscribe && swift test --filter RebrandInfoPlistTests 2>&1 | tee /tmp/24-01-task1.log; tail -20 /tmp/24-01-task1.log</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f PSTranscribe/Tests/PSTranscribeTests/RebrandInfoPlistTests.swift`
    - Contains all 5 `@Test func` declarations: `grep -cE '^\s+@Test func (bundleNameIsPSTranscribe|bundleIdentifierIsPSTranscribe|executableNameIsPSTranscribe|sparkleFeedURLPointsAtPSTranscribe|displayNameAndMicUsageMentionPSTranscribe)\(' PSTranscribe/Tests/PSTranscribeTests/RebrandInfoPlistTests.swift` returns 5
    - Header comment present: `grep -c "Phase 24 (NYQUIST-01)" PSTranscribe/Tests/PSTranscribeTests/RebrandInfoPlistTests.swift` returns 1
    - No `Bundle.main` use (per Risk #2): `grep -c "Bundle.main" PSTranscribe/Tests/PSTranscribeTests/RebrandInfoPlistTests.swift` returns 0
    - Direct-file-IO present: `grep -c 'URL(fileURLWithPath: "Sources/PSTranscribe/Info.plist")' PSTranscribe/Tests/PSTranscribeTests/RebrandInfoPlistTests.swift` returns 1
    - `swift test --filter RebrandInfoPlistTests` exits 0 (5 passing tests, 0 failures)
  </acceptance_criteria>
  <done>5 @Test methods written, suite passes locally, file commits cleanly via the plan summary commit step.</done>
</task>

<task type="auto">
  <name>Task 2: Re-audit 01-VALIDATION.md (per D-01 + D-03 — bump last_audited, migrate REBR-08 to WITHDRAWN, drop Manual-Only section)</name>
  <files>.planning/milestones/v1.0-phases/01-rebrand/01-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v1.0-phases/01-rebrand/01-VALIDATION.md (existing 2026-04-27 audit; we APPEND a 2026-05-05 audit block, do not delete the prior one)
    - .planning/milestones/v1.0-phases/01-rebrand/01-VERIFICATION.md (REBR-08 row — sourced as `Source: 01-VERIFICATION.md REBR-08` for the WITHDRAWN row reason)
    - .planning/phases/24-nyquist-sweep-v1-0/24-PATTERNS.md (lines 388-458 — VALIDATION.md Edit Pattern Assignments; lines 416-425 establish the WITHDRAWN row format; lines 429-434 establish the second `## Validation Audit` block append pattern)
    - .planning/phases/24-nyquist-sweep-v1-0/24-CONTEXT.md (D-01 keeps `status: approved`; D-03 lenient policy — no Manual-Only rows)
  </read_first>
  <action>
    Use the Edit tool to perform the following deltas on `.planning/milestones/v1.0-phases/01-rebrand/01-VALIDATION.md`:

    DELTA 1 — Frontmatter:
    - Change line 8 from `last_audited: 2026-04-27` to `last_audited: 2026-05-05`.
    - Keep `status: approved`, `nyquist_compliant: true`, `wave_0_complete: true` (per D-01).

    DELTA 2 — Per-Task Verification Map (lines 38-46):
    Replace the existing 6-row table with this 8-row table (REBR-01..07 each get a unit row pointing at `RebrandInfoPlistTests`; REBR-05 cross-references `WorkflowSecretsTests` created in Plan 24-05; REBR-08 becomes WITHDRAWN). Use the row format from 24-PATTERNS.md:

    ```markdown
    | Task ID | Plan | Wave | Requirement | Test Type | Automated Command | Status | Notes |
    |---------|------|------|-------------|-----------|-------------------|--------|-------|
    | 24-01-01 | 24-01 | 1 | REBR-01 | unit | `cd PSTranscribe && swift test --filter RebrandInfoPlistTests/bundleNameIsPSTranscribe` | green | Re-audit 2026-05-05 (D-01) |
    | 24-01-02 | 24-01 | 1 | REBR-02 | unit | `cd PSTranscribe && swift test --filter RebrandInfoPlistTests/bundleIdentifierIsPSTranscribe` | green | Re-audit 2026-05-05 (D-01) |
    | 24-01-03 | 24-01 | 1 | REBR-03 | unit | `cd PSTranscribe && swift test` (compile-time proof; `@testable import PSTranscribe` resolves) | green | Re-audit 2026-05-05 (D-01); frontmatter `source/pstranscribe` covered separately by Plan 24-03 |
    | 24-01-04 | 24-01 | 1 | REBR-04 | unit | `cd PSTranscribe && swift test --filter RebrandInfoPlistTests/executableNameIsPSTranscribe` | green | Re-audit 2026-05-05 (D-01) |
    | 24-05-XX | 24-05 | 1 | REBR-05 | unit | `cd PSTranscribe && swift test --filter WorkflowSecretsTests` | pending | Workflow secrets test created in Plan 24-05; cross-reference here |
    | 24-01-05 | 24-01 | 1 | REBR-06 | unit | `cd PSTranscribe && swift test --filter RebrandInfoPlistTests/sparkleFeedURLPointsAtPSTranscribe` | green | Re-audit 2026-05-05 (D-01) |
    | 24-01-06 | 24-01 | 1 | REBR-07 | unit | `cd PSTranscribe && swift test --filter RebrandInfoPlistTests/displayNameAndMicUsageMentionPSTranscribe` | green | Re-audit 2026-05-05 (D-01) |
    | 24-01-07 | 24-01 | 1 | REBR-08 | WITHDRAWN | n/a — WITHDRAWN | withdrawn | Code deleted post-v1.0 in commit `4ef30e0` (`migrateUserDefaultsIfNeeded()` and `hasMigratedFromTome` sentinel removed; upgrade window closed). v1.0 milestone audit verified live migration on 2026-04-14. Source: 01-VERIFICATION.md REBR-08 row. |
    ```

    DELTA 3 — Manual-Only Verifications section (lines 64-71): DELETE the entire section header (`## Manual-Only Verifications`), the table, and the trailing italic sentence. Per D-03, no Manual-Only rows. The REBR-01 visual-UI inspection that previously lived here is now encoded in `RebrandInfoPlistTests.bundleNameIsPSTranscribe()` (source-of-truth assertion); the REBR-08 manual row is migrated to the WITHDRAWN row in Delta 2.

    DELTA 4 — Append a SECOND audit block AFTER the existing `## Validation Audit 2026-04-27` block (after line 116). Use this exact content:

    ```markdown

    ---

    ## Validation Audit 2026-05-05 (Phase 24 — NYQUIST-01)

    | Metric | Count |
    |--------|-------|
    | Gaps found | 1 (Phase 1 originally approved on `swift build` + grep alone — D-01 brings Phase 1 into Phase 24's uniform test-target sweep) |
    | Resolved | 1 (RebrandInfoPlistTests.swift adds 5 `@Test` methods backing REBR-01/02/04/06/07) |
    | Withdrawn | 1 (REBR-08; code deleted post-v1.0 in `4ef30e0`) |
    | Escalated | 0 |
    | Document updates | Frontmatter `last_audited` bumped to 2026-05-05; Per-Task Map rewritten with unit rows and one WITHDRAWN row per D-03; Manual-Only Verifications section removed (D-03 lenient policy supersedes prior 2026-04-27 manual rows). |

    ### Audit Method

    - Created `PSTranscribe/Tests/PSTranscribeTests/RebrandInfoPlistTests.swift` (5 `@Test` methods: `bundleNameIsPSTranscribe`, `bundleIdentifierIsPSTranscribe`, `executableNameIsPSTranscribe`, `sparkleFeedURLPointsAtPSTranscribe`, `displayNameAndMicUsageMentionPSTranscribe`).
    - Pattern: direct file IO `URL(fileURLWithPath: "Sources/PSTranscribe/Info.plist")` + `PropertyListSerialization` (NOT `Bundle.main` — test bundle is the runner, not the app; per Phase 24 RESEARCH.md Risk #2).
    - Ran `cd PSTranscribe && swift test --filter RebrandInfoPlistTests` — exits 0, 5 passing tests.
    - Ran full `cd PSTranscribe && swift test` — exits 0.

    ### Notes

    - REBR-03 (Package.swift `name: "PSTranscribe"`) is verified at compile-time by `@testable import PSTranscribe` resolving in every test file in the target. The frontmatter `source/pstranscribe` half of REBR-03 (TranscriptLogger.swift:151) is asserted separately by Plan 24-03 (Phase 8 audit) in `FrontmatterSourceTagTests.swift` — see 08-VALIDATION.md.
    - REBR-05 (workflow file rebrand) is asserted by `WorkflowSecretsTests.swift` created in Plan 24-05 (Phase 02 audit) — cross-referenced here so Plan 24-01's audit closes atomically without waiting on 24-05's wave. The cross-reference row's status reads `pending` until 24-05 ships and is flipped at that time by the Plan 24-05 close step.
    - The 2026-04-27 audit block is preserved above for audit-trail continuity. This 2026-05-05 block supersedes the manual-only entries (REBR-01 visual UI, REBR-08 runtime migration) under D-03.
    ```

    DELTA 5 — Validation Sign-Off section (lines 76-84): Replace the line `**Approval:** approved 2026-04-27 (retroactive audit)` with `**Approval:** re-approved 2026-05-05 (Phase 24 NYQUIST-01 — Swift Testing-backed; supersedes 2026-04-27 build+grep audit per D-01)`. Update `Feedback latency < 30s` line to `Feedback latency < 90s` (the full `swift test` suite runs ~15-90s on macos-26 — preserving honesty).

    Verify the diff is exactly five deltas (frontmatter, table, Manual-Only delete, audit append, sign-off line) — no other changes.
  </action>
  <verify>
    <automated>grep -E "^last_audited:|^status:|^nyquist_compliant:" .planning/milestones/v1.0-phases/01-rebrand/01-VALIDATION.md && grep -c "Validation Audit 2026-05-05" .planning/milestones/v1.0-phases/01-rebrand/01-VALIDATION.md && grep -c "WITHDRAWN" .planning/milestones/v1.0-phases/01-rebrand/01-VALIDATION.md && grep -cE '^## Manual-Only Verifications' .planning/milestones/v1.0-phases/01-rebrand/01-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^last_audited: 2026-05-05" .planning/milestones/v1.0-phases/01-rebrand/01-VALIDATION.md` returns 1
    - `grep -c "^status: approved" .planning/milestones/v1.0-phases/01-rebrand/01-VALIDATION.md` returns 1
    - `grep -c "^nyquist_compliant: true" .planning/milestones/v1.0-phases/01-rebrand/01-VALIDATION.md` returns 1
    - `grep -c "Validation Audit 2026-05-05" .planning/milestones/v1.0-phases/01-rebrand/01-VALIDATION.md` returns 1 (new audit block appended)
    - `grep -c "Validation Audit 2026-04-27" .planning/milestones/v1.0-phases/01-rebrand/01-VALIDATION.md` returns 1 (prior block preserved)
    - `grep -cE '^## Manual-Only Verifications' .planning/milestones/v1.0-phases/01-rebrand/01-VALIDATION.md` returns 0 (section removed per D-03)
    - `grep -c "REBR-08 | WITHDRAWN" .planning/milestones/v1.0-phases/01-rebrand/01-VALIDATION.md` returns 1
    - `grep -c "Source: 01-VERIFICATION.md REBR-08" .planning/milestones/v1.0-phases/01-rebrand/01-VALIDATION.md` returns 1
    - `grep -cE 'swift test --filter RebrandInfoPlistTests/(bundleNameIsPSTranscribe|bundleIdentifierIsPSTranscribe|executableNameIsPSTranscribe|sparkleFeedURLPointsAtPSTranscribe|displayNameAndMicUsageMentionPSTranscribe)' .planning/milestones/v1.0-phases/01-rebrand/01-VALIDATION.md` returns 5 (one per REBR-01/02/04/06/07 row)
  </acceptance_criteria>
  <done>VALIDATION.md re-audited per D-01 + D-03; frontmatter date bumped, REBR-08 migrated to WITHDRAWN, Manual-Only section removed, second audit block appended preserving the 2026-04-27 audit trail.</done>
</task>

<task type="auto">
  <name>Task 3: Run full swift test locally + open PR (full-suite gate)</name>
  <files>(no file changes — verification + PR)</files>
  <read_first>
    - .planning/phases/24-nyquist-sweep-v1-0/24-RESEARCH.md (Acceptance Criteria Glossary at line 332-346 — `swift test exits 0 in PSTranscribe/`, `build-check.yml green on PR <#>`)
    - .github/workflows/build-check.yml (the CI gate this plan piggybacks on; line ~46 is the `swift test` step)
  </read_first>
  <action>
    Run the full Swift Testing suite locally and confirm green:
    ```
    cd PSTranscribe && swift test 2>&1 | tee /tmp/24-01-fullsuite.log; tail -30 /tmp/24-01-fullsuite.log
    ```
    Expected: exits 0; the suite count grew by 5 tests (RebrandInfoPlistTests). Pre-Plan-24 baseline was ~50 tests (from 24-RESEARCH.md Test Framework table); post-this-plan should be ~55.

    If the full suite is RED for reasons unrelated to RebrandInfoPlistTests (e.g., a flaky VisualRegression test from Phase 23), STOP and escalate — Phase 24's contract is "all suite green," not "my-tests-pass-others-flake." A red full suite blocks plan close.

    No PR creation in this task — the PR is opened once at the Phase 24 close (orchestrator's responsibility), not per-plan. The CI gate verification is deferred to the final plan close: ALL 5 plans' work is committed and pushed at the end of Phase 24, and the CI green check covers the entire phase.
  </action>
  <verify>
    <automated>cd PSTranscribe && swift test 2>&1 | tail -5 | grep -E "Test Suite '.*' passed|^Executed [0-9]+ tests, with 0 failures"</automated>
  </verify>
  <acceptance_criteria>
    - `cd PSTranscribe && swift test` exits 0 (full suite green)
    - Test count is at least 5 greater than pre-plan baseline (RebrandInfoPlistTests adds 5)
    - No new failures in any pre-existing test (Phase 23 VisualRegression suite remains green)
  </acceptance_criteria>
  <done>Full Swift Testing suite green locally; ready to commit + close plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → filesystem | Test reads `Sources/PSTranscribe/Info.plist` from the package root; no writes; no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01-01 | Information Disclosure | RebrandInfoPlistTests.swift reads Info.plist | accept | Info.plist is committed source; no secrets present (Sparkle EdDSA pubkey is a public key by design). No mitigation needed. |
| T-24-01-02 | Tampering | If a future commit silently changes CFBundleIdentifier away from `com.pstranscribe.app`, the rebrand contract breaks | mitigate | `bundleIdentifierIsPSTranscribe()` test asserts the exact string; CI gate enforces it. |
| T-24-01-03 | Repudiation | The 2026-04-27 audit basis (build+grep alone) becomes harder to defend over time | mitigate | The 2026-05-05 audit block is APPENDED, not replacing the 2026-04-27 block — full audit trail preserved per D-01 self-audit posture. |
</threat_model>

<verification>
- `swift test --filter RebrandInfoPlistTests` exits 0 with 5 passing tests
- Full `swift test` exits 0 (no regressions in pre-existing 50+ tests)
- `01-VALIDATION.md` frontmatter shows `last_audited: 2026-05-05`, `status: approved`, `nyquist_compliant: true`
- `01-VALIDATION.md` REBR-08 row reads `Test Type: WITHDRAWN` with the literal string `Source: 01-VERIFICATION.md REBR-08`
- `01-VALIDATION.md` no longer contains `## Manual-Only Verifications` section header
- `01-VALIDATION.md` contains both audit blocks: `Validation Audit 2026-04-27` AND `Validation Audit 2026-05-05`
- The 2026-04-27 audit block content is byte-identical to its prior state (preserved verbatim)
- No production source code changes (per D-03 — Phase 24 is audit, not fix; if the planner finds REBR-XX broken, escalate)
</verification>

<success_criteria>
1. RebrandInfoPlistTests.swift exists with 5 `@Test` methods; `swift test --filter RebrandInfoPlistTests` passes
2. 01-VALIDATION.md re-audited per D-01: `last_audited: 2026-05-05`, REBR-08 → WITHDRAWN, Manual-Only Verifications section removed
3. Full `swift test` suite green locally (no regression)
4. PROCESS-01 frontmatter (`requirements-completed: [NYQUIST-01]`) on the eventual SUMMARY.md
5. NYQUIST-01 closed: ROADMAP.md success criterion 1 ("`01-VALIDATION.md` exists for v1.0 Phase 1 with assertions covering UserDefaults migration [WITHDRAWN], bundle ID, app name, executable rename — tests run green") satisfied (with WITHDRAWN noted on UserDefaults migration per D-03)
</success_criteria>

<output>
After completion, create `.planning/phases/24-nyquist-sweep-v1-0/24-01-SUMMARY.md` with:
- Frontmatter: `requirements-completed: [NYQUIST-01]` (PROCESS-01 in effect)
- What shipped: 5 @Test methods + 01-VALIDATION.md re-audit
- D-01 referenced explicitly in the body (re-audit posture)
- D-03 referenced explicitly (Manual-Only section removal, REBR-08 WITHDRAWN)
- The 2026-04-27 audit trail preservation noted

Commit message style: NO Claude attribution (per user-level CLAUDE.md). Subject: `feat(24-01): backfill Phase 1 Rebrand Nyquist coverage (NYQUIST-01)` or similar; body explains the re-audit posture briefly.
</output>
