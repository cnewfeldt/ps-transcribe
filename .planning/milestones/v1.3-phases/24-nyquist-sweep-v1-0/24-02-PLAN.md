---
phase: 24-nyquist-sweep-v1-0
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift
  - PSTranscribe/Tests/PSTranscribeTests/RecoveredSessionTypeTests.swift
  - .planning/milestones/v1.0-phases/10-final-defect-fixes-obsidian-deeplink/10-VALIDATION.md
autonomous: true
requirements: [NYQUIST-05]
tags: [nyquist, validation, obsidian, recovered-session-type]

must_haves:
  truths:
    - "Honors D-02 (one @Test per testable requirement ID): SESS-06 (Obsidian deep-link URL) cross-referenced via existing ObsidianURLTests.swift; D-05 (recoveredType inference) → 2 @Test methods (voiceMemo + callCapture branches), one per behaviorally-distinct path"
    - "Honors D-03 (WITHDRAWN policy): D-04 view-internal switch (.cancel transition) marked Test Type: WITHDRAWN with Source: 10-VERIFICATION.md; no Manual-Only Verifications section in 10-VALIDATION.md after edit"
    - "Honors D-04 (flat test files named by behavior): RecoveredSessionTypeTests.swift placed at PSTranscribe/Tests/PSTranscribeTests/"
    - "RecoveredSessionTypeTests.swift exists with 2 @Test methods covering D-05 recoveredType inference (voiceMemo + callCapture branches)"
    - "ContentView.swift exposes a pure `recoveredSessionType(transcriptPath:vaultVoicePath:)` helper (Open Question #2 resolved: lift to free function for direct test)"
    - "swift test --filter RecoveredSessionTypeTests passes (exit 0) locally"
    - "Full swift test suite passes (exit 0) locally"
    - "10-VALIDATION.md frontmatter shows status: approved AND nyquist_compliant: true AND wave_0_complete: true AND last_audited: 2026-05-05"
    - "10-VALIDATION.md SESS-06 row cross-references existing ObsidianURLTests.swift (no new test file for SESS-06)"
    - "10-VALIDATION.md D-04 row has Test Type: WITHDRAWN with Source: 10-VERIFICATION.md (view-internal switch not unit-testable)"
    - "10-VALIDATION.md no longer contains a Manual-Only Verifications section (D-03 lenient policy)"
  artifacts:
    - path: "PSTranscribe/Tests/PSTranscribeTests/RecoveredSessionTypeTests.swift"
      provides: "Pure-function tests for the inference logic that maps a transcript path under a vault VoiceMemos prefix to .voiceMemo, else .callCapture"
      contains: "@Suite(\"RecoveredSessionTypeTests\")"
      min_lines: 30
    - path: "PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift"
      provides: "Lifted `recoveredSessionType(transcriptPath:vaultVoicePath:) -> SessionType` free function (no behavior change; mechanical extract method)"
      contains: "func recoveredSessionType("
    - path: ".planning/milestones/v1.0-phases/10-final-defect-fixes-obsidian-deeplink/10-VALIDATION.md"
      provides: "Approved validation contract for Phase 10 with unit + WITHDRAWN rows; no Manual-Only entries"
      contains: "status: approved"
  key_links:
    - from: "PSTranscribe/Tests/PSTranscribeTests/RecoveredSessionTypeTests.swift"
      to: "PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift"
      via: "@testable import PSTranscribe + direct call to recoveredSessionType(transcriptPath:vaultVoicePath:)"
      pattern: "recoveredSessionType\\("
    - from: ".planning/milestones/v1.0-phases/10-final-defect-fixes-obsidian-deeplink/10-VALIDATION.md"
      to: "PSTranscribe/Tests/PSTranscribeTests/ObsidianURLTests.swift"
      via: "Per-Task Map row for SESS-06 cites existing 8-test suite (cross-reference, no new file)"
      pattern: "swift test --filter ObsidianURLTests"
---

<objective>
Backfill `10-VALIDATION.md` (Obsidian Deep-link + Defect Cleanup) with real Swift Testing assertions and approve the contract.

Purpose: Phase 10's draft VALIDATION.md has 5 rows (3 manual). D-03 lenient policy converts the manual rows to WITHDRAWN-or-unit. The recoveredType inference at `ContentView.swift:325-331` is pure logic with no view dependency — RESEARCH Open Question #2 resolves to "lift to a free function" because it's a mechanical extract-method with zero behavior change. SESS-06 (Obsidian URL construction) is already covered by `ObsidianURLTests.swift` (8 tests); cross-reference, do not duplicate.

Output:
- 1 production source edit (extract pure helper from ContentView.swift)
- 1 new test file: `RecoveredSessionTypeTests.swift` (2 `@Test` methods)
- 1 approved VALIDATION.md: `10-VALIDATION.md` with frontmatter flipped, SESS-06 cross-referenced, D-05 unit-tested, D-04 WITHDRAWN, Manual-Only section removed
- 1 SUMMARY.md (with `requirements-completed: [NYQUIST-05]`)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-nyquist-sweep-v1-0/24-CONTEXT.md
@.planning/phases/24-nyquist-sweep-v1-0/24-RESEARCH.md
@.planning/phases/24-nyquist-sweep-v1-0/24-PATTERNS.md
@.planning/milestones/v1.0-phases/10-final-defect-fixes-obsidian-deeplink/10-VALIDATION.md
@.planning/milestones/v1.0-phases/10-final-defect-fixes-obsidian-deeplink/10-VERIFICATION.md
@PSTranscribe/Tests/PSTranscribeTests/ObsidianURLTests.swift
@PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift

<interfaces>
<!-- The recoveredType inference logic currently inline at ContentView.swift:325-331. -->
<!-- Verified via RESEARCH.md Per-Requirement API Audit Phase 10 row 10-D-05. -->

Current shape (inline in a `.task` block or computed property — read ContentView.swift:325-331 to confirm exact form):
```swift
// Inline today:
let inferredType: SessionType = checkpoint.transcriptPath.hasPrefix(settings.vaultVoicePath)
    ? .voiceMemo
    : .callCapture
```

Lifted target shape (this plan's extract method):
```swift
/// Phase 24 (NYQUIST-05): pure helper for crash-recovered session type inference.
/// Lifted from inline `.task` block to enable direct unit testing in
/// RecoveredSessionTypeTests.swift. No behavior change — same prefix check, same fallback.
func recoveredSessionType(transcriptPath: String, vaultVoicePath: String) -> SessionType {
    transcriptPath.hasPrefix(vaultVoicePath) ? .voiceMemo : .callCapture
}
```

The function lives at file scope in `ContentView.swift` (or in a small extension), accessible from the test target via `@testable import PSTranscribe`. SessionType is the existing enum from Models.swift (verify via grep before lifting).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract `recoveredSessionType(transcriptPath:vaultVoicePath:)` from ContentView.swift inline logic</name>
  <files>PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift</files>
  <read_first>
    - PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift (lines 320-340 — the inline `.task` block where the inference currently lives; confirm exact line and surrounding context BEFORE editing)
    - PSTranscribe/Sources/PSTranscribe/Models/Models.swift (confirm `SessionType` enum signature — used by the new free function's return type)
    - .planning/phases/24-nyquist-sweep-v1-0/24-RESEARCH.md (Open Question #2 at line 460-463 — Recommendation: "Lift it. The lift is mechanical, no behavior change..."; Per-Requirement API Audit Phase 10 row 10-D-05 at line 197 — recoveredType inference logic location)
    - .planning/phases/24-nyquist-sweep-v1-0/24-PATTERNS.md (lines 307-335 — Pattern Assignments § RecoveredSessionTypeTests; bias: lift to free function, NOT a static method on a type)
  </read_first>
  <behavior>
    The lifted function preserves the existing inference: given a `transcriptPath` (e.g., `/Users/cary/Vault/VoiceMemos/2026-04-07-foo.md`) and a `vaultVoicePath` (e.g., `/Users/cary/Vault/VoiceMemos`), if `transcriptPath.hasPrefix(vaultVoicePath)` returns true, the inferred type is `.voiceMemo`; otherwise `.callCapture`. NO behavior change — this is a mechanical extract-method.
    Test will follow in Task 2 — this task only lifts the helper.
  </behavior>
  <action>
    Read `PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift` lines 300-340 (covering the `.task` block at 317 and the inference at 325-331 per RESEARCH.md). Confirm the exact inline shape.

    Use the Edit tool to:
    1. Add a file-scope free function (above `struct ContentView` or just inside the `import` block) with this exact signature and body:
       ```swift
       /// Phase 24 (NYQUIST-05): pure helper for crash-recovered session type inference.
       /// Lifted from inline `.task` block to enable direct unit testing in
       /// RecoveredSessionTypeTests.swift. No behavior change — same prefix check, same fallback.
       /// Visibility is `internal` (default) so `@testable import PSTranscribe` reaches it.
       func recoveredSessionType(transcriptPath: String, vaultVoicePath: String) -> SessionType {
           transcriptPath.hasPrefix(vaultVoicePath) ? .voiceMemo : .callCapture
       }
       ```
    2. Replace the inline ternary at the existing call site (the `.task` block around line 325-331) with a call to the new free function:
       ```swift
       let inferredType = recoveredSessionType(
           transcriptPath: checkpoint.transcriptPath,
           vaultVoicePath: settings.vaultVoicePath
       )
       ```
       (Variable name `inferredType` may differ — preserve the existing local binding name; the change is purely the RHS.)

    DO NOT change visibility (leave at default `internal` — `@testable import` reaches it). DO NOT add `static` (D-04 prefers free function over static method per RESEARCH Open Question #2). DO NOT add fileprivate.

    Verify build:
    ```
    cd PSTranscribe && swift build 2>&1 | tail -10
    ```
    Must exit 0. If it doesn't, the most likely cause is `SessionType` being declared in a way that requires explicit qualification — qualify with the appropriate type prefix and re-build.
  </action>
  <verify>
    <automated>cd PSTranscribe && swift build 2>&1 | tail -5 && grep -c "func recoveredSessionType(transcriptPath:" PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift && grep -c "recoveredSessionType(" PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift</automated>
  </verify>
  <acceptance_criteria>
    - `cd PSTranscribe && swift build` exits 0 (no new compile errors)
    - Function declaration present: `grep -c '^func recoveredSessionType(transcriptPath: String, vaultVoicePath: String) -> SessionType {' PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift` returns 1
    - Function called from inside ContentView: `grep -c 'recoveredSessionType(' PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift` returns at least 2 (declaration + at least one call site)
    - Inline ternary at the original site is gone: `grep -cE 'transcriptPath\.hasPrefix\(.*vaultVoicePath.*\) \? \.voiceMemo : \.callCapture' PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift` returns 0 (the only place this expression now lives is inside the new function body)
    - Phase 24 attribution comment present: `grep -c "Phase 24 (NYQUIST-05)" PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift` returns 1
  </acceptance_criteria>
  <done>Pure helper extracted, build green, no behavior change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create RecoveredSessionTypeTests.swift with 2 @Test methods (D-05 inference)</name>
  <files>PSTranscribe/Tests/PSTranscribeTests/RecoveredSessionTypeTests.swift</files>
  <read_first>
    - PSTranscribe/Tests/PSTranscribeTests/ObsidianURLTests.swift (canonical pure-function analog; copy the `@Suite` + `@Test` shape verbatim)
    - PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift (Task 1 just added the helper; confirm signature is exactly `func recoveredSessionType(transcriptPath: String, vaultVoicePath: String) -> SessionType`)
    - .planning/phases/24-nyquist-sweep-v1-0/24-PATTERNS.md (lines 307-335 — Pattern Assignments § RecoveredSessionTypeTests; lines 312-329 give exact test shape to copy)
  </read_first>
  <behavior>
    Test 1 (10-D-05 happy path): `voiceMemoPathInferredFromVaultPrefix()` — given `transcriptPath` under `vaultVoicePath`, function returns `.voiceMemo`.
    Test 2 (10-D-05 fallback): `callCaptureFallbackWhenNotUnderVaultPrefix()` — given `transcriptPath` NOT under `vaultVoicePath`, function returns `.callCapture`.
  </behavior>
  <action>
    Create `PSTranscribe/Tests/PSTranscribeTests/RecoveredSessionTypeTests.swift` with exactly:
    ```swift
    // RecoveredSessionTypeTests
    // Phase 24 (NYQUIST-05) — Phase 10 D-05: crash-recovered session type icon inference.
    //
    // Asserts the pure-function helper extracted from ContentView.swift in Plan 24-02 Task 1.
    // The helper maps a transcript path under a vault VoiceMemos prefix to .voiceMemo, else
    // .callCapture. No view harness; pure string-prefix logic.

    import Testing
    import Foundation
    @testable import PSTranscribe

    @Suite("RecoveredSessionTypeTests")
    struct RecoveredSessionTypeTests {

        @Test func voiceMemoPathInferredFromVaultPrefix() {
            let result = recoveredSessionType(
                transcriptPath: "/Users/cary/Vault/VoiceMemos/2026-04-07-foo.md",
                vaultVoicePath: "/Users/cary/Vault/VoiceMemos"
            )
            #expect(result == .voiceMemo)
        }

        @Test func callCaptureFallbackWhenNotUnderVaultPrefix() {
            let result = recoveredSessionType(
                transcriptPath: "/Users/cary/Vault/Calls/2026-04-07-bar.md",
                vaultVoicePath: "/Users/cary/Vault/VoiceMemos"
            )
            #expect(result == .callCapture)
        }
    }
    ```

    No `.serialized` (pure function, no shared state). No `@MainActor`. The `@testable import PSTranscribe` is required because `recoveredSessionType` is `internal` (default) by Task 1's design.

    Run the focused suite:
    ```
    cd PSTranscribe && swift test --filter RecoveredSessionTypeTests
    ```
    Both tests must pass. If `result == .voiceMemo` fails, the prefix check has a subtle bug (e.g., trailing slash mismatch); STOP and inspect the helper's exact behavior — DO NOT modify the test to accept a wrong answer.
  </action>
  <verify>
    <automated>cd PSTranscribe && swift test --filter RecoveredSessionTypeTests 2>&1 | tee /tmp/24-02-task2.log; tail -10 /tmp/24-02-task2.log</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f PSTranscribe/Tests/PSTranscribeTests/RecoveredSessionTypeTests.swift`
    - Contains both `@Test func` declarations: `grep -cE '@Test func (voiceMemoPathInferredFromVaultPrefix|callCaptureFallbackWhenNotUnderVaultPrefix)\(' PSTranscribe/Tests/PSTranscribeTests/RecoveredSessionTypeTests.swift` returns 2
    - `@testable import PSTranscribe` present: `grep -c '^@testable import PSTranscribe' PSTranscribe/Tests/PSTranscribeTests/RecoveredSessionTypeTests.swift` returns 1
    - Header comment cites NYQUIST-05: `grep -c "Phase 24 (NYQUIST-05)" PSTranscribe/Tests/PSTranscribeTests/RecoveredSessionTypeTests.swift` returns 1
    - `cd PSTranscribe && swift test --filter RecoveredSessionTypeTests` exits 0 (2 passing, 0 failures)
  </acceptance_criteria>
  <done>2 @Test methods written, suite passes locally.</done>
</task>

<task type="auto">
  <name>Task 3: Approve 10-VALIDATION.md (D-03 lenient — flip frontmatter, cross-reference SESS-06, WITHDRAW D-04)</name>
  <files>.planning/milestones/v1.0-phases/10-final-defect-fixes-obsidian-deeplink/10-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v1.0-phases/10-final-defect-fixes-obsidian-deeplink/10-VALIDATION.md (existing draft — frontmatter has `status: draft`, `nyquist_compliant: false`, `wave_0_complete: false`; we flip all three)
    - .planning/milestones/v1.0-phases/10-final-defect-fixes-obsidian-deeplink/10-VERIFICATION.md (sourced as `Source: 10-VERIFICATION.md` for the WITHDRAWN row reasons)
    - PSTranscribe/Tests/PSTranscribeTests/ObsidianURLTests.swift (count tests: should match RESEARCH.md claim of "8 tests"; cross-reference cell needs the exact suite name)
    - .planning/phases/24-nyquist-sweep-v1-0/24-PATTERNS.md (VALIDATION.md Edit Pattern Assignments at lines 388-458 — frontmatter shape, WITHDRAWN row format)
  </read_first>
  <action>
    Use the Edit tool to perform the following deltas on `.planning/milestones/v1.0-phases/10-final-defect-fixes-obsidian-deeplink/10-VALIDATION.md`:

    DELTA 1 — Frontmatter (lines 1-8):
    - Line 4: `status: draft` → `status: approved`
    - Line 5: `nyquist_compliant: false` → `nyquist_compliant: true`
    - Line 6: `wave_0_complete: false` → `wave_0_complete: true`
    - Add `last_audited: 2026-05-05` after `created: 2026-04-07`

    DELTA 2 — Per-Task Verification Map (lines 38-46): Replace the existing 5-row table with this 4-row table (D-03 lenient: SESS-06 cross-references existing tests, D-04 becomes WITHDRAWN, D-05 gets the new RecoveredSessionTypeTests):

    ```markdown
    | Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status | Notes |
    |---------|------|------|-------------|-----------|-------------------|-------------|--------|-------|
    | 24-02-01 | 24-02 | 1 | SESS-06 | unit | `cd PSTranscribe && swift test --filter ObsidianURLTests` | ✅ existing | green | Cross-reference: existing 8 `@Test` methods in `ObsidianURLTests.swift` cover `makeObsidianURL` + `obsidianVaultForPath`; no new test added in Plan 24-02 |
    | 24-02-02 | 24-02 | 1 | D-05 (recoveredType inference) | unit | `cd PSTranscribe && swift test --filter RecoveredSessionTypeTests/voiceMemoPathInferredFromVaultPrefix` | ✅ new | green | Plan 24-02 Task 2 added `RecoveredSessionTypeTests.swift`; helper lifted from `ContentView.swift` `.task` block (mechanical extract method, no behavior change) |
    | 24-02-03 | 24-02 | 1 | D-05 (recoveredType inference) | unit | `cd PSTranscribe && swift test --filter RecoveredSessionTypeTests/callCaptureFallbackWhenNotUnderVaultPrefix` | ✅ new | green | Plan 24-02 Task 2 fallback branch |
    | 24-02-04 | 24-02 | 1 | D-04 (exhaustive Speaker switch in removeUtterance) | WITHDRAWN | n/a — WITHDRAWN | n/a | withdrawn | View-internal `case .you / .them / .named(let lbl)` switch in `ContentView.swift:472-476` is not cleanly unit-testable without a SwiftUI view harness. Compile-time exhaustiveness is enforced by Swift; runtime behavior was verified at v1.0 ship. Source: 10-VERIFICATION.md D-04 row. |
    ```

    DELTA 3 — Manual-Only Verifications section (lines 60-67): DELETE the entire section (header + table). Per D-03, no Manual-Only rows. The three rows that previously lived there:
    - "Obsidian opens to correct note (SESS-06)" — covered by `ObsidianURLTests.swift` URL construction (the open-in-Obsidian launch is OS-mediated and not testable from Swift Testing; the URL we construct IS tested).
    - "Disabled menu tooltip shows (D-03)" — note: this is the *Phase 10's* internal D-03 (a phase-decision label inside Phase 10), not the Phase 24 D-03 lenient policy. The tooltip is SwiftUI rendering; covered by Phase 23 snapshot tests if applicable, otherwise out of unit-test scope. Add a row in the Audit `### Notes` cross-referencing.
    - "Crash-recovered session type icon (D-05)" — NOW covered by `RecoveredSessionTypeTests.swift` (this plan).

    DELTA 4 — Wave 0 Requirements section (lines 51-57): Replace with:
    ```markdown
    ## Wave 0 Requirements

    - [x] `PSTranscribe/Tests/PSTranscribeTests/ObsidianURLTests.swift` — 8 `@Test` methods covering SESS-06 (already shipped pre-Phase-24)
    - [x] `PSTranscribe/Tests/PSTranscribeTests/RecoveredSessionTypeTests.swift` — 2 `@Test` methods covering D-05 inference (added in Plan 24-02)

    **Wave 0 complete.**
    ```

    DELTA 5 — Append a new audit block AFTER the Validation Sign-Off section (or before "**Approval:**" line — preserve the original draft's Sign-Off list, then add the audit block, then update the Approval line):

    ```markdown

    ---

    ## Validation Audit 2026-05-05 (Phase 24 — NYQUIST-05)

    | Metric | Count |
    |--------|-------|
    | Gaps found | 1 (Phase 10 draft VALIDATION.md had 3 manual rows + 1 partial) |
    | Resolved | 2 (D-05 inference now unit-tested via `RecoveredSessionTypeTests`; SESS-06 cross-referenced to pre-existing `ObsidianURLTests`) |
    | Withdrawn | 1 (D-04 exhaustive switch — view-internal, compile-time-checked, not unit-testable) |
    | Escalated | 0 |
    | Document updates | Frontmatter flipped to approved/true/true; `last_audited: 2026-05-05` added; Per-Task Map rewritten with unit + WITHDRAWN rows; Manual-Only Verifications section removed (D-03 lenient policy). |

    ### Audit Method

    - Lifted `recoveredSessionType(transcriptPath:vaultVoicePath:)` from inline `ContentView.swift:325-331` to a file-scope free function. Mechanical extract method, no behavior change. (Plan 24-02 Task 1.)
    - Created `PSTranscribe/Tests/PSTranscribeTests/RecoveredSessionTypeTests.swift` with 2 `@Test` methods (`voiceMemoPathInferredFromVaultPrefix`, `callCaptureFallbackWhenNotUnderVaultPrefix`). Plan 24-02 Task 2.
    - Cross-referenced existing `ObsidianURLTests.swift` (8 `@Test` methods, shipped pre-Phase-24, covers `makeObsidianURL` + `obsidianVaultForPath`) for SESS-06 instead of duplicating tests.
    - Ran `cd PSTranscribe && swift test --filter RecoveredSessionTypeTests` → 2 passing.
    - Ran `cd PSTranscribe && swift test --filter ObsidianURLTests` → 8 passing.
    - Ran full `cd PSTranscribe && swift test` → exits 0.

    ### Notes

    - SESS-06 cross-referenced rather than duplicated — existing `ObsidianURLTests.swift` is the source of truth for Obsidian URL construction. Adding a new behavior-named file here would mean two suites covering the same behavior.
    - Phase 10 internal D-03 (disabled-menu tooltip) is a SwiftUI rendering concern. Phase 23's snapshot tests cover `LibraryEntryRow` rendering across appearances; if the tooltip becomes a regression target, file a Phase 23 follow-up to extend the snapshot fixtures.
    - Phase 10 internal D-04 (exhaustive Speaker switch) is enforced by Swift's compile-time exhaustiveness checker. Runtime behavior was verified at v1.0 ship in `10-VERIFICATION.md`. WITHDRAWN here means "not unit-testable from this target," not "not enforced" — the language enforces it.
    ```

    DELTA 6 — Validation Sign-Off section (lines 70-78):
    - Update each `[ ]` to `[x]` (all bullets satisfied per the audit above)
    - Update `Feedback latency < 15s` to `Feedback latency < 90s` (full suite runtime; preserves honesty post-Phase-23)
    - Replace `**Approval:** pending` with `**Approval:** approved 2026-05-05 (Phase 24 NYQUIST-05)`
  </action>
  <verify>
    <automated>grep -E "^last_audited:|^status:|^nyquist_compliant:|^wave_0_complete:" .planning/milestones/v1.0-phases/10-final-defect-fixes-obsidian-deeplink/10-VALIDATION.md && grep -c "WITHDRAWN" .planning/milestones/v1.0-phases/10-final-defect-fixes-obsidian-deeplink/10-VALIDATION.md && grep -cE '^## Manual-Only Verifications' .planning/milestones/v1.0-phases/10-final-defect-fixes-obsidian-deeplink/10-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^status: approved" .planning/milestones/v1.0-phases/10-final-defect-fixes-obsidian-deeplink/10-VALIDATION.md` returns 1
    - `grep -c "^nyquist_compliant: true" .planning/milestones/v1.0-phases/10-final-defect-fixes-obsidian-deeplink/10-VALIDATION.md` returns 1
    - `grep -c "^wave_0_complete: true" .planning/milestones/v1.0-phases/10-final-defect-fixes-obsidian-deeplink/10-VALIDATION.md` returns 1
    - `grep -c "^last_audited: 2026-05-05" .planning/milestones/v1.0-phases/10-final-defect-fixes-obsidian-deeplink/10-VALIDATION.md` returns 1
    - `grep -cE '^## Manual-Only Verifications' .planning/milestones/v1.0-phases/10-final-defect-fixes-obsidian-deeplink/10-VALIDATION.md` returns 0 (section removed)
    - `grep -c "swift test --filter RecoveredSessionTypeTests" .planning/milestones/v1.0-phases/10-final-defect-fixes-obsidian-deeplink/10-VALIDATION.md` returns at least 2
    - `grep -c "swift test --filter ObsidianURLTests" .planning/milestones/v1.0-phases/10-final-defect-fixes-obsidian-deeplink/10-VALIDATION.md` returns at least 1 (SESS-06 cross-ref)
    - `grep -c "Source: 10-VERIFICATION.md" .planning/milestones/v1.0-phases/10-final-defect-fixes-obsidian-deeplink/10-VALIDATION.md` returns at least 1 (D-04 WITHDRAWN row)
    - `grep -c "Validation Audit 2026-05-05" .planning/milestones/v1.0-phases/10-final-defect-fixes-obsidian-deeplink/10-VALIDATION.md` returns 1
  </acceptance_criteria>
  <done>10-VALIDATION.md approved per D-03; SESS-06 cross-referenced; D-05 unit-tested; D-04 WITHDRAWN; Manual-Only section removed.</done>
</task>

<task type="auto">
  <name>Task 4: Run full swift test locally (full-suite gate)</name>
  <files>(no file changes — verification)</files>
  <read_first>
    - .planning/phases/24-nyquist-sweep-v1-0/24-RESEARCH.md (Acceptance Criteria Glossary)
  </read_first>
  <action>
    ```
    cd PSTranscribe && swift test 2>&1 | tee /tmp/24-02-fullsuite.log; tail -30 /tmp/24-02-fullsuite.log
    ```
    Expected: exits 0; suite count grew by 2 tests vs. Plan 24-01's baseline (RecoveredSessionTypeTests).
    If full suite is RED for unrelated reasons, STOP and escalate.
  </action>
  <verify>
    <automated>cd PSTranscribe && swift test 2>&1 | tail -5 | grep -E "Test Suite '.*' passed|^Executed [0-9]+ tests, with 0 failures"</automated>
  </verify>
  <acceptance_criteria>
    - `cd PSTranscribe && swift test` exits 0
    - Suite count grew by exactly 2 (RecoveredSessionTypeTests) vs. Plan 24-01 close
    - No new failures in any pre-existing test
  </acceptance_criteria>
  <done>Full Swift Testing suite green locally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → ContentView.swift internal symbols | `@testable import PSTranscribe` reaches `internal` free function `recoveredSessionType` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02-01 | Tampering | Lifting `recoveredSessionType` from inline could change semantics if the original ternary had a subtle quirk we missed | mitigate | Read ContentView.swift:300-340 BEFORE editing (read_first); preserve EXACT ternary behavior (`hasPrefix` check, `.voiceMemo` truthy branch, `.callCapture` falsy branch). Compile + test gates catch any divergence. |
| T-24-02-02 | Information Disclosure | Test exposes the internal helper signature externally | accept | Internal visibility (default) — `@testable import` is the test-target-only mechanism. No external API surface change. |
| T-24-02-03 | Denial of Service (compile) | If `SessionType` requires explicit qualification or differs from inferred return type, build breaks | mitigate | Run `swift build` immediately after the edit; if red, qualify the type or back out the lift and use indirect testing instead (per RESEARCH.md A8 fallback). |
</threat_model>

<verification>
- `recoveredSessionType` is a file-scope free function in ContentView.swift with `internal` visibility
- `RecoveredSessionTypeTests.swift` has 2 passing `@Test` methods
- `swift test --filter RecoveredSessionTypeTests` exits 0
- Full `swift test` exits 0
- 10-VALIDATION.md frontmatter: `status: approved`, `nyquist_compliant: true`, `wave_0_complete: true`, `last_audited: 2026-05-05`
- 10-VALIDATION.md SESS-06 row cites `swift test --filter ObsidianURLTests` (cross-reference, no new file)
- 10-VALIDATION.md D-04 row reads `Test Type: WITHDRAWN` with `Source: 10-VERIFICATION.md` pointer
- 10-VALIDATION.md no longer contains `## Manual-Only Verifications` section
- The lift in ContentView.swift introduced ZERO behavior changes (mechanical extract)
</verification>

<success_criteria>
1. RecoveredSessionTypeTests.swift exists with 2 passing `@Test` methods
2. ContentView.swift exposes `recoveredSessionType(transcriptPath:vaultVoicePath:) -> SessionType` as a file-scope free function
3. 10-VALIDATION.md frontmatter approved per D-03 (no Manual-Only rows; SESS-06 cross-referenced; D-04 WITHDRAWN; D-05 unit-tested)
4. PROCESS-01 frontmatter on SUMMARY.md
5. NYQUIST-05 closed: ROADMAP.md success criterion 5 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/24-nyquist-sweep-v1-0/24-02-SUMMARY.md` with:
- Frontmatter: `requirements-completed: [NYQUIST-05]`
- Body: notes the lift from inline ternary to free function (Open Question #2 resolution); D-04 WITHDRAWN with reason; SESS-06 cross-reference
- Mention that this is the only Phase 24 plan touching production source (ContentView.swift) — and the change is a mechanical extract method with zero behavior change

Commit message style: NO Claude attribution. Subject: `feat(24-02): backfill Phase 10 Obsidian Nyquist coverage (NYQUIST-05)` or similar.
</output>
