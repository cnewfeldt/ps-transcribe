---
phase: 24-nyquist-sweep-v1-0
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - PSTranscribe/Tests/PSTranscribeTests/TranscriptStoreClearTests.swift
  - PSTranscribe/Tests/PSTranscribeTests/FrontmatterSourceTagTests.swift
  - .planning/milestones/v1.0-phases/08-code-defect-fixes/08-VALIDATION.md
autonomous: true
requirements: [NYQUIST-04]
tags: [nyquist, validation, defects, transcript-store, frontmatter-source-tag]

must_haves:
  truths:
    - "Honors D-02 (one @Test per testable requirement ID): DEFC clear() and source-tag rows each get a single @Test; existing SpeakerCodableTests + TranscriptParserTests are cross-referenced for already-covered behaviors (no duplicate test creation)"
    - "Honors D-03 (WITHDRAWN policy): D-04 LibraryEntryRow file-exists caching and STAB-01 e2e crash recovery marked Test Type: WITHDRAWN with Source: 08-VERIFICATION.md; no Manual-Only Verifications section in 08-VALIDATION.md after edit"
    - "Honors D-04 (flat test files named by behavior): TranscriptStoreClearTests.swift + FrontmatterSourceTagTests.swift placed at PSTranscribe/Tests/PSTranscribeTests/"
    - "TranscriptStoreClearTests.swift exists with 1 @Test method asserting transcriptStore.clear() empties accumulated state"
    - "FrontmatterSourceTagTests.swift exists with 1 @Test method asserting finalized transcript frontmatter contains '- source/pstranscribe' and NOT 'source/tome'"
    - "swift test --filter TranscriptStoreClearTests passes (exit 0)"
    - "swift test --filter FrontmatterSourceTagTests passes (exit 0)"
    - "Full swift test suite passes (exit 0) locally"
    - "08-VALIDATION.md frontmatter shows status: approved AND nyquist_compliant: true AND wave_0_complete: true AND last_audited: 2026-05-05"
    - "08-VALIDATION.md D-01 (Speaker.named) cross-references existing SpeakerCodableTests.swift + TranscriptParserTests.swift"
    - "08-VALIDATION.md D-04 LibraryEntryRow file-exists caching has Test Type: WITHDRAWN with Source: 08-VERIFICATION.md"
    - "08-VALIDATION.md STAB-01 end-to-end has Test Type: WITHDRAWN with Source: 08-VERIFICATION.md (force-quit out of scope per Phase 24 D-03)"
    - "08-VALIDATION.md no longer contains a Manual-Only Verifications section"
  artifacts:
    - path: "PSTranscribe/Tests/PSTranscribeTests/TranscriptStoreClearTests.swift"
      provides: "Direct test of TranscriptStore.clear() — appends utterances, calls clear(), asserts state is empty"
      contains: "@Suite(\"TranscriptStoreClearTests\")"
      min_lines: 25
    - path: "PSTranscribe/Tests/PSTranscribeTests/FrontmatterSourceTagTests.swift"
      provides: "End-to-end test that TranscriptLogger.startSession + endSession + finalizeFrontmatter writes '- source/pstranscribe' (NOT 'source/tome') in YAML frontmatter"
      contains: "@Suite(\"FrontmatterSourceTagTests\""
      min_lines: 35
    - path: ".planning/milestones/v1.0-phases/08-code-defect-fixes/08-VALIDATION.md"
      provides: "Approved validation contract for Phase 8 with unit + WITHDRAWN rows"
      contains: "status: approved"
  key_links:
    - from: "PSTranscribe/Tests/PSTranscribeTests/TranscriptStoreClearTests.swift"
      to: "PSTranscribe/Sources/PSTranscribe/Models/TranscriptStore.swift (or wherever TranscriptStore lives)"
      via: "@testable import PSTranscribe + direct instantiation + clear() call"
      pattern: "transcriptStore\\.clear\\("
    - from: "PSTranscribe/Tests/PSTranscribeTests/FrontmatterSourceTagTests.swift"
      to: "PSTranscribe/Sources/PSTranscribe/Storage/TranscriptLogger.swift line 151 (- source/pstranscribe)"
      via: "TranscriptLogger.startSession + endSession + finalizeFrontmatter round-trip; read finalized file as String; substring assertion"
      pattern: "source/pstranscribe"
    - from: ".planning/milestones/v1.0-phases/08-code-defect-fixes/08-VALIDATION.md"
      to: "PSTranscribe/Tests/PSTranscribeTests/SpeakerCodableTests.swift + TranscriptParserTests.swift"
      via: "Per-Task Map row for D-01 cross-references existing 6+2 test methods"
      pattern: "swift test --filter (SpeakerCodableTests|TranscriptParserTests)"
---

<objective>
Backfill `08-VALIDATION.md` (Code Defect Fixes) with real Swift Testing assertions and approve the contract.

Purpose: Phase 8's draft VALIDATION.md has 6 rows (1 manual). D-01 (Speaker.named codable) is already covered by `SpeakerCodableTests.swift` (6 tests) + `TranscriptParserTests.swift` (2 tests) — cross-reference. STAB-03 (`transcriptStore.clear()` on stop) is unit-testable by direct TranscriptStore instantiation. REBR-03 (frontmatter `source/pstranscribe`) is unit-testable via TranscriptLogger round-trip. STAB-01 end-to-end (crash recovery) is WITHDRAWN per Phase 24 D-03 (force-quit out of scope). LibraryEntryRow file-exists caching is WITHDRAWN (SwiftUI lifecycle).

Output:
- 1 new test file: `TranscriptStoreClearTests.swift` (1 `@Test`)
- 1 new test file: `FrontmatterSourceTagTests.swift` (1 `@Test`)
- 1 approved VALIDATION.md: `08-VALIDATION.md`
- 1 SUMMARY.md (with `requirements-completed: [NYQUIST-04]`)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-nyquist-sweep-v1-0/24-CONTEXT.md
@.planning/phases/24-nyquist-sweep-v1-0/24-RESEARCH.md
@.planning/phases/24-nyquist-sweep-v1-0/24-PATTERNS.md
@.planning/milestones/v1.0-phases/08-code-defect-fixes/08-VALIDATION.md
@.planning/milestones/v1.0-phases/08-code-defect-fixes/08-VERIFICATION.md
@PSTranscribe/Tests/PSTranscribeTests/DictationLoggerTests.swift
@PSTranscribe/Tests/PSTranscribeTests/SpeakerCodableTests.swift
@PSTranscribe/Tests/PSTranscribeTests/TranscriptParserTests.swift

<interfaces>
<!-- TranscriptStore: needs to be discovered during read_first (RESEARCH.md Risk #11). -->
<!-- Spot-checked claim: `@Observable @MainActor final class TranscriptStore` per RESEARCH.md A6. -->
<!-- Verify the exact API surface (init signature, mutation methods, state accessor) BEFORE writing the test. -->

Likely TranscriptStore shape (verify via grep):
```swift
@Observable
@MainActor
final class TranscriptStore {
    var utterances: [Utterance] = []   // or similar
    func append(_ utterance: Utterance) { ... }
    func clear() {
        utterances.removeAll()
    }
}
```

TranscriptLogger frontmatter assertion shape (from RESEARCH.md, lines 184-185 — verified at TranscriptLogger.swift:151):
```
- source/pstranscribe          # MUST appear in finalized frontmatter
- source/tome                  # MUST NOT appear (rebrand invariant)
```

End-to-end round-trip pattern (from DictationLoggerTests.swift lines 8-33, adapted for TranscriptLogger):
```swift
let dir = try tempDir()
defer { try? FileManager.default.removeItem(at: dir) }
let logger = TranscriptLogger()
try await logger.startSession(sourceApp: ..., vaultPath: dir.path, ...)
// append at least one utterance so frontmatter has content to finalize
try await logger.appendUtterance(text: "test", timestamp: Date())
let url = await logger.endSession()
await logger.finalizeFrontmatter(...)
let contents = try String(contentsOf: url!, encoding: .utf8)
#expect(contents.contains("- source/pstranscribe"))
#expect(!contents.contains("source/tome"))
```

The exact `TranscriptLogger.startSession` parameter list — verify via grep `func startSession` in `PSTranscribe/Sources/PSTranscribe/Storage/TranscriptLogger.swift` BEFORE writing the test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TranscriptStoreClearTests.swift (STAB-03 — clear() empties state)</name>
  <files>PSTranscribe/Tests/PSTranscribeTests/TranscriptStoreClearTests.swift</files>
  <read_first>
    - PSTranscribe/Sources/PSTranscribe/ (grep for `class TranscriptStore` — confirm location, init signature, append + clear methods, the state accessor name (`utterances` / `entries` / something else); RESEARCH.md A6 spot-checked but final API surface is verify-on-read)
    - PSTranscribe/Tests/PSTranscribeTests/AppSettingsTests.swift (canonical `@MainActor`-isolated test analog if TranscriptStore is `@MainActor`; copy the `@Test @MainActor` shape AND `@Suite(.serialized)` if needed)
    - PSTranscribe/Tests/PSTranscribeTests/SessionCoordinatorTests.swift (sibling test for an `@Observable @MainActor` class; shows construction shape)
    - .planning/phases/24-nyquist-sweep-v1-0/24-RESEARCH.md (Risk #11 at line 410 — "Spot-checked existing source — TranscriptStore is `@Observable @MainActor final class TranscriptStore`. Constructable. Verify in plan-phase exec.")
  </read_first>
  <behavior>
    Test 1 (STAB-03): `clearEmptiesAccumulatedState()` — instantiate `TranscriptStore`, append 2-3 utterances (use whatever the public mutation method is — likely `append(_:)` or similar), assert state is non-empty, call `.clear()`, assert state is empty.
  </behavior>
  <action>
    STEP 1 — Verify TranscriptStore API surface. Grep first:
    ```
    grep -rn "class TranscriptStore" PSTranscribe/Sources/PSTranscribe/
    grep -rn "transcriptStore\." PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift | head -20
    ```
    Read the `TranscriptStore` source file fully. Note:
    - The init signature (does it take args or is it bare `init()`?)
    - The state accessor name (`utterances` / `entries` / `messages` / etc.)
    - The append method signature (`func append(_ utterance: Utterance)` / `appendUtterance(_:)` / etc.)
    - Whether `@MainActor`-isolated (you'll need `@Test @MainActor` + likely `@Suite(.serialized)` if mutating shared state)

    STEP 2 — Create `PSTranscribe/Tests/PSTranscribeTests/TranscriptStoreClearTests.swift`. Skeleton (adapt to actual API surface from STEP 1):
    ```swift
    // TranscriptStoreClearTests
    // Phase 24 (NYQUIST-04) — Phase 8 STAB-03: stopSession clears transcriptStore state.
    //
    // Asserts that calling .clear() on a populated TranscriptStore empties its accumulated
    // state. ContentView.swift:526 (start) and :665 (stop) call this; the unit test isolates
    // the data-store behavior from the view lifecycle.

    import Testing
    import Foundation
    @testable import PSTranscribe

    @Suite("TranscriptStoreClearTests", .serialized)  // remove .serialized if TranscriptStore is not @MainActor
    @MainActor
    struct TranscriptStoreClearTests {

        @Test func clearEmptiesAccumulatedState() {
            let store = TranscriptStore()
            // append 2-3 items via the discovered API
            // (e.g.) store.append(Utterance(speaker: .you, text: "hi", offset: 0))
            // (e.g.) store.append(Utterance(speaker: .them, text: "hello", offset: 5))
            #expect(!store.utterances.isEmpty)  // adjust accessor name per STEP 1
            store.clear()
            #expect(store.utterances.isEmpty)
        }
    }
    ```

    Important: you may need to adjust:
    - `@Suite("TranscriptStoreClearTests", .serialized)` → drop `.serialized` if TranscriptStore is not `@MainActor` (pure-data class). If `@MainActor`, keep `.serialized` AND `@MainActor` on the suite struct.
    - `store.utterances` → real accessor name from STEP 1.
    - The `Utterance(...)` constructor call — verify the fixture shape via `grep -n "struct Utterance" PSTranscribe/Sources/PSTranscribe/`.
    - If TranscriptStore's append takes a different argument shape, adapt accordingly.

    STEP 3 — Run the focused suite:
    ```
    cd PSTranscribe && swift test --filter TranscriptStoreClearTests
    ```
    Must exit 0 with 1 passing test. If it fails because of an API mismatch (wrong accessor / wrong append signature), correct the test to match the real API — do NOT change the production source.
  </action>
  <verify>
    <automated>cd PSTranscribe && swift test --filter TranscriptStoreClearTests 2>&1 | tee /tmp/24-03-task1.log; tail -10 /tmp/24-03-task1.log</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f PSTranscribe/Tests/PSTranscribeTests/TranscriptStoreClearTests.swift`
    - Contains the test method: `grep -c '@Test func clearEmptiesAccumulatedState()' PSTranscribe/Tests/PSTranscribeTests/TranscriptStoreClearTests.swift` returns 1
    - Calls `.clear()`: `grep -c 'store\.clear()' PSTranscribe/Tests/PSTranscribeTests/TranscriptStoreClearTests.swift` returns at least 1
    - Header cites NYQUIST-04: `grep -c "Phase 24 (NYQUIST-04)" PSTranscribe/Tests/PSTranscribeTests/TranscriptStoreClearTests.swift` returns 1
    - `cd PSTranscribe && swift test --filter TranscriptStoreClearTests` exits 0 (1 passing)
  </acceptance_criteria>
  <done>1 @Test method written, suite passes locally.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create FrontmatterSourceTagTests.swift (REBR-03 phase-8 closure — finalized frontmatter contains '- source/pstranscribe', NOT 'source/tome')</name>
  <files>PSTranscribe/Tests/PSTranscribeTests/FrontmatterSourceTagTests.swift</files>
  <read_first>
    - PSTranscribe/Tests/PSTranscribeTests/DictationLoggerTests.swift (canonical analog: `@Suite(.serialized)`, `tempDir()`, defer cleanup, full session round-trip, file-content substring assertion at lines 25-32)
    - PSTranscribe/Sources/PSTranscribe/Storage/TranscriptLogger.swift (CRITICAL: grep for `func startSession` to get exact parameter list; line 151 confirmed `- source/pstranscribe` per RESEARCH.md; grep for `func endSession` and `func finalizeFrontmatter` to know the round-trip API)
    - .planning/phases/24-nyquist-sweep-v1-0/24-PATTERNS.md (lines 152-228 — Pattern Assignments § TranscriptLoggerSecurityTests; the frontmatter-source assertion at lines 202-207 is the exact shape this test uses, just isolated in its own file rather than folded into TranscriptLoggerSecurityTests)
    - .planning/phases/24-nyquist-sweep-v1-0/24-RESEARCH.md (Per-Requirement Audit Phase 08 row "08-REBR-03 (frontmatter `source/pstranscribe`)" at line 184-185 — confirms TranscriptLogger.swift:151 and LocalFileWriter.swift:124 both write the tag; recommendation: end-to-end via TranscriptLogger only, LocalFileWriter is Phase 18.1 territory)
  </read_first>
  <behavior>
    Test 1 (REBR-03 phase-8 closure): `finalizedFrontmatterContainsSourcePstranscribe()` — full TranscriptLogger round-trip in a tempDir; after `endSession` + `finalizeFrontmatter`, read the resulting file as a String; assert it contains `"- source/pstranscribe"` AND does NOT contain `"source/tome"`.
  </behavior>
  <action>
    STEP 1 — Verify the TranscriptLogger round-trip API. Grep:
    ```
    grep -nE "func (startSession|endSession|finalizeFrontmatter|appendUtterance)" PSTranscribe/Sources/PSTranscribe/Storage/TranscriptLogger.swift
    ```
    Note the exact parameter labels and types for each. Record:
    - `startSession(...)` — what's the full param list? (vaultPath, sourceApp, sessionType, etc.)
    - `endSession() -> URL?` — async or sync? returns the file URL.
    - `finalizeFrontmatter(...)` — what params does it take? (Likely a transcript text or relies on accumulated state.)
    - `appendUtterance(...)` — what params? (text, timestamp, speaker?)

    STEP 2 — Create `PSTranscribe/Tests/PSTranscribeTests/FrontmatterSourceTagTests.swift`:
    ```swift
    // FrontmatterSourceTagTests
    // Phase 24 (NYQUIST-04) — Phase 8 REBR-03 closure: finalized transcript frontmatter
    // contains the rebrand source tag '- source/pstranscribe' and DOES NOT contain the
    // legacy 'source/tome' tag. Verifies TranscriptLogger.swift:151.
    //
    // End-to-end round-trip: startSession + appendUtterance + endSession + finalizeFrontmatter
    // in a UUID-scoped tempDir; defer cleanup. Pattern lifted from DictationLoggerTests.swift.

    import Testing
    import Foundation
    @testable import PSTranscribe

    @Suite("FrontmatterSourceTagTests", .serialized)
    struct FrontmatterSourceTagTests {

        private func tempDir() throws -> URL {
            let dir = FileManager.default.temporaryDirectory
                .appendingPathComponent("FrontmatterSourceTagTests-\(UUID().uuidString)", isDirectory: true)
            try FileManager.default.createDirectory(at: dir, withIntermediateDirectories: true)
            return dir
        }

        @Test func finalizedFrontmatterContainsSourcePstranscribe() async throws {
            let dir = try tempDir()
            defer { try? FileManager.default.removeItem(at: dir) }

            let logger = TranscriptLogger()
            // Adapt the param list to the real signature found in STEP 1
            try await logger.startSession(/* sourceApp: "Teams", vaultPath: dir.path, sessionType: .callCapture, ... */)
            try await logger.appendUtterance(text: "test utterance", timestamp: Date())
            let url = await logger.endSession()
            // finalizeFrontmatter — adapt to real signature
            await logger.finalizeFrontmatter(/* ... */)

            guard let url else { Issue.record("endSession returned nil"); return }
            let contents = try String(contentsOf: url, encoding: .utf8)
            #expect(contents.contains("- source/pstranscribe"),
                    "Finalized frontmatter must contain '- source/pstranscribe' rebrand tag")
            #expect(!contents.contains("source/tome"),
                    "Finalized frontmatter must NOT contain legacy 'source/tome' tag")
        }
    }
    ```

    NOTE: The `startSession`/`endSession`/`finalizeFrontmatter`/`appendUtterance` call signatures in the skeleton are placeholders. STEP 1's grep gives you the real signatures — use those.

    STEP 3 — Run the focused suite:
    ```
    cd PSTranscribe && swift test --filter FrontmatterSourceTagTests
    ```
    Must exit 0 with 1 passing test. If `.contains("- source/pstranscribe")` fails, the test is correct — the production code must contain the tag (which RESEARCH.md verified at line 151). Inspect the actual finalized file to determine why; do NOT modify the test to match a wrong answer.

    Per RESEARCH.md A4 (Risk #4) — `validatedVaultPath` and friends are file-private; this test goes through public surface (startSession + endSession + finalizeFrontmatter), which is the required pattern.
  </action>
  <verify>
    <automated>cd PSTranscribe && swift test --filter FrontmatterSourceTagTests 2>&1 | tee /tmp/24-03-task2.log; tail -10 /tmp/24-03-task2.log</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f PSTranscribe/Tests/PSTranscribeTests/FrontmatterSourceTagTests.swift`
    - `.serialized` suite trait: `grep -c '@Suite("FrontmatterSourceTagTests", .serialized)' PSTranscribe/Tests/PSTranscribeTests/FrontmatterSourceTagTests.swift` returns 1
    - tempDir helper present: `grep -c 'private func tempDir()' PSTranscribe/Tests/PSTranscribeTests/FrontmatterSourceTagTests.swift` returns 1
    - Asserts presence of pstranscribe tag: `grep -c '"- source/pstranscribe"' PSTranscribe/Tests/PSTranscribeTests/FrontmatterSourceTagTests.swift` returns at least 1
    - Asserts absence of tome tag: `grep -c '"source/tome"' PSTranscribe/Tests/PSTranscribeTests/FrontmatterSourceTagTests.swift` returns at least 1 (in a `!contents.contains(...)` context)
    - Header cites NYQUIST-04: `grep -c "Phase 24 (NYQUIST-04)" PSTranscribe/Tests/PSTranscribeTests/FrontmatterSourceTagTests.swift` returns 1
    - `cd PSTranscribe && swift test --filter FrontmatterSourceTagTests` exits 0 (1 passing)
  </acceptance_criteria>
  <done>1 @Test method written, suite passes locally.</done>
</task>

<task type="auto">
  <name>Task 3: Approve 08-VALIDATION.md (D-03 lenient — flip frontmatter, cross-reference Speaker.named, WITHDRAW STAB-01 e2e + LibraryEntryRow caching)</name>
  <files>.planning/milestones/v1.0-phases/08-code-defect-fixes/08-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v1.0-phases/08-code-defect-fixes/08-VALIDATION.md (existing draft — frontmatter has `status: draft`, `nyquist_compliant: false`, `wave_0_complete: false`)
    - .planning/milestones/v1.0-phases/08-code-defect-fixes/08-VERIFICATION.md (sourced as `Source: 08-VERIFICATION.md` for WITHDRAWN row reasons)
    - PSTranscribe/Tests/PSTranscribeTests/SpeakerCodableTests.swift (count tests for cross-reference cell)
    - PSTranscribe/Tests/PSTranscribeTests/TranscriptParserTests.swift (count `.named`-related tests for cross-reference cell)
  </read_first>
  <action>
    Use the Edit tool to perform these deltas on `.planning/milestones/v1.0-phases/08-code-defect-fixes/08-VALIDATION.md`:

    DELTA 1 — Frontmatter (lines 1-8):
    - Line 4: `status: draft` → `status: approved`
    - Line 5: `nyquist_compliant: false` → `nyquist_compliant: true`
    - Line 6: `wave_0_complete: false` → `wave_0_complete: true`
    - Add `last_audited: 2026-05-05` after `created: 2026-04-06`

    DELTA 2 — Per-Task Verification Map (lines 38-46): Replace the existing 6-row table with this 6-row table:

    ```markdown
    | Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status | Notes |
    |---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|-------|
    | 24-03-01 | 24-03 | 1 | D-01a (Speaker.named codable round-trip) | — | Speaker.named encodes/decodes via custom CodingKeys | unit | `cd PSTranscribe && swift test --filter SpeakerCodableTests` | ✅ existing | green | Cross-reference: 6 `@Test` methods in `SpeakerCodableTests.swift` cover round-trip + legacy raw-string decode |
    | 24-03-02 | 24-03 | 1 | D-01b (TranscriptParser maps "Speaker N" to .named) | — | Parser produces `.named(label)` for diarized utterances | unit | `cd PSTranscribe && swift test --filter TranscriptParserTests` | ✅ existing | green | Cross-reference: `TranscriptParserTests.otherSpeakerMapsToNamed` + `diarizedSpeakerMapsToNamed` cover the `.named` branch |
    | 24-03-03 | 24-03 | 1 | STAB-03 (transcriptStore.clear on stop) | — | stopSession clears accumulated transcript state | unit | `cd PSTranscribe && swift test --filter TranscriptStoreClearTests` | ✅ new | green | Plan 24-03 Task 1 added `TranscriptStoreClearTests.swift` |
    | 24-03-04 | 24-03 | 1 | REBR-03 (frontmatter source/pstranscribe — phase 8 closure) | — | Finalized transcript frontmatter contains '- source/pstranscribe' and not 'source/tome' | unit | `cd PSTranscribe && swift test --filter FrontmatterSourceTagTests` | ✅ new | green | Plan 24-03 Task 2 added `FrontmatterSourceTagTests.swift`; cross-references the rebrand half asserted in `RebrandInfoPlistTests` (Plan 24-01) |
    | 24-03-05 | 24-03 | 1 | LibraryEntryRow file-exists caching | — | n/a | WITHDRAWN | n/a — WITHDRAWN | n/a | withdrawn | `@State private var fileExists` + `.onAppear` is SwiftUI lifecycle, not unit-testable. Behavior verified at code level in `08-VERIFICATION.md` row 5; visual rendering covered by Phase 23 LibraryView snapshot tests. Source: 08-VERIFICATION.md row 5. |
    | 24-03-06 | 24-03 | 1 | STAB-01 (crash recovery end-to-end) | — | After force-quit, next launch surfaces incomplete session via checkpoint scan | WITHDRAWN | n/a — WITHDRAWN | n/a | withdrawn | Force-quit / SIGKILL mid-session is out of scope per Phase 24 D-03 (no integration scaffolding). The `SessionStore.scanIncompleteCheckpoints` round-trip portion IS unit-tested by `CheckpointRoundTripTests.swift` (Plan 24-05); the end-to-end crash-recovery UX flow is the WITHDRAWN portion. Source: 08-VERIFICATION.md human-verification item. |
    ```

    DELTA 3 — Wave 0 Requirements section (lines 52-56): Replace with:
    ```markdown
    ## Wave 0 Requirements

    - [x] `PSTranscribe/Tests/PSTranscribeTests/SpeakerCodableTests.swift` — 6 `@Test` methods covering Speaker.named codable (already shipped; cross-reference)
    - [x] `PSTranscribe/Tests/PSTranscribeTests/TranscriptParserTests.swift` — `.named` parser cases (already shipped; cross-reference)
    - [x] `PSTranscribe/Tests/PSTranscribeTests/TranscriptStoreClearTests.swift` — STAB-03 (added in Plan 24-03 Task 1)
    - [x] `PSTranscribe/Tests/PSTranscribeTests/FrontmatterSourceTagTests.swift` — REBR-03 phase-8 closure (added in Plan 24-03 Task 2)

    **Wave 0 complete.**
    ```

    DELTA 4 — Manual-Only Verifications section (lines 60-65): DELETE the entire section. Per D-03, no Manual-Only rows. The "Crash recovery end-to-end (STAB-01)" row migrates to the WITHDRAWN row in DELTA 2; the "Speaker colored badges (D-02)" row is a Phase 23 visual-regression concern (snapshot tests cover it) — note in audit `### Notes`.

    DELTA 5 — Append a new audit block before the Validation Sign-Off section (or at the end of the file):

    ```markdown

    ---

    ## Validation Audit 2026-05-05 (Phase 24 — NYQUIST-04)

    | Metric | Count |
    |--------|-------|
    | Gaps found | 1 (Phase 8 draft VALIDATION.md had 1 manual STAB-01 row + 1 missing test target for STAB-03/REBR-03) |
    | Resolved | 4 (D-01a/b cross-referenced; STAB-03 unit-tested via TranscriptStoreClearTests; REBR-03 phase-8 closure unit-tested via FrontmatterSourceTagTests) |
    | Withdrawn | 2 (LibraryEntryRow caching — SwiftUI lifecycle; STAB-01 e2e — force-quit out of scope per Phase 24 D-03) |
    | Escalated | 0 |
    | Document updates | Frontmatter flipped to approved/true/true; `last_audited: 2026-05-05` added; Per-Task Map rewritten with unit + WITHDRAWN rows; Manual-Only Verifications section removed (D-03 lenient policy). |

    ### Audit Method

    - Cross-referenced existing `SpeakerCodableTests.swift` (6 `@Test` methods covering Speaker.named codable + legacy raw-string decode) for Phase 8 D-01a — no new test added.
    - Cross-referenced existing `TranscriptParserTests.swift` (`.named` speaker mapping cases) for Phase 8 D-01b — no new test added.
    - Created `PSTranscribe/Tests/PSTranscribeTests/TranscriptStoreClearTests.swift` (1 `@Test`: `clearEmptiesAccumulatedState`). Plan 24-03 Task 1.
    - Created `PSTranscribe/Tests/PSTranscribeTests/FrontmatterSourceTagTests.swift` (1 `@Test`: `finalizedFrontmatterContainsSourcePstranscribe`). End-to-end round-trip via `TranscriptLogger.startSession` + `appendUtterance` + `endSession` + `finalizeFrontmatter`; reads finalized file; asserts `"- source/pstranscribe"` present, `"source/tome"` absent. Plan 24-03 Task 2.
    - Ran `cd PSTranscribe && swift test --filter TranscriptStoreClearTests` → 1 passing.
    - Ran `cd PSTranscribe && swift test --filter FrontmatterSourceTagTests` → 1 passing.
    - Ran full `cd PSTranscribe && swift test` → exits 0.

    ### Notes

    - LibraryEntryRow `@State private var fileExists` + `.onAppear` is SwiftUI lifecycle, not unit-testable from this target. Phase 23 snapshot tests for LibraryView cover visual rendering of the missing-file badge across Light/Dark/System appearances.
    - Speaker colored-badges (Phase 8 internal D-02) is also a Phase 23 visual-regression concern — covered by LibraryView/TranscriptView snapshot baselines.
    - STAB-01 end-to-end crash recovery is WITHDRAWN per Phase 24 D-03 lenient policy. The pure round-trip portion (`SessionStore.writeCheckpoint` → fresh instance → `scanIncompleteCheckpoints`) IS unit-tested by `CheckpointRoundTripTests.swift` in Plan 24-05; that test closes the data-layer half of STAB-01. The WITHDRAWN portion is the force-quit + relaunch + UI-surfaces-it user flow.
    ```

    DELTA 6 — Validation Sign-Off section (lines 71-77):
    - Update each `[ ]` to `[x]`
    - `Feedback latency < 15s` → `Feedback latency < 90s`
    - Replace `**Approval:** pending` with `**Approval:** approved 2026-05-05 (Phase 24 NYQUIST-04)`
  </action>
  <verify>
    <automated>grep -E "^last_audited:|^status:|^nyquist_compliant:|^wave_0_complete:" .planning/milestones/v1.0-phases/08-code-defect-fixes/08-VALIDATION.md && grep -c "WITHDRAWN" .planning/milestones/v1.0-phases/08-code-defect-fixes/08-VALIDATION.md && grep -cE '^## Manual-Only Verifications' .planning/milestones/v1.0-phases/08-code-defect-fixes/08-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^status: approved" .planning/milestones/v1.0-phases/08-code-defect-fixes/08-VALIDATION.md` returns 1
    - `grep -c "^nyquist_compliant: true" .planning/milestones/v1.0-phases/08-code-defect-fixes/08-VALIDATION.md` returns 1
    - `grep -c "^wave_0_complete: true" .planning/milestones/v1.0-phases/08-code-defect-fixes/08-VALIDATION.md` returns 1
    - `grep -c "^last_audited: 2026-05-05" .planning/milestones/v1.0-phases/08-code-defect-fixes/08-VALIDATION.md` returns 1
    - `grep -cE '^## Manual-Only Verifications' .planning/milestones/v1.0-phases/08-code-defect-fixes/08-VALIDATION.md` returns 0
    - `grep -c "swift test --filter TranscriptStoreClearTests" .planning/milestones/v1.0-phases/08-code-defect-fixes/08-VALIDATION.md` returns at least 1
    - `grep -c "swift test --filter FrontmatterSourceTagTests" .planning/milestones/v1.0-phases/08-code-defect-fixes/08-VALIDATION.md` returns at least 1
    - `grep -c "swift test --filter SpeakerCodableTests" .planning/milestones/v1.0-phases/08-code-defect-fixes/08-VALIDATION.md` returns at least 1
    - `grep -c "Source: 08-VERIFICATION.md" .planning/milestones/v1.0-phases/08-code-defect-fixes/08-VALIDATION.md` returns at least 2
    - `grep -c "Validation Audit 2026-05-05" .planning/milestones/v1.0-phases/08-code-defect-fixes/08-VALIDATION.md` returns 1
  </acceptance_criteria>
  <done>08-VALIDATION.md approved per D-03; cross-references + new tests + WITHDRAWN reasons all present.</done>
</task>

<task type="auto">
  <name>Task 4: Run full swift test (full-suite gate)</name>
  <files>(no file changes)</files>
  <read_first>
    - .planning/phases/24-nyquist-sweep-v1-0/24-RESEARCH.md (Acceptance Criteria Glossary)
  </read_first>
  <action>
    ```
    cd PSTranscribe && swift test 2>&1 | tee /tmp/24-03-fullsuite.log; tail -30 /tmp/24-03-fullsuite.log
    ```
    Expected: exits 0; suite count grew by 2 tests vs. Plan 24-02's baseline.
  </action>
  <verify>
    <automated>cd PSTranscribe && swift test 2>&1 | tail -5 | grep -E "Test Suite '.*' passed|^Executed [0-9]+ tests, with 0 failures"</automated>
  </verify>
  <acceptance_criteria>
    - `cd PSTranscribe && swift test` exits 0
    - Suite count grew by exactly 2 (TranscriptStoreClear + FrontmatterSourceTag) vs. Plan 24-02 close
    - No regressions in pre-existing tests
  </acceptance_criteria>
  <done>Full Swift Testing suite green locally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → tempDir filesystem | Each test creates a UUID-scoped tempDir, writes via TranscriptLogger actor, reads back, defer-cleans |
| Test process → TranscriptStore internal symbols | `@testable import PSTranscribe` reaches `internal` types |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03-01 | Tampering | If FrontmatterSourceTagTests asserts a different tag than what TranscriptLogger.swift:151 actually writes, the rebrand invariant could regress silently | mitigate | Test asserts both presence (`- source/pstranscribe`) AND absence (`source/tome`) — covers both halves of the rebrand. CI enforces. |
| T-24-03-02 | Information Disclosure | TranscriptStore tests expose internal API shape | accept | Internal visibility (default); `@testable import` is the supported test mechanism. |
| T-24-03-03 | Denial of Service | A malformed `Utterance` fixture or wrong API surface in TranscriptStoreClearTests breaks the build | mitigate | STEP 1's grep is mandatory before writing the test — verifies API surface against current source. |
</threat_model>

<verification>
- TranscriptStoreClearTests.swift has 1 passing `@Test`
- FrontmatterSourceTagTests.swift has 1 passing `@Test` asserting both presence and absence of expected/legacy tags
- 08-VALIDATION.md frontmatter: approved/true/true/last_audited 2026-05-05
- 08-VALIDATION.md cross-references SpeakerCodableTests + TranscriptParserTests for D-01a/b
- 08-VALIDATION.md WITHDRAWN rows for LibraryEntryRow caching + STAB-01 e2e, both with `Source: 08-VERIFICATION.md`
- 08-VALIDATION.md no longer contains `## Manual-Only Verifications` section
- No production source code changes (D-03 invariant; this plan is audit-only)
</verification>

<success_criteria>
1. TranscriptStoreClearTests.swift + FrontmatterSourceTagTests.swift exist with 1 passing `@Test` each
2. 08-VALIDATION.md frontmatter approved per D-03
3. Full `swift test` suite green locally
4. PROCESS-01 frontmatter on SUMMARY.md
5. NYQUIST-04 closed: ROADMAP.md success criterion 4 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/24-nyquist-sweep-v1-0/24-03-SUMMARY.md` with:
- Frontmatter: `requirements-completed: [NYQUIST-04]`
- Body: notes that REBR-03's phase-8 closure (frontmatter tag) is asserted here, complementing Plan 24-01's rebrand-Info.plist closure; STAB-01 is split between the WITHDRAWN e2e portion (here) and the round-trip portion (Plan 24-05's CheckpointRoundTripTests)

Commit message style: NO Claude attribution. Subject: `feat(24-03): backfill Phase 8 Defects Nyquist coverage (NYQUIST-04)` or similar.
</output>
