---
phase: 26.1-close-gap-process-03-lint-summaries-failure-on-v1-0-archive
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [PROCESS-03]
tags: [process-03, lint-summaries, frontmatter-migration, v1.0-archive, gap-closure, audit-v1.3]
gap_closure: true
gap_source: .planning/v1.3-MILESTONE-AUDIT.md (PROCESS-03 blocker, closure-path step 1)

files_modified:
  - .planning/milestones/v1.0-phases/01-rebrand/01-01-SUMMARY.md
  - .planning/milestones/v1.0-phases/02-security-stability/02-01-SUMMARY.md
  - .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-01-SUMMARY.md
  - .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-02-SUMMARY.md
  - .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-03-SUMMARY.md
  - .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-04-SUMMARY.md
  - .planning/milestones/v1.0-phases/08-code-defect-fixes/08-01-SUMMARY.md
  - .planning/milestones/v1.0-phases/08-code-defect-fixes/08-02-SUMMARY.md

must_haves:
  truths:
    - "Honors D1 / D2 (CONTEXT.md): each of the 8 affected SUMMARY files contains the canonical YAML key `requirements-completed: []` (empty list literal, hyphen form per lint rule D-01)."
    - "Honors G1 (CONTEXT.md verification gate): `bash scripts/lint-summaries.sh` exits 0 from the project root."
    - "Honors G1 success-marker invariant: the final summary line of the lint run is `55 SUMMARY files checked, 0 failures, 0 warnings` and the trailing `OK` line is emitted."
    - "Honors G2 (no regressions): the lint corpus count stays at 55 — no SUMMARY files renamed, deleted, or created by this plan."
    - "Honors G3 (frontmatter-only): for each of the 8 files, `git diff <file>` shows changes ONLY inside the leading `---`...`---` YAML frontmatter block. The body of every modified SUMMARY (everything after the closing `---` of the frontmatter) is byte-for-byte identical to its pre-change state."
    - "Honors D3 (CONTEXT.md ordering): the new `requirements-completed: []` line is inserted inside the YAML frontmatter block of each file. Insertion location is Claude's Discretion within the frontmatter; the recommended location is immediately after the `tags:` block (preserving stable, predictable diff shape across the 8 files)."
    - "Honors D5 (CONTEXT.md commit convention) AND project commit policy: a single commit lands the 8 edits, message follows `fix(26.1): add requirements-completed: [] to 8 v1.0-archive SUMMARYs (PROCESS-03 closure)` shape, with NO Claude attribution footer (no `Co-Authored-By: Claude`, no robot emoji, no `Generated with Claude Code`)."
    - "Out-of-scope guard: `scripts/lint-summaries.sh` is NOT modified. `.github/workflows/lint-summaries.yml` is NOT modified. No `*-PLAN.md` files under `.planning/milestones/v1.0-phases/` are modified. No application Swift code is touched."
  artifacts:
    - path: .planning/milestones/v1.0-phases/01-rebrand/01-01-SUMMARY.md
      provides: "Canonical `requirements-completed: []` key inside the YAML frontmatter block"
      contains: "requirements-completed: []"
    - path: .planning/milestones/v1.0-phases/02-security-stability/02-01-SUMMARY.md
      provides: "Canonical `requirements-completed: []` key inside the YAML frontmatter block"
      contains: "requirements-completed: []"
    - path: .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-01-SUMMARY.md
      provides: "Canonical `requirements-completed: []` key inside the YAML frontmatter block"
      contains: "requirements-completed: []"
    - path: .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-02-SUMMARY.md
      provides: "Canonical `requirements-completed: []` key inside the YAML frontmatter block"
      contains: "requirements-completed: []"
    - path: .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-03-SUMMARY.md
      provides: "Canonical `requirements-completed: []` key inside the YAML frontmatter block"
      contains: "requirements-completed: []"
    - path: .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-04-SUMMARY.md
      provides: "Canonical `requirements-completed: []` key inside the YAML frontmatter block"
      contains: "requirements-completed: []"
    - path: .planning/milestones/v1.0-phases/08-code-defect-fixes/08-01-SUMMARY.md
      provides: "Canonical `requirements-completed: []` key inside the YAML frontmatter block"
      contains: "requirements-completed: []"
    - path: .planning/milestones/v1.0-phases/08-code-defect-fixes/08-02-SUMMARY.md
      provides: "Canonical `requirements-completed: []` key inside the YAML frontmatter block"
      contains: "requirements-completed: []"
  key_links:
    - from: "8 v1.0-archive SUMMARY frontmatters"
      to: "scripts/lint-summaries.sh check C1 (presence of `requirements-completed:`)"
      via: "yq frontmatter parse OR grep fallback (fm_has_key_grep) — both must observe the key"
      pattern: "^requirements-completed:"
    - from: "scripts/lint-summaries.sh"
      to: ".github/workflows/lint-summaries.yml CI gate `Lint Summaries`"
      via: "script exit code 0 unblocks the CI job for any PR touching SUMMARY/PLAN/workflow files"
      pattern: "exit 0 AND `0 failures, 0 warnings`"
---

<objective>
Restore `scripts/lint-summaries.sh` to exit 0 against the full SUMMARY corpus by adding the canonical `requirements-completed: []` YAML frontmatter key to the 8 v1.0-archive SUMMARY files that currently fail check C1 (missing-key, D-07). Closes the only blocker in `v1.3-MILESTONE-AUDIT.md` and unblocks the `Lint Summaries` CI gate for every future PR.

Purpose: Phase 24 commit `86a4871` restored the v1.0-phases archive into the lint-scanned tree without applying the Phase 22 frontmatter migration to those files. The result is `passed-then-regressed` for PROCESS-03 — the gate exists and is wired but exits non-zero against `main`, blocking every PR that touches `*-SUMMARY.md`, `*-PLAN.md`, or the lint workflow itself. The audit prescribes exactly one fix: insert `requirements-completed: []` (empty list, explicitly valid per lint rule D-04) into the 8 affected files. No code paths, network, auth, or data flows are touched.

Output: 8 modified SUMMARY files (frontmatter-only diff), 1 atomic commit, `bash scripts/lint-summaries.sh` exits 0 emitting `55 SUMMARY files checked, 0 failures, 0 warnings`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.3-MILESTONE-AUDIT.md
@.planning/phases/26.1-close-gap-process-03-lint-summaries-failure-on-v1-0-archive/26.1-CONTEXT.md
@scripts/lint-summaries.sh
@.github/workflows/lint-summaries.yml

<interfaces>
<!-- The lint contract the 8 edits must satisfy. Read directly from scripts/lint-summaries.sh. -->

Lint script rules (`scripts/lint-summaries.sh` header):
- D-01: canonical key is `requirements-completed` (hyphen form, NOT underscore).
- D-04: empty list `[]` is explicitly valid — this is the closure form prescribed by the audit.
- D-07: presence + subset (`SUMMARY.requirements-completed ⊆ PLAN.requirements`). Empty list is trivially a subset of any set, so C3 cannot fail post-migration.

Lint script checks (must all hold post-migration):
- C1 (FAIL): SUMMARY frontmatter contains `requirements-completed:` key. — Currently fails on all 8 targets.
- C2 (FAIL): SUMMARY frontmatter does NOT contain `requirements_completed:` (underscore form). — Pre-pass on all 8 targets; do not introduce the underscore form.
- C3 (FAIL): every ID in `SUMMARY.requirements-completed` appears in sibling `PLAN.requirements`. — Trivially pre-pass with empty list `[]`.
- C4 (WARN): SUMMARY frontmatter contains a deprecated sibling field (`requirements-scaffolded`, `requirements-supports`, `requirements-touched-but-not-completed`). — Pre-pass on all 8 targets; do not introduce these.

Lint script success markers (final stdout block):
```
55 SUMMARY files checked, 0 failures, 0 warnings
OK — no failures.
```
Exit code: 0.

Frontmatter shapes observed in the 8 targets (read each file's first 6 lines yourself before editing):
- 6 files have INLINE-LIST `tags:` on a single line (e.g., `tags: [rebrand, swift, package, rename]`), followed immediately by `dependency_graph:` on the next line. Insertion point: a new line `requirements-completed: []` between the `tags:` line and `dependency_graph:`.
- 2 files (`03-03-SUMMARY.md`, `03-04-SUMMARY.md`) use BLOCK-LIST `tags:` — `tags:` on its own line, followed by one or more `  - item` lines. Insertion point: a new line `requirements-completed: []` AFTER the last `  - item` line of the `tags:` block (i.e., before whatever key comes next in the frontmatter).

Canonical value (CONTEXT.md D1 + lint rule D-04): `requirements-completed: []` (inline empty-list form). Do NOT use `requirements-completed:` followed by a blank list — `[]` is shorter and unambiguous.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `requirements-completed: []` to all 8 v1.0-archive SUMMARY frontmatters</name>

  <read_first>
    - .planning/phases/26.1-close-gap-process-03-lint-summaries-failure-on-v1-0-archive/26.1-CONTEXT.md — locked decisions D1, D2, D3, D5.
    - scripts/lint-summaries.sh — rules D-01, D-04, D-07 and checks C1..C4 (header comments, lines 1-30). Confirm the canonical key spelling and the empty-list validity firsthand. Do NOT skim — the script is the contract.
    - Each of the 8 target files (first ~10 lines, frontmatter only) — confirm whether the `tags:` block is inline-list or block-list shape before inserting. Do NOT assume.
  </read_first>

  <files>
    - .planning/milestones/v1.0-phases/01-rebrand/01-01-SUMMARY.md
    - .planning/milestones/v1.0-phases/02-security-stability/02-01-SUMMARY.md
    - .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-01-SUMMARY.md
    - .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-02-SUMMARY.md
    - .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-03-SUMMARY.md
    - .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-04-SUMMARY.md
    - .planning/milestones/v1.0-phases/08-code-defect-fixes/08-01-SUMMARY.md
    - .planning/milestones/v1.0-phases/08-code-defect-fixes/08-02-SUMMARY.md
  </files>

  <action>
    For each of the 8 files listed in `<files>`, insert the line `requirements-completed: []` inside the leading YAML frontmatter block (between the opening `---` and the closing `---`). Use the **Edit** tool (NOT Write — Edit preserves the rest of the file byte-for-byte, which is required by G3 / `must_haves.truths`).

    **Insertion location (per CONTEXT.md D3 — Claude's Discretion within the frontmatter):**

    - **Inline-list `tags:` shape** (6 files: 01-01, 02-01, 03-01, 03-02, 08-01, 08-02):
      The frontmatter has a single line like `tags: [rebrand, swift, package, rename]` followed by `dependency_graph:` on the next line. Use Edit with:
        - `old_string`: the existing `tags:` line (verbatim, including the full bracketed list).
        - `new_string`: the same `tags:` line, then a newline, then `requirements-completed: []`.
      This places the new key immediately after `tags:` and before `dependency_graph:` — stable, predictable diff shape across all 6 files.

    - **Block-list `tags:` shape** (2 files: 03-03, 03-04):
      The frontmatter has `tags:` on its own line followed by one or more `  - item` lines, then the next top-level key. Insertion strategy: find the LAST `  - <item>` line under `tags:` (the one immediately before the next top-level key, which is unindented). Use Edit with:
        - `old_string`: that last `  - <item>` line PLUS the next line (the next top-level key, unindented) — together as a 2-line anchor so the Edit is uniquely matchable.
        - `new_string`: the same last `  - <item>` line, then a newline, then `requirements-completed: []`, then a newline, then the next top-level key line.
      If the 2-line anchor is not unique (rare), fall back to including 1-2 more lines of context in `old_string` until uniqueness is achieved. Read the file first; do not guess.

    **Constraints (per `must_haves` and CONTEXT.md scope):**
    - Insert EXACTLY the literal string `requirements-completed: []` (canonical hyphen form per lint rule D-01; empty inline list per D-04). NOT `requirements_completed:` (underscore — would FAIL C2). NOT `requirements-completed:` followed by a blank line (valid YAML but verbose; `[]` is the locked canonical per CONTEXT.md "Claude's Discretion" guidance).
    - Do NOT modify ANY other frontmatter key. Do NOT reorder existing keys. Do NOT touch ANY content after the closing `---` of the frontmatter — the body of each SUMMARY must remain byte-for-byte identical (G3 invariant).
    - Do NOT modify `scripts/lint-summaries.sh`. Do NOT modify `.github/workflows/lint-summaries.yml`. Do NOT modify any `*-PLAN.md` file under `.planning/milestones/v1.0-phases/`. Do NOT touch Swift source code.
  </action>

  <verify>
    <automated>
      # All 8 files must contain the canonical key inside the frontmatter block.
      # (`awk` scopes the grep to the leading `---`...`---` block to prevent false positives from body prose.)
      for f in \
        .planning/milestones/v1.0-phases/01-rebrand/01-01-SUMMARY.md \
        .planning/milestones/v1.0-phases/02-security-stability/02-01-SUMMARY.md \
        .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-01-SUMMARY.md \
        .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-02-SUMMARY.md \
        .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-03-SUMMARY.md \
        .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-04-SUMMARY.md \
        .planning/milestones/v1.0-phases/08-code-defect-fixes/08-01-SUMMARY.md \
        .planning/milestones/v1.0-phases/08-code-defect-fixes/08-02-SUMMARY.md; do
        awk '/^---$/{n++; if(n==2) exit} n==1 && /^requirements-completed:[[:space:]]*\[\]/{found=1} END{exit !found}' "$f" \
          || { echo "FAIL: $f missing canonical key in frontmatter"; exit 1; }
      done
      echo "PASS: all 8 files contain requirements-completed: [] in frontmatter"

      # No file got an accidental underscore form (would fail C2).
      for f in \
        .planning/milestones/v1.0-phases/01-rebrand/01-01-SUMMARY.md \
        .planning/milestones/v1.0-phases/02-security-stability/02-01-SUMMARY.md \
        .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-01-SUMMARY.md \
        .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-02-SUMMARY.md \
        .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-03-SUMMARY.md \
        .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-04-SUMMARY.md \
        .planning/milestones/v1.0-phases/08-code-defect-fixes/08-01-SUMMARY.md \
        .planning/milestones/v1.0-phases/08-code-defect-fixes/08-02-SUMMARY.md; do
        if awk '/^---$/{n++; if(n==2) exit} n==1 && /^requirements_completed:/' "$f" | grep -q .; then
          echo "FAIL: $f introduced deprecated underscore form"; exit 1
        fi
      done
      echo "PASS: no underscore form introduced"

      # git diff --stat shows exactly the 8 expected files changed, nothing else.
      git diff --name-only | sort > /tmp/26.1-changed.txt
      cat > /tmp/26.1-expected.txt <<'EOF'
.planning/milestones/v1.0-phases/01-rebrand/01-01-SUMMARY.md
.planning/milestones/v1.0-phases/02-security-stability/02-01-SUMMARY.md
.planning/milestones/v1.0-phases/03-session-management-recording-naming/03-01-SUMMARY.md
.planning/milestones/v1.0-phases/03-session-management-recording-naming/03-02-SUMMARY.md
.planning/milestones/v1.0-phases/03-session-management-recording-naming/03-03-SUMMARY.md
.planning/milestones/v1.0-phases/03-session-management-recording-naming/03-04-SUMMARY.md
.planning/milestones/v1.0-phases/08-code-defect-fixes/08-01-SUMMARY.md
.planning/milestones/v1.0-phases/08-code-defect-fixes/08-02-SUMMARY.md
EOF
      sort /tmp/26.1-expected.txt -o /tmp/26.1-expected.txt
      diff /tmp/26.1-changed.txt /tmp/26.1-expected.txt && echo "PASS: exactly 8 files changed"

      # Body untouched: for each file, the diff must contain ZERO hunks whose @@ context line
      # is positioned after the closing frontmatter `---`. Equivalent simpler check: every
      # added/removed line in the diff must occur at a line number inside the frontmatter
      # block. Implement as a per-file check that the unified diff has exactly one
      # added line (`+requirements-completed: []`) and zero removed lines.
      for f in \
        .planning/milestones/v1.0-phases/01-rebrand/01-01-SUMMARY.md \
        .planning/milestones/v1.0-phases/02-security-stability/02-01-SUMMARY.md \
        .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-01-SUMMARY.md \
        .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-02-SUMMARY.md \
        .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-03-SUMMARY.md \
        .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-04-SUMMARY.md \
        .planning/milestones/v1.0-phases/08-code-defect-fixes/08-01-SUMMARY.md \
        .planning/milestones/v1.0-phases/08-code-defect-fixes/08-02-SUMMARY.md; do
        added=$(git diff -- "$f" | grep -v '^+++' | grep -c '^+' || true)
        removed=$(git diff -- "$f" | grep -v '^---' | grep -c '^-' || true)
        if [[ "$added" != "1" || "$removed" != "0" ]]; then
          echo "FAIL: $f diff is not frontmatter-only (added=$added removed=$removed; expected 1/0)"; exit 1
        fi
      done
      echo "PASS: all 8 diffs are single-line frontmatter additions, body untouched"
    </automated>
  </verify>

  <acceptance_criteria>
    - For each of the 8 files, the frontmatter block (between the leading `---` and the next `---`) contains a line matching the regex `^requirements-completed:[[:space:]]*\[\]$`.
    - For each of the 8 files, the frontmatter block does NOT contain a line matching `^requirements_completed:` (underscore form).
    - `git diff --name-only` lists exactly the 8 expected paths — no extras, no omissions.
    - For each of the 8 files, `git diff <file>` adds exactly 1 line and removes 0 lines (proves body untouched, G3 invariant).
    - `scripts/lint-summaries.sh` and `.github/workflows/lint-summaries.yml` are NOT in `git diff --name-only`.
    - No `*-PLAN.md` under `.planning/milestones/v1.0-phases/` is in `git diff --name-only`.
  </acceptance_criteria>

  <done>
    All 8 target SUMMARY frontmatters contain `requirements-completed: []`; the 4 automated verify blocks above all print their PASS lines; no file outside the 8 is in the diff; body content is byte-for-byte identical pre/post for every modified file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run `bash scripts/lint-summaries.sh` and confirm exit 0 [BLOCKING GATE]</name>

  <read_first>
    - scripts/lint-summaries.sh — re-read lines 168-180 to confirm the EXACT success-marker phrasing emitted on exit 0. The script prints `%d SUMMARY files checked, %d failures, %d warnings\n` and then `OK — no failures.\n`. Use that exact phrasing in the verification regex.
    - .github/workflows/lint-summaries.yml — confirm the workflow invokes `bash scripts/lint-summaries.sh` (no extra flags) so local exit 0 implies CI exit 0.
  </read_first>

  <files>
    (verification-only — no files modified in this task)
  </files>

  <action>
    Run `bash scripts/lint-summaries.sh` from the project root. Capture the full stdout AND the exit code. Do NOT pass any arguments — the script defaults to scanning `.planning/milestones/` (DEFAULT_ROOT), which is the same scope CI uses.

    If the exit code is 0 AND the final summary line reads `55 SUMMARY files checked, 0 failures, 0 warnings` AND the `OK — no failures.` line is present, this task passes. Move to Task 3.

    If the exit code is non-zero, OR the file count is not 55, OR there is any `[FAIL ...]` line in the output: STOP. Do NOT commit. Report the failing line(s) to the user and route back to Task 1 for correction. Do NOT proceed to Task 3 with a red gate — the entire purpose of this plan is to flip this gate green.
  </action>

  <verify>
    <automated>
      # Capture script output and exit code in one run.
      output=$(bash scripts/lint-summaries.sh 2>&1)
      rc=$?

      if [[ $rc -ne 0 ]]; then
        echo "FAIL: lint-summaries.sh exited with code $rc (expected 0)"
        echo "--- script output ---"
        printf '%s\n' "$output"
        exit 1
      fi

      # The script tail must contain the canonical OK markers.
      printf '%s\n' "$output" | grep -E '^55 SUMMARY files checked, 0 failures, 0 warnings$' \
        || { echo "FAIL: missing '55 SUMMARY files checked, 0 failures, 0 warnings' line"; printf '%s\n' "$output"; exit 1; }

      # The literal `OK` line uses an em-dash-like char in the script (`— no failures.`); match loosely.
      printf '%s\n' "$output" | grep -F 'OK' | grep -F 'no failures' \
        || { echo "FAIL: missing 'OK ... no failures' success line"; printf '%s\n' "$output"; exit 1; }

      # No [FAIL ...] lines anywhere in the output.
      if printf '%s\n' "$output" | grep -q '\[FAIL'; then
        echo "FAIL: output contains [FAIL ...] lines"
        printf '%s\n' "$output"
        exit 1
      fi

      echo "PASS: scripts/lint-summaries.sh exited 0 with canonical success markers"
    </automated>
  </verify>

  <acceptance_criteria>
    - `bash scripts/lint-summaries.sh` exits with code 0.
    - stdout contains the literal line `55 SUMMARY files checked, 0 failures, 0 warnings`.
    - stdout contains a line beginning with `OK` and containing `no failures`.
    - stdout contains zero `[FAIL ...]` lines.
    - File count of 55 confirms G2: no SUMMARY files were renamed/deleted/created (pre-state was also 55).
  </acceptance_criteria>

  <done>
    Lint script exits 0 with the canonical `55 / 0 / 0` summary and the OK line. The `Lint Summaries` CI gate will now pass for any subsequent PR.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit the migration as a single atomic commit (no Claude attribution)</name>

  <read_first>
    - .planning/phases/26.1-close-gap-process-03-lint-summaries-failure-on-v1-0-archive/26.1-CONTEXT.md — D5 commit-message shape.
    - The global commit-style rule from $HOME/.claude/CLAUDE.md (the planner already enforces this in the action below): NO `Co-Authored-By: Claude` trailer, NO robot emoji, NO `Generated with Claude Code` footer, NO attribution of any form.
  </read_first>

  <files>
    (commits the 8 files from Task 1 — no new file edits in this task)
  </files>

  <action>
    Stage exactly the 8 files modified in Task 1 (use explicit `git add <path>` for each — NOT `git add -A` and NOT `git add .`), then create a single commit.

    Commit-message shape (per CONTEXT.md D5):

    ```
    fix(26.1): add requirements-completed: [] to 8 v1.0-archive SUMMARYs (PROCESS-03 closure)

    Closes the only blocker from v1.3-MILESTONE-AUDIT.md. Phase 24 commit 86a4871
    restored the v1.0-phases archive into the lint-scanned tree without applying
    the Phase 22 frontmatter migration on those files, causing scripts/lint-summaries.sh
    to exit 1 (55 files, 8 failures, 0 warnings).

    Empty list `[]` is explicitly valid per lint rule D-04. The audit prescribes
    this exact closure form — forensic backfill of v1.0-era REQ-IDs is explicitly
    out of scope (CONTEXT.md D2, deferred).

    Files migrated:
      .planning/milestones/v1.0-phases/01-rebrand/01-01-SUMMARY.md
      .planning/milestones/v1.0-phases/02-security-stability/02-01-SUMMARY.md
      .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-01-SUMMARY.md
      .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-02-SUMMARY.md
      .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-03-SUMMARY.md
      .planning/milestones/v1.0-phases/03-session-management-recording-naming/03-04-SUMMARY.md
      .planning/milestones/v1.0-phases/08-code-defect-fixes/08-01-SUMMARY.md
      .planning/milestones/v1.0-phases/08-code-defect-fixes/08-02-SUMMARY.md

    Post: bash scripts/lint-summaries.sh exits 0 (55 files, 0 failures, 0 warnings).
    ```

    **Commit policy (non-negotiable, per global CLAUDE.md):**
    - Do NOT append `Co-Authored-By: Claude <noreply@anthropic.com>`.
    - Do NOT append `🤖 Generated with [Claude Code](https://claude.com/claude-code)`.
    - Do NOT append any other attribution trailer.
    - Use a heredoc for the commit message to preserve formatting. Run hooks as configured (do NOT pass `--no-verify`).

    Do NOT push. The user controls when to push. This plan ends at the local commit.
  </action>

  <verify>
    <automated>
      # Exactly one new commit, on HEAD.
      msg=$(git log -1 --pretty=%B)

      # Subject matches the prescribed shape.
      printf '%s\n' "$msg" | head -1 | grep -E '^fix\(26\.1\): add requirements-completed: \[\] to 8 v1\.0-archive SUMMARYs \(PROCESS-03 closure\)$' \
        || { echo "FAIL: commit subject does not match prescribed shape"; printf '%s\n' "$msg"; exit 1; }

      # Commit touches exactly the 8 expected files.
      git diff-tree --no-commit-id --name-only -r HEAD | sort > /tmp/26.1-committed.txt
      cat > /tmp/26.1-expected-commit.txt <<'EOF'
.planning/milestones/v1.0-phases/01-rebrand/01-01-SUMMARY.md
.planning/milestones/v1.0-phases/02-security-stability/02-01-SUMMARY.md
.planning/milestones/v1.0-phases/03-session-management-recording-naming/03-01-SUMMARY.md
.planning/milestones/v1.0-phases/03-session-management-recording-naming/03-02-SUMMARY.md
.planning/milestones/v1.0-phases/03-session-management-recording-naming/03-03-SUMMARY.md
.planning/milestones/v1.0-phases/03-session-management-recording-naming/03-04-SUMMARY.md
.planning/milestones/v1.0-phases/08-code-defect-fixes/08-01-SUMMARY.md
.planning/milestones/v1.0-phases/08-code-defect-fixes/08-02-SUMMARY.md
EOF
      sort /tmp/26.1-expected-commit.txt -o /tmp/26.1-expected-commit.txt
      diff /tmp/26.1-committed.txt /tmp/26.1-expected-commit.txt && echo "PASS: commit contains exactly 8 expected files"

      # No Claude attribution anywhere in the commit message.
      if printf '%s\n' "$msg" | grep -qiE 'co-authored-by:[[:space:]]*claude|generated with[[:space:]]+\[?claude|🤖|claude\.com/claude-code|noreply@anthropic\.com'; then
        echo "FAIL: commit message contains Claude attribution (violates global CLAUDE.md)"
        printf '%s\n' "$msg"
        exit 1
      fi
      echo "PASS: no Claude attribution in commit message"

      # Working tree clean (commit landed, nothing left staged or unstaged).
      if [[ -n "$(git status --porcelain | grep -E '^.M|^M.|^A.|^.A')" ]]; then
        echo "FAIL: working tree has uncommitted changes after commit"
        git status --short
        exit 1
      fi
      echo "PASS: working tree clean for the 8 target files"
    </automated>
  </verify>

  <acceptance_criteria>
    - `git log -1 --pretty=%s` matches `fix(26.1): add requirements-completed: [] to 8 v1.0-archive SUMMARYs (PROCESS-03 closure)`.
    - `git diff-tree --no-commit-id --name-only -r HEAD` lists exactly the 8 expected SUMMARY paths.
    - `git log -1 --pretty=%B` does NOT contain `Co-Authored-By: Claude`, `🤖`, `Generated with Claude Code`, `claude.com/claude-code`, or `noreply@anthropic.com` (case-insensitive).
    - `git status --porcelain` is empty for the 8 target files (no leftover unstaged edits).
    - Commit is NOT pushed (the plan ends at local commit; pushing is a separate user-initiated action).
  </acceptance_criteria>

  <done>
    Single commit on HEAD, subject matches the prescribed shape, exactly the 8 expected files changed, no Claude attribution in the message body or trailers, working tree clean.
  </done>
</task>

</tasks>

<threat_model>
N/A — frontmatter-only migration. No code paths, network calls, authentication flows, data flows, or user-input boundaries are touched. The lint script (`scripts/lint-summaries.sh`) is read-only for this phase. The CI workflow (`.github/workflows/lint-summaries.yml`) is read-only for this phase. No Swift source code under `PSTranscribe/` is touched. Risk surface: zero.
</threat_model>

<verification>
End-to-end gate check (run after Task 3):

```bash
# 1. Lint script exits 0.
bash scripts/lint-summaries.sh
echo "exit: $?"   # expect: 0

# 2. Exactly one new commit, exactly 8 files, no attribution.
git log -1 --stat --pretty=fuller
git log -1 --pretty=%B | grep -ciE 'co-authored-by:[[:space:]]*claude|🤖|generated with[[:space:]]+\[?claude'
# expect: 0

# 3. Lint corpus count preserved at 55.
find .planning/milestones -type f -name '*-SUMMARY.md' | wc -l
# expect: 55
```

All three checks must pass. If `bash scripts/lint-summaries.sh` does not exit 0, the phase is NOT closed regardless of commit state.
</verification>

<success_criteria>
The phase is DONE when ALL of the following are simultaneously true:

1. `bash scripts/lint-summaries.sh` exits 0 from the project root.
2. The lint summary line reads `55 SUMMARY files checked, 0 failures, 0 warnings`.
3. All 8 specified SUMMARY files contain the canonical key `requirements-completed: []` inside their YAML frontmatter block.
4. The body of each of the 8 SUMMARY files (everything after the closing `---` of the frontmatter) is byte-for-byte identical to its pre-change state.
5. A single commit on HEAD with the prescribed `fix(26.1): ...` subject, exactly 8 files changed, no Claude attribution in subject or body.

These map 1:1 to the 5 must-haves in CONTEXT.md and the audit's closure criteria. PROCESS-03 returns to `satisfied` in the v1.3 milestone audit. The `Lint Summaries` CI gate unblocks every subsequent PR.
</success_criteria>

<output>
After completion, create `.planning/phases/26.1-close-gap-process-03-lint-summaries-failure-on-v1-0-archive/26.1-01-SUMMARY.md` per the standard summary template. The SUMMARY's own `requirements-completed:` frontmatter field should list `[PROCESS-03]` — this plan completes PROCESS-03's closure.
</output>
