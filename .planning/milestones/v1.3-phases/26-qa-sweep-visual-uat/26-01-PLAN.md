---
phase: 26-qa-sweep-visual-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/26-qa-sweep-visual-uat/26-UAT.md
autonomous: false
requirements: [QA-01, QA-02, QA-03, QA-04, QA-05, QA-06, QA-07, QA-08, QA-09]
tags: [qa, visual-uat, manual-attestation, phase-21-bridge, phase-19-backlog, v1.3]

must_haves:
  truths:
    - "Honors D-01 (hybrid environment): QA-01 / QA-02 ran live with Maccy AND Alfred (one already installed, missing one installed for the cycle); QA-03 split into 03a (single-display PASS) and 03b (multi-monitor UNTESTABLE-this-cycle with code-pointer gap note); QA-04 WITHDRAWN with codebase-verified non-sandboxed reasoning citing PSTranscribe.entitlements + AppSettings.swift:82,97"
    - "Honors D-02 (batch-triage size-based routing): all 9 scenarios run before any fix-routing decision; PASS / FAIL is logged inline during the sweep; routing is decided ONLY after the full sweep is complete; FAILs <1hr AND ≤2 files route to 26-NN-PLAN.md (in-phase), larger fixes route to .planning/todos/pending/qa-NN-<short>.md"
    - "Honors D-03 (text attestation, citation rows for QA-06..09): no screenshots / recordings; QA-06..QA-09 cite 21-HUMAN-UAT.md Tests 1-4 (passed 2026-05-01, user attestation) rather than re-execute"
    - "Honors D-04 (single execution plan + 26-UAT.md filename + CI todo NOT folded): one 26-01-PLAN.md covers full sweep; artifact filename is 26-UAT.md (matches ROADMAP success criterion 1); ci-build-check-failing-on-main.md is explicitly out of scope"
    - "Build provenance: local debug build from main HEAD via `swift build -c debug` (or Xcode Run); not v2.2.0 release notarized binary"
    - "QA-04 WITHDRAW reasoning is codebase-verified BEFORE writing the row: `grep -r 'withSecurityScope' PSTranscribe/Sources` returns 0 hits; `grep 'com.apple.security.app-sandbox' PSTranscribe/Sources/PSTranscribe/PSTranscribe.entitlements` returns 0 hits"
    - "Citation rows verified BEFORE writing: 21-HUMAN-UAT.md exists at `.planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md` with `status: resolved` and contains Test 1 through Test 4"
    - "26-UAT.md created at .planning/phases/26-qa-sweep-visual-uat/26-UAT.md with frontmatter `status: in-progress` at start, transitioning to `resolved` (no UNTESTABLE-this-cycle rows) or `approved-with-debt` (at least one UNTESTABLE-this-cycle row, e.g., QA-03b) at close"
    - "26-UAT.md contains rows for QA-01, QA-02, QA-03a, QA-03b, QA-04, QA-05, QA-06, QA-07, QA-08, QA-09 (10 row IDs across 9 scenarios; QA-03 split per Phase 24/25 PARTIAL a/b convention)"
    - "26-UAT.md QA-06..09 citation rows literally contain `Source: .planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md (Test N, passed 2026-05-01, user attestation)` (one per QA-06/07/08/09 mapping to Tests 1/2/3/4)"
    - "QA-04 WITHDRAW row reasoning explicitly cites `PSTranscribe.entitlements` AND `AppSettings.swift:82,97` AND the zero-hit grep for `withSecurityScope` / `startAccessing` / `stopAccessing` — not the speculative bookmark framing from PITFALLS.md line 519"
    - "QA-03b UNTESTABLE-this-cycle row has a `code_ref:` field pointing at `PSTranscribe/Sources/PSTranscribe/App/DictationWindowController.swift` (the multi-screen positioning math at lines 124-141 currently uses NSScreen.main.visibleFrame; multi-monitor coverage requires NSScreen.screens iteration)"
    - "QA-05 mechanism is debug-only override of `ModelUpdateService.diskSpaceProvider` (already test-injectable at line 72) to return a low-byte value; OR mark UNTESTABLE-this-cycle if the override surface cannot be reached from a debug build without source modification — planner reads ModelUpdateService.swift first to confirm reachability"
    - "Each FAIL row has a `routing:` line valued either `in-phase: 26-NN-PLAN.md` or `todo: qa-NN-<short>.md` per D-02 size rule"
    - "## Summary block totals match row count (split rows count separately per Phase 24/25 PARTIAL convention; QA-03 splits = total 10, no split = total 9)"
    - "## Cross-References footer cites REQUIREMENTS.md / ROADMAP.md / PITFALLS.md lines 513-528 / 21-HUMAN-UAT.md / 25-CONTEXT.md D-03 / ci-build-check-failing-on-main.md (explicitly NOT folded)"
    - "Conditional-extension protocol documented inline in this plan: after the sweep, if batch-triage routes a FAIL in-phase, a separate 26-02-PLAN.md is authored (NOT in this planning pass); if routed to a todo, file lands in .planning/todos/pending/qa-NN-<short>.md"
  artifacts:
    - path: ".planning/phases/26-qa-sweep-visual-uat/26-UAT.md"
      provides: "Manual UAT attestation for QA-01..QA-09 (10 rows across 9 scenarios; QA-03 split into 03a/03b)"
      contains: "status: in-progress (start) or resolved | approved-with-debt (close)"
      min_lines: 120
  key_links:
    - from: ".planning/phases/26-qa-sweep-visual-uat/26-UAT.md"
      to: ".planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md"
      via: "QA-06..09 citation rows literally cite 21-HUMAN-UAT.md (Tests 1-4)"
      pattern: "21-HUMAN-UAT\\.md.*passed 2026-05-01"
    - from: ".planning/phases/26-qa-sweep-visual-uat/26-UAT.md"
      to: ".planning/REQUIREMENTS.md"
      via: "QA-NN row IDs match REQUIREMENTS.md QA-01..QA-09 definitions"
      pattern: "QA-0[1-9]"
    - from: ".planning/phases/26-qa-sweep-visual-uat/26-UAT.md"
      to: "PSTranscribe/Sources/PSTranscribe/PSTranscribe.entitlements"
      via: "QA-04 WITHDRAW reasoning explicitly cites entitlements file (no com.apple.security.app-sandbox key) + AppSettings.swift:82,97 + zero-hit grep result"
      pattern: "PSTranscribe\\.entitlements|AppSettings\\.swift:(82|97)|withSecurityScope"
    - from: ".planning/phases/26-qa-sweep-visual-uat/26-UAT.md"
      to: "PSTranscribe/Sources/PSTranscribe/App/DictationWindowController.swift"
      via: "QA-03b UNTESTABLE-this-cycle code_ref points at multi-screen positioning gap"
      pattern: "DictationWindowController\\.swift"
---

<objective>
Run the 9 deferred QA scenarios (5 from Phase 19's "Looks Done But Isn't" backlog: QA-01..QA-05; 4 from Phase 21's titlebar visual UAT cross-ref: QA-06..QA-09) against a local debug build of PS Transcribe from `main` HEAD and produce `26-UAT.md` documenting PASS / FAIL / WITHDRAWN / UNTESTABLE-this-cycle per scenario.

Per CONTEXT.md decisions:
- D-01 (hybrid environment): QA-01 / QA-02 run live with both Maccy and Alfred; QA-03 splits into 03a (single-display PASS-able) + 03b (multi-monitor UNTESTABLE-this-cycle); QA-04 entirely WITHDRAWN with codebase-verified non-sandboxed reasoning.
- D-02 (batch-triage routing): all 9 scenarios run before any fix decision; size threshold (<1hr AND ≤2 files) determines in-phase plan vs deferred todo.
- D-03 (text attestation, QA-06..09 citation): no screenshots; QA-06..09 cite 21-HUMAN-UAT.md Tests 1-4 (passed 2026-05-01) rather than re-execute.
- D-04 (single plan + 26-UAT.md filename + CI todo NOT folded): this is the sole initial plan; ci-build-check-failing-on-main.md is reviewed and explicitly out of scope.

Purpose: ROADMAP success criterion 1 + Phase 25 D-03 forward cross-ref to Phase 26 (QA-06..09 must surface as rows in 26-UAT.md, even via citation) + QA-FUT-01 closure.

Output:
- 1 new artifact: `.planning/phases/26-qa-sweep-visual-uat/26-UAT.md` with 10 rows (QA-01, QA-02, QA-03a, QA-03b, QA-04, QA-05, QA-06, QA-07, QA-08, QA-09); frontmatter `status: in-progress` -> `resolved` | `approved-with-debt` at close.
- 0..N conditional fix plans `26-NN-PLAN.md` for in-phase remediations per D-02 size rule (NOT authored in this planning pass — appended only IF batch-triage routes a FAIL in-phase).
- 0..N conditional deferred todos `.planning/todos/pending/qa-NN-<short>.md` per D-02 (anything bigger / architectural).
- 1 SUMMARY.md (created at plan close, includes `requirements-completed: [QA-01, QA-02, QA-03, QA-04, QA-05, QA-06, QA-07, QA-08, QA-09]`).

Conditional-extension protocol (READ THIS BEFORE THE BATCH-TRIAGE TASK):
- IF a FAIL fix is <1hr AND ≤2 files: author `26-02-PLAN.md` (or 26-03, etc.) in this same phase directory using the same execution-plan skeleton as this plan but with smaller scope. Re-run the failing scenario at the end of that plan, flip the row in 26-UAT.md from FAIL to PASS, and update the routing line.
- IF a FAIL fix is bigger / architectural: create `.planning/todos/pending/qa-NN-<short>.md` (e.g., `qa-05-disk-space-preflight-warning.md`) using the frontmatter pattern from `.planning/todos/pending/ci-build-check-failing-on-main.md`. Title prefix matches the QA-NN that surfaced it.
- Status terminal state: if QA-03b lands as UNTESTABLE-this-cycle and no other UNTESTABLE rows are added, terminal state is `approved-with-debt`. If every row is PASS / WITHDRAWN / citation, terminal state is `resolved`. Do NOT block on `resolved`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-qa-sweep-visual-uat/26-CONTEXT.md
@.planning/phases/26-qa-sweep-visual-uat/26-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/milestones/v1.2-phases/18.1-shared-save-destinations-local-file/18.1-UAT.md
@.planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md
@.planning/phases/24-nyquist-sweep-v1-0/24-HUMAN-UAT.md
@.planning/phases/25-nyquist-sweep-v1-2/25-CONTEXT.md
@.planning/todos/pending/ci-build-check-failing-on-main.md
@PSTranscribe/Sources/PSTranscribe/PSTranscribe.entitlements
@PSTranscribe/Sources/PSTranscribe/Settings/AppSettings.swift
@PSTranscribe/Sources/PSTranscribe/App/DictationCoordinator.swift
@PSTranscribe/Sources/PSTranscribe/App/DictationWindowController.swift
@PSTranscribe/Sources/PSTranscribe/Views/DictationHUD.swift
@PSTranscribe/Sources/PSTranscribe/Services/ModelUpdateService.swift
@PSTranscribe/Sources/PSTranscribe/Services/SaveDestinations.swift

<interfaces>
<!-- Source-of-truth values verified 2026-05-08 via direct grep against PSTranscribe/Sources -->

# Entitlements (PSTranscribe/Sources/PSTranscribe/PSTranscribe.entitlements) — full file:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <key>com.apple.security.device.screen-capture</key>
    <true/>
</dict>
</plist>
```
NO `com.apple.security.app-sandbox` key. App is non-sandboxed. Underwrites QA-04 WITHDRAW.

# AppSettings.swift folder URL persistence (Settings/AppSettings.swift:81-97):
```swift
// line 81-83: localFileRoot
var localFileRoot: String {
    didSet { UserDefaults.standard.set(localFileRoot, forKey: "localFileRoot") }
}

// line 96-98: obsidianFolderPath
var obsidianFolderPath: String {
    didSet { UserDefaults.standard.set(obsidianFolderPath, forKey: "obsidianFolderPath") }
}
```
Plain `String` paths in `UserDefaults`, NOT `bookmarkData(options: .withSecurityScope)`. Underwrites QA-04 WITHDRAW.

# Security-scoped bookmark API usage (verified 2026-05-08):
```
$ grep -r "withSecurityScope\|startAccessing\|stopAccessing" PSTranscribe/Sources
(no output — zero hits)
```

# DictationCoordinator clipboard write site (App/DictationCoordinator.swift:413-415):
```swift
pb.setString(text, forType: .string)
pb.setData(Data(), forType: NSPasteboard.PasteboardType("org.nspasteboard.TransientType"))
pb.setData(Data(), forType: NSPasteboard.PasteboardType("org.nspasteboard.AutoGeneratedType"))
```
Both privacy markers written. Code path under test by QA-01 / QA-02 — Maccy and Alfred MUST honor these markers and exclude the transcript from history.

# DictationWindowController positioning math (App/DictationWindowController.swift:120-141):
```swift
private func reposition() {
    guard let panel = window, let screen = NSScreen.main else { return }
    let screenFrame = screen.visibleFrame
    // ... width clamping ...
    let bottomBandTop = screenFrame.minY + (screenFrame.height / 4)
    let bottomBandMid = (screenFrame.minY + bottomBandTop) / 2
    let x = screenFrame.midX - (panelSize.width / 2)
    let y = bottomBandMid - (panelSize.height / 2)
    panel.setFrame(NSRect(x: x, y: y, width: panelSize.width, height: panelSize.height), display: false)
}
```
Uses `NSScreen.main` only — single-display safe; multi-monitor scenarios (primary, secondary-left/right, vertical, mid-recording disconnect) would require iterating `NSScreen.screens` and tracking the screen the dictation hotkey was triggered on. Underwrites QA-03b UNTESTABLE-this-cycle code_ref.

# ModelUpdateService.diskSpaceProvider (Services/ModelUpdateService.swift:70-72) — TEST-INJECTABLE:
```swift
/// Test-injectable disk-space provider. Default: real URL.resourceValues lookup.
/// Tests override via `service.diskSpaceProvider = { _ in 100 }` for low-disk simulation.
var diskSpaceProvider: @Sendable (URL) throws -> Int64 = { url in
    // ... real lookup ...
}
```
And the preflight call site (lines 212-222):
```swift
// Disk-space preflight (MODEL-10 / Pitfall #14).
do {
    try FileManager.default.createDirectory(at: modelsRoot, withIntermediateDirectories: true)
    try checkDiskSpace(needed: totalBytes)
} catch let ModelUpdateError.insufficientDiskSpace(needed, available) {
    updateState = .blocked(reason: .insufficientDiskSpace(needed: needed, available: available))
    return
} catch {
    updateState = .failed(message: error.localizedDescription)
    return
}
```
QA-05 mechanism: `diskSpaceProvider` is `var` and `@Sendable` — overridable from a debug-only code path (e.g., a `#if DEBUG`-gated swap in `PSTranscribeApp.init` or `ModelUpdateService.shared` setup). Confirms QA-05 is reachable WITHOUT shipping a debug build flag — but a one-line debug-only override IS a source modification that must be reverted before commit. Mark UNTESTABLE-this-cycle ONLY if the user is unwilling to apply and revert this temporary debug-only override.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight verification — confirm codebase-verified facts that underwrite QA-04 WITHDRAW and citation-row provenance</name>
  <files>none (read-only verification)</files>
  <read_first>
    - PSTranscribe/Sources/PSTranscribe/PSTranscribe.entitlements (entire file — confirm only audio-input + screen-capture keys; NO com.apple.security.app-sandbox)
    - PSTranscribe/Sources/PSTranscribe/Settings/AppSettings.swift lines 81-83 + 96-98 (confirm localFileRoot + obsidianFolderPath are `var ... : String` with `UserDefaults.standard.set(..., forKey:)` didSet — NOT `bookmarkData(options:)`)
    - .planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md (confirm `status: resolved`, contains Test 1 / Test 2 / Test 3 / Test 4, all 4 marked `result: passed (user attestation 2026-05-01)`)
    - .planning/phases/26-qa-sweep-visual-uat/26-CONTEXT.md (D-01 QA-04 WITHDRAW rationale; D-03 citation row contract)
  </read_first>
  <action>
    Run these four shell verifications BEFORE any sweep step. If ANY fails, STOP and escalate to the user — the WITHDRAW row OR a citation row is no longer codebase-supported and the plan needs revision before proceeding.

    Verification 1 — non-sandboxed entitlements (QA-04 WITHDRAW underwriter):
    ```
    grep -E "com.apple.security.app-sandbox" PSTranscribe/Sources/PSTranscribe/PSTranscribe.entitlements
    ```
    Expected output: empty (zero matches). If ANY match: STOP — app is now sandboxed and QA-04 needs to be re-executed, not WITHDRAWN.

    Verification 2 — zero security-scoped bookmark API usage (QA-04 WITHDRAW underwriter):
    ```
    grep -r "withSecurityScope\|startAccessing\|stopAccessing" PSTranscribe/Sources
    ```
    Expected output: empty (zero matches). If ANY match: STOP — security-scoped bookmarks are now in use and QA-04 needs to be re-executed.

    Verification 3 — UserDefaults string-path persistence (QA-04 WITHDRAW underwriter):
    ```
    grep -nE "var (localFileRoot|obsidianFolderPath): String" PSTranscribe/Sources/PSTranscribe/Settings/AppSettings.swift
    ```
    Expected output: exactly 2 matches at lines 81 and 96 (or near; minor line drift acceptable as long as both vars resolve as `: String` with UserDefaults didSet). If output shows `bookmarkData` or `Data` or count != 2: STOP and re-read AppSettings.swift.

    Verification 4 — 21-HUMAN-UAT.md citation source (QA-06..09 citation underwriter):
    ```
    grep -E "^status: resolved" .planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md && grep -cE "^### [1234]\." .planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md && grep -c "passed (user attestation 2026-05-01)" .planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md
    ```
    Expected: `status: resolved` line printed; second grep returns 4 (Tests 1-4); third grep returns at least 4 (one passed attestation per test). If any check fails: STOP — the citation source is no longer authoritative.

    Record the results inline as comments on the eventual 26-UAT.md row authoring (Task 12). Do not write to 26-UAT.md yet.
  </action>
  <verify>
    <automated>grep -c "com.apple.security.app-sandbox" PSTranscribe/Sources/PSTranscribe/PSTranscribe.entitlements; echo "---"; grep -rc "withSecurityScope\|startAccessing\|stopAccessing" PSTranscribe/Sources; echo "---"; grep -cE "var (localFileRoot|obsidianFolderPath): String" PSTranscribe/Sources/PSTranscribe/Settings/AppSettings.swift; echo "---"; grep -cE "^### [1234]\." .planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md; echo "---"; grep -c "passed (user attestation 2026-05-01)" .planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "com.apple.security.app-sandbox" PSTranscribe/Sources/PSTranscribe/PSTranscribe.entitlements` returns 0
    - `grep -rc "withSecurityScope\|startAccessing\|stopAccessing" PSTranscribe/Sources` returns 0 (or, if grep emits one line per file, every line ends in `:0`)
    - `grep -cE "var (localFileRoot|obsidianFolderPath): String" PSTranscribe/Sources/PSTranscribe/Settings/AppSettings.swift` returns exactly 2
    - `grep -cE "^### [1234]\." .planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md` returns 4
    - `grep -c "passed (user attestation 2026-05-01)" .planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md` returns at least 4
    - `grep -E "^status: resolved" .planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md` prints one line
  </acceptance_criteria>
  <done>QA-04 WITHDRAW reasoning is codebase-verified; QA-06..09 citation source is intact at status: resolved with all 4 tests user-attested 2026-05-01.</done>
</task>

<task type="manual">
  <name>Task 2: Build app from main HEAD and confirm dictation hotkey fires (smoke test)</name>
  <files>none (build artifact only; not source-modified)</files>
  <read_first>
    - .planning/phases/26-qa-sweep-visual-uat/26-CONTEXT.md (D-03 — local debug build from main HEAD; not Sparkle release)
  </read_first>
  <action>
    1. Confirm `git status` is clean (or stash any in-progress edits to a separate branch). Confirm `git rev-parse HEAD` matches `git rev-parse origin/main` OR is intentionally ahead of main with phase-26-related commits only.
    2. Build PS Transcribe from `main` HEAD:
       ```
       cd PSTranscribe && swift build -c debug
       ```
       Confirm exit 0. The built binary lives at `PSTranscribe/.build/debug/PSTranscribe` (CLI tool).
    3. For UI testing, run via Xcode: open `PSTranscribe/Package.swift` in Xcode, select the `PSTranscribe` scheme, Product > Run. Confirm the app launches without errors in the Xcode console.
    4. Confirm dictation hotkey fires: press the configured global hotkey (default `Cmd+Shift+D` per Phase 18 D-08), confirm the floating HUD appears at bottom-center of the primary screen, speak a short phrase, end dictation, confirm the transcribed text is on the clipboard (Cmd-V into a text field).
    5. Note in your sweep notes: build provenance (`git rev-parse HEAD` short SHA), build timestamp, macOS version (`sw_vers -productVersion`), Xcode version (`xcodebuild -version | head -1`).
    6. Ask user: "Which clipboard manager is currently installed — Maccy or Alfred?" and "Is the missing one already installed for this cycle, or do we need to install it now?" Record both manager versions for QA-01 / QA-02 PASS criteria.
    7. If a manager is missing, install it:
       - Maccy missing: `brew install --cask maccy` (free).
       - Alfred missing: download from https://www.alfredapp.com/ (Powerpack required for clipboard history; user-driven download).
    8. Launch both clipboard managers, confirm they are running concurrently and have clipboard history visible (settings panel of each).
  </action>
  <verify>
    <manual>User confirms: (a) `swift build -c debug` exits 0 from main HEAD, (b) the built app launches via Xcode without console errors, (c) the dictation hotkey triggers the HUD and a short dictation produces clipboard text, (d) both Maccy and Alfred are running with clipboard history visible.</manual>
  </verify>
  <acceptance_criteria>
    - User attests: `swift build -c debug` exited 0 (yes/no)
    - User attests: app launches via Xcode without errors (yes/no)
    - User attests: dictation hotkey fires and produces clipboard text (yes/no — smoke test)
    - User attests: both Maccy AND Alfred are running with clipboard history visible (yes/no — D-01 hybrid environment ready)
    - Build provenance recorded (git SHA, macOS version, Xcode version) for inclusion in 26-UAT.md preamble
  </acceptance_criteria>
  <done>Debug build runs; dictation smoke test passes; both clipboard managers active. Environment ready for QA-01 onward.</done>
</task>

<task type="manual">
  <name>Task 3: QA-01 — Verify clipboard history exclusion under Maccy with privacy markers</name>
  <files>none</files>
  <read_first>
    - .planning/REQUIREMENTS.md line 15 (QA-01 definition: clipboard history exclusion under Maccy with `org.nspasteboard.TransientType` + `AutoGeneratedType`)
    - PSTranscribe/Sources/PSTranscribe/App/DictationCoordinator.swift lines 413-415 (privacy markers written: `org.nspasteboard.TransientType` + `org.nspasteboard.AutoGeneratedType`)
    - .planning/phases/26-qa-sweep-visual-uat/26-CONTEXT.md Specifics §"Maccy + Alfred privacy-marker semantics" (PASS criterion is ABSENCE in manager UI, not positive event log)
  </read_first>
  <action>
    Pre-condition: Maccy is running. Confirm Maccy version (Maccy menubar > Maccy > About). Confirm Maccy "Ignore Items" / privacy settings are at defaults (Maccy menubar > Preferences > Storage > "Ignore Items" — typically empty list at default; Maccy honors `org.nspasteboard.TransientType` + `AutoGeneratedType` automatically).

    Test sequence:
    1. Open Maccy's history pane (default hotkey Cmd+Shift+C OR menubar icon) and note the most recent item — call it the "baseline marker."
    2. Trigger PS Transcribe dictation hotkey (Cmd+Shift+D), speak a unique phrase you can recognize (e.g., "QA test phrase alpha"), end dictation. Confirm the phrase is on the clipboard via Cmd-V into Notes.app.
    3. Open Maccy's history pane again. Look for the dictation phrase ("QA test phrase alpha") in Maccy's history list.
    4. PASS criterion: the dictation phrase MUST NOT appear in Maccy's history list. The "baseline marker" you noted in step 1 should still be the most recent (or be displaced only by a non-dictation clipboard event you generated separately, e.g., a manual Cmd-C of unrelated text).
    5. Edge cases to also verify:
       - Wait 5 seconds after dictation ends and check Maccy again (confirms no delayed addition).
       - Trigger a second dictation with a different phrase and confirm the same exclusion behavior.
       - If Maccy DOES show the phrase: capture Maccy's full version + privacy settings + a console excerpt (Console.app > search "Maccy") and mark the row as `result: fail` with detailed `reported:` notes.

    Write sweep notes (NOT 26-UAT.md yet — that's Task 12): record Maccy version, privacy-settings state, baseline marker, dictation phrase used, observed outcome, PASS / FAIL.
  </action>
  <verify>
    <manual>User confirms: dictation phrase did NOT appear in Maccy's history list (PASS) OR did appear (FAIL with version + settings + console excerpt captured).</manual>
  </verify>
  <acceptance_criteria>
    - User attests: dictation phrase does/does-not appear in Maccy's history list (PASS or FAIL with notes)
    - Maccy version recorded
    - Maccy "Ignore Items" / privacy-settings state recorded (the PASS row in 26-UAT.md will reference these)
    - If FAIL: full reproduction details captured (version, settings, console excerpt) for routing in Task 11
  </acceptance_criteria>
  <done>QA-01 result attested by user; sweep notes record version + settings + outcome.</done>
</task>

<task type="manual">
  <name>Task 4: QA-02 — Verify clipboard history exclusion under Alfred with privacy markers</name>
  <files>none</files>
  <read_first>
    - .planning/REQUIREMENTS.md line 16 (QA-02 definition: clipboard history exclusion under Alfred clipboard history)
    - PSTranscribe/Sources/PSTranscribe/App/DictationCoordinator.swift lines 413-415 (same privacy markers as QA-01)
    - .planning/phases/26-qa-sweep-visual-uat/26-CONTEXT.md Specifics §"Maccy + Alfred privacy-marker semantics"
  </read_first>
  <action>
    Pre-condition: Alfred is running with Powerpack license active (clipboard history requires Powerpack). Confirm Alfred version (Alfred menubar > About Alfred). Confirm Alfred Clipboard History is enabled (Alfred Preferences > Features > Clipboard History > Keep History "On"). Note Alfred's Universal Action / privacy settings (Alfred Preferences > Features > Clipboard History > Mac Apps to Ignore — typically empty at defaults; Alfred honors `org.nspasteboard.TransientType` + `AutoGeneratedType` per the nspasteboard.org informal contract).

    Test sequence (mirrors QA-01 for Alfred):
    1. Open Alfred's clipboard history (default hotkey Option+Cmd+C OR Alfred bar > "clipboard"). Note the most recent item — "baseline marker."
    2. Trigger dictation, speak a unique phrase (e.g., "QA test phrase bravo"), end dictation. Confirm via Cmd-V into Notes.app.
    3. Open Alfred's clipboard history again. Look for the dictation phrase.
    4. PASS criterion: the dictation phrase MUST NOT appear in Alfred's clipboard history.
    5. Edge cases:
       - Wait 5 seconds and re-check.
       - Trigger a second dictation with a different phrase.
       - If Alfred DOES show the phrase: capture Alfred version + Clipboard History settings + Console.app excerpt and mark `result: fail`.

    Sweep notes: Alfred version, Powerpack status, Clipboard History settings, baseline marker, dictation phrase, outcome, PASS / FAIL.
  </action>
  <verify>
    <manual>User confirms: dictation phrase did NOT appear in Alfred's clipboard history (PASS) OR did appear (FAIL with version + settings + console excerpt captured).</manual>
  </verify>
  <acceptance_criteria>
    - User attests: dictation phrase does/does-not appear in Alfred's clipboard history (PASS or FAIL with notes)
    - Alfred version + Powerpack status recorded
    - Alfred Clipboard History settings recorded
    - If FAIL: full reproduction details captured for routing in Task 11
  </acceptance_criteria>
  <done>QA-02 result attested by user; sweep notes record version + settings + outcome.</done>
</task>

<task type="manual">
  <name>Task 5: QA-03a — Verify single-display HUD positioning</name>
  <files>none</files>
  <read_first>
    - .planning/REQUIREMENTS.md line 17 (QA-03 definition — multi-monitor matrix; D-01 splits into 03a single-display PASS-able + 03b multi-monitor UNTESTABLE-this-cycle)
    - PSTranscribe/Sources/PSTranscribe/App/DictationWindowController.swift lines 120-141 (positioning math: `NSScreen.main.visibleFrame`, bottom-quarter band, x = midX - panelWidth/2, y = bottomBandMid - panelHeight/2)
    - .planning/phases/26-qa-sweep-visual-uat/26-CONTEXT.md D-01 + Claude's Discretion §"QA-03 single-display PASS criteria"
  </read_first>
  <action>
    Single-display test (D-01 — multi-monitor sub-scenarios are QA-03b, not this row).

    Test sequence:
    1. Confirm only one display is connected (no external monitors, no Sidecar/iPad mirror).
    2. Trigger dictation hotkey. Observe the floating HUD position.
    3. PASS criteria (all four must hold):
       - HUD appears at the bottom of the primary screen (vertically centered within the bottom quarter band per the code at lines 134-141).
       - HUD is horizontally centered (x = midX - panelWidth/2).
       - HUD does NOT clip below the Dock (the bottom-quarter band uses `screenFrame.minY` from `visibleFrame`, which excludes the Dock).
       - HUD respects the menubar / notch safe-area at top (HUD is in the bottom quarter, so this is automatic but confirm no overlap with content).
    4. Variant tests:
       - Hide the Dock, re-trigger dictation, confirm HUD position adapts (visibleFrame includes the Dock area now).
       - Show the Dock at the side (System Settings > Desktop & Dock > Position on screen), re-trigger dictation, confirm HUD adapts.
       - On a notched MacBook (if available), confirm HUD does not overlap the notch — bottom-of-primary positioning makes this trivial; record "notch-safe by construction" if applicable.
    5. End dictation cleanly each time.

    Sweep notes: display configuration (built-in display only, model + resolution + scaling), Dock position, HUD position observed, PASS / FAIL.
  </action>
  <verify>
    <manual>User confirms: HUD appears at bottom-center of primary screen, no clipping under Dock, no overlap with menubar/notch (PASS) OR has positioning issue (FAIL with reproduction details).</manual>
  </verify>
  <acceptance_criteria>
    - User attests: HUD position is correct in single-display mode (PASS or FAIL)
    - Display info recorded (model, resolution, scaling, Dock position, notch presence)
    - HUD position observations recorded (vertically in bottom quarter? horizontally centered? no clipping?)
    - If FAIL: code_ref pointing at DictationWindowController.swift lines 120-141 captured for routing in Task 11
  </acceptance_criteria>
  <done>QA-03a result attested by user.</done>
</task>

<task type="manual">
  <name>Task 6: QA-03b — Acknowledge multi-monitor sub-scenarios as UNTESTABLE-this-cycle</name>
  <files>none</files>
  <read_first>
    - .planning/REQUIREMENTS.md line 17 (full QA-03 definition — primary/secondary-left/secondary-right/vertical/mid-recording disconnect)
    - PSTranscribe/Sources/PSTranscribe/App/DictationWindowController.swift lines 120-141 (current code uses NSScreen.main only; multi-monitor would need NSScreen.screens iteration + tracking which screen the hotkey was triggered on)
    - .planning/phases/26-qa-sweep-visual-uat/26-CONTEXT.md D-01 (multi-monitor split + rejected alternatives: Sidecar/iPad rejected for added latency; synthesized NSScreen mock test rejected because it conflicts with manual-sweep boundary)
    - .planning/phases/26-qa-sweep-visual-uat/26-CONTEXT.md Deferred §"Multi-monitor HUD QA when hardware is available"
  </read_first>
  <action>
    No execution. This task captures the UNTESTABLE-this-cycle decision in the sweep notes for Task 12 to encode in 26-UAT.md.

    Confirm with user:
    1. "You do not have a multi-monitor rig available for this cycle?" (yes — hardware unavailable)
    2. "Is Sidecar / iPad mirror acceptable as a second display?" (no per D-01 — adds latency / confounding factors)
    3. "Should we promote QA-03b to a deferred todo?" (no per CONTEXT.md Deferred §"Multi-monitor HUD QA when hardware is available" — the gap note in 26-UAT.md is sufficient; no debt-creation for a hardware-availability question that resolves naturally next cycle)

    Sweep notes: record QA-03b as `result: untestable-this-cycle` with reason (hardware unavailable; alternatives rejected per D-01) and code_ref (`PSTranscribe/Sources/PSTranscribe/App/DictationWindowController.swift` — current `NSScreen.main` math at lines 120-141 is single-display only; multi-monitor coverage requires `NSScreen.screens` iteration).
  </action>
  <verify>
    <manual>User confirms: hardware is unavailable; Sidecar/iPad rejected; UNTESTABLE-this-cycle is the correct disposition (no todo promoted).</manual>
  </verify>
  <acceptance_criteria>
    - User attests: hardware unavailable for this cycle (yes/no)
    - User attests: Sidecar / iPad alternative rejected per D-01 rationale (yes/no)
    - User attests: no deferred todo created — the UNTESTABLE-this-cycle row in 26-UAT.md is the canonical tracker (yes/no)
    - Sweep notes record code_ref pointing at DictationWindowController.swift lines 120-141
  </acceptance_criteria>
  <done>QA-03b disposition captured as UNTESTABLE-this-cycle with code_ref ready for 26-UAT.md authoring.</done>
</task>

<task type="auto">
  <name>Task 7: QA-04 — Acknowledge entire scenario as WITHDRAWN with codebase-verified reasoning</name>
  <files>none</files>
  <read_first>
    - .planning/REQUIREMENTS.md line 18 (QA-04 definition — security-scoped bookmark survival across relaunch + reboot)
    - PSTranscribe/Sources/PSTranscribe/PSTranscribe.entitlements (entire file — only audio-input + screen-capture; no app-sandbox)
    - PSTranscribe/Sources/PSTranscribe/Settings/AppSettings.swift lines 81-83 + 96-98 (UserDefaults string-path persistence)
    - .planning/phases/26-qa-sweep-visual-uat/26-CONTEXT.md D-01 (QA-04 entirely WITHDRAWN with codebase-verified reasoning)
    - .planning/phases/26-qa-sweep-visual-uat/26-CONTEXT.md Specifics §"QA-04's WITHDRAW reason must be the codebase-verified one, not the speculative one"
    - .planning/research/PITFALLS.md lines 513-528 (the speculative bookmark framing that QA-04 was originally derived from)
  </read_first>
  <action>
    No execution. This task records the WITHDRAW reasoning for Task 12 to encode in 26-UAT.md.

    The Task 1 verifications already proved the codebase-verified facts:
    - Entitlements file has only `audio-input` + `screen-capture` keys (no `com.apple.security.app-sandbox`).
    - `localFileRoot` and `obsidianFolderPath` persist as plain `String` in `UserDefaults` (`AppSettings.swift:82,97`), not as `bookmarkData(options: .withSecurityScope)`.
    - `grep -r "withSecurityScope\|startAccessing\|stopAccessing" PSTranscribe/Sources` returns zero matches.

    Sweep notes: WITHDRAW reasoning for 26-UAT.md row body MUST cite all three facts above. The speculative PITFALLS.md framing of "security-scoped bookmark survival" was about a sandboxed scenario that did not materialize — UserDefaults string-path persistence is guaranteed by Apple's UserDefaults contract and survives both relaunch and reboot trivially. Future readers must not be left with a "trust Apple's API" shorthand — the precise reason is "the API isn't in use here."

    No reboot test, no relaunch test. The premise of the test does not exist in the shipped code.
  </action>
  <verify>
    <automated>echo "QA-04 WITHDRAW reasoning recorded; verification facts already confirmed in Task 1"; grep -c "com.apple.security.app-sandbox" PSTranscribe/Sources/PSTranscribe/PSTranscribe.entitlements</automated>
  </verify>
  <acceptance_criteria>
    - Sweep notes record QA-04 as `result: WITHDRAWN` with the three codebase-verified reasons listed verbatim
    - Sweep notes do NOT cite the speculative PITFALLS.md framing as the reason (only as the original derivation source for context)
    - Re-run of Task 1's Verification 1 still returns 0 matches for `com.apple.security.app-sandbox`
  </acceptance_criteria>
  <done>QA-04 WITHDRAW disposition captured with codebase-verified reasoning ready for 26-UAT.md authoring.</done>
</task>

<task type="manual">
  <name>Task 8: QA-05 — Verify disk-space preflight warning triggers via debug-only diskSpaceProvider override</name>
  <files>PSTranscribe/Sources/PSTranscribe/App/PSTranscribeApp.swift (TEMPORARY edit — reverted at end of task)</files>
  <read_first>
    - .planning/REQUIREMENTS.md line 19 (QA-05 definition — disk-space preflight warning is dismissible when free space below threshold)
    - PSTranscribe/Sources/PSTranscribe/Services/ModelUpdateService.swift lines 70-72 (test-injectable `diskSpaceProvider` — `var ... @Sendable (URL) throws -> Int64`)
    - PSTranscribe/Sources/PSTranscribe/Services/ModelUpdateService.swift lines 212-222 (preflight call site — `checkDiskSpace(needed: totalBytes)` throws `ModelUpdateError.insufficientDiskSpace` which sets `updateState = .blocked(reason: .insufficientDiskSpace(...))`)
    - .planning/phases/26-qa-sweep-visual-uat/26-CONTEXT.md D-04 Claude's Discretion §"QA-05 disk-space preflight test mechanics" (debug-only override of threshold; revert before commit)
  </read_first>
  <action>
    Pre-flight: confirm `git status` is clean BEFORE this task. The edit is intentional and temporary — it MUST be reverted at the end of the task.

    Mechanism: `ModelUpdateService.diskSpaceProvider` is `var` and `@Sendable`. A debug-only one-line override in `PSTranscribeApp.init` (or wherever `ModelUpdateService` is instantiated) can force the preflight to fail by returning a tiny byte count.

    Test sequence:
    1. Find `PSTranscribeApp.swift` (or whichever file owns the `ModelUpdateService` instance — likely `PSTranscribeApp.init` or a `@MainActor` setup function). Read the instantiation site.
    2. Apply a `#if DEBUG`-gated override RIGHT AFTER the `ModelUpdateService` instance is created:
       ```swift
       #if DEBUG
       // Phase 26 QA-05 temporary: force disk-space preflight to fail.
       // REVERT BEFORE COMMIT.
       modelUpdateService.diskSpaceProvider = { _ in 100 }  // 100 bytes — far below any model size
       #endif
       ```
       (Adapt the variable name to the actual instance name in your codebase.)
    3. Build and run via Xcode (`Product > Run` with Debug configuration).
    4. Trigger a model update check (Settings > Updates > "Check for Update" button, or whatever the manifest-fetch trigger is).
    5. PASS criteria:
       - The update flow surfaces a "Not enough disk space" warning (text from `ModelUpdateService.swift:614` "Not enough disk space available. Needs N MB, M MB free.").
       - The warning is DISMISSIBLE (close button works, or click-outside dismisses, or app continues normally).
       - The update is DEFERRED — no model files are downloaded, no staging directory is created.
    6. End the test: quit the app.
    7. **REVERT THE OVERRIDE.** Use `git diff` to confirm the only change is the `#if DEBUG` block you added; use `git checkout -- <file>` to revert. Re-run `git status` and confirm clean.
    8. Re-build to confirm the codebase compiles cleanly without the override.

    If the `diskSpaceProvider` override surface is not reachable from `PSTranscribeApp.init` (e.g., the service is owned deeper in the dependency graph and re-assignment is not allowed): mark QA-05 as UNTESTABLE-this-cycle with a code-pointer gap note pointing at `ModelUpdateService.swift:70-72` + the instantiation site, and create a deferred todo `qa-05-disk-space-preflight-warning.md` per the CONTEXT.md Deferred guidance.

    Sweep notes: warning text observed verbatim, dismissibility behavior, deferral behavior, override revert confirmation, PASS / FAIL.
  </action>
  <verify>
    <manual>User confirms: warning surfaces with dismissible alert and update is deferred (PASS); OR mark UNTESTABLE-this-cycle if the override surface is not reachable. After verification: `git status` is clean (override reverted).</manual>
  </verify>
  <acceptance_criteria>
    - User attests: warning text matches "Not enough disk space available" (or equivalent from ModelUpdateService.swift:614) AND is dismissible AND update is deferred (PASS or FAIL)
    - User attests: temporary `#if DEBUG` override has been reverted (`git status` shows clean) BEFORE proceeding to next task
    - If UNTESTABLE-this-cycle: deferred todo `qa-05-disk-space-preflight-warning.md` created with code_ref to ModelUpdateService.swift:70-72
    - If FAIL: full reproduction details + override behavior captured for routing in Task 11
  </acceptance_criteria>
  <done>QA-05 result attested by user OR marked UNTESTABLE-this-cycle; debug override reverted; git status clean.</done>
</task>

<task type="auto">
  <name>Task 9: QA-06 / QA-07 / QA-08 / QA-09 — Capture citation rows from 21-HUMAN-UAT.md (no fresh execution)</name>
  <files>none</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 20-23 (QA-06..09 definitions: Light/Dark/System preference + unfocused-window edge case)
    - .planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md (Tests 1-4 each `result: passed (user attestation 2026-05-01)`; status: resolved)
    - .planning/phases/26-qa-sweep-visual-uat/26-CONTEXT.md D-03 (citation rows, not re-execution; explicit mapping QA-06->Test1, QA-07->Test2, QA-08->Test3, QA-09->Test4)
    - .planning/phases/25-nyquist-sweep-v1-2/25-CONTEXT.md D-03 (Phase 25 forward cross-ref to Phase 26 — citation rows discharge that contract)
  </read_first>
  <action>
    No execution. Task 1's Verification 4 already confirmed `21-HUMAN-UAT.md` exists at `status: resolved` with all 4 Tests user-attested 2026-05-01. This task records the mapping for Task 12.

    Mapping (per D-03 + 21-HUMAN-UAT.md content):
    - **QA-06** (Light preference flips titlebar to cream Chronicle bridge active) -> 21-HUMAN-UAT.md Test 1 ("Scenario A revisited — titlebar strip flips to Dark when picker = Dark while macOS = Light"). NOTE: 21-HUMAN-UAT.md Test 1's content is actually about Dark picker on Light macOS — the "Light flips to cream" assertion is the inverse described in the same test's expectation ("re-flips to cream when picker is set back to Light or System"). The mapping is correct: Test 1 covers BOTH directions of the Light/Dark titlebar flip. If sweep author finds the mapping ambiguous, escalate; do not silently re-map.
    - **QA-07** (Dark preference flips titlebar to system-dark material) -> 21-HUMAN-UAT.md Test 2 ("Scenario B revisited — titlebar strip flips to Light when picker = Light while macOS = Dark"). Same inverse-coverage note as QA-06.
    - **QA-08** (System preference follows OS appearance change live, no app restart) -> 21-HUMAN-UAT.md Test 3 ("Scenario C revisited — System mode follows live macOS Light↔Dark toggle on the titlebar").
    - **QA-09** (Preference toggle while window is unfocused still updates titlebar correctly) -> 21-HUMAN-UAT.md Test 4 ("Settings window titlebar early-return behavior under preference change"). NOTE: Test 4 is about Settings-window early-return, not unfocused-main-window. The QA-09 phrasing "preference toggle while window is unfocused" is closest to Test 4's "across all three picker values" coverage; the unfocused-window edge case is implicit in the Settings-window-while-main-is-unfocused state. If the mapping is contested, escalate.

    Sweep notes: citation row source string for each: `Source: .planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md (Test N, passed 2026-05-01, user attestation)` where N is 1/2/3/4 for QA-06/07/08/09 respectively. Status for each: `pass (citation)`.
  </action>
  <verify>
    <automated>grep -c "passed (user attestation 2026-05-01)" .planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md; grep -cE "^### [1234]\." .planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "passed (user attestation 2026-05-01)" .planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md` returns at least 4
    - `grep -cE "^### [1234]\." .planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md` returns 4
    - Sweep notes record citation source string template + QA-NN-to-Test-N mapping (06->1, 07->2, 08->3, 09->4)
  </acceptance_criteria>
  <done>QA-06..09 citation row content captured for 26-UAT.md authoring.</done>
</task>

<task type="manual">
  <name>Task 10: Confirm sweep is complete — all 9 scenarios have a recorded outcome</name>
  <files>none</files>
  <read_first>
    - sweep notes from Tasks 3-9
  </read_first>
  <action>
    Confirm all 9 scenarios have a recorded disposition before proceeding to batch-triage:
    - QA-01: PASS / FAIL with notes (Task 3)
    - QA-02: PASS / FAIL with notes (Task 4)
    - QA-03a: PASS / FAIL with notes (Task 5)
    - QA-03b: UNTESTABLE-this-cycle with code_ref (Task 6)
    - QA-04: WITHDRAWN with codebase-verified reasoning (Task 7)
    - QA-05: PASS / FAIL with notes OR UNTESTABLE-this-cycle (Task 8)
    - QA-06: pass (citation) — Source: 21-HUMAN-UAT.md Test 1 (Task 9)
    - QA-07: pass (citation) — Source: 21-HUMAN-UAT.md Test 2 (Task 9)
    - QA-08: pass (citation) — Source: 21-HUMAN-UAT.md Test 3 (Task 9)
    - QA-09: pass (citation) — Source: 21-HUMAN-UAT.md Test 4 (Task 9)

    If any scenario is missing a recorded outcome, return to that task and complete it before continuing. Do NOT make routing decisions yet (those happen in Task 11).
  </action>
  <verify>
    <manual>User confirms: every scenario above has a recorded disposition in the sweep notes.</manual>
  </verify>
  <acceptance_criteria>
    - 10 dispositions recorded (one per QA-NN row, with QA-03 split into 03a/03b = 10 rows for 9 scenarios)
    - No scenario is in "pending" or undetermined state
  </acceptance_criteria>
  <done>Sweep is complete; ready for batch-triage in Task 11.</done>
</task>

<task type="manual">
  <name>Task 11: Batch-triage FAILs — apply D-02 size-based routing rule</name>
  <files>none (routing decisions; conditional file creation happens in follow-up plans)</files>
  <read_first>
    - .planning/phases/26-qa-sweep-visual-uat/26-CONTEXT.md D-02 (batch-triage AFTER full sweep, size-based routing: <1hr AND ≤2 files = in-phase 26-NN-PLAN.md; bigger / architectural = .planning/todos/pending/qa-NN-<short>.md)
    - .planning/todos/pending/ci-build-check-failing-on-main.md (canonical todo frontmatter shape)
    - .planning/phases/26-qa-sweep-visual-uat/26-PATTERNS.md §"qa-NN-<short>.md" (filename + body template)
    - sweep notes from Tasks 3-9 (FAIL list)
  </read_first>
  <action>
    Read FAILs as a group (not one-by-one). For each FAIL:

    1. Estimate fix size:
       - File count: how many production source files would need a one-line or small-block edit?
       - Time estimate: <1hr if mechanical (e.g., add a missing privacy marker, off-by-one HUD origin, threshold parameterization). 1-3hr if requires understanding a subsystem (e.g., HUD positioning rewrite for multi-monitor). >3hr if architectural (e.g., new sandboxing model, new permission flow).
    2. Apply the rule:
       - <1hr AND ≤2 files: ROUTE IN-PHASE — note in sweep results that a follow-up plan `26-NN-PLAN.md` will be authored (NOT in this planning pass; the orchestrator is told via the `## Conditional follow-up` section of this plan).
       - Bigger / architectural: ROUTE TO TODO — note the todo filename to be created (`qa-NN-<short>.md` with title prefix matching QA-NN; example: `qa-05-disk-space-preflight-warning.md`).
    3. Record routing decisions inline in sweep notes — Task 12 will encode them in 26-UAT.md `routing:` fields.

    If there are zero FAILs (all PASS / WITHDRAWN / citation): no routing needed. Skip to Task 12.

    Conditional follow-up (DO NOT EXECUTE IN THIS PLAN — for orchestrator awareness):
    - For each in-phase route: a separate `26-02-PLAN.md` (or 26-03, etc.) will be authored AFTER this plan closes. That plan creates a small fix + re-runs the failing scenario + flips the row in 26-UAT.md from FAIL to PASS. The new plan number increments per fix.
    - For each todo route: create `.planning/todos/pending/qa-NN-<short>.md` AS PART OF Task 12 (the file is data-only; the fix itself is deferred). Use the `ci-build-check-failing-on-main.md` frontmatter shape.
  </action>
  <verify>
    <manual>User confirms: routing decisions recorded for every FAIL; user agrees with each size estimate (in-phase vs todo) per D-02; conditional plan numbering and todo filenames are noted.</manual>
  </verify>
  <acceptance_criteria>
    - Every FAIL has a routing disposition: in-phase (with target plan number) OR todo (with target filename)
    - No FAIL is in undetermined state
    - User attests each size estimate is reasonable
    - If zero FAILs: explicit note "no FAILs — no routing needed"
  </acceptance_criteria>
  <done>Routing complete; ready to author 26-UAT.md in Task 12.</done>
</task>

<task type="auto">
  <name>Task 12: Author 26-UAT.md from sweep results + create deferred todos for routed FAILs</name>
  <files>
    .planning/phases/26-qa-sweep-visual-uat/26-UAT.md
    (conditional, only if Task 11 routed FAILs to todos)
    .planning/todos/pending/qa-NN-<short>.md (one file per todo-routed FAIL)
  </files>
  <read_first>
    - .planning/phases/26-qa-sweep-visual-uat/26-PATTERNS.md §"26-UAT.md — multi-scenario manual UAT artifact" (frontmatter shape, per-row patterns, ## Summary, ## Cross-References)
    - .planning/milestones/v1.2-phases/18.1-shared-save-destinations-local-file/18.1-UAT.md (PASS / FAIL row precedent + ## Summary count block)
    - .planning/phases/24-nyquist-sweep-v1-0/24-HUMAN-UAT.md (`approved-with-debt` terminal-state + minimal frontmatter precedent)
    - .planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md (citation row source format)
    - .planning/todos/pending/ci-build-check-failing-on-main.md (todo frontmatter + body shape, only if any FAILs routed to todos)
    - sweep notes from Tasks 3-11
  </read_first>
  <action>
    Use the Write tool to create `.planning/phases/26-qa-sweep-visual-uat/26-UAT.md` with the EXACT structure below. Substitute `<...>` placeholders with values from sweep notes. Do NOT paraphrase the frontmatter or row scaffolds — they are normative.

    --- BEGIN 26-UAT.md SCAFFOLD ---

    ```markdown
    ---
    status: in-progress
    phase: 26-qa-sweep-visual-uat
    source:
      - .planning/REQUIREMENTS.md
      - .planning/ROADMAP.md
      - .planning/research/PITFALLS.md
      - .planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md
      - .planning/phases/25-nyquist-sweep-v1-2/25-CONTEXT.md
    started: <ISO 8601 timestamp at sweep start, e.g., 2026-05-08T18:00:00Z>
    updated: <ISO 8601 timestamp at close, e.g., 2026-05-08T19:30:00Z>
    # Add at close (replace `status: in-progress` above with the terminal value):
    # If every row is PASS / WITHDRAWN / pass (citation):
    #   status: resolved
    #   resolved: <ISO timestamp>
    # If at least one row is untestable-this-cycle (e.g., QA-03b):
    #   status: approved-with-debt
    # If FAILs were routed in-phase and resolved by 26-NN-PLAN.md fix plans:
    #   resolved_by:
    #     - 26-NN (description of fix)
    ---

    ## Build Provenance

    - **Build:** local debug from `main` HEAD via `swift build -c debug` (or Xcode Run); not a Sparkle release.
    - **git SHA:** <short SHA from Task 2>
    - **macOS:** <version from Task 2>
    - **Xcode:** <version from Task 2>
    - **Clipboard managers:** Maccy <version> + Alfred <version> (one was already installed; <Maccy|Alfred> was installed temporarily for QA-01 / QA-02 — cleanup at user discretion).

    ## Current Test

    [complete]

    ## Tests

    ### 1. QA-01 — Clipboard history exclusion under Maccy
    expected: Triggering a dictation session writes the transcribed text to the clipboard with `org.nspasteboard.TransientType` + `AutoGeneratedType` privacy markers (DictationCoordinator.swift:413-415). Maccy honors these markers and does NOT add the dictation phrase to its history list.
    result: <pass | fail>
    <if pass:>
    note: "Tested with Maccy <version>; default privacy settings (Ignore Items list empty). Dictation phrase '<phrase>' not present in Maccy history; baseline marker '<marker>' remained the most recent item."
    <if fail:>
    reported: "<concrete observation; Maccy version + settings + Console.app excerpt>"
    severity: <major | minor>
    routing: <in-phase: 26-NN-PLAN.md | todo: qa-01-<short>.md>

    ### 2. QA-02 — Clipboard history exclusion under Alfred
    expected: Same privacy markers as QA-01. Alfred honors them and does NOT add the dictation phrase to its clipboard history.
    result: <pass | fail>
    <similar note: / reported: + severity: + routing: as QA-01>

    ### 3a. QA-03a — Single-display HUD positioning
    expected: HUD appears at the bottom-of-primary-screen position (vertically centered within the bottom-quarter band of `NSScreen.main.visibleFrame`, horizontally centered at `screenFrame.midX - panelWidth/2`); does not clip below Dock; respects safe-area insets at top (no overlap with menubar / notch). Code path: `DictationWindowController.swift:120-141`.
    result: <pass | fail>
    <similar note: / reported: + severity: + routing:>

    ### 3b. QA-03b — Multi-monitor HUD positioning sub-scenarios
    expected: HUD renders at the correct position across the 4-config matrix (primary / secondary-left / secondary-right / vertical) plus mid-recording disconnect, using `NSScreen.screens` positioning math at `DictationWindowController.swift`.
    result: untestable-this-cycle
    reason: "User does not have a multi-monitor rig available for this cycle. Synthesized NSScreen mock test rejected (D-01) — turns QA-03 into a code phase, conflicts with the manual-sweep boundary of this phase. Sidecar / iPad as second display rejected — adds latency and a confounding factor that muddies any FAIL signal."
    gap: "Capture this case in a future opportunistic phase or one-off when an external display is on hand. Not promoted to a todo to avoid creating debt for a hardware-availability question that resolves naturally on the next cycle."
    code_ref: "PSTranscribe/Sources/PSTranscribe/App/DictationWindowController.swift (NSScreen.main positioning math at lines 120-141; multi-monitor coverage requires NSScreen.screens iteration + tracking which screen the dictation hotkey was triggered on)"

    ### 4. QA-04 — Security-scoped bookmark survival across relaunch + reboot
    expected: <speculative original framing — folder URLs persist via bookmarkData(options: .withSecurityScope) and survive both app relaunch and macOS reboot>
    result: WITHDRAWN
    reason: "PS Transcribe is non-sandboxed: `PSTranscribe/Sources/PSTranscribe/PSTranscribe.entitlements` contains only `com.apple.security.device.audio-input` and `com.apple.security.device.screen-capture` — no `com.apple.security.app-sandbox` key. `localFileRoot` and `obsidianFolderPath` persist as raw `String` paths in `UserDefaults` (`AppSettings.swift:82,97`), not as `bookmarkData(options: .withSecurityScope)`. There is no `startAccessing` / `stopAccessing` call anywhere in `PSTranscribe/Sources` (verified 2026-05-08 via `grep -r 'withSecurityScope|startAccessing|stopAccessing' PSTranscribe/Sources` — zero matches). The QA-04 premise from `research/PITFALLS.md` line 519 was speculative about a sandboxed scenario that did not materialize; UserDefaults string-path persistence is guaranteed by Apple's UserDefaults contract and survives both relaunch and reboot trivially. The API isn't in use here — there is no bookmark layer to test."

    ### 5. QA-05 — Disk-space preflight warning is dismissible when free disk space is below the threshold
    expected: When `ModelUpdateService` runs its preflight check (`checkDiskSpace(needed: totalBytes)` at `ModelUpdateService.swift:212-222`) and free space is insufficient, the warning surface ("Not enough disk space available. Needs N MB, M MB free." per `ModelUpdateService.swift:614`) shows, is dismissible, and the update is deferred (no model files downloaded, no staging directory created).
    result: <pass | fail | untestable-this-cycle>
    <if pass:>
    note: "Tested via debug-only override of `ModelUpdateService.diskSpaceProvider` (test-injectable point at `ModelUpdateService.swift:70-72`) returning a low-byte value; warning text matched 'Not enough disk space available'; alert was dismissible; update deferred. Override reverted before commit; `git status` clean."
    <if fail:>
    reported: "<observed warning text vs. expected; dismissibility behavior; deferral behavior>"
    severity: <major | minor>
    routing: <in-phase: 26-NN-PLAN.md | todo: qa-05-disk-space-preflight-warning.md>
    <if untestable-this-cycle:>
    reason: "<why the override surface was unreachable from a debug build without source modification beyond the one-line override>"
    gap: "Capture in a real-disk-fill follow-up phase per CONTEXT.md Deferred §'Real-disk-fill QA-05 mechanism'."
    code_ref: "PSTranscribe/Sources/PSTranscribe/Services/ModelUpdateService.swift:70-72 (diskSpaceProvider) + 212-222 (preflight call site)"

    ### 6. QA-06 — Light preference flips titlebar to cream Chronicle bridge active
    expected: Picker = Light while macOS = Light (or System): main window titlebar strip flips to cream `#FAFAF7` Chronicle paper background; toolbar centered title text uses NSColor.labelColor and remains legible. Re-flips correctly when picker is set back to System or Dark.
    result: pass (citation)
    source: .planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md (Test 1, passed 2026-05-01, user attestation)

    ### 7. QA-07 — Dark preference flips titlebar to system-dark material (Chronicle bridge inactive)
    expected: Picker = Dark while macOS = Dark (or System): main window titlebar strip flips to system-dark material; cream Chronicle paper background does NOT persist. Re-flips correctly when picker is set back to System or Light.
    result: pass (citation)
    source: .planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md (Test 2, passed 2026-05-01, user attestation)

    ### 8. QA-08 — System preference follows OS appearance change live (no app restart required)
    expected: Picker = System: toggling System Settings -> Appearance from Light -> Dark -> Light propagates to the titlebar strip without app restart. Cream paint shows under Aqua effective appearance and clears under Dark Aqua.
    result: pass (citation)
    source: .planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md (Test 3, passed 2026-05-01, user attestation)

    ### 9. QA-09 — Preference toggle while window is unfocused still updates titlebar correctly
    expected: With Settings window or another window focused (main window unfocused), changing the appearance preference via the Settings Picker still propagates to the main window titlebar without losing the preference value or requiring a focus change. Settings window keeps its native AppKit titlebar (no Chronicle paper paint) — the early-return at applyChronicleTitlebar (lines 342-348) holds across all three picker values.
    result: pass (citation)
    source: .planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md (Test 4, passed 2026-05-01, user attestation)

    ## Summary

    total: 10
    passed: <count of result: pass>
    issues: <count of result: fail>
    withdrawn: 1
    untestable-this-cycle: <count of result: untestable-this-cycle; minimum 1 for QA-03b>
    citations: 4
    pending: 0
    skipped: 0
    blocked: 0
    <if any untestable-this-cycle row exists, add this line:>
    approved-with-debt: <total - issues - pending>

    ## Gaps

    <if any FAIL routed in-phase, list per-issue blocks per the 18.1-UAT.md `## Gaps` shape with `truth:` / `status:` / `resolved_by:` / `reason:` / `severity:` / `test:` / `root_cause:` / `artifacts:` / `missing:` / `debug_session:`>
    <if zero in-phase fixes, write:>
    None for Phase 26's scope routed in-phase. <if any todos created:> Deferred fix candidates tracked in `.planning/todos/pending/qa-NN-<short>.md` files (one per todo-routed FAIL).

    ## Cross-References

    - **REQUIREMENTS.md** lines 15-23 — QA-01..QA-09 requirement IDs
    - **ROADMAP.md** §"Phase 26: QA Sweep + Visual UAT" — phase goal + 6 success criteria
    - **research/PITFALLS.md** lines 513-528 — "Looks Done But Isn't" Checklist (origin of QA-01..05)
    - **milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md** — citation source for QA-06..09 (passed 2026-05-01, user attestation)
    - **phases/25-nyquist-sweep-v1-2/25-CONTEXT.md** D-03 — Phase 25's cross-reference contract that QA-06..09 lives in 26-UAT.md (discharged by the citation rows above)
    - **todos/pending/ci-build-check-failing-on-main.md** — CI infrastructure debt; reviewed at score 0.6 by `gsd-sdk query todo.match-phase 26`, NOT folded into Phase 26 (D-04, surface mismatch — Phase 26 tests user-facing app behavior, not CI infrastructure)
    ```

    --- END 26-UAT.md SCAFFOLD ---

    AFTER writing 26-UAT.md, for each FAIL routed to a todo, use the Write tool to create `.planning/todos/pending/qa-NN-<short>.md` using this template (mirrors `ci-build-check-failing-on-main.md`):

    ```markdown
    ---
    title: <short description of fix needed>
    status: pending
    created: 2026-05-08
    priority: <high | medium | low>
    blocking: false
    source: phase-26
    ---

    ## Context

    <one paragraph: which QA-NN failed in 26-UAT.md, what the FAIL signal was, why D-02 routed it here (size > 1hr OR > 2 files OR architectural)>

    ## Issue

    <concrete reproduction steps, observed vs. expected behavior, log excerpts>

    **Code paths involved:**
    - `PSTranscribe/Sources/PSTranscribe/<file>.swift:<line>` — <what's there now>

    **Suggested fixes:**
    - <option 1>
    - <option 2>

    ## Why deferred from Phase 26

    <per D-02: fix exceeds <1hr OR >2 files threshold; OR architectural change requiring its own planning cycle>

    ## Acceptance criteria for closing this todo

    - [ ] <concrete deterministic check 1>
    - [ ] <concrete deterministic check 2>
    - [ ] QA-NN re-run in a future UAT cycle PASSes
    ```

    FINAL STEPS:
    1. Compute the Summary block counts. Verify they equal the row count (10 if QA-03 split, 9 if not split — but QA-03 IS split per D-01, so total = 10).
    2. Set frontmatter `status:` to:
       - `resolved` if every row is PASS / WITHDRAWN / pass (citation), no untestable-this-cycle and no FAIL.
       - `approved-with-debt` if at least one row is untestable-this-cycle (e.g., QA-03b).
       - Leave at `in-progress` only if FAILs were routed in-phase and follow-up fix plans haven't shipped yet (the executor of the follow-up plan flips it on close).
    3. Update the `updated:` timestamp to the close time.
    4. If `status: resolved`, also add a `resolved:` timestamp.
    5. Verify all citation rows literally contain `Source: .planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md (Test N, passed 2026-05-01, user attestation)` — mapping QA-06->1, QA-07->2, QA-08->3, QA-09->4.
    6. Verify QA-04 reasoning literally cites `PSTranscribe.entitlements` AND `AppSettings.swift:82,97` AND the zero-hit grep result.
  </action>
  <verify>
    <automated>test -f .planning/phases/26-qa-sweep-visual-uat/26-UAT.md && grep -cE '^### [0-9]+(a|b)?\. QA-0[1-9]' .planning/phases/26-qa-sweep-visual-uat/26-UAT.md && grep -c 'WITHDRAWN' .planning/phases/26-qa-sweep-visual-uat/26-UAT.md && grep -c '21-HUMAN-UAT\.md' .planning/phases/26-qa-sweep-visual-uat/26-UAT.md && grep -c 'passed 2026-05-01, user attestation' .planning/phases/26-qa-sweep-visual-uat/26-UAT.md && grep -cE 'PSTranscribe\.entitlements|AppSettings\.swift:(82|97)' .planning/phases/26-qa-sweep-visual-uat/26-UAT.md && grep -cE '^status: (in-progress|resolved|approved-with-debt)$' .planning/phases/26-qa-sweep-visual-uat/26-UAT.md && grep -c '^total: ' .planning/phases/26-qa-sweep-visual-uat/26-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/26-qa-sweep-visual-uat/26-UAT.md`
    - `grep -cE '^### [0-9]+(a|b)?\. QA-0[1-9]' .planning/phases/26-qa-sweep-visual-uat/26-UAT.md` returns 10 (QA-01, QA-02, QA-03a, QA-03b, QA-04, QA-05, QA-06, QA-07, QA-08, QA-09)
    - `grep -c 'WITHDRAWN' .planning/phases/26-qa-sweep-visual-uat/26-UAT.md` returns at least 1 (QA-04 row)
    - `grep -c '21-HUMAN-UAT\.md' .planning/phases/26-qa-sweep-visual-uat/26-UAT.md` returns at least 5 (4 citation rows + 1 source list entry; possibly more if Cross-References block lists it again)
    - `grep -c 'passed 2026-05-01, user attestation' .planning/phases/26-qa-sweep-visual-uat/26-UAT.md` returns 4 (one per QA-06/07/08/09 citation row)
    - `grep -cE 'PSTranscribe\.entitlements|AppSettings\.swift:(82|97)' .planning/phases/26-qa-sweep-visual-uat/26-UAT.md` returns at least 1 (QA-04 reasoning cites both)
    - `grep -cE '^status: (in-progress|resolved|approved-with-debt)$' .planning/phases/26-qa-sweep-visual-uat/26-UAT.md` returns 1 (frontmatter status is exactly one of the three terminal/transition values)
    - `grep -c '^total: ' .planning/phases/26-qa-sweep-visual-uat/26-UAT.md` returns 1 (## Summary block exists)
    - `grep -c 'Cross-References' .planning/phases/26-qa-sweep-visual-uat/26-UAT.md` returns at least 1 (footer present)
    - `grep -c 'ci-build-check-failing-on-main.md' .planning/phases/26-qa-sweep-visual-uat/26-UAT.md` returns at least 1 (CI todo explicitly NOT folded — D-04)
    - `grep -cE 'QA-0[1-9]' .planning/phases/26-qa-sweep-visual-uat/26-UAT.md` returns at least 9 (one mention per QA-NN scenario)
    - For each FAIL with routing to todo, the corresponding `.planning/todos/pending/qa-NN-<short>.md` exists with frontmatter `status: pending` and `source: phase-26`
  </acceptance_criteria>
  <done>26-UAT.md authored with all 10 rows + Summary + Cross-References + frontmatter at terminal status; any todo-routed FAILs have their todo files created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local debug build → clipboard managers (Maccy, Alfred) | Dictation writes to `NSPasteboard.general` with privacy markers `org.nspasteboard.TransientType` + `AutoGeneratedType`; third-party managers honor the markers and exclude from history (informal nspasteboard.org contract) |
| User → 26-UAT.md text attestation | User self-reports PASS / FAIL / WITHDRAWN / UNTESTABLE-this-cycle observations; no automated visual diff or screenshot to corroborate |
| Test process → temporary debug-only override (QA-05) | One-line `#if DEBUG`-gated override of `ModelUpdateService.diskSpaceProvider`; reverted before commit |

## STRIDE Threat Register

ASVS Level 1. Phase 26 ships a single Markdown UAT artifact + 0..N optional todo files. No new production code, no new dependencies, no I/O beyond reading committed source files and the temporary debug-only override in QA-05 (which is reverted before commit). Surface is minimal.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Spoofing | n/a — UAT artifact carries user attestation, no auth boundary | accept | N/A — text attestation IS the trust model per D-03; future readers can re-run the sweep to verify |
| T-26-02 | Tampering | If a future commit silently removes the `org.nspasteboard.TransientType` / `AutoGeneratedType` writes from `DictationCoordinator.swift:413-415`, QA-01 / QA-02 PASS rows in 26-UAT.md become stale | mitigate | The QA-01 / QA-02 expectations explicitly cite the line numbers; future regressions are caught by Phase 23 snapshot tests OR a follow-up VALIDATION.md with grep gates; this phase does not introduce a new test |
| T-26-03 | Repudiation | n/a — Phase 26 is audit-only; all evidence is in committed Markdown (26-UAT.md + optional todos) | accept | N/A — full audit trail in git history (commit message references QA-FUT-01) |
| T-26-04 | Information Disclosure | The temporary QA-05 debug-only override could leak into a commit if not reverted; risk = a `#if DEBUG` block stays in source | mitigate | Task 8 acceptance criteria explicitly require `git status` clean before next task; user attests to the revert; pre-commit grep for `Phase 26 QA-05 temporary` would catch a stuck override (recommend but not enforced this phase) |
| T-26-05 | Denial of Service | n/a — manual UAT; no production code path under load | accept | N/A — sweep runs once per cycle, no recurring resource cost |
| T-26-06 | Elevation of Privilege | The QA-05 override mutates a `var` on the global `ModelUpdateService` instance from a debug-only code path; non-issue when `#if DEBUG` is correctly gated | accept | `#if DEBUG` gate is mandatory in the Task 8 action; release builds (Sparkle) do not include the override surface |

No high-severity threats. Disposition is `accept` for the manual-UAT trust model and `mitigate` for the QA-05 override revert + future regression-test cross-ref.
</threat_model>

<verification>
- 26-UAT.md exists at `.planning/phases/26-qa-sweep-visual-uat/26-UAT.md` with valid YAML frontmatter
- All 9 QA-NN scenarios have rows; QA-03 is split into 03a / 03b (10 row IDs total)
- QA-06..09 rows literally cite `21-HUMAN-UAT.md` with `passed 2026-05-01, user attestation` substring (one per row)
- QA-04 WITHDRAW reasoning explicitly cites `PSTranscribe.entitlements`, `AppSettings.swift:82,97`, and the zero-hit grep result for `withSecurityScope` / `startAccessing` / `stopAccessing` — NOT the speculative `bookmarkData` framing
- QA-03b UNTESTABLE-this-cycle row has a `code_ref:` field pointing at `DictationWindowController.swift`
- ## Summary block counts equal row count (split rows count separately per Phase 24/25 PARTIAL convention; total = 10)
- ## Cross-References footer cites all six canonical sources: REQUIREMENTS.md, ROADMAP.md, PITFALLS.md, 21-HUMAN-UAT.md, 25-CONTEXT.md, ci-build-check-failing-on-main.md (the last explicitly NOT folded per D-04)
- Frontmatter `status:` transitions correctly: `in-progress` at sweep start; at close, `resolved` (no UNTESTABLE / no FAIL) or `approved-with-debt` (at least one UNTESTABLE row, e.g., QA-03b)
- For each FAIL routed in-phase per D-02: a separate `26-NN-PLAN.md` exists in the same phase directory (NOT authored in this initial plan; appended in a follow-up planning pass)
- For each FAIL routed to todo per D-02: a `.planning/todos/pending/qa-NN-<short>.md` exists with `status: pending` and `source: phase-26`
- Codebase invariants underwriting WITHDRAW: `grep -c "com.apple.security.app-sandbox" PSTranscribe/Sources/PSTranscribe/PSTranscribe.entitlements` returns 0; `grep -r "withSecurityScope|startAccessing|stopAccessing" PSTranscribe/Sources` returns zero matches
- Codebase invariants underwriting citations: `21-HUMAN-UAT.md` exists with `status: resolved` and contains all 4 Tests with user attestation 2026-05-01
- After QA-05 task: `git status` is clean (debug-only override reverted)
</verification>

<success_criteria>
1. 26-UAT.md authored at `.planning/phases/26-qa-sweep-visual-uat/26-UAT.md` with frontmatter at terminal status (`resolved` or `approved-with-debt`)
2. All 9 QA-NN scenarios attested in 26-UAT.md (with QA-03 PARTIAL split into 03a / 03b — 10 row IDs)
3. QA-06..09 cite `21-HUMAN-UAT.md` Tests 1-4 (passed 2026-05-01, user attestation) without re-execution per D-03
4. QA-04 entirely WITHDRAWN with codebase-verified reasoning citing `PSTranscribe.entitlements` + `AppSettings.swift:82,97` + zero-hit grep — not the speculative `bookmarkData` framing
5. QA-03 split: 03a (single-display PASS) + 03b (multi-monitor UNTESTABLE-this-cycle with code_ref to `DictationWindowController.swift`)
6. ROADMAP success criterion 1 satisfied (26-UAT.md exists with documented PASS/FAIL per item)
7. ROADMAP success criterion 5 satisfied (Phase 21 titlebar visual UAT QA-06..09 surfaces as citation rows in 26-UAT.md)
8. ROADMAP success criterion 6 satisfied (FAIL routing dual-track honored — in-phase plans for <1hr ≤2-file fixes per D-02; deferred todos in `.planning/todos/pending/qa-NN-<short>.md` for bigger / architectural fixes)
9. CI todo `ci-build-check-failing-on-main.md` is referenced in Cross-References as explicitly NOT folded per D-04
10. SUMMARY.md frontmatter includes `requirements-completed: [QA-01, QA-02, QA-03, QA-04, QA-05, QA-06, QA-07, QA-08, QA-09]` (PROCESS-01 in effect)
11. QA-FUT-01 closed in REQUIREMENTS.md / ROADMAP.md tracking (the ROADMAP requirement IDs map directly to QA-01..QA-09; this plan's 26-UAT.md discharges all 9)
</success_criteria>

<output>
After completion, create `.planning/phases/26-qa-sweep-visual-uat/26-01-SUMMARY.md` with:
- Frontmatter: `requirements-completed: [QA-01, QA-02, QA-03, QA-04, QA-05, QA-06, QA-07, QA-08, QA-09]` (PROCESS-01 in effect)
- What shipped: `26-UAT.md` (10 rows; status terminal value) + 0..N `.planning/todos/pending/qa-NN-<short>.md` (one per todo-routed FAIL) + 0..N `26-NN-PLAN.md` follow-up fix plans (authored separately, not in this initial plan)
- D-01 referenced explicitly (hybrid environment + QA-03 PARTIAL split + QA-04 WITHDRAWN with codebase-verified reasoning)
- D-02 referenced explicitly (batch-triage routing decisions for each FAIL — in-phase vs todo per <1hr AND ≤2-file rule)
- D-03 referenced explicitly (text attestation only; QA-06..09 cite 21-HUMAN-UAT.md Tests 1-4 with passed 2026-05-01 user attestation)
- D-04 referenced explicitly (single 26-01-PLAN.md for the sweep; 26-UAT.md filename per ROADMAP success criterion 1; CI infrastructure todo explicitly NOT folded)
- Note that `ci-build-check-failing-on-main.md` was reviewed at score 0.6 by `gsd-sdk query todo.match-phase 26` and intentionally excluded; future surface is a separate CI-focused phase

Commit message style: NO Claude attribution (per user-level CLAUDE.md). Subject: `feat(26-01): execute QA sweep + visual UAT (QA-FUT-01 closure)` or similar; body explains the WITHDRAWN, UNTESTABLE-this-cycle, and citation row decisions plus any todo-routed FAILs.
</output>

## Conditional follow-up (orchestrator awareness — not an executor task)

This is the SOLE initial plan for Phase 26 per D-04. After Task 12 closes, the executor evaluates the FAIL routing decisions made in Task 11 and:

- **For each in-phase routed FAIL** (size <1hr AND ≤2 files per D-02): trigger a fresh `/gsd-plan-phase 26` invocation in CONTINUE mode (or invoke the planner directly with phase=26 and an inline directive to author `26-02-PLAN.md`, `26-03-PLAN.md`, etc.). Each follow-up plan implements the fix, re-runs the failing scenario in the live build, and updates the corresponding row in 26-UAT.md from FAIL to PASS (also updating `resolved_by:` in the frontmatter).
- **For each todo-routed FAIL** (anything bigger or architectural per D-02): the todo file at `.planning/todos/pending/qa-NN-<short>.md` was already created in Task 12. No further plan work in Phase 26; the todo is the canonical tracker. Phase 26 ships with `status: approved-with-debt` (because at least one UNTESTABLE-this-cycle row exists for QA-03b) regardless of whether the todo is later resolved.
- **If zero FAILs**: Phase 26 closes immediately after Task 12. Terminal status is `approved-with-debt` if QA-03b is UNTESTABLE-this-cycle (likely), otherwise `resolved`.
