---
phase: 22-process-frontmatter-standard
plan: 03
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - $HOME/.claude/get-shit-done/workflows/execute-plan.md
  - $HOME/.claude/agents/gsd-doc-writer.md
autonomous: true
requirements: [PROCESS-02]
must_haves:
  truths:
    - "`execute-plan.md` `<step name=\"create_summary\">` documents the verbatim-copy contract from PLAN.md `requirements:` to SUMMARY `requirements-completed:` with explicit empty-list handling and post-emission audit instructions."
    - "`gsd-doc-writer.md` agent file carries a cross-reference note pointing to the canonical SUMMARY-generation contract in `execute-plan.md` so any future SUMMARY emission added to that agent inherits the same contract."
    - "Neither file emits or documents the underscore form `requirements_completed` and neither emits sibling fields."
  artifacts:
    - path: "$HOME/.claude/get-shit-done/workflows/execute-plan.md"
      provides: "Tightened `<step name=\"create_summary\">` block with the verbatim-copy contract, empty-list rule (D-04), and ban on sibling fields (D-05)."
      contains: "requirements-completed"
    - path: "$HOME/.claude/agents/gsd-doc-writer.md"
      provides: "Cross-reference note to the canonical SUMMARY contract in execute-plan.md."
      contains: "requirements-completed"
  key_links:
    - from: "execute-plan.md `<step name=\"create_summary\">`"
      to: "PLAN.md `requirements:` field"
      via: "verbatim copy at SUMMARY creation time (D-08)"
      pattern: "requirements-completed"
---

<objective>
Tighten the SUMMARY-emission contract in `~/.claude/get-shit-done/workflows/execute-plan.md` so it documents D-04 (empty list valid), D-05 (sibling fields deprecated), and D-08 (verbatim copy from PLAN.md). Add a cross-reference in `~/.claude/agents/gsd-doc-writer.md` so any future SUMMARY emission added to that agent inherits the same contract.

Purpose: Generator pathways are the runtime mechanism that turns the template's promise into reality at every plan completion. The contract text already exists at line 336 in execute-plan.md — this plan tightens it to make D-04/D-05/D-08 explicit and forward-reference the lint script (Plan 22-04) that will enforce subset-correctness post-emission.
Output: Updated `execute-plan.md` `<step name="create_summary">` and a new cross-reference paragraph in `gsd-doc-writer.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.3-phases/22-process-frontmatter-standard/22-CONTEXT.md

<interfaces>
<!-- Current state of `<step name="create_summary">` in execute-plan.md, lines 333-345 -->

```markdown
<step name="create_summary">
Create `{phase}-{plan}-SUMMARY.md` at `.planning/phases/XX-name/`. Use `$HOME/.claude/get-shit-done/templates/summary.md`.

**Frontmatter:** phase, plan, subsystem, tags | requires/provides/affects | tech-stack.added/patterns | key-files.created/modified | key-decisions | requirements-completed (**MUST** copy `requirements` array from PLAN.md frontmatter verbatim) | duration ($DURATION), completed ($PLAN_END_TIME date).

Title: `# Phase [X] Plan [Y]: [Name] Summary`

One-liner SUBSTANTIVE: "JWT auth with refresh rotation using jose library" not "Authentication implemented"

Include: duration, start/end times, task count, file count.

Next: more plans → "Ready for {next-plan}" | last → "Phase complete, ready for next step".
</step>
```

The verbatim-copy line ALREADY documents the core contract; this plan EXTENDS it to also state:
1. Empty list `[]` is valid when zero requirements were delivered (D-04). Author may down-edit IF actual delivery differed from declaration.
2. Sibling fields (`requirements-scaffolded`, `requirements-supports`, `requirements-touched-but-not-completed`) are deprecated; do NOT emit them. If historical context is needed, capture in inline YAML comment per Phase 18-01-SUMMARY.md style.
3. Lint script (`scripts/lint-summaries.sh`, landing in Plan 22-04) enforces SUMMARY.requirements-completed ⊆ PLAN.requirements (subset check, D-07).

`gsd-doc-writer.md` agent does NOT currently emit phase SUMMARY frontmatter (it produces `.planning/codebase/*.md` documents). However, CONTEXT.md canonical_refs lists it as an "alternate SUMMARY emission path" — the agent's prompt mentions SUMMARY context generally. This plan adds a defensive cross-reference: if any future contributor extends `gsd-doc-writer.md` to emit phase SUMMARY frontmatter, they MUST follow the contract in `execute-plan.md` `<step name="create_summary">`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tighten `<step name="create_summary">` in execute-plan.md</name>
  <files>$HOME/.claude/get-shit-done/workflows/execute-plan.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/workflows/execute-plan.md lines 333-345 (the `<step name="create_summary">` block — confirm exact current text)
    - .planning/milestones/v1.3-phases/22-process-frontmatter-standard/22-CONTEXT.md D-04, D-05, D-07, D-08
    - .planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-01-SUMMARY.md line 53 (canonical empty-list rationale comment in the wild — the doc note will reference this file)
  </read_first>
  <behavior>
    - The `<step name="create_summary">` block contains a paragraph titled `**`requirements-completed` contract:**` that explicitly states the four contract rules (verbatim copy, empty-list validity, no siblings, lint enforces subset).
    - The existing line `**Frontmatter:** phase, plan, ...` is preserved (it lists fields in order); the new contract paragraph is INSERTED after it but before `Title: # Phase [X] Plan [Y]: [Name] Summary`.
    - No other content in `execute-plan.md` is altered.
  </behavior>
  <action>
    Per D-04, D-05, D-07, D-08 (CONTEXT.md), tighten the `<step name="create_summary">` block in `~/.claude/get-shit-done/workflows/execute-plan.md`.

    Use the Edit tool. Insert the following paragraph BETWEEN the existing `**Frontmatter:**` line (line ~336) and the existing `Title: \`# Phase [X] Plan [Y]: [Name] Summary\`` line (line ~338). Keep the surrounding lines exactly as they currently are.

    INSERT after the `**Frontmatter:**` line:

    ```
    **`requirements-completed` contract** (Phase 22, locked):
    1. **Verbatim copy** (D-08): SUMMARY `requirements-completed:` MUST start as a verbatim copy of PLAN.md `requirements:`. The author MAY down-edit if actual delivery differed from declaration (e.g., scaffolding-only plans land `[]` even when the PLAN declared IDs — see Phase 18-01-SUMMARY.md line 53 for the canonical empty-list rationale comment style).
    2. **Empty list valid** (D-04): `requirements-completed: []` is valid YAML and a valid value when zero requirements were delivered. Inline comment explaining why is encouraged but not required by lint.
    3. **No sibling fields** (D-05): do NOT emit `requirements-scaffolded`, `requirements-supports`, or `requirements-touched-but-not-completed`. These are deprecated; the lint script (`scripts/lint-summaries.sh`) flags them. Capture historical context as inline YAML comments above `requirements-completed:` instead.
    4. **Subset enforcement** (D-07): the lint script enforces SUMMARY.requirements-completed ⊆ PLAN.requirements. Down-editing is allowed; up-editing (adding IDs not in the PLAN) is a lint failure.
    ```

    Do NOT modify any other line, indentation, or section. Do NOT touch the existing `**Frontmatter:**` line — leave it intact (the new paragraph follows it).
  </action>
  <verify>
    <automated>grep -c '`requirements-completed` contract' /Users/cary/.claude/get-shit-done/workflows/execute-plan.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '`requirements-completed` contract' ~/.claude/get-shit-done/workflows/execute-plan.md` returns `1`.
    - `grep -c 'D-04' ~/.claude/get-shit-done/workflows/execute-plan.md` returns at least `1` (the contract paragraph references D-04).
    - `grep -c 'D-05' ~/.claude/get-shit-done/workflows/execute-plan.md` returns at least `1`.
    - `grep -c 'D-07' ~/.claude/get-shit-done/workflows/execute-plan.md` returns at least `1`.
    - `grep -c 'D-08' ~/.claude/get-shit-done/workflows/execute-plan.md` returns at least `1`.
    - `grep -c 'requirements-scaffolded' ~/.claude/get-shit-done/workflows/execute-plan.md` returns at least `1` (deprecation explicitly named).
    - `grep -c 'lint-summaries.sh' ~/.claude/get-shit-done/workflows/execute-plan.md` returns at least `1` (forward reference to Plan 22-04 lint).
    - `grep -c 'requirements_completed' ~/.claude/get-shit-done/workflows/execute-plan.md` returns `0` (no underscore form anywhere in the file).
    - `awk '/<step name=\"create_summary\">/,/<\/step>/' ~/.claude/get-shit-done/workflows/execute-plan.md | grep -c '`requirements-completed` contract'` returns `1` — confirms the insertion landed INSIDE the `<step>` block, not elsewhere.
    - The line containing `Title: \`# Phase [X] Plan [Y]: [Name] Summary\`` still exists in the file (verified with grep) and falls AFTER the contract paragraph (verified by grep -n line ordering).
  </acceptance_criteria>
  <done>
    `<step name="create_summary">` contains the four-rule contract paragraph documenting D-04, D-05, D-07, D-08. The file no longer needs any other change for PROCESS-02.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add cross-reference note in gsd-doc-writer.md</name>
  <files>$HOME/.claude/agents/gsd-doc-writer.md</files>
  <read_first>
    - $HOME/.claude/agents/gsd-doc-writer.md (read the front matter / opening prose; the executor must place the cross-reference in a sensible location near where SUMMARY-related guidance, if any, currently lives. If no SUMMARY guidance exists, place the note near the top of the agent's "What this agent does" section.)
    - .planning/milestones/v1.3-phases/22-process-frontmatter-standard/22-CONTEXT.md canonical_refs (lists `gsd-doc-writer.md` as the alternate SUMMARY emission path)
  </read_first>
  <behavior>
    - `gsd-doc-writer.md` carries a paragraph titled `## Phase SUMMARY frontmatter (cross-reference)` that:
      1. States this agent does not currently emit phase SUMMARY frontmatter.
      2. States that IF a future change extends this agent to do so, the canonical contract lives in `~/.claude/get-shit-done/workflows/execute-plan.md` `<step name="create_summary">`.
      3. Names the canonical key `requirements-completed` (hyphen) and explicitly bans the underscore form and sibling fields.
    - No other change to `gsd-doc-writer.md`.
  </behavior>
  <action>
    Per CONTEXT.md (canonical_refs identifies `gsd-doc-writer.md` as an alternate SUMMARY emission path), add a defensive cross-reference paragraph.

    Use the Edit tool. APPEND the following block at the very end of `~/.claude/agents/gsd-doc-writer.md` (after the file's last existing line). If the file ends without a trailing newline, add one before the new content.

    ```markdown

    ## Phase SUMMARY frontmatter (cross-reference)

    This agent does NOT currently emit phase plan SUMMARY frontmatter (`.planning/milestones/**/*-SUMMARY.md`). Phase SUMMARYs are emitted by `~/.claude/get-shit-done/workflows/execute-plan.md` `<step name="create_summary">`.

    If a future change extends this agent to emit phase SUMMARY frontmatter, the canonical contract is locked in Phase 22 (`~/.claude/get-shit-done/workflows/execute-plan.md` `<step name="create_summary">` — read it before writing any SUMMARY-emission code path). Key rules:

    - The canonical key is `requirements-completed` (hyphen). The underscore form `requirements_completed` is forbidden.
    - Sibling fields (`requirements-scaffolded`, `requirements-supports`, `requirements-touched-but-not-completed`) are deprecated and flagged by `scripts/lint-summaries.sh`.
    - Empty list `[]` is valid; verbatim-copy from PLAN.md `requirements:` is the default; lint enforces subset (`SUMMARY.requirements-completed ⊆ PLAN.requirements`).
    ```

    Do NOT modify any existing content in the file. The cross-reference is purely additive.
  </action>
  <verify>
    <automated>grep -c '## Phase SUMMARY frontmatter (cross-reference)' /Users/cary/.claude/agents/gsd-doc-writer.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '## Phase SUMMARY frontmatter (cross-reference)' ~/.claude/agents/gsd-doc-writer.md` returns `1`.
    - `grep -c 'requirements-completed' ~/.claude/agents/gsd-doc-writer.md` returns at least `1`.
    - `grep -c 'requirements_completed' ~/.claude/agents/gsd-doc-writer.md` returns `0` (no underscore form, even in the deprecation note — the underscore appears only as a forbidden form, but here we name only the hyphen form positively; the deprecation paragraph names siblings, not the underscore).
    - `grep -c 'execute-plan.md' ~/.claude/agents/gsd-doc-writer.md` returns at least `1` (cross-reference target named).
    - `grep -c 'lint-summaries.sh' ~/.claude/agents/gsd-doc-writer.md` returns at least `1`.
    - `tail -1 ~/.claude/agents/gsd-doc-writer.md | grep -E '^\s*$|requirements'` succeeds — the file does not end mid-paragraph (last non-blank line is the closing bullet of the cross-reference block).
  </acceptance_criteria>
  <done>
    `gsd-doc-writer.md` carries the cross-reference paragraph. Future contributors extending the agent to emit phase SUMMARY frontmatter will be steered to the canonical contract.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Doc edits in workflow + agent files. No runtime code path, no I/O, no auth, no data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | n/a | n/a | n/a | Phase 22 is process/CI/docs only. No new attack surface introduced. |
</threat_model>

<verification>
- `<step name="create_summary">` in execute-plan.md carries the 4-rule contract paragraph naming D-04, D-05, D-07, D-08, the sibling-field deprecation, and the lint script forward-reference.
- `gsd-doc-writer.md` carries the cross-reference paragraph naming `requirements-completed`, `execute-plan.md`, and `lint-summaries.sh`.
- Zero occurrences of `requirements_completed` (underscore) across both files.
</verification>

<success_criteria>
ROADMAP success criterion #2 (generation flow populates the field automatically by parsing PLAN.md requirement references) is documented in the canonical contract location and cross-referenced from the alternate emission path. PROCESS-02 is satisfied for the documentation half; the runtime half (`<step name="create_summary">` already executes the verbatim copy via the contract documented at line 336) is reaffirmed and tightened.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.3-phases/22-process-frontmatter-standard/22-03-SUMMARY.md` using `~/.claude/get-shit-done/templates/summary.md`. The SUMMARY's `requirements-completed:` field MUST be `[PROCESS-02]`.
</output>
