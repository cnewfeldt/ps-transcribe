---
phase: 22-process-frontmatter-standard
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - $HOME/.claude/get-shit-done/templates/summary.md
  - $HOME/.claude/get-shit-done/templates/summary-standard.md
  - $HOME/.claude/get-shit-done/templates/summary-minimal.md
autonomous: true
requirements: [PROCESS-01]
must_haves:
  truths:
    - "All three SUMMARY templates carry a `requirements-completed:` field in their frontmatter."
    - "The empty-list canonical syntax `requirements-completed: []` is documented in the templates."
    - "No template uses the deprecated underscore form `requirements_completed`."
  artifacts:
    - path: "$HOME/.claude/get-shit-done/templates/summary.md"
      provides: "Full SUMMARY template with `requirements-completed:` and updated schema documentation."
      contains: "requirements-completed:"
    - path: "$HOME/.claude/get-shit-done/templates/summary-standard.md"
      provides: "Standard SUMMARY template with `requirements-completed: []` field added."
      contains: "requirements-completed:"
    - path: "$HOME/.claude/get-shit-done/templates/summary-minimal.md"
      provides: "Minimal SUMMARY template with `requirements-completed: []` field added (D-03 — minimal stays minimal but never optional on traceability)."
      contains: "requirements-completed:"
  key_links:
    - from: "All three templates"
      to: "execute-plan.md `<step name=\"create_summary\">`"
      via: "shared canonical key string `requirements-completed`"
      pattern: "requirements-completed"
---

<objective>
Add `requirements-completed: []` to the two SUMMARY templates that lack it (`summary-standard.md`, `summary-minimal.md`) per D-03, and tighten the schema documentation in `summary.md` (which already has the field). All three templates emit the canonical hyphenated key — no underscore form, no sibling fields.

Purpose: Templates are the upstream source for every future SUMMARY.md. Until they all carry the field, the dogfood-from-day-one promise (Phase 22's own SUMMARYs use the canonical format) cannot hold.
Output: Three updated template files in `~/.claude/get-shit-done/templates/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.3-phases/22-process-frontmatter-standard/22-CONTEXT.md
@.planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-01-SUMMARY.md

<interfaces>
<!-- Current state of each template's frontmatter. The executor must replicate the existing field-list shape; only add the new key in the right spot. -->

**`summary.md`** (already has the field at line 41):
```yaml
requirements-completed: []  # REQUIRED — Copy ALL requirement IDs from this plan's `requirements` frontmatter field.
```
Action here = TIGHTEN the comment to also mention: empty list is valid (D-04), and the canonical example for plans that legitimately deliver nothing is Phase 18-01 SUMMARY's empty-list rationale comment.

**`summary-standard.md`** (lacks the field; current frontmatter ends at line 17 with `key-decisions:`):
```yaml
key-decisions:
  - "Decision 1"
duration: Xmin
completed: YYYY-MM-DD
---
```
Action here = INSERT `requirements-completed: []` between `key-decisions:` and `duration:` (in the same position as `summary.md` line 41).

**`summary-minimal.md`** (lacks the field; current frontmatter ends at line 15 with `key-decisions: []`):
```yaml
key-decisions: []
duration: Xmin
completed: YYYY-MM-DD
---
```
Action here = INSERT `requirements-completed: []` between `key-decisions: []` and `duration:`.

**Canonical empty-list rationale comment** (gold-standard example from CONTEXT.md "Specific Ideas", taken from 18-01-SUMMARY.md line 53):
```yaml
requirements-completed: []  # Wave 0 ships test scaffolding only -- no requirement is delivered until its implementing wave (18-02..08) lands and un-disables the corresponding @Suite.
```
This comment is NOT auto-emitted; the docs in `summary.md` should reference 18-01 as the canonical example for legitimately-empty plans.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add `requirements-completed: []` to summary-standard.md and summary-minimal.md</name>
  <files>$HOME/.claude/get-shit-done/templates/summary-standard.md, $HOME/.claude/get-shit-done/templates/summary-minimal.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary-standard.md (current state — confirm `key-decisions:` is the last frontmatter key before `duration:`)
    - $HOME/.claude/get-shit-done/templates/summary-minimal.md (current state — confirm `key-decisions: []` is the last frontmatter key before `duration:`)
    - $HOME/.claude/get-shit-done/templates/summary.md lines 41 (the existing canonical placement and inline comment style)
    - .planning/milestones/v1.3-phases/22-process-frontmatter-standard/22-CONTEXT.md D-03 (lock: all three templates carry the field; minimal stays minimal but never optional on traceability) and D-04 (empty list is valid)
  </read_first>
  <behavior>
    - `summary-standard.md` frontmatter contains a `requirements-completed: []` line positioned BETWEEN `key-decisions:` (with its `- "Decision 1"` child) and `duration:`.
    - `summary-minimal.md` frontmatter contains a `requirements-completed: []` line positioned BETWEEN `key-decisions: []` and `duration:`.
    - Inline comment on the new line: `# REQUIRED — Copy requirement IDs from PLAN.md `requirements:` verbatim. Empty list `[]` is valid; see Phase 18-01 for canonical empty-list rationale.`
    - Neither file gains the underscore form or any sibling field.
  </behavior>
  <action>
    Per D-03 (CONTEXT.md), add the canonical `requirements-completed:` field to the two templates that lack it.

    Use the Edit tool. For each file, insert the EXACT line below at the indicated location.

    **File 1 — `~/.claude/get-shit-done/templates/summary-standard.md`:**

    Find the existing two-line block:
    ```
    key-decisions:
      - "Decision 1"
    duration: Xmin
    ```

    Replace it with:
    ```
    key-decisions:
      - "Decision 1"
    requirements-completed: []  # REQUIRED — Copy requirement IDs from PLAN.md `requirements:` verbatim. Empty list `[]` is valid; see Phase 18-01 for canonical empty-list rationale.
    duration: Xmin
    ```

    **File 2 — `~/.claude/get-shit-done/templates/summary-minimal.md`:**

    Find the existing two-line block:
    ```
    key-decisions: []
    duration: Xmin
    ```

    Replace it with:
    ```
    key-decisions: []
    requirements-completed: []  # REQUIRED — Copy requirement IDs from PLAN.md `requirements:` verbatim. Empty list `[]` is valid; see Phase 18-01 for canonical empty-list rationale.
    duration: Xmin
    ```

    Do NOT touch any other line. Do NOT change `key-decisions` shape, indentation, or value. Do NOT modify the body of either template.
  </action>
  <verify>
    <automated>grep -c '^requirements-completed:' /Users/cary/.claude/get-shit-done/templates/summary-standard.md /Users/cary/.claude/get-shit-done/templates/summary-minimal.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^requirements-completed:' ~/.claude/get-shit-done/templates/summary-standard.md` returns `1`.
    - `grep -c '^requirements-completed:' ~/.claude/get-shit-done/templates/summary-minimal.md` returns `1`.
    - `grep -c 'requirements_completed' ~/.claude/get-shit-done/templates/summary-standard.md ~/.claude/get-shit-done/templates/summary-minimal.md ~/.claude/get-shit-done/templates/summary.md` reports `0` underscore matches across all three files.
    - In `summary-standard.md`, the line numbers satisfy: line of `key-decisions:` < line of `requirements-completed:` < line of `duration:` (verified with `grep -n`).
    - In `summary-minimal.md`, the line numbers satisfy: line of `key-decisions:` < line of `requirements-completed:` < line of `duration:` (verified with `grep -n`).
    - The new line in both files contains the literal substring ``Empty list `[]` is valid``.
    - `git diff --stat ~/.claude/get-shit-done/templates/` reports exactly 2 files changed (the two we edited).
  </acceptance_criteria>
  <done>
    Both `summary-standard.md` and `summary-minimal.md` carry `requirements-completed: []` with the canonical inline comment, positioned between `key-decisions` and `duration`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tighten `requirements-completed` doc in summary.md</name>
  <files>$HOME/.claude/get-shit-done/templates/summary.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md (read in full — confirm line 41 currently reads `requirements-completed: []  # REQUIRED — Copy ALL requirement IDs from this plan's \`requirements\` frontmatter field.`)
    - .planning/milestones/v1.3-phases/22-process-frontmatter-standard/22-CONTEXT.md D-04 (empty list is valid; inline rationale comment encouraged but not required) and D-05 (sibling fields are NOT canonical) and the "Specific Ideas" block (canonical empty-list comment example references 18-01-SUMMARY.md line 53)
    - .planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-01-SUMMARY.md line 53 (canonical empty-list rationale comment in the wild)
  </read_first>
  <behavior>
    - The existing single-line comment on `requirements-completed: []` in `summary.md` line 41 is replaced with a multi-line documentation block (still valid YAML) that:
      1. States the field is REQUIRED.
      2. States that empty list `[]` is valid when the plan delivers no user-visible requirement.
      3. References Phase 18-01-SUMMARY.md as the canonical empty-list rationale example.
      4. States that sibling fields (`requirements-scaffolded`, `requirements-supports`, `requirements-touched-but-not-completed`) are deprecated and lint will flag them.
    - No other change in `summary.md`.
  </behavior>
  <action>
    Per D-04 (empty list valid) and D-05 (siblings deprecated), tighten the doc on the existing `requirements-completed:` line in `~/.claude/get-shit-done/templates/summary.md`.

    Use the Edit tool. Replace EXACTLY this line at line 41:
    ```
    requirements-completed: []  # REQUIRED — Copy ALL requirement IDs from this plan's `requirements` frontmatter field.
    ```

    With this block (preserves YAML validity — `requirements-completed` remains a list-typed key, comments are YAML-legal anywhere):
    ```
    # requirements-completed (REQUIRED): list of requirement IDs this plan delivered.
    # Copy ALL IDs from this plan's `requirements` frontmatter field verbatim.
    # Empty list `[]` is valid when the plan delivered no user-visible requirement
    # (e.g. Wave 0 test scaffolding). See `.planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-01-SUMMARY.md`
    # line 53 for the canonical empty-list rationale comment style.
    # Sibling fields (`requirements-scaffolded`, `requirements-supports`,
    # `requirements-touched-but-not-completed`) are deprecated as of Phase 22
    # and the lint script will flag them.
    requirements-completed: []
    ```

    Do NOT touch any other line. Do NOT alter the example block at lines 167-235 or any other section. The frontmatter context block (`<frontmatter_guidance>` etc.) at the bottom of the file may stay unchanged.
  </action>
  <verify>
    <automated>grep -nE '^# requirements-completed \(REQUIRED\)' /Users/cary/.claude/get-shit-done/templates/summary.md && grep -nE '^requirements-completed: \[\]' /Users/cary/.claude/get-shit-done/templates/summary.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^# requirements-completed (REQUIRED):' ~/.claude/get-shit-done/templates/summary.md` returns `1`.
    - `grep -c '^requirements-completed: \[\]$' ~/.claude/get-shit-done/templates/summary.md` returns `1`.
    - `grep -c 'Sibling fields' ~/.claude/get-shit-done/templates/summary.md` returns at least `1` (the deprecation note is present).
    - `grep -c '18-01-SUMMARY.md' ~/.claude/get-shit-done/templates/summary.md` returns at least `1` (canonical example is referenced).
    - `grep -c 'requirements_completed' ~/.claude/get-shit-done/templates/summary.md` returns `0` (no underscore form).
    - `git diff ~/.claude/get-shit-done/templates/summary.md` shows changes confined to the lines around line 41 (the doc block expansion); no other section changes.
  </acceptance_criteria>
  <done>
    `summary.md` carries the canonical `requirements-completed: []` line preceded by a multi-line YAML-comment doc block that documents empty-list validity (D-04), the canonical 18-01 example, and the sibling-field deprecation (D-05).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Template-file edits in `~/.claude/get-shit-done/templates/`. No runtime code path, no I/O surface, no data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | n/a | n/a | n/a | Phase 22 is process/CI/docs only. No new attack surface introduced. |
</threat_model>

<verification>
- All three templates contain `requirements-completed:` (hyphenated).
- Zero occurrences of `requirements_completed` (underscore) across the three template files.
- `summary.md` doc block references 18-01 canonical example AND sibling-field deprecation.
- The `requirements-completed: []` line in `summary.md` is still present after the doc block expansion (still emitted into populated SUMMARYs).
</verification>

<success_criteria>
All three SUMMARY templates emit the canonical `requirements-completed:` key with the documented empty-list-valid semantics. ROADMAP success criterion #1 (template includes the field with documented schema) is satisfied. v1.3+ SUMMARYs generated from these templates will carry the field.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.3-phases/22-process-frontmatter-standard/22-02-SUMMARY.md` using `~/.claude/get-shit-done/templates/summary.md` (the very template just updated — dogfood). The SUMMARY's `requirements-completed:` field MUST be `[PROCESS-01]`.
</output>
