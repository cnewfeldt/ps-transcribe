---
phase: 22-process-frontmatter-standard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [PROCESS-01]
must_haves:
  truths:
    - "REQUIREMENTS.md PROCESS-01..03 wording uses the canonical hyphenated key `requirements-completed` (not underscore)."
    - "No new requirements are added; only the three existing PROCESS bullets are reworded."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Hyphenated PROCESS-01..03 wording — single source of truth for the canonical key."
      contains: "requirements-completed"
  key_links:
    - from: ".planning/REQUIREMENTS.md PROCESS-01..03"
      to: "every downstream artifact in this phase (templates, generator, lint, migration)"
      via: "shared canonical key string"
      pattern: "requirements-completed"
---

<objective>
Reword REQUIREMENTS.md PROCESS-01, PROCESS-02, and PROCESS-03 to use the canonical hyphenated key `requirements-completed` (D-01, D-02). This is the FIRST task of Phase 22 — every other plan reads from these bullets, so the vocabulary fix must land before templates, generator, lint script, or migration touch the same string.

Purpose: Establish single-source-of-truth vocabulary. CONTEXT.md D-02 explicitly tasks the planner with this rewording as the first step.
Output: Three updated bullets in `.planning/REQUIREMENTS.md` under the "Process (`PROCESS-FUT-01`)" section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/milestones/v1.3-phases/22-process-frontmatter-standard/22-CONTEXT.md

<interfaces>
<!-- Current state of the three target bullets in REQUIREMENTS.md (lines 44-48). -->
<!-- These are the EXACT strings to replace. -->

```markdown
### Process (`PROCESS-FUT-01`) — SUMMARY.md frontmatter standard

- [ ] **PROCESS-01**: Add `requirements_completed` (list) field to SUMMARY.md template frontmatter
- [ ] **PROCESS-02**: Update SUMMARY.md generation flow (gsd-doc-writer agent + manual workflows) to populate the field automatically by parsing PLAN.md requirement references
- [ ] **PROCESS-03**: Add a lint check (CI or pre-commit) that fails when SUMMARY.md is committed without `requirements_completed`
```

Each `requirements_completed` (underscore) MUST become `requirements-completed` (hyphen). No other text changes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reword PROCESS-01..03 from underscore to hyphen in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (read in full so the executor sees the exact current state of lines 44-48 and the surrounding section header at line 44)
    - .planning/milestones/v1.3-phases/22-process-frontmatter-standard/22-CONTEXT.md (D-01, D-02 — locks the canonical hyphen form and orders this task FIRST)
  </read_first>
  <behavior>
    - PROCESS-01 bullet text contains `requirements-completed` (hyphen) and no longer contains `requirements_completed` (underscore).
    - PROCESS-02 bullet text is unchanged in meaning; only references to the field name flip to hyphen — but PROCESS-02 does not literally name the field, so no edit is needed there beyond verifying.
    - PROCESS-03 bullet text contains `requirements-completed` (hyphen) and no longer contains `requirements_completed` (underscore).
    - The `### Process (`PROCESS-FUT-01`) — SUMMARY.md frontmatter standard` heading is unchanged.
    - No other line in REQUIREMENTS.md changes.
  </behavior>
  <action>
    Per D-02 (CONTEXT.md), update the three PROCESS-* bullets in `.planning/REQUIREMENTS.md` so the field name uses the canonical hyphenated form (D-01).

    Use the Edit tool, NOT sed, for both edits (so a diff review is clean and reviewable). Make these two exact replacements:

    Replacement 1 — PROCESS-01 (line ~46):
    OLD:
    ```
    - [ ] **PROCESS-01**: Add `requirements_completed` (list) field to SUMMARY.md template frontmatter
    ```
    NEW:
    ```
    - [ ] **PROCESS-01**: Add `requirements-completed` (list) field to SUMMARY.md template frontmatter
    ```

    Replacement 2 — PROCESS-03 (line ~48):
    OLD:
    ```
    - [ ] **PROCESS-03**: Add a lint check (CI or pre-commit) that fails when SUMMARY.md is committed without `requirements_completed`
    ```
    NEW:
    ```
    - [ ] **PROCESS-03**: Add a lint check (CI or pre-commit) that fails when SUMMARY.md is committed without `requirements-completed`
    ```

    PROCESS-02 (line ~47) does NOT name the field literally and does NOT need editing — verify via grep.

    Do NOT touch any other content. Do NOT alter checkbox state (`- [ ]` stays `- [ ]`). Do NOT touch the section heading. Do NOT renumber.
  </action>
  <verify>
    <automated>grep -nE '^\- \[ \] \*\*PROCESS-0(1|3)\*\*' /Users/cary/Development/ai-development/ps-transcribe/.planning/REQUIREMENTS.md | grep -v 'requirements_completed' | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - Command `grep -c 'requirements_completed' .planning/REQUIREMENTS.md` returns `0` (zero underscore occurrences).
    - Command `grep -c 'requirements-completed' .planning/REQUIREMENTS.md` returns `2` (one hit in PROCESS-01, one hit in PROCESS-03).
    - Command `grep -E '^\- \[ \] \*\*PROCESS-01\*\*:' .planning/REQUIREMENTS.md` returns exactly one line, and that line contains the substring `` `requirements-completed` `` (backtick-wrapped, hyphen).
    - Command `grep -E '^\- \[ \] \*\*PROCESS-03\*\*:' .planning/REQUIREMENTS.md` returns exactly one line, and that line contains the substring `` `requirements-completed` `` (backtick-wrapped, hyphen).
    - `git diff --stat .planning/REQUIREMENTS.md` reports exactly 1 file changed with at most 2 insertions and 2 deletions (one line edit per replacement).
    - `git diff .planning/REQUIREMENTS.md` shows ZERO additions or deletions outside lines containing `PROCESS-01` or `PROCESS-03`.
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md PROCESS-01 and PROCESS-03 use the hyphenated canonical form. Zero `requirements_completed` underscore tokens remain in the file. Diff is minimal (≤ 2 line edits).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only edit; no code paths, no auth, no I/O surface, no data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | n/a | n/a | n/a | Phase 22 is process/CI/docs only. No new attack surface introduced. |
</threat_model>

<verification>
- `grep -c 'requirements_completed' .planning/REQUIREMENTS.md` returns 0.
- `grep -c 'requirements-completed' .planning/REQUIREMENTS.md` returns 2.
- All other PROCESS-* bullet wording semantically unchanged.
</verification>

<success_criteria>
PROCESS-01 and PROCESS-03 in `.planning/REQUIREMENTS.md` use `requirements-completed` (hyphen). The single-source-of-truth vocab fix is committed before any template/generator/lint plan starts.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.3-phases/22-process-frontmatter-standard/22-01-SUMMARY.md` using `~/.claude/get-shit-done/templates/summary.md`. The SUMMARY's `requirements-completed:` field MUST be `[PROCESS-01]` (this plan reaffirms the spec text underpinning PROCESS-01; PROCESS-02 and PROCESS-03 are delivered in subsequent plans).
</output>
