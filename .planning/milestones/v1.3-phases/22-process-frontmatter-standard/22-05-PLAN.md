---
phase: 22-process-frontmatter-standard
plan: 05
type: execute
wave: 4
depends_on: [22-04]
files_modified:
  - .github/workflows/lint-summaries.yml
autonomous: true
requirements: [PROCESS-03]
must_haves:
  truths:
    - "`.github/workflows/lint-summaries.yml` exists, runs on `macos-26`, triggers on PRs to main when SUMMARY.md or PLAN.md files change, installs `yq` via Homebrew, and invokes `bash scripts/lint-summaries.sh`."
    - "Workflow is path-filtered so doc-only PRs that touch other files do not trigger a lint job; PRs that touch SUMMARY.md or PLAN.md DO trigger it."
    - "Workflow is a separate file from `build-check.yml` (D-11) so a doc-only PR does not block on `swift build`."
  artifacts:
    - path: ".github/workflows/lint-summaries.yml"
      provides: "CI gate that runs `scripts/lint-summaries.sh` on path-filtered PRs."
      contains: "lint-summaries.sh"
      min_lines: 20
  key_links:
    - from: ".github/workflows/lint-summaries.yml"
      to: "scripts/lint-summaries.sh"
      via: "shell invocation in a job step"
      pattern: "bash scripts/lint-summaries.sh"
---

<objective>
Wire the lint script into GitHub Actions as a CI-only gate (D-09) — a new path-filtered workflow `.github/workflows/lint-summaries.yml` that runs on `macos-26` (D-11), installs `yq` via Homebrew (D-10), and invokes `bash scripts/lint-summaries.sh`. The workflow is separate from `build-check.yml` so a doc-only PR does not wait on `swift build`.

Purpose: Without CI enforcement, the lint script is advisory. PROCESS-03 explicitly requires CI failure when SUMMARY.md ships without `requirements-completed`.
Output: A single new workflow file at `.github/workflows/lint-summaries.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.3-phases/22-process-frontmatter-standard/22-CONTEXT.md
@.github/workflows/build-check.yml
@scripts/lint-summaries.sh

<interfaces>
<!-- Existing workflow style (build-check.yml) is the pattern reference: macos-26, single job, minimal step list, branch trigger on PRs to main. -->
<!-- Pinning a third-party action by full SHA matches build-check.yml's `actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4` pattern — reuse the same SHA. -->

**Required behaviour:**
1. Trigger: pull requests targeting `main` whose change set touches at least one of:
   - `**/*-SUMMARY.md`
   - `**/*-PLAN.md`
   - `scripts/lint-summaries.sh`
   - `.github/workflows/lint-summaries.yml` itself
2. Runner: `macos-26` (matches `build-check.yml`).
3. Steps:
   a. `actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4` (same pin as build-check.yml).
   b. Install yq via Homebrew: `brew install yq` (the runner has Homebrew preinstalled).
   c. Verify yq present: `yq --version`.
   d. Run the lint script: `bash scripts/lint-summaries.sh`.
4. Job is named `lint-summaries`. Workflow name `Lint Summaries`.
5. No `permissions:` block expansion — read-only checkout is sufficient (no secrets used, no statuses written).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create .github/workflows/lint-summaries.yml</name>
  <files>.github/workflows/lint-summaries.yml</files>
  <read_first>
    - .github/workflows/build-check.yml (style and pin reference — copy the checkout SHA pin and macos-26 runner declaration verbatim)
    - scripts/lint-summaries.sh (verify the invocation form `bash scripts/lint-summaries.sh` is what the script's --help advertises)
    - .planning/milestones/v1.3-phases/22-process-frontmatter-standard/22-CONTEXT.md D-09, D-10, D-11
  </read_first>
  <behavior>
    - File `.github/workflows/lint-summaries.yml` exists.
    - Workflow's `on.pull_request.paths` filter contains `**/*-SUMMARY.md`, `**/*-PLAN.md`, `scripts/lint-summaries.sh`, and `.github/workflows/lint-summaries.yml`.
    - Workflow declares `runs-on: macos-26`.
    - Workflow installs `yq` via `brew install yq` and runs `bash scripts/lint-summaries.sh`.
    - Workflow is independent of `build-check.yml` — neither file references the other.
  </behavior>
  <action>
    Create `/Users/cary/Development/ai-development/ps-transcribe/.github/workflows/lint-summaries.yml` with the following exact contents. Use the Write tool.

    ```yaml
    name: Lint Summaries

    on:
      pull_request:
        branches: [main]
        paths:
          - '**/*-SUMMARY.md'
          - '**/*-PLAN.md'
          - 'scripts/lint-summaries.sh'
          - '.github/workflows/lint-summaries.yml'

    jobs:
      lint-summaries:
        runs-on: macos-26
        steps:
          - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4

          - name: Install yq
            run: |
              brew install yq
              yq --version

          - name: Lint SUMMARY frontmatter
            run: bash scripts/lint-summaries.sh
    ```

    Do NOT modify `build-check.yml`. Do NOT add a top-level `permissions:` block. Do NOT add a `concurrency:` group (the workflow is short and idempotent).
  </action>
  <verify>
    <automated>test -f /Users/cary/Development/ai-development/ps-transcribe/.github/workflows/lint-summaries.yml && grep -c 'runs-on: macos-26' /Users/cary/Development/ai-development/ps-transcribe/.github/workflows/lint-summaries.yml</automated>
  </verify>
  <acceptance_criteria>
    - File `.github/workflows/lint-summaries.yml` exists.
    - `grep -c '^name: Lint Summaries$' .github/workflows/lint-summaries.yml` returns `1`.
    - `grep -c 'runs-on: macos-26' .github/workflows/lint-summaries.yml` returns `1`.
    - `grep -c "'\\*\\*/\\*-SUMMARY.md'" .github/workflows/lint-summaries.yml` returns `1` (path-filter for SUMMARY).
    - `grep -c "'\\*\\*/\\*-PLAN.md'" .github/workflows/lint-summaries.yml` returns `1` (path-filter for PLAN).
    - `grep -c 'bash scripts/lint-summaries.sh' .github/workflows/lint-summaries.yml` returns `1` (script invocation).
    - `grep -c 'brew install yq' .github/workflows/lint-summaries.yml` returns `1`.
    - `grep -c '34e114876b0b11c390a56381ad16ebd13914f8d5' .github/workflows/lint-summaries.yml` returns `1` (checkout SHA pin matches build-check.yml — verified via `diff <(grep checkout@ .github/workflows/build-check.yml) <(grep checkout@ .github/workflows/lint-summaries.yml)` exits 0).
    - Workflow YAML parses cleanly: `yq eval '.jobs.lint-summaries.runs-on' .github/workflows/lint-summaries.yml` returns the literal string `macos-26`.
    - `build-check.yml` is unchanged: `git diff .github/workflows/build-check.yml` produces no output.
  </acceptance_criteria>
  <done>
    `.github/workflows/lint-summaries.yml` exists with the locked shape (macos-26, path-filtered, brew install yq, bash invocation). Will activate on the next PR that touches a SUMMARY or PLAN file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → repo files | Public CI runner reads checked-out code. The lint script is read-only and has no secrets access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05-01 | Tampering | actions/checkout action | mitigate | Action is pinned by full commit SHA (`34e114876b0b11c390a56381ad16ebd13914f8d5`), matching the existing build-check.yml convention. SHA pinning prevents tag retargeting. |
| T-22-05-02 | Elevation of Privilege | Workflow runner | accept | No `permissions:` block; runner uses default GITHUB_TOKEN (read-only on PRs from forks). No status writes, no secrets used. |
</threat_model>

<verification>
- Workflow file present and well-formed YAML.
- macos-26 runner; path-filtered to SUMMARY/PLAN/script/workflow file; brew install yq + bash invocation.
- build-check.yml untouched.
</verification>

<success_criteria>
ROADMAP success criterion #3 (CI or pre-commit lint check) is wired. Once Plan 22-06 migrates the archived SUMMARYs and the migration commit is pushed via PR, the workflow will exit 0 against main; subsequent PRs that drop or break a SUMMARY's `requirements-completed:` field will fail this gate.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.3-phases/22-process-frontmatter-standard/22-05-SUMMARY.md` using `~/.claude/get-shit-done/templates/summary.md`. The SUMMARY's `requirements-completed:` field MUST be `[PROCESS-03]`.
</output>
