---
phase: 22-process-frontmatter-standard
plan: 06
type: execute
wave: 5
depends_on: [22-04, 22-05]
files_modified:
  - scripts/migrate-summary-frontmatter.sh
  - .planning/milestones/v1.2-phases/16-foundation/16-01-SUMMARY.md
  - .planning/milestones/v1.2-phases/16-foundation/16-02-SUMMARY.md
  - .planning/milestones/v1.2-phases/16-foundation/16-03-SUMMARY.md
  - .planning/milestones/v1.2-phases/16-foundation/16-04-SUMMARY.md
  - .planning/milestones/v1.2-phases/17-model-auto-update/17-01-SUMMARY.md
  - .planning/milestones/v1.2-phases/17-model-auto-update/17-02-SUMMARY.md
  - .planning/milestones/v1.2-phases/17-model-auto-update/17-03-SUMMARY.md
  - .planning/milestones/v1.2-phases/17-model-auto-update/17-05-SUMMARY.md
  - .planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-01-SUMMARY.md
  - .planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-02-SUMMARY.md
  - .planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-03-SUMMARY.md
  - .planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-04-SUMMARY.md
  - .planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-05-SUMMARY.md
  - .planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-07-SUMMARY.md
  - .planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-09-SUMMARY.md
  - .planning/milestones/v1.2-phases/18.1-shared-save-destinations-local-file/18.1-07-SUMMARY.md
  - .planning/milestones/v1.2-phases/18.1-shared-save-destinations-local-file/18.1-08-SUMMARY.md
  - .planning/milestones/v1.2-phases/18.1-shared-save-destinations-local-file/18.1-09-SUMMARY.md
autonomous: true
requirements: [PROCESS-01, PROCESS-02, PROCESS-03]
must_haves:
  truths:
    - "Every archived `*-SUMMARY.md` under `.planning/milestones/` carries `requirements-completed:` (canonical hyphen, D-01)."
    - "Zero `requirements_completed:` (underscore) tokens remain in any archived SUMMARY."
    - "Every deprecated sibling field has been stripped from YAML and re-attached as inline comments above `requirements-completed:` (D-06 format: `# scaffolded: [DICT-02, DICT-03]  # historical sibling field, retired in Phase 22`)."
    - "The 5 SUMMARY files that previously lacked any requirement field now have `requirements-completed:` populated by reading the sibling PLAN.md `requirements:` field verbatim (D-14)."
    - "`bash scripts/lint-summaries.sh` (from Plan 22-04) exits 0 against the entire repo after migration."
    - "`scripts/migrate-summary-frontmatter.sh` is git-rm'd in the same commit that produces the migration (D-13 — reproducibility lives in git history)."
  artifacts:
    - path: "scripts/migrate-summary-frontmatter.sh"
      provides: "One-shot migration script (created and removed in same commit per D-13)."
      min_lines: 50
    - path: ".planning/milestones/**/*-SUMMARY.md"
      provides: "Migrated archived SUMMARYs — uniformly canonical."
      contains: "requirements-completed:"
  key_links:
    - from: "migrate-summary-frontmatter.sh"
      to: "scripts/lint-summaries.sh"
      via: "post-migration lint must exit 0"
      pattern: "lint-summaries.sh"
---

<objective>
Land the one-shot migration of all archived SUMMARYs to the canonical Phase 22 form (D-12, D-13, D-14):
1. Rename `requirements_completed:` → `requirements-completed:` (7 files).
2. Strip deprecated sibling fields and re-attach them as inline YAML comments (6 files).
3. Add missing `requirements-completed:` by reading sibling PLAN.md `requirements:` verbatim (5 files).

Per D-13: a single ephemeral script `scripts/migrate-summary-frontmatter.sh` is written, executed, the modified SUMMARYs are committed, and the script is `git rm`'d in the SAME commit. Reproducibility lives in git history.

Purpose: Make the lint gate (Plan 22-04 + 22-05) achieve a green state across the entire repo. After this plan ships, every Phase 23+ SUMMARY participates in the same uniform contract and a regression in any future SUMMARY blocks the merge.

**IMPORTANT — ground-truth deviation from CONTEXT.md counts:**
CONTEXT.md D-13/D-14 cited "5 rename + ~6 sibling-strip + 4 missing = ~15 files" but the actual disk inventory is **7 rename + 6 sibling-strip + 5 missing = 18 files** (the 18.1 phase directory was added late in v1.2 and CONTEXT.md's sample count is slightly stale). D-12 explicitly locks "full migration of all 33 archived SUMMARYs" as the success criterion, so the migration script must address ALL 18 actual files — not the stale CONTEXT count. The full list is in this plan's `files_modified:` frontmatter.

Output: A single git commit with subject `chore(planning): migrate SUMMARY frontmatter to canonical requirements-completed` containing:
- The 18 SUMMARY edits.
- The script CREATION + DELETION (both in the same commit — created, executed, `git rm`'d before commit).
- `bash scripts/lint-summaries.sh` exits 0 in the commit's tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.3-phases/22-process-frontmatter-standard/22-CONTEXT.md
@scripts/lint-summaries.sh

<interfaces>
<!-- Ground-truth file inventory (verified at planning time, 2026-05-02): -->

**Group A — RENAME `requirements_completed:` → `requirements-completed:` (7 files):**
- `.planning/milestones/v1.2-phases/16-foundation/16-01-SUMMARY.md`
- `.planning/milestones/v1.2-phases/16-foundation/16-02-SUMMARY.md`
- `.planning/milestones/v1.2-phases/16-foundation/16-03-SUMMARY.md`
- `.planning/milestones/v1.2-phases/16-foundation/16-04-SUMMARY.md`
- `.planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-09-SUMMARY.md`
- `.planning/milestones/v1.2-phases/18.1-shared-save-destinations-local-file/18.1-07-SUMMARY.md`
- `.planning/milestones/v1.2-phases/18.1-shared-save-destinations-local-file/18.1-08-SUMMARY.md`

Operation: a single in-place s/^requirements_completed:/requirements-completed:/ on each file. Preserve all surrounding lines (including any trailing list values, inline comments, etc.).

**Group B — STRIP sibling fields, RE-ATTACH as inline comments above `requirements-completed:` (6 files):**
- `.planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-01-SUMMARY.md` — has `requirements-scaffolded: [...]`
- `.planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-02-SUMMARY.md` — has `requirements-scaffolded: [...]`
- `.planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-03-SUMMARY.md` — has `requirements-supports: [...]`
- `.planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-04-SUMMARY.md` — has `requirements-supports: [...]`
- `.planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-05-SUMMARY.md` — has `requirements-supports: [...]`
- `.planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-07-SUMMARY.md` — has `requirements-touched-but-not-completed: [...]`

Operation per D-06 LOCKED format:
- For each sibling field, capture the field name (without the `requirements-` prefix — i.e. `scaffolded`, `supports`, or `touched-but-not-completed`) and its YAML list value.
- Insert a comment line ABOVE the existing `requirements-completed:` line with the EXACT format (locked in D-06):
  ```
  # scaffolded: [DICT-02, DICT-03]  # historical sibling field, retired in Phase 22
  ```
  (Replace `scaffolded` with the actual sibling name, replace the bracketed list with the actual list value.)
- DELETE the sibling field's YAML key+value lines from frontmatter.

**Group C — ADD missing `requirements-completed:` by copying sibling PLAN.md `requirements:` verbatim (5 files, D-14):**
- `.planning/milestones/v1.2-phases/17-model-auto-update/17-01-SUMMARY.md` ← read 17-01-PLAN.md `requirements:`
- `.planning/milestones/v1.2-phases/17-model-auto-update/17-02-SUMMARY.md` ← read 17-02-PLAN.md `requirements:`
- `.planning/milestones/v1.2-phases/17-model-auto-update/17-03-SUMMARY.md` ← read 17-03-PLAN.md `requirements:`
- `.planning/milestones/v1.2-phases/17-model-auto-update/17-05-SUMMARY.md` ← read 17-05-PLAN.md `requirements:`
- `.planning/milestones/v1.2-phases/18.1-shared-save-destinations-local-file/18.1-09-SUMMARY.md` ← read 18.1-09-PLAN.md `requirements:`

Operation per D-14: read `requirements:` from each sibling PLAN.md (use `yq --front-matter=extract eval '.requirements' "$plan"`). Insert a new line `requirements-completed: <verbatim list>` into the SUMMARY's frontmatter, positioned just before the closing `---` or just after `key-decisions` (Claude's discretion — the position must be inside the frontmatter block; consistency with existing siblings is preferred). If a sibling PLAN.md has empty/no `requirements:` field, emit `requirements-completed: []`.

**Group D — RENAME-COLLISION (Group A + Group C overlap on Phase 18.1):**
The two 18.1 files (18.1-07, 18.1-08) are in Group A. 18.1-09 is in Group C. No collision — they are three distinct files.

**Final post-migration state:**
- Every SUMMARY under `.planning/milestones/` has exactly one canonical `requirements-completed:` key.
- Zero `requirements_completed:` underscore keys.
- Zero unstripped sibling fields.
- `bash scripts/lint-summaries.sh` exits 0 with `0 failures, 0 warnings`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write scripts/migrate-summary-frontmatter.sh (one-shot migration)</name>
  <files>scripts/migrate-summary-frontmatter.sh</files>
  <read_first>
    - scripts/lint-summaries.sh (style reference — same `set -euo pipefail`, same yq dependency check, same color helpers)
    - scripts/build_swift_app.sh (additional style reference)
    - .planning/milestones/v1.3-phases/22-process-frontmatter-standard/22-CONTEXT.md D-06 (locked comment format), D-13 (one-shot ephemeral script), D-14 (verbatim copy from sibling PLAN.md `requirements:`)
    - .planning/milestones/v1.2-phases/16-foundation/16-01-SUMMARY.md (rename target — verify shape of `requirements_completed: [SC-1]` line)
    - .planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-01-SUMMARY.md (sibling-strip target — note the trailing inline comment on `requirements-completed: []  # Wave 0 ships test scaffolding only -- no requirement is delivered until its implementing wave...` MUST be preserved AS-IS; only the separate `requirements-scaffolded:` line is stripped)
    - .planning/milestones/v1.2-phases/17-model-auto-update/17-01-PLAN.md (verify the executor can read `requirements:` from the PLAN frontmatter; this is the source for Group C add-missing)
  </read_first>
  <behavior>
    - `scripts/migrate-summary-frontmatter.sh` exists, is executable, runs `set -euo pipefail`.
    - Running `bash scripts/migrate-summary-frontmatter.sh` performs all three operations (rename / strip-and-comment / add-missing) and prints a per-file disposition log.
    - Script is idempotent — running it a second time leaves files unchanged (Group A: no `requirements_completed:` keys remain; Group B: no sibling fields remain; Group C: every target SUMMARY already has `requirements-completed:`).
    - Script preserves all OTHER content in each SUMMARY (only the specific YAML lines change; body, decisions, headers stay byte-identical).
    - Script exits 0 on successful migration; non-zero if any operation fails.
  </behavior>
  <action>
    Create `/Users/cary/Development/ai-development/ps-transcribe/scripts/migrate-summary-frontmatter.sh` with the exact contents below. Use the Write tool, then `chmod +x` it.

    The script encodes the three groups via explicit file lists (no globbing magic — explicit lists make the diff reviewable and the script self-documenting per D-13's reproducibility-via-git-history goal).

    ```bash
    #!/usr/bin/env bash
    # scripts/migrate-summary-frontmatter.sh
    #
    # ONE-SHOT migration of archived SUMMARY.md frontmatter to the Phase 22 canonical form.
    # Per D-13: this script is executed exactly once, the resulting commit includes the
    # script's deletion (`git rm`), and reproducibility lives in git history.
    #
    # Locks (.planning/milestones/v1.3-phases/22-process-frontmatter-standard/22-CONTEXT.md):
    #   D-01: canonical key is `requirements-completed` (hyphen).
    #   D-06: stripped sibling fields are re-attached as inline YAML comments above
    #         `requirements-completed:` in the format
    #         `# <sibling-name>: [<ids>]  # historical sibling field, retired in Phase 22`.
    #   D-13: one-shot script — git rm in the same commit that produces the migration.
    #   D-14: missing-field SUMMARYs receive `requirements-completed:` copied verbatim from
    #         the sibling PLAN.md `requirements:` field.
    #
    # Operations (per group):
    #   Group A — rename `requirements_completed:` → `requirements-completed:` (7 files).
    #   Group B — strip sibling field, re-attach as inline comment (6 files).
    #   Group C — add missing field by copying sibling PLAN.requirements (5 files).

    set -euo pipefail

    if ! command -v yq >/dev/null 2>&1; then
      echo "ERROR: yq is required (brew install yq)" >&2
      exit 2
    fi

    REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
    cd "$REPO_ROOT"

    # ---- Group A: rename underscore → hyphen ----
    GROUP_A=(
      ".planning/milestones/v1.2-phases/16-foundation/16-01-SUMMARY.md"
      ".planning/milestones/v1.2-phases/16-foundation/16-02-SUMMARY.md"
      ".planning/milestones/v1.2-phases/16-foundation/16-03-SUMMARY.md"
      ".planning/milestones/v1.2-phases/16-foundation/16-04-SUMMARY.md"
      ".planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-09-SUMMARY.md"
      ".planning/milestones/v1.2-phases/18.1-shared-save-destinations-local-file/18.1-07-SUMMARY.md"
      ".planning/milestones/v1.2-phases/18.1-shared-save-destinations-local-file/18.1-08-SUMMARY.md"
    )

    rename_underscore() {
      local f="$1"
      if grep -q '^requirements_completed:' "$f"; then
        # macOS BSD sed in-place needs `-i ''`. Use a portable `perl -i` so this works on Linux runners too.
        perl -i -pe 's/^requirements_completed:/requirements-completed:/' "$f"
        echo "[A] renamed underscore key in $f"
      else
        echo "[A] (idempotent) $f already canonical"
      fi
    }

    # ---- Group B: strip sibling, re-attach as inline comment ----
    # Format per D-06:
    #   # <sibling-name>: [<ids>]  # historical sibling field, retired in Phase 22
    # where <sibling-name> = sibling key minus the `requirements-` prefix.

    GROUP_B=(
      ".planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-01-SUMMARY.md|requirements-scaffolded"
      ".planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-02-SUMMARY.md|requirements-scaffolded"
      ".planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-03-SUMMARY.md|requirements-supports"
      ".planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-04-SUMMARY.md|requirements-supports"
      ".planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-05-SUMMARY.md|requirements-supports"
      ".planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-07-SUMMARY.md|requirements-touched-but-not-completed"
    )

    strip_sibling() {
      local entry="$1"
      local f="${entry%%|*}"
      local sibling="${entry##*|}"
      local short_name="${sibling#requirements-}"

      # If sibling not present, skip (idempotency).
      if ! grep -q "^${sibling}:" "$f"; then
        echo "[B] (idempotent) ${sibling} already stripped from $f"
        return 0
      fi

      # Capture the sibling line's value (everything after the colon, trimmed).
      local sibling_line
      sibling_line="$(grep "^${sibling}:" "$f" | head -1)"
      # Extract the bracketed list portion. The sibling lines in the corpus look like:
      #   requirements-scaffolded: [DICT-02]  # Plan frontmatter listed DICT-02; deferred ...
      # We want everything from the first `[` to its matching `]`, OR the trailing scalar
      # if it's not a flow list. yq normalises this for us.
      local sibling_value
      sibling_value="$(yq --front-matter=extract eval ".[\"${sibling}\"]" "$f" 2>/dev/null)"
      if [[ -z "$sibling_value" || "$sibling_value" == "null" ]]; then
        echo "[B] WARNING: empty/null value for ${sibling} in $f — stripping line only" >&2
        sibling_value="[]"
      fi
      # Compact a multi-line yq list back to flow style: yq emits as bullets; transform.
      # Easiest: round-trip via yq with `-o=json` then ad-hoc YAML; OR detect inline `[...]`
      # in the original sibling_line and reuse it directly.
      local inline_list
      inline_list="$(printf '%s\n' "$sibling_line" | sed -nE 's/^[^:]+:[[:space:]]*(\[[^]]*\]).*/\1/p')"
      if [[ -n "$inline_list" ]]; then
        sibling_value="$inline_list"
      else
        # Compact the multiline output from yq back to flow form: strip leading "- " and join with ", ".
        sibling_value="$(printf '[%s]' "$(echo "$sibling_value" | sed -E 's/^- //; s/^"(.*)"$/\1/' | paste -sd ',' - | sed 's/,/, /g')")"
      fi

      local comment_line="# ${short_name}: ${sibling_value}  # historical sibling field, retired in Phase 22"

      # Two-step edit:
      #   1. Insert comment_line above the existing `requirements-completed:` line.
      #   2. Delete the sibling key's line.
      # Use perl -i so behaviour is identical on macOS and Linux.
      COMMENT_LINE="$comment_line" SIBLING="$sibling" perl -i -pe '
        BEGIN { $c = $ENV{COMMENT_LINE}; $s = $ENV{SIBLING}; $inserted = 0; }
        if (!$inserted && /^requirements-completed:/) {
          print "$c\n";
          $inserted = 1;
        }
      ' "$f"
      perl -i -ne "print unless /^${sibling}:/" "$f"
      echo "[B] stripped ${sibling} from $f → comment: ${comment_line}"
    }

    # ---- Group C: add missing field by copying sibling PLAN.requirements ----
    GROUP_C=(
      ".planning/milestones/v1.2-phases/17-model-auto-update/17-01-SUMMARY.md"
      ".planning/milestones/v1.2-phases/17-model-auto-update/17-02-SUMMARY.md"
      ".planning/milestones/v1.2-phases/17-model-auto-update/17-03-SUMMARY.md"
      ".planning/milestones/v1.2-phases/17-model-auto-update/17-05-SUMMARY.md"
      ".planning/milestones/v1.2-phases/18.1-shared-save-destinations-local-file/18.1-09-SUMMARY.md"
    )

    add_missing_field() {
      local summary="$1"
      local plan="${summary%-SUMMARY.md}-PLAN.md"

      if grep -q '^requirements-completed:' "$summary"; then
        echo "[C] (idempotent) $summary already has requirements-completed:"
        return 0
      fi

      if [[ ! -f "$plan" ]]; then
        echo "[C] WARNING: sibling PLAN.md missing for $summary — emitting empty list" >&2
        local payload="[]"
      else
        local payload
        payload="$(yq --front-matter=extract eval '.requirements' "$plan" 2>/dev/null)"
        if [[ -z "$payload" || "$payload" == "null" ]]; then
          payload="[]"
        else
          # Convert multiline yq output back to inline flow form for readability.
          payload="$(printf '[%s]' "$(echo "$payload" | sed -E 's/^- //; s/^"(.*)"$/\1/' | paste -sd ',' - | sed 's/,/, /g')")"
        fi
      fi

      # Insert before the closing `---` of the frontmatter (the SECOND `---` line in the file).
      INSERT_LINE="requirements-completed: ${payload}" perl -i -pe '
        BEGIN { $line = $ENV{INSERT_LINE}; $count = 0; $inserted = 0; }
        if (/^---$/) {
          $count++;
          if ($count == 2 && !$inserted) {
            print "$line\n";
            $inserted = 1;
          }
        }
      ' "$summary"
      echo "[C] added requirements-completed: ${payload} to $summary (from ${plan#./})"
    }

    echo "=== Group A: rename underscore key → hyphen key ==="
    for f in "${GROUP_A[@]}"; do
      rename_underscore "$f"
    done

    echo
    echo "=== Group B: strip sibling field, re-attach as comment ==="
    for entry in "${GROUP_B[@]}"; do
      strip_sibling "$entry"
    done

    echo
    echo "=== Group C: add missing field from sibling PLAN.md ==="
    for f in "${GROUP_C[@]}"; do
      add_missing_field "$f"
    done

    echo
    echo "Migration complete. Run 'bash scripts/lint-summaries.sh' to verify."
    ```

    After writing, `chmod +x scripts/migrate-summary-frontmatter.sh`.
  </action>
  <verify>
    <automated>test -x /Users/cary/Development/ai-development/ps-transcribe/scripts/migrate-summary-frontmatter.sh && grep -c 'historical sibling field, retired in Phase 22' /Users/cary/Development/ai-development/ps-transcribe/scripts/migrate-summary-frontmatter.sh</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/migrate-summary-frontmatter.sh` exists and is executable.
    - Script starts with `#!/usr/bin/env bash`.
    - Script contains `set -euo pipefail` (verified with `grep -c 'set -euo pipefail' scripts/migrate-summary-frontmatter.sh` ≥ 1).
    - Script defines exactly 3 file groups: GROUP_A (7 entries), GROUP_B (6 entries), GROUP_C (5 entries) — verified by `grep -cE '^GROUP_[ABC]=' scripts/migrate-summary-frontmatter.sh` returning `3` and counting `\.planning/` lines inside each array via `awk` if needed.
    - Script's D-06 comment format string contains `historical sibling field, retired in Phase 22` literal (verified with `grep -c 'historical sibling field, retired in Phase 22' scripts/migrate-summary-frontmatter.sh` returns at least `1`).
    - Script depends only on `bash`, `perl`, `yq`, `grep`, `sed`, `paste` (verified with `grep -E 'python|node|npm|npx' scripts/migrate-summary-frontmatter.sh` returning 0 matches).
  </acceptance_criteria>
  <done>
    Migration script exists, is executable, encodes the three groups explicitly, and uses the locked D-06 comment format.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run migration, verify lint passes, commit, then git-rm the script</name>
  <files>
    .planning/milestones/v1.2-phases/16-foundation/16-01-SUMMARY.md,
    .planning/milestones/v1.2-phases/16-foundation/16-02-SUMMARY.md,
    .planning/milestones/v1.2-phases/16-foundation/16-03-SUMMARY.md,
    .planning/milestones/v1.2-phases/16-foundation/16-04-SUMMARY.md,
    .planning/milestones/v1.2-phases/17-model-auto-update/17-01-SUMMARY.md,
    .planning/milestones/v1.2-phases/17-model-auto-update/17-02-SUMMARY.md,
    .planning/milestones/v1.2-phases/17-model-auto-update/17-03-SUMMARY.md,
    .planning/milestones/v1.2-phases/17-model-auto-update/17-05-SUMMARY.md,
    .planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-01-SUMMARY.md,
    .planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-02-SUMMARY.md,
    .planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-03-SUMMARY.md,
    .planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-04-SUMMARY.md,
    .planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-05-SUMMARY.md,
    .planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-07-SUMMARY.md,
    .planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-09-SUMMARY.md,
    .planning/milestones/v1.2-phases/18.1-shared-save-destinations-local-file/18.1-07-SUMMARY.md,
    .planning/milestones/v1.2-phases/18.1-shared-save-destinations-local-file/18.1-08-SUMMARY.md,
    .planning/milestones/v1.2-phases/18.1-shared-save-destinations-local-file/18.1-09-SUMMARY.md,
    scripts/migrate-summary-frontmatter.sh
  </files>
  <read_first>
    - scripts/migrate-summary-frontmatter.sh (just written in Task 1 — verify the script's group lists match the 18 files in this task's `<files>` section)
    - scripts/lint-summaries.sh (will be invoked post-migration to verify exit 0)
    - .planning/milestones/v1.3-phases/22-process-frontmatter-standard/22-CONTEXT.md D-13 (one-shot pattern: script created, executed, git-rm'd in same commit; commit message body should include per-file disposition table)
  </read_first>
  <behavior>
    - All 18 SUMMARY files modified to canonical form.
    - `bash scripts/lint-summaries.sh` exits 0 with `0 failures, 0 warnings` after migration.
    - The migration script itself is removed from the working tree before commit (`git rm scripts/migrate-summary-frontmatter.sh`).
    - A single commit captures the 18 SUMMARY edits + the script create+delete (in commit history, the script appears in the commit's diff as added-then-removed).
    - Commit subject: `chore(planning): migrate SUMMARY frontmatter to canonical requirements-completed`
    - Commit body includes a per-file disposition table (rename / sibling-strip / add-missing) per CONTEXT.md "Specific Ideas".
  </behavior>
  <action>
    Execute the migration end-to-end. Run these commands in order from the repo root.

    1. **Stage and run the script:**
       ```bash
       cd /Users/cary/Development/ai-development/ps-transcribe
       git add scripts/migrate-summary-frontmatter.sh
       bash scripts/migrate-summary-frontmatter.sh
       ```
       Expect output blocks `=== Group A ===`, `=== Group B ===`, `=== Group C ===` with per-file `[A]`, `[B]`, `[C]` lines.

    2. **Run lint to verify clean state:**
       ```bash
       bash scripts/lint-summaries.sh
       ```
       MUST exit 0. The final line MUST report `<N> SUMMARY files checked, 0 failures, 0 warnings` (where N is the total count of `.planning/milestones/**/*-SUMMARY.md` files, expected ≥ 33 — the v1.0/v1.1/v1.2/v1.3 archive plus the 5 SUMMARYs Phase 22 itself emits when each plan completes; if Phase 22's own SUMMARYs aren't yet written when this plan executes, only the archived files count).
       
       If lint exits non-zero: do NOT commit. Read the failure lines, identify which file the migration script handled wrong, fix the script (Task 1's edits, then re-run Task 2's step 1). Iterate until lint passes.

    3. **Spot-check three representative files manually:**
       ```bash
       grep -A1 '^requirements-completed:' /Users/cary/Development/ai-development/ps-transcribe/.planning/milestones/v1.2-phases/16-foundation/16-01-SUMMARY.md | head -3
       grep -B1 '^requirements-completed:' /Users/cary/Development/ai-development/ps-transcribe/.planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-01-SUMMARY.md | head -5
       grep '^requirements-completed:' /Users/cary/Development/ai-development/ps-transcribe/.planning/milestones/v1.2-phases/17-model-auto-update/17-01-SUMMARY.md
       ```
       - 16-01 should show `requirements-completed: [SC-1]` (rename happened, list preserved).
       - 18-01 should show `# scaffolded: [DICT-01, DICT-02, ...]  # historical sibling field, retired in Phase 22` directly above `requirements-completed: []  # Wave 0 ...` (the existing inline rationale comment on the canonical line is preserved; only the separate sibling-field line was stripped).
       - 17-01 should show `requirements-completed: [<IDs from 17-01-PLAN.md>]` (added from sibling PLAN).

    4. **Sanity-check no underscore tokens remain:**
       ```bash
       grep -rln '^requirements_completed:' /Users/cary/Development/ai-development/ps-transcribe/.planning/milestones/ || echo "OK: zero underscore matches"
       ```
       Output MUST be `OK: zero underscore matches`.

    5. **Sanity-check no sibling-field YAML keys remain:**
       ```bash
       grep -rE '^(requirements-scaffolded|requirements-supports|requirements-touched-but-not-completed):' /Users/cary/Development/ai-development/ps-transcribe/.planning/milestones/ || echo "OK: zero sibling-key matches"
       ```
       Output MUST be `OK: zero sibling-key matches`. (The sibling NAMES still appear inside the new comment lines like `# scaffolded: [...]`, but those start with `#`, not the un-commented key form, so this regex correctly distinguishes them.)

    6. **Stage the SUMMARY changes and remove the script:**
       ```bash
       git add /Users/cary/Development/ai-development/ps-transcribe/.planning/milestones/
       git rm scripts/migrate-summary-frontmatter.sh
       ```

    7. **Commit with the locked subject and a per-file disposition table in the body** (D-13 reproducibility-via-history). Use a heredoc to preserve formatting. Generate the disposition table dynamically by running `git diff --cached --name-only` and classifying each file against GROUP_A/B/C. A safe approach is to write the body to a temp file first, then use it as the commit message file. Example:

       ```bash
       cat > /tmp/22-06-commit.txt <<'EOF'
       chore(planning): migrate SUMMARY frontmatter to canonical requirements-completed

       Phase 22 (D-12, D-13, D-14): one-shot migration of all archived SUMMARY.md
       frontmatter to the canonical `requirements-completed` (hyphen) key.

       | File | Disposition |
       |------|-------------|
       | 16-foundation/16-01-SUMMARY.md | rename (underscore → hyphen) |
       | 16-foundation/16-02-SUMMARY.md | rename (underscore → hyphen) |
       | 16-foundation/16-03-SUMMARY.md | rename (underscore → hyphen) |
       | 16-foundation/16-04-SUMMARY.md | rename (underscore → hyphen) |
       | 17-model-auto-update/17-01-SUMMARY.md | add-missing (from sibling PLAN.requirements) |
       | 17-model-auto-update/17-02-SUMMARY.md | add-missing |
       | 17-model-auto-update/17-03-SUMMARY.md | add-missing |
       | 17-model-auto-update/17-05-SUMMARY.md | add-missing |
       | 18-hotkey.../18-01-SUMMARY.md | sibling-strip (scaffolded → comment) |
       | 18-hotkey.../18-02-SUMMARY.md | sibling-strip (scaffolded → comment) |
       | 18-hotkey.../18-03-SUMMARY.md | sibling-strip (supports → comment) |
       | 18-hotkey.../18-04-SUMMARY.md | sibling-strip (supports → comment) |
       | 18-hotkey.../18-05-SUMMARY.md | sibling-strip (supports → comment) |
       | 18-hotkey.../18-07-SUMMARY.md | sibling-strip (touched-but-not-completed → comment) |
       | 18-hotkey.../18-09-SUMMARY.md | rename (underscore → hyphen) |
       | 18.1-shared.../18.1-07-SUMMARY.md | rename (underscore → hyphen) |
       | 18.1-shared.../18.1-08-SUMMARY.md | rename (underscore → hyphen) |
       | 18.1-shared.../18.1-09-SUMMARY.md | add-missing (from sibling PLAN.requirements) |

       Verified post-migration: `bash scripts/lint-summaries.sh` exits 0 with
       0 failures, 0 warnings. The migration script `scripts/migrate-summary-frontmatter.sh`
       is git-rm'd in this same commit per D-13 (reproducibility lives in git history).
       EOF
       git commit -F /tmp/22-06-commit.txt
       rm /tmp/22-06-commit.txt
       ```

       Per the global commit-style rule (CLAUDE.md): NO `Co-Authored-By` trailer, NO `🤖 Generated with` footer, NO Claude attribution.

    8. **Final verification:**
       ```bash
       git show --stat HEAD | head -30
       bash scripts/lint-summaries.sh
       ```
       The `git show` output should list 18 SUMMARY files modified + `scripts/migrate-summary-frontmatter.sh` removed. The lint should exit 0.
  </action>
  <verify>
    <automated>cd /Users/cary/Development/ai-development/ps-transcribe && bash scripts/lint-summaries.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/lint-summaries.sh` exits 0 (status code 0).
    - The script's final stdout line matches the regex `^[0-9]+ SUMMARY files checked, 0 failures, 0 warnings$`.
    - `grep -rln '^requirements_completed:' .planning/milestones/` produces zero output (no underscore keys remain).
    - `grep -rE '^(requirements-scaffolded|requirements-supports|requirements-touched-but-not-completed):' .planning/milestones/` produces zero output (all sibling-field YAML keys stripped).
    - `grep -rln '^requirements-completed:' .planning/milestones/` produces at least 33 lines (every archived SUMMARY now has the canonical key — actual count depends on how many files exist plus any v1.3 SUMMARYs already written).
    - `find .planning/milestones -name '*-SUMMARY.md' | wc -l` returns the same number as `grep -rln '^requirements-completed:' .planning/milestones/ | wc -l` — every SUMMARY has exactly one canonical key.
    - `test ! -f scripts/migrate-summary-frontmatter.sh` succeeds — the migration script was removed from the working tree.
    - `git log -1 --pretty=format:'%s'` returns exactly `chore(planning): migrate SUMMARY frontmatter to canonical requirements-completed`.
    - `git log -1 --pretty=format:'%b' | grep -c 'add-missing'` returns at least `5` (the disposition table mentions add-missing 5 times — once per Group C file).
    - `git log -1 --pretty=format:'%b' | grep -c 'rename'` returns at least `7` (Group A, 7 files).
    - `git log -1 --pretty=format:'%b' | grep -c 'sibling-strip'` returns at least `6` (Group B, 6 files).
    - `git log -1 --pretty=format:'%b' | grep -E 'Co-Authored-By|Generated with|robot'` returns 0 matches (no Claude attribution per CLAUDE.md).
    - `git show --stat HEAD | grep -c 'SUMMARY.md'` returns at least `18` (one line per modified SUMMARY in the diff stat).
    - The 18-01-SUMMARY.md spot-check: `grep -c 'historical sibling field, retired in Phase 22' .planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-01-SUMMARY.md` returns at least `1` (D-06 comment landed).
    - The 18-01-SUMMARY.md preserved-content check: `grep -c 'Wave 0 ships test scaffolding' .planning/milestones/v1.2-phases/18-hotkey-dictation-plain-folder-output/18-01-SUMMARY.md` returns at least `1` (the original `requirements-completed: []` inline rationale comment was NOT clobbered by the strip operation).
    - The 16-01-SUMMARY.md spot-check: `grep -E '^requirements-completed: \[SC-1\]' .planning/milestones/v1.2-phases/16-foundation/16-01-SUMMARY.md` returns 1 line (rename preserved the existing list value `[SC-1]`).
    - The 17-01-SUMMARY.md spot-check: `grep -c '^requirements-completed:' .planning/milestones/v1.2-phases/17-model-auto-update/17-01-SUMMARY.md` returns `1` (Group C add-missing succeeded).
  </acceptance_criteria>
  <done>
    Single migration commit on disk; 18 SUMMARYs canonicalized; migration script git-rm'd; `bash scripts/lint-summaries.sh` exits 0 against the entire repo. ROADMAP success criterion #4 (Phase 22's own SUMMARY.md files include `requirements-completed`) holds for the migrated archive — and the lint gate now mechanically enforces criterion #4 for every Phase 23+ SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Local file edits inside `.planning/milestones/`. No code path, no I/O surface, no auth, no data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-06-01 | Information Disclosure | Migration script preservation | accept | Script is git-rm'd in the same commit per D-13. Reproducibility via `git log -p` on the commit retains the script body for audit; no information loss. |
| T-22-06-02 | Tampering | Wrong sibling-field value transcribed | mitigate | Acceptance criteria spot-check 18-01 SUMMARY for the locked D-06 comment format string AND for the preserved original inline rationale comment. Failure of either check blocks the commit. |
</threat_model>

<verification>
- `bash scripts/lint-summaries.sh` exits 0 against the migrated repo with `0 failures, 0 warnings`.
- All 18 target files modified; each spot-check passes (rename, sibling-strip, add-missing).
- Migration script git-rm'd in the same commit as the SUMMARY edits.
- Commit subject matches the locked form; body carries the disposition table.
- No Claude attribution in the commit message.
</verification>

<success_criteria>
ROADMAP success criterion #4 (v1.3 phases produce SUMMARY.md files that include `requirements-completed`) is enforced for the entire archive (full migration per D-12) AND mechanically guarded for every future SUMMARY by the lint workflow from Plan 22-05. Phase 22 ships green: lint passes, archive uniform, dogfood-from-day-one delivered.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.3-phases/22-process-frontmatter-standard/22-06-SUMMARY.md` using `~/.claude/get-shit-done/templates/summary.md`. The SUMMARY's `requirements-completed:` field MUST be `[PROCESS-01, PROCESS-02, PROCESS-03]` — this plan is the closing keystone that completes all three PROCESS-* requirements (PROCESS-01 by enforcing the canonical field on all archived SUMMARYs; PROCESS-02 by exercising the verbatim-copy contract end-to-end on Group C; PROCESS-03 by closing the loop on the lint gate's green state).
</output>
