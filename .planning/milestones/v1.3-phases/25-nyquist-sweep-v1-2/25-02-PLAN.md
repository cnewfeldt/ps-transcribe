---
phase: 25-nyquist-sweep-v1-2
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift
  - PSTranscribe/Tests/PSTranscribeTests/DesignTokensAdaptivePaletteTests.swift
  - .planning/milestones/v1.2-phases/20-dark-mode-parity/20-VALIDATION.md
autonomous: true
requirements: [NYQUIST-06]
tags: [nyquist, validation, dark-mode, design-tokens, phase-20, v1.2]

must_haves:
  truths:
    - "Honors D-01 (cite-only snapshot posture): WITHDRAWN rows for REQ-20.4b/20.5/20.6 cite 20-VERIFICATION.md (and 20-01-SUMMARY.md / 20-02-SUMMARY.md for REQ-20.5 byte-equality proof); no new snapshot tests added"
    - "Honors D-02 (PARTIAL a/b row split): REQ-20.4 split into 20.4a UNIT + 20.4b WITHDRAWN; both halves preserve SPEC traceability via adjacent rows"
    - "Honors D-04 (smallest-first plan ordering): Plan 25-02 lands second, extends shared PreferredColorSchemeGrepGateTests.swift created by Plan 25-01"
    - "Honors Phase 24 D-04 (flat behavior-named test files): new file at PSTranscribe/Tests/PSTranscribeTests/DesignTokensAdaptivePaletteTests.swift; PreferredColorSchemeGrepGateTests.swift extension stays in-file"
    - "PreferredColorSchemeGrepGateTests.swift extended with 4 new @Test methods (W-02 added the 4th): REQ-20.1 (zero `.preferredColorScheme(.light|.dark)` literal outside #Preview, directory-walk over Views/ per W-01), REQ-20.3 deletion half (extension Color block absent from TranscriptView.swift), REQ-20.3 promotion half (11 legacy tokens — bg0/bg1/bg2/fg1/fg2/fg3/accent1/accent2/recordRed/speakerTeal/speakerAmber — defined via Color(light:dark:) in DesignTokens.swift), REQ-20.4a (per-view hex-literal grep, directory-walk per W-01)"
    - "DesignTokensAdaptivePaletteTests.swift created with @Test method(s) covering REQ-20.2 (17 Chronicle tokens have distinct light/dark RGB triples via NSColor bridging through NSAppearance.performAsCurrentDrawingAppearance)"
    - "DesignTokensAdaptivePaletteTests suite annotated @Suite(.serialized) — NSAppearance.current is process-global, parallel tests would race"
    - "Test-strength trade-off (W-04 made explicit): allChronicleTokensHaveDistinctLightAndDarkVariants asserts only `light != dark` per token; byte-equality of light variants ('light variant equals current literal byte-for-byte' per Phase 20 Constraint) is independently certified by 20-01-SUMMARY.md / 20-02-SUMMARY.md hex transcripts which the REQ-20.5 WITHDRAWN row already cites — Phase 25 does not duplicate that proof"
    - "20-VALIDATION.md created from scratch at .planning/milestones/v1.2-phases/20-dark-mode-parity/20-VALIDATION.md with frontmatter status: approved, nyquist_compliant: true, wave_0_complete: true, last_audited: 2026-05-07"
    - "20-VALIDATION.md Per-Task Verification Map contains 8 rows: REQ-20.1 (UNIT), REQ-20.2 (UNIT), REQ-20.3 deletion half (UNIT), REQ-20.3 promotion half (UNIT — W-02 fix), REQ-20.4a (UNIT), REQ-20.4b (WITHDRAWN), REQ-20.5 (WITHDRAWN), REQ-20.6 (WITHDRAWN)"
    - "20-VALIDATION.md WITHDRAWN rows include literal `Source: 20-VERIFICATION.md` pointer (REQ-20.5 also cites 20-01-SUMMARY.md + 20-02-SUMMARY.md byte-equality proof)"
    - "swift test --filter PreferredColorSchemeGrepGateTests passes (exit 0) locally — Plan 25-01's 3 @Tests + Plan 25-02's 4 @Tests = 7 total"
    - "swift test --filter DesignTokensAdaptivePaletteTests passes (exit 0) locally"
    - "Full swift test suite passes (exit 0) locally with no regressions"
    - "build-check.yml CI green on the PR (swift test step exits 0 on macos-26)"
  artifacts:
    - path: "PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift"
      provides: "Extended source-grep guardrails — adds REQ-20.1/20.3 deletion+promotion/20.4a coverage to the file Plan 25-01 created (4 tests added by Plan 25-02; 7 total)"
      contains: "legacyPromotedTokensAreDefinedAsColorLightDarkInDesignTokens"
      min_lines: 180
    - path: "PSTranscribe/Tests/PSTranscribeTests/DesignTokensAdaptivePaletteTests.swift"
      provides: "Adaptive palette runtime resolution — 17 Chronicle tokens produce distinct RGB triples under .aqua vs .darkAqua via NSColor bridging"
      contains: "@Suite(\"DesignTokensAdaptivePaletteTests\", .serialized)"
      min_lines: 80
    - path: ".planning/milestones/v1.2-phases/20-dark-mode-parity/20-VALIDATION.md"
      provides: "Phase 20 Nyquist validation contract — full per-task verification map covering REQ-20.1..20.6 with PARTIAL a/b split"
      contains: "nyquist_compliant: true"
      min_lines: 100
  key_links:
    - from: "PSTranscribe/Tests/PSTranscribeTests/DesignTokensAdaptivePaletteTests.swift"
      to: "PSTranscribe/Sources/PSTranscribe/Design/DesignTokens.swift"
      via: "@testable import PSTranscribe — references Color.paper, Color.ink, ... 17 Chronicle tokens; NSColor(swiftUIColor:) bridges to runtime palette"
      pattern: "Color\\.(paper|paperWarm|paperSoft|rule|ruleStrong|ink|inkMuted|inkFaint|inkGhost|accentInk|accentSoft|accentTint|spk2Bg|spk2Fg|spk2Rail|recRed|liveGreen)"
    - from: "PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift"
      to: "PSTranscribe/Sources/PSTranscribe/Views/TranscriptView.swift"
      via: "URL(fileURLWithPath:) + readSource — substring-absence assertion on `extension Color`"
      pattern: "readSource\\(\"Views/TranscriptView.swift\"\\)"
    - from: ".planning/milestones/v1.2-phases/20-dark-mode-parity/20-VALIDATION.md"
      to: "PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift + DesignTokensAdaptivePaletteTests.swift"
      via: "Per-Task Verification Map Automated Command column citing swift test --filter <suite>/<method>"
      pattern: "swift test --filter (PreferredColorSchemeGrepGateTests|DesignTokensAdaptivePaletteTests)"
---

<objective>
Create `20-VALIDATION.md` from scratch and ship the Swift Testing coverage that backs every TESTABLE requirement row, closing NYQUIST-06.

Purpose: Phase 20 (Chronicle adaptive light/dark token palette) shipped 2026-05-01 without Nyquist validation. Phase 25 D-04 lands this plan SECOND (smallest-first ordering — 25-01 went first). It extends the shared `PreferredColorSchemeGrepGateTests.swift` (created by Plan 25-01) with three REQ-20 tests and creates a new `DesignTokensAdaptivePaletteTests.swift` for REQ-20.2 (17 Chronicle tokens light/dark resolution). Honors D-01 (cite-only — no new snapshot tests), D-02 (PARTIAL `a`/`b` split for REQ-20.4), and D-04 (one plan per VALIDATION.md, atomic close).

Output:
- 1 extended test file: `PreferredColorSchemeGrepGateTests.swift` with 3 new `@Test` methods (REQ-20.1, REQ-20.3, REQ-20.4a)
- 1 new test file: `DesignTokensAdaptivePaletteTests.swift` with `@Test` covering REQ-20.2 (17 Chronicle tokens have distinct light/dark RGB triples via NSColor bridging)
- 1 new VALIDATION.md: `20-VALIDATION.md` at status: approved with 7 verification map rows
- 1 SUMMARY.md (created at plan close, includes `requirements-completed: [NYQUIST-06]`)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-nyquist-sweep-v1-2/25-CONTEXT.md
@.planning/phases/25-nyquist-sweep-v1-2/25-PATTERNS.md
@.planning/phases/25-nyquist-sweep-v1-2/25-01-PLAN.md
@.planning/milestones/v1.0-phases/01-rebrand/01-VALIDATION.md
@.planning/milestones/v1.2-phases/20-dark-mode-parity/20-SPEC.md
@.planning/milestones/v1.2-phases/20-dark-mode-parity/20-VERIFICATION.md
@.planning/milestones/v1.2-phases/20-dark-mode-parity/20-VERIFIED.md
@.planning/milestones/v1.2-phases/20-dark-mode-parity/20-01-SUMMARY.md
@.planning/milestones/v1.2-phases/20-dark-mode-parity/20-02-SUMMARY.md
@PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift
@PSTranscribe/Tests/PSTranscribeTests/WorkflowSecretsTests.swift
@PSTranscribe/Sources/PSTranscribe/Design/DesignTokens.swift
@PSTranscribe/Sources/PSTranscribe/Views/TranscriptView.swift

<interfaces>
<!-- Source-of-truth values verified 2026-05-07 via direct grep against Sources/PSTranscribe -->

# DesignTokens.swift — 17 Chronicle tokens via Color(light:dark:) (Sources/PSTranscribe/Design/DesignTokens.swift:31-115)
Token names verified at audit: paper, paperWarm, paperSoft, rule, ruleStrong, ink, inkMuted, inkFaint, inkGhost, accentInk, accentSoft, accentTint, spk2Bg, spk2Fg, spk2Rail, recRed, liveGreen.

Note on `youBg` / `youFg` (lines 103-104): defined as `Color.ink` / `Color.paper` (token-of-token chain — auto-flips because the underlying tokens are adaptive). 17 tokens total per Phase 20 REQ-20.2 includes these.

# Color(light:dark:) bridge (Sources/PSTranscribe/Design/DesignTokens.swift:11-21) — production code under test
```swift
extension Color {
    init(light: Color, dark: Color) {
        let lightNS = NSColor(light)
        let darkNS = NSColor(dark)
        let dynamic = NSColor(name: nil) { appearance in
            let isDark = appearance.bestMatch(from: [.darkAqua, .vibrantDark]) != nil
            return isDark ? darkNS : lightNS
        }
        self = Color(nsColor: dynamic)
    }
}
```

# TranscriptView.swift — REQ-20.3 (line count 203; previously had `extension Color` block at 207-228 in the pre-Phase-20 build; Phase 20 deleted it)
Verified 2026-05-07: `wc -l TranscriptView.swift` returns 203 lines; `grep "extension Color" TranscriptView.swift` returns 0 matches.

# Test runtime: cwd at `swift test` time is `PSTranscribe/`. Source files at `Sources/PSTranscribe/...`.
# `readSource(_:)` and `stripPreviewBlocks(_:)` helpers ALREADY EXIST in PreferredColorSchemeGrepGateTests.swift (created by Plan 25-01) — Plan 25-02 extends the same suite, reusing both helpers.

# NSColor bridging pattern for runtime palette resolution (Phase-25-introduced; mirrors DesignTokens.swift:11-21 closure-based dynamic resolution):
```swift
private func rgb(of color: Color, under appearanceName: NSAppearance.Name) -> (r: CGFloat, g: CGFloat, b: CGFloat)? {
    guard let appearance = NSAppearance(named: appearanceName) else { return nil }
    var result: (CGFloat, CGFloat, CGFloat)?
    appearance.performAsCurrentDrawingAppearance {
        let nsColor = NSColor(color)
        guard let srgb = nsColor.usingColorSpace(.sRGB) else { return }
        result = (srgb.redComponent, srgb.greenComponent, srgb.blueComponent)
    }
    return result
}
```

# Hex-literal grep target views (REQ-20.4a) — non-DesignTokens.swift files MUST NOT contain `Color(red:` literals.
Grep target roster (per Phase 20 SPEC Boundaries — surfaces audited):
- Views/ContentView.swift, Views/TranscriptView.swift, Views/SettingsView.swift, Views/NotionTagSheet.swift, Views/OnboardingView.swift, Views/CaptureDock.swift, Views/ControlBar.swift, Views/DetailsPane.swift, Views/LibrarySidebar.swift
DesignTokens.swift is excluded from this grep — it's the SINGLE source of truth for hex literals (Phase 20 Constraint).

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend PreferredColorSchemeGrepGateTests.swift with REQ-20.1, REQ-20.3, REQ-20.4a source-grep tests</name>
  <files>PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift</files>
  <read_first>
    - PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift (created by Plan 25-01 — read the existing 3 @Test methods + readSource + stripPreviewBlocks helpers; new tests append to the existing struct, reusing both helpers)
    - PSTranscribe/Tests/PSTranscribeTests/WorkflowSecretsTests.swift (canonical source-grep precedent — reference for substring-absence assertion shape, lines 37-44 noTomeOrTokenOrSuppressionInBuildCheck)
    - PSTranscribe/Sources/PSTranscribe/Views/TranscriptView.swift (203 lines total — verify CURRENT state: NO `extension Color` block; line count + grep against this file are the assertions)
    - PSTranscribe/Sources/PSTranscribe/Design/DesignTokens.swift (lines 31-115 — confirm 17 Chronicle tokens use Color(light:dark:); this file is the EXCLUSION from the hex-literal grep)
    - .planning/phases/25-nyquist-sweep-v1-2/25-PATTERNS.md (lines 207-262 — provides exact assertion patterns for REQ-20.1 noPreferredColorSchemeOutsideAppRoot variant, REQ-20.3 extension-Color deletion, REQ-20.4a per-view hex-literal grep)
    - .planning/milestones/v1.2-phases/20-dark-mode-parity/20-SPEC.md (lines 17-30 — REQ-20.1 grep gate phrasing; REQ-20.3 extension Color deletion phrasing; REQ-20.4 hex-literal acceptance phrasing)
    - .planning/milestones/v1.2-phases/20-dark-mode-parity/20-VERIFIED.md (REQ-by-REQ evidence with grep transcripts — particularly REQ-20.1 zero-hits proof and REQ-20.4 per-view hex-literal proof; tests should match the same assertion shapes)
  </read_first>
  <behavior>
    Test 1 (REQ-20.1): `noForcedPreferredColorSchemeAnywhere()` — enumerate every `.swift` file under `Sources/PSTranscribe/Views/` at test time via `swiftFilesUnder("Views")` (W-01 fix — directory walk, no hardcoded roster), strip `#Preview` blocks, assert each stripped source contains zero `.preferredColorScheme(.light)` AND zero `.preferredColorScheme(.dark)` literal calls. NOTE: this is DISTINCT from Plan 25-01's `noPreferredColorSchemeOutsideAppRoot` (which forbids ANY `.preferredColorScheme(` outside the app root). Plan 25-02's REQ-20.1 test specifically asserts the FORCED `.light` / `.dark` literal forms are gone — Plan 25-01's test is the broader app-root-only invariant. Both assertions are valid; Plan 25-02 adds a tighter forced-literal check that backs Phase 20's specific deletion contract.
    Test 2 (REQ-20.3 deletion half): `transcriptViewDoesNotDefineExtensionColor()` — read `Views/TranscriptView.swift`, assert it does NOT contain the literal substring `extension Color`. Phase 20 deleted the legacy `extension Color { ... }` block at TranscriptView.swift:207-228; this test is the deletion tripwire.
    Test 3 (REQ-20.3 promotion half — W-02 fix): `legacyPromotedTokensAreDefinedAsColorLightDarkInDesignTokens()` — read `Design/DesignTokens.swift`, assert each of the 11 promoted legacy tokens (`bg0`, `bg1`, `bg2`, `fg1`, `fg2`, `fg3`, `accent1`, `accent2`, `recordRed`, `speakerTeal`, `speakerAmber`) appears with the `static let <name> = Color(\n        light:` definition pattern. STRUCTURAL contract only — does NOT assert light != dark RGB values. Phase 20 REQ-20.3 + acceptance criterion line 83 require legacy tokens be DEFINED via `Color(light:dark:)` syntax in DesignTokens.swift; their colors stay both-sides-equal in Wave 1 by design (DesignTokens.swift:175-177 explicit comment), so an adaptive RGB assertion would fail. This test closes the second half of REQ-20.3 (deletion + promotion) and explicitly covers the "11 legacy tokens" half of ROADMAP success criterion 1.
    Test 4 (REQ-20.4a): `viewSourcesContainNoHexColorLiterals()` — enumerate every `.swift` file under `Sources/PSTranscribe/Views/` at test time via `swiftFilesUnder("Views")` (W-01 fix — directory walk; supersedes original 9-file hardcoded roster; DesignTokens.swift is naturally excluded since it lives under `Design/`, not `Views/`), strip `#Preview` blocks, assert each does NOT contain `Color(red:`. Phase 20 Constraint: DesignTokens.swift is the single source of truth for hex literals.
    All four tests reuse the existing `readSource(_:)`, `stripPreviewBlocks(_:)`, and `swiftFilesUnder(_:)` helpers from Plan 25-01. No new helpers introduced.
  </behavior>
  <action>
    Use the Edit tool to extend `PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift`. Plan 25-01 created the file with 3 @Test methods (`preferredColorSchemeOnlyAtAppRootReadingFromSettings`, `noPreferredColorSchemeOutsideAppRoot`, `settingsViewContainsAppearancePicker`) and 3 helpers (`readSource`, `stripPreviewBlocks`, `swiftFilesUnder`). Plan 25-02 ADDS 4 more @Test methods inside the same `struct PreferredColorSchemeGrepGateTests` body.

    DELTA 1 — Update the header doc-comment to reflect Plan 25-02's addition. Find the existing line:
    ```swift
    // Phase 25 (NYQUIST-07) — Phase 21 REQ-21.2a / REQ-21.3a / REQ-21.4.
    // (Plan 25-02 extends this file with REQ-20.1 / REQ-20.3 / REQ-20.4a tests.)
    ```
    Replace with:
    ```swift
    // Phase 25 (NYQUIST-06 + NYQUIST-07) — Phase 20 REQ-20.1 / REQ-20.3 / REQ-20.4a +
    //                                       Phase 21 REQ-21.2a / REQ-21.3a / REQ-21.4.
    ```

    Also update the body of the header that lists invariants to include the four new ones. Find the block starting `//   - Phase 21 REQ-21.2a / REQ-21.4: every \`.preferredColorScheme(...)\` call in` and ending at the line before `// Working-directory assumption:`. Insert AFTER the REQ-21.3a invariant line (so the order is REQ-21 first, then REQ-20):
    ```swift
    //   - Phase 20 REQ-20.1: no Swift source under `Sources/PSTranscribe/Views/`
    //     contains `.preferredColorScheme(.light)` or `.preferredColorScheme(.dark)`
    //     OUTSIDE `#Preview` blocks (forced-override deletion contract).
    //   - Phase 20 REQ-20.3 (deletion half): `Views/TranscriptView.swift` does not
    //     contain an `extension Color` block (legacy palette moved out).
    //   - Phase 20 REQ-20.3 (promotion half): each of the 11 promoted legacy tokens
    //     is defined in `Design/DesignTokens.swift` via `Color(light:dark:)` syntax
    //     (W-02 fix — closes the second half of the deletion+promotion contract).
    //   - Phase 20 REQ-20.4a: per-view hex-literal grep — no view source under
    //     `Sources/PSTranscribe/Views/` contains `Color(red:` literals
    //     (DesignTokens.swift is the single source of truth for hex literals).
    ```

    DELTA 2 — Append 4 new @Test methods at the end of the struct. Insert AFTER the existing `settingsViewContainsAppearancePicker()` method body (which is currently the LAST method) and BEFORE the closing brace `}` of `struct PreferredColorSchemeGrepGateTests`:

    ```swift

        // MARK: - Phase 20 NYQUIST-06 (Plan 25-02)

        // REQ-20.1: no forced .preferredColorScheme(.light)/.dark literal calls in any
        // view source outside #Preview blocks. Phase 20 deleted ContentView.swift:251
        // (`.preferredColorScheme(.light)`) and NotionTagSheet.swift:130
        // (`.preferredColorScheme(.dark)`); this test is the deletion tripwire and is
        // tighter than `noPreferredColorSchemeOutsideAppRoot` — that test forbids ANY
        // `.preferredColorScheme(` call outside the app root; this test additionally
        // asserts the specific forced-literal forms are gone.
        // W-01 fix: directory walk via `swiftFilesUnder("Views")` so future-added
        // view files are automatically in scope.
        @Test func noForcedPreferredColorSchemeAnywhere() throws {
            let surfaces = try swiftFilesUnder("Views")
            for path in surfaces {
                let stripped = stripPreviewBlocks(try readSource(path))
                #expect(!stripped.contains(".preferredColorScheme(.light)"),
                        "\(path) must not call .preferredColorScheme(.light) outside #Preview (Phase 20 REQ-20.1)")
                #expect(!stripped.contains(".preferredColorScheme(.dark)"),
                        "\(path) must not call .preferredColorScheme(.dark) outside #Preview (Phase 20 REQ-20.1)")
            }
        }

        // REQ-20.3 (deletion half): Phase 20 deleted the legacy `extension Color`
        // block at TranscriptView.swift:207-228 (legacy bg0/bg1/fg1/etc. palette moved
        // into DesignTokens.swift as Color(light:dark:) definitions). This test is the
        // deletion tripwire — re-introduction of any `extension Color` block in
        // TranscriptView.swift fails this test.
        @Test func transcriptViewDoesNotDefineExtensionColor() throws {
            let src = try readSource("Views/TranscriptView.swift")
            #expect(!src.contains("extension Color"),
                    "Views/TranscriptView.swift must not define `extension Color` (Phase 20 REQ-20.3 deletion half — legacy palette moved to DesignTokens.swift)")
        }

        // REQ-20.3 (promotion half — W-02 fix): each of the 11 promoted legacy tokens
        // (bg0/bg1/bg2/fg1/fg2/fg3/accent1/accent2/recordRed/speakerTeal/speakerAmber)
        // is defined in Design/DesignTokens.swift using the `Color(light:dark:)` form.
        // STRUCTURAL contract only — does NOT assert light != dark RGB values.
        // The legacy tokens intentionally have light == dark in Wave 1 (DesignTokens.swift:175-177
        // explicit comment "the legacy tokens themselves stay both-sides-equal in
        // Wave 1 to keep the gate trivially passable"). Phase 20 SPEC line 29 + line 83
        // require legacy tokens be DEFINED via `Color(light:dark:)` syntax; this test
        // closes the second half of the deletion+promotion contract and explicitly
        // covers the "11 legacy tokens" half of ROADMAP success criterion 1.
        @Test func legacyPromotedTokensAreDefinedAsColorLightDarkInDesignTokens() throws {
            let src = try readSource("Design/DesignTokens.swift")
            let legacyTokens = [
                "bg0", "bg1", "bg2",
                "fg1", "fg2", "fg3",
                "accent1", "accent2",
                "recordRed", "speakerTeal", "speakerAmber",
            ]
            #expect(legacyTokens.count == 11,
                    "Phase 20 REQ-20.3 specifies 11 promoted legacy tokens; roster has \(legacyTokens.count)")
            for name in legacyTokens {
                let prefix = "static let \(name) = Color("
                guard let range = src.range(of: prefix) else {
                    #expect(Bool(false),
                            "DesignTokens.swift must define `static let \(name) = Color(...)` (Phase 20 REQ-20.3 promotion contract)")
                    continue
                }
                // Look in the next ~80 characters for the `light:` initializer label.
                // Color(light:dark:) is a multi-line initializer — `light:` appears
                // inside the parens, typically on the next line indented by 8 spaces.
                let scanEnd = src.index(range.upperBound, offsetBy: 80, limitedBy: src.endIndex) ?? src.endIndex
                let lookahead = src[range.upperBound..<scanEnd]
                #expect(lookahead.contains("light:"),
                        "Token `\(name)` must be defined via Color(light:dark:) initializer in DesignTokens.swift (Phase 20 REQ-20.3 promotion contract — found definition but no `light:` label within next 80 chars)")
            }
        }

        // REQ-20.4a (structural half of D-02 PARTIAL split): no view source outside
        // DesignTokens.swift contains `Color(red:` hex literals. DesignTokens.swift is
        // the SINGLE source of truth for hex literals per Phase 20 Constraint
        // ("After Phase 20, DesignTokens.swift is the only file containing
        // Color(red:green:blue:) literals or Color(light:dark:) definitions").
        // W-01 fix: directory walk via `swiftFilesUnder("Views")` so future-added
        // view files are automatically in scope. DesignTokens.swift lives under
        // `Design/` so it is naturally not enumerated.
        @Test func viewSourcesContainNoHexColorLiterals() throws {
            let surfaces = try swiftFilesUnder("Views")
            for path in surfaces {
                let stripped = stripPreviewBlocks(try readSource(path))
                #expect(!stripped.contains("Color(red:"),
                        "\(path) must not contain `Color(red:` hex literal (Phase 20 REQ-20.4a — DesignTokens.swift is the single source of truth)")
            }
        }
    ```

    No `.serialized` change (still pure file reads). No `@MainActor` change. No new imports.

    After applying the deltas, verify by running:
    ```
    cd PSTranscribe && swift test --filter PreferredColorSchemeGrepGateTests
    ```
    Expected: 7 tests pass (Plan 25-01's 3 + Plan 25-02's 4). If `viewSourcesContainNoHexColorLiterals` finds an unexpected hit, that's a real Phase 20 regression — STOP and escalate, do not loosen the assertion. If `legacyPromotedTokensAreDefinedAsColorLightDarkInDesignTokens` fails for a token name, the legacy-token roster has drifted (DesignTokens.swift extension at lines 178-232) — re-read DesignTokens.swift and reconcile the roster, do NOT remove tokens from the assertion list.
  </action>
  <verify>
    <automated>cd PSTranscribe && swift test --filter PreferredColorSchemeGrepGateTests 2>&1 | tee /tmp/25-02-task1.log; tail -25 /tmp/25-02-task1.log</automated>
  </verify>
  <acceptance_criteria>
    - File still exists: `test -f PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift`
    - Contains all 7 `@Test func` declarations: `grep -cE '^\s+@Test func (preferredColorSchemeOnlyAtAppRootReadingFromSettings|noPreferredColorSchemeOutsideAppRoot|settingsViewContainsAppearancePicker|noForcedPreferredColorSchemeAnywhere|transcriptViewDoesNotDefineExtensionColor|legacyPromotedTokensAreDefinedAsColorLightDarkInDesignTokens|viewSourcesContainNoHexColorLiterals)\(' PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift` returns 7
    - Legacy-token method exists with all 11 names in the roster: `grep -c '"\(bg0|bg1|bg2|fg1|fg2|fg3|accent1|accent2|recordRed|speakerTeal|speakerAmber\)"' PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift` returns 11
    - Directory-walk usage (W-01 fix): `grep -c 'try swiftFilesUnder("Views")' PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift` returns at least 3 (used by `noPreferredColorSchemeOutsideAppRoot` from Plan 25-01 + `noForcedPreferredColorSchemeAnywhere` + `viewSourcesContainNoHexColorLiterals` from Plan 25-02)
    - Header updated for both NYQUIST IDs: `grep -c "Phase 25 (NYQUIST-06 + NYQUIST-07)" PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift` returns 1
    - Header lists Phase 20 invariants: `grep -cE 'Phase 20 REQ-20\.(1|3|4a)' PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift` returns at least 4 (REQ-20.1, REQ-20.3 deletion, REQ-20.3 promotion, REQ-20.4a — one per invariant in header doc-comment)
    - MARK comment for the new section: `grep -c "MARK: - Phase 20 NYQUIST-06" PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift` returns 1
    - `cd PSTranscribe && swift test --filter PreferredColorSchemeGrepGateTests` exits 0 with 7 passing tests
    - No `.serialized` annotation introduced: `grep -c '\.serialized' PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift` returns 0
  </acceptance_criteria>
  <done>4 new @Test methods appended to PreferredColorSchemeGrepGateTests.swift (W-02 added 4th — legacyPromotedTokensAreDefinedAsColorLightDarkInDesignTokens); suite passes with 7 tests total.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create DesignTokensAdaptivePaletteTests.swift covering REQ-20.2 (17 Chronicle tokens distinct light/dark)</name>
  <files>PSTranscribe/Tests/PSTranscribeTests/DesignTokensAdaptivePaletteTests.swift</files>
  <read_first>
    - PSTranscribe/Sources/PSTranscribe/Design/DesignTokens.swift (lines 11-21 — Color(light:dark:) extension; lines 31-115 — 17 Chronicle tokens; lines 103-104 — youBg/youFg token-of-token chain; verify CURRENT token names + count before writing assertions)
    - PSTranscribe/Tests/PSTranscribeTests/AppSettingsTests.swift (canonical `.serialized` precedent — line 5 `@Suite("AppSettings v1.2 keys", .serialized)`; pattern reused here for NSAppearance.current process-global mutation)
    - PSTranscribe/Tests/PSTranscribeTests/WorkflowSecretsTests.swift (alternative analog — pure-function suite shape; this file does not need .serialized; contrast with AppSettingsTests for the .serialized rationale)
    - .planning/phases/25-nyquist-sweep-v1-2/25-PATTERNS.md (lines 264-348 — exact suite shape, NSColor bridging helper rationale, .serialized requirement explanation, tokenized-iteration assertion pattern)
    - .planning/phases/25-nyquist-sweep-v1-2/25-CONTEXT.md (D-04 Claude's Discretion — bias toward NSColor bridging over SwiftUI environment-trait rendering; .serialized mandatory for NSAppearance.current mutation)
    - .planning/milestones/v1.2-phases/20-dark-mode-parity/20-VERIFIED.md (REQ-20.2 token count proof — verify the token roster matches what this test asserts on)
  </read_first>
  <behavior>
    Test 1 (REQ-20.2): `allChronicleTokensHaveDistinctLightAndDarkVariants()` — single iteration test over 17 Chronicle tokens. For each token: resolve under `.aqua` and `.darkAqua` via `NSAppearance(named:)?.performAsCurrentDrawingAppearance { ... }`, bridge SwiftUI Color to NSColor via `NSColor(color)`, convert to sRGB color space, extract `(redComponent, greenComponent, blueComponent)`. Assert `light != dark` for each token (the structural contract: each is defined as `Color(light:dark:)`, not a single hex literal).
    Token roster (17 tokens; verified at audit 2026-05-07 against DesignTokens.swift lines 31-115):
      paper, paperWarm, paperSoft, rule, ruleStrong, ink, inkMuted, inkFaint, inkGhost, accentInk, accentSoft, accentTint, spk2Bg, spk2Fg, spk2Rail, recRed, liveGreen
    NOTE on `youBg` / `youFg`: defined as `Color.ink` / `Color.paper` (token-of-token chain — auto-flips because the underlying tokens are adaptive). Asserting on these is redundant with `ink` / `paper` — including them per Phase 20 SPEC's "17 Chronicle tokens" contract risks the test failing if SwiftUI's resolution ordering of nested `Color` references differs from the underlying token. PLANNER PICK per CONTEXT.md: include the 17-named-tokens roster above (paper..liveGreen, NOT youBg/youFg) since youBg/youFg are derived from paper/ink. The "17 Chronicle tokens" SPEC contract is satisfied by 17 directly-defined `Color(light:dark:)` tokens; youBg/youFg are accessors over those tokens, not separate definitions.
    Pre-condition for the test: `NSAppearance(named: .aqua)` and `NSAppearance(named: .darkAqua)` MUST resolve to non-nil on macos-26 (stable AppKit API). The `try #require(...)` on the `rgb(of:under:)` result fails fast on a future macOS that drops these names.
  </behavior>
  <action>
    Use the Write tool to create `PSTranscribe/Tests/PSTranscribeTests/DesignTokensAdaptivePaletteTests.swift` (file does not exist yet).

    Use this EXACT content:

    ```swift
    // DesignTokensAdaptivePaletteTests
    // Phase 25 (NYQUIST-06) — Phase 20 REQ-20.2.
    //
    // Asserts that the 17 Chronicle tokens in DesignTokens.swift resolve to
    // DIFFERENT RGB triples under .aqua vs .darkAqua appearance, proving each
    // token is defined as Color(light:dark:) (not a single-hex literal).
    //
    // Technique: bridge SwiftUI.Color -> NSColor via `NSColor(_ color: Color)`,
    // resolve under explicit appearances via
    // `NSAppearance(named:)?.performAsCurrentDrawingAppearance`, convert to sRGB
    // and compare redComponent/greenComponent/blueComponent.
    //
    // This exercises the same dynamic-NSColor mechanism DesignTokens.swift uses
    // internally (see DesignTokens.swift:11-21) — the test resolves the same
    // closure-based resolution path through `performAsCurrentDrawingAppearance`.
    //
    // `.serialized` is MANDATORY: `performAsCurrentDrawingAppearance` mutates
    // `NSAppearance.current`, which is process-global. Two parallel tests setting
    // different appearances would race. (Phase 25 D-04 Claude's Discretion lock.)
    //
    // Token roster (17 tokens, from DesignTokens.swift lines 31-115 directly-defined
    // Color(light:dark:) entries — youBg/youFg are token-of-token aliases over
    // ink/paper and are not separately tested; their adaptiveness derives from
    // paper/ink, which ARE tested).

    import Testing
    import Foundation
    import AppKit
    import SwiftUI
    @testable import PSTranscribe

    @Suite("DesignTokensAdaptivePaletteTests", .serialized)
    struct DesignTokensAdaptivePaletteTests {

        /// Resolves a SwiftUI Color under an explicit NSAppearance.Name and returns
        /// the sRGB RGB triple. Returns nil if the appearance can't be created
        /// (caller should `try #require` the result to fail fast on unsupported
        /// macOS appearance names).
        private func rgb(of color: Color, under appearanceName: NSAppearance.Name) -> (r: CGFloat, g: CGFloat, b: CGFloat)? {
            guard let appearance = NSAppearance(named: appearanceName) else { return nil }
            var result: (CGFloat, CGFloat, CGFloat)?
            appearance.performAsCurrentDrawingAppearance {
                let nsColor = NSColor(color)
                guard let srgb = nsColor.usingColorSpace(.sRGB) else { return }
                result = (srgb.redComponent, srgb.greenComponent, srgb.blueComponent)
            }
            return result
        }

        @Test func allChronicleTokensHaveDistinctLightAndDarkVariants() throws {
            // 17 directly-defined Chronicle tokens (Phase 20 REQ-20.2)
            let tokens: [(name: String, color: Color)] = [
                ("paper",      .paper),
                ("paperWarm",  .paperWarm),
                ("paperSoft",  .paperSoft),
                ("rule",       .rule),
                ("ruleStrong", .ruleStrong),
                ("ink",        .ink),
                ("inkMuted",   .inkMuted),
                ("inkFaint",   .inkFaint),
                ("inkGhost",   .inkGhost),
                ("accentInk",  .accentInk),
                ("accentSoft", .accentSoft),
                ("accentTint", .accentTint),
                ("spk2Bg",     .spk2Bg),
                ("spk2Fg",     .spk2Fg),
                ("spk2Rail",   .spk2Rail),
                ("recRed",     .recRed),
                ("liveGreen",  .liveGreen),
            ]
            #expect(tokens.count == 17,
                    "Phase 20 REQ-20.2 specifies 17 Chronicle tokens; roster has \(tokens.count)")

            for (name, color) in tokens {
                let light = try #require(rgb(of: color, under: .aqua),
                                         "\(name): light variant unresolvable under .aqua")
                let dark = try #require(rgb(of: color, under: .darkAqua),
                                        "\(name): dark variant unresolvable under .darkAqua")
                // Compound inequality: at least one component must differ. Single-hex
                // tokens would resolve identically under both appearances.
                let identical = (light.r == dark.r) && (light.g == dark.g) && (light.b == dark.b)
                #expect(!identical,
                        "\(name): light \(light) and dark \(dark) variants are identical — token must be defined as Color(light:dark:) (Phase 20 REQ-20.2)")
            }
        }
    }
    ```

    No additional `@Test` methods. No XCTest. No snapshot assertions.

    After writing the file, verify by running:
    ```
    cd PSTranscribe && swift test --filter DesignTokensAdaptivePaletteTests
    ```
    Expected: 1 test passes. If the test FAILS for a specific token (e.g., `liveGreen: light and dark variants are identical`), that's a real Phase 20 regression — STOP and escalate, do not modify the test to accept it. The DesignTokens.swift definition is the source of truth.

    If the test fails because `NSAppearance(named: .aqua)` returns nil on the runner, that's a macos-26 / Xcode regression — escalate.
  </action>
  <verify>
    <automated>cd PSTranscribe && swift test --filter DesignTokensAdaptivePaletteTests 2>&1 | tee /tmp/25-02-task2.log; tail -25 /tmp/25-02-task2.log</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f PSTranscribe/Tests/PSTranscribeTests/DesignTokensAdaptivePaletteTests.swift`
    - Contains exactly 1 `@Test func` declaration: `grep -cE '^\s+@Test func allChronicleTokensHaveDistinctLightAndDarkVariants\(' PSTranscribe/Tests/PSTranscribeTests/DesignTokensAdaptivePaletteTests.swift` returns 1
    - `.serialized` annotation present (mandatory for NSAppearance.current mutation): `grep -c '@Suite("DesignTokensAdaptivePaletteTests", .serialized)' PSTranscribe/Tests/PSTranscribeTests/DesignTokensAdaptivePaletteTests.swift` returns 1
    - `@testable import PSTranscribe` present (needed for Color extension static members): `grep -c '@testable import PSTranscribe' PSTranscribe/Tests/PSTranscribeTests/DesignTokensAdaptivePaletteTests.swift` returns 1
    - AppKit + SwiftUI imports present: `grep -cE 'import (AppKit|SwiftUI)' PSTranscribe/Tests/PSTranscribeTests/DesignTokensAdaptivePaletteTests.swift` returns 2
    - Header marker present: `grep -c "Phase 25 (NYQUIST-06)" PSTranscribe/Tests/PSTranscribeTests/DesignTokensAdaptivePaletteTests.swift` returns 1
    - Token roster has all 17 names: `grep -cE '\("(paper|paperWarm|paperSoft|rule|ruleStrong|ink|inkMuted|inkFaint|inkGhost|accentInk|accentSoft|accentTint|spk2Bg|spk2Fg|spk2Rail|recRed|liveGreen)",' PSTranscribe/Tests/PSTranscribeTests/DesignTokensAdaptivePaletteTests.swift` returns 17
    - performAsCurrentDrawingAppearance API used: `grep -c 'performAsCurrentDrawingAppearance' PSTranscribe/Tests/PSTranscribeTests/DesignTokensAdaptivePaletteTests.swift` returns 1
    - `cd PSTranscribe && swift test --filter DesignTokensAdaptivePaletteTests` exits 0 with 1 passing test
  </acceptance_criteria>
  <done>DesignTokensAdaptivePaletteTests.swift written; @Suite passes with 17/17 tokens resolving to distinct light/dark RGB triples.</done>
</task>

<task type="auto">
  <name>Task 3: Create 20-VALIDATION.md from scratch with 7 verification map rows + run full test suite</name>
  <files>.planning/milestones/v1.2-phases/20-dark-mode-parity/20-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v1.0-phases/01-rebrand/01-VALIDATION.md (canonical post-Phase-24 shape — frontmatter, Test Infrastructure section, Sampling Rate, Per-Task Verification Map column shape, Validation Sign-Off, Validation Audit block)
    - .planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md (sibling created by Plan 25-01 — mirror its shape exactly for sweep consistency, with Phase 20 reqs in place of Phase 21 reqs and NO out-of-scope cross-ref section since Phase 20 has no post-fix scope extension)
    - .planning/milestones/v1.2-phases/20-dark-mode-parity/20-SPEC.md (REQ-20.1..20.6 source — verify req text matches what 20-VALIDATION.md cites)
    - .planning/milestones/v1.2-phases/20-dark-mode-parity/20-VERIFICATION.md (final-gate sign-off — used as `Source:` for WITHDRAWN rows)
    - .planning/milestones/v1.2-phases/20-dark-mode-parity/20-VERIFIED.md (REQ-by-REQ evidence; useful precedent for the assertion specifics)
    - .planning/milestones/v1.2-phases/20-dark-mode-parity/20-01-SUMMARY.md (light-side hex byte-equality proof — used as additional Source: for REQ-20.5 WITHDRAWN row)
    - .planning/milestones/v1.2-phases/20-dark-mode-parity/20-02-SUMMARY.md (companion to 20-01 — used as additional Source: for REQ-20.5)
    - .planning/phases/25-nyquist-sweep-v1-2/25-CONTEXT.md (D-01 cite-only; D-02 PARTIAL a/b split for REQ-20.4; D-04 smallest-first; Claude's Discretion on REQ-20.5 WITHDRAWN reason wording referencing both byte-equality proof AND user attestation)
    - .planning/phases/25-nyquist-sweep-v1-2/25-PATTERNS.md (lines 352-430 — exact 7-row Per-Task Map template + Validation Audit block template)
  </read_first>
  <action>
    Use the Write tool to create `.planning/milestones/v1.2-phases/20-dark-mode-parity/20-VALIDATION.md` (file does not exist).

    Use this EXACT content (mirrors `21-VALIDATION.md` shape with Phase-20-specific reqs and no Phase-26-style out-of-scope section since Phase 20 has no post-SPEC scope extension):

    ```markdown
    ---
    phase: 20
    slug: dark-mode-parity
    status: approved
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-05-07
    last_audited: 2026-05-07
    ---

    # Phase 20 -- Validation Strategy

    > Per-phase validation contract for feedback sampling during execution.

    Backfilled by Phase 25 (NYQUIST-06) on 2026-05-07. Phase 20 shipped 2026-05-01 without Nyquist coverage; this validation contract certifies the structural half of every REQ-20.x with Swift Testing assertions and cites `20-VERIFICATION.md` (and `20-01-SUMMARY.md` / `20-02-SUMMARY.md` byte-equality transcripts for REQ-20.5) for the visual halves per Phase 25 D-01 (cite-only snapshot posture) and D-02 (PARTIAL `a`/`b` split for compound contracts).

    ---

    ## Test Infrastructure

    | Property | Value |
    |----------|-------|
    | **Framework** | Swift Testing (`import Testing`) — `PSTranscribeTests` target in `PSTranscribe/Package.swift` |
    | **Config file** | `PSTranscribe/Package.swift` — `.testTarget(name: "PSTranscribeTests", ...)` |
    | **Quick run command** | `cd PSTranscribe && swift test --filter <suite>` |
    | **Full suite command** | `cd PSTranscribe && swift test` |
    | **Estimated runtime** | ~15-90 seconds (full suite, macos-26) |

    ---

    ## Sampling Rate

    - **After every task commit:** Run focused `swift test --filter <suite>`
    - **After every plan wave:** Run full `swift test` locally
    - **Before merge:** `build-check.yml` CI green on PR (macos-26, Xcode 26)
    - **Max feedback latency:** 90 seconds

    ---

    ## Per-Task Verification Map

    Compound REQs are split into TESTABLE structural halves (`Xa`) and WITHDRAWN visual halves (`Xb`) per Phase 25 D-02. Visual halves cite `20-VERIFICATION.md` per Phase 25 D-01 cite-only posture (Phase 23's snapshot infrastructure is not extended here — see Audit Notes).

    | Task ID | Plan | Wave | Requirement | Test Type | Automated Command | Status | Notes |
    |---------|------|------|-------------|-----------|-------------------|--------|-------|
    | 25-02-01 | 25-02 | 2 | REQ-20.1 | unit | `cd PSTranscribe && swift test --filter PreferredColorSchemeGrepGateTests/noForcedPreferredColorSchemeAnywhere` | green | Forced-override deletion contract: zero `.preferredColorScheme(.light)` / `.preferredColorScheme(.dark)` literal calls in any view source outside `#Preview`. Companion `noPreferredColorSchemeOutsideAppRoot` (Plan 25-01) covers the broader app-root-only invariant |
    | 25-02-02 | 25-02 | 2 | REQ-20.2 | unit | `cd PSTranscribe && swift test --filter DesignTokensAdaptivePaletteTests/allChronicleTokensHaveDistinctLightAndDarkVariants` | green | All 17 Chronicle tokens resolve to distinct RGB triples under `.aqua` vs `.darkAqua` (NSColor bridging via `performAsCurrentDrawingAppearance`); proves each is defined as `Color(light:dark:)`, not a single-hex literal |
    | 25-02-03a | 25-02 | 2 | REQ-20.3 (deletion) | unit | `cd PSTranscribe && swift test --filter PreferredColorSchemeGrepGateTests/transcriptViewDoesNotDefineExtensionColor` | green | Legacy `extension Color` block (TranscriptView.swift:207-228 in pre-Phase-20 build) deleted; tripwire on re-introduction |
    | 25-02-03b | 25-02 | 2 | REQ-20.3 (promotion) | unit | `cd PSTranscribe && swift test --filter PreferredColorSchemeGrepGateTests/legacyPromotedTokensAreDefinedAsColorLightDarkInDesignTokens` | green | W-02 fix: 11 legacy tokens (bg0/bg1/bg2/fg1/fg2/fg3/accent1/accent2/recordRed/speakerTeal/speakerAmber) defined via `Color(light:dark:)` syntax in DesignTokens.swift (structural — light==dark by Wave 1 design per DesignTokens.swift:175-177; closes the "11 legacy tokens" half of ROADMAP success criterion 1) |
    | 25-02-04 | 25-02 | 2 | REQ-20.4a | unit | `cd PSTranscribe && swift test --filter PreferredColorSchemeGrepGateTests/viewSourcesContainNoHexColorLiterals` | green | Structural half (D-02 split): per-view hex-literal grep — no `Color(red:` literals in any view file outside DesignTokens.swift (single-source-of-truth Constraint) |
    | 25-02-05 | 25-02 | 2 | REQ-20.4b | WITHDRAWN | n/a -- WITHDRAWN | withdrawn | Visual half (D-02 split): runtime appearance-toggle re-renders all 10 surfaces without app restart. Manual UAT covers; no CI-runnable harness for transition-state re-renders (Phase 25 D-01 cite-only). Source: 20-VERIFICATION.md Row groups A-F. |
    | 25-02-06 | 25-02 | 2 | REQ-20.5 | WITHDRAWN | n/a -- WITHDRAWN | withdrawn | Light-mode pixel stability gate. Source-level byte-equality already proven via Plan 20-01 / 20-02 SUMMARY hex transcripts (each Color(light:dark:) light hex equals the pre-Phase-20 literal byte-for-byte); visual attestation in 20-VERIFICATION.md Row group D. No CI-runnable visual diff in scope (Phase 25 D-01). Source: 20-01-SUMMARY.md + 20-02-SUMMARY.md + 20-VERIFICATION.md. |
    | 25-02-07 | 25-02 | 2 | REQ-20.6 | WITHDRAWN | n/a -- WITHDRAWN | withdrawn | DictationHUD vibrancy adapts via NSPanel `.hudWindow` material; system-controlled, not a SwiftUI invariant. Visual attestation in 20-VERIFICATION.md Row group F. Source: 20-VERIFICATION.md. |

    *Status: pending / green / red / flaky / withdrawn*

    *withdrawn: requirement is satisfied at the product level (per `20-VERIFICATION.md`) but the specific contract is not automatable within Phase 25's cite-only scope (D-01). Source pointer in Notes column points at where the human-attested evidence lives.*

    ---

    ## Wave 0 Requirements

    - Existing Swift Testing test target (`PSTranscribe/Tests/PSTranscribeTests/`) already meets infra requirements — no new dependencies, no `Package.swift` edits.
    - `build-check.yml` CI runs `swift test` on macos-26 (inherited from Phase 23 D-06).

    **Wave 0 complete:** No tooling installation needed; sampling rate met via existing Swift Testing target + CI.

    ---

    ## Validation Sign-Off

    - [x] All tasks have automated verify or are explicitly WITHDRAWN per Phase 25 D-01/D-02
    - [x] Sampling continuity: no 3 consecutive tasks without automated verify (7 rows; 4 UNIT + 3 WITHDRAWN; pattern: U-U-U-U-W-W-W)
    - [x] Wave 0 covers all MISSING references (no missing references — existing test target suffices)
    - [x] No watch-mode flags
    - [x] Feedback latency < 90s
    - [x] `nyquist_compliant: true` set in frontmatter

    **Approval:** approved 2026-05-07 (Phase 25 NYQUIST-06 — Swift Testing-backed; new VALIDATION.md, no prior audit)

    ---

    ## Validation Audit 2026-05-07 (Phase 25 -- NYQUIST-06)

    | Metric | Count |
    |--------|-------|
    | Gaps found | 0 (no MISSING test coverage; Phase 20 shipped with manual UAT only — Phase 25 backfills automated coverage for the structural half of every REQ) |
    | Resolved | 4 (REQ-20.1 forced-literal grep gate; REQ-20.2 17-token light/dark variance via NSColor bridging; REQ-20.3 extension-Color deletion grep; REQ-20.4a per-view hex-literal grep) |
    | Withdrawn | 3 (REQ-20.4b visual half + REQ-20.5 light-mode pixel stability + REQ-20.6 DictationHUD vibrancy per D-01 cite-only + D-02 PARTIAL split) |
    | Escalated | 0 |
    | Document updates | New 20-VALIDATION.md created at `status: approved` / `nyquist_compliant: true`; created 2026-05-07. |

    ### Audit Method

    - Extended `PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift` (file created in Plan 25-01) with 4 new `@Test` methods: `noForcedPreferredColorSchemeAnywhere`, `transcriptViewDoesNotDefineExtensionColor`, `legacyPromotedTokensAreDefinedAsColorLightDarkInDesignTokens` (W-02 fix), `viewSourcesContainNoHexColorLiterals`
    - Created `PSTranscribe/Tests/PSTranscribeTests/DesignTokensAdaptivePaletteTests.swift`:
        - 1 `@Test` method: `allChronicleTokensHaveDistinctLightAndDarkVariants` (iterates over 17 Chronicle tokens)
        - Pattern: NSColor bridging via `NSAppearance(named:)?.performAsCurrentDrawingAppearance { NSColor(swiftUIColor) ... .usingColorSpace(.sRGB) }` — Phase-25-introduced technique (no in-house analog); rationale documented in file header
        - `.serialized` annotation MANDATORY: `NSAppearance.current` is process-global; parallel tests would race
    - Ran `cd PSTranscribe && swift test --filter PreferredColorSchemeGrepGateTests` — exits 0 (6 tests: Plan 25-01's 3 + Plan 25-02's 3)
    - Ran `cd PSTranscribe && swift test --filter DesignTokensAdaptivePaletteTests` — exits 0 (1 test, 17 tokens iterated)
    - Ran full `cd PSTranscribe && swift test` — exits 0 (no regressions)

    ### Notes

    - **D-01 cite-only posture:** Phase 23's 15 snapshot baselines (`PSTranscribe/Tests/PSTranscribeTests/__Snapshots__/`) prove static palette correctness across light/dark/system but are NOT extended in Phase 25. WITHDRAWN rows for REQ-20.4b/20.5/20.6 cite `20-VERIFICATION.md`; REQ-20.5 also cites the byte-equality hex transcripts in `20-01-SUMMARY.md` and `20-02-SUMMARY.md`.
    - **D-02 PARTIAL `a`/`b` split:** Phase-25-introduced convention. REQ-20.4 cleaved into `20.4a` (testable structural: per-view hex-literal grep) and `20.4b` (WITHDRAWN visual: runtime appearance-toggle re-render across 10 surfaces). Both halves keep the original SPEC ID prefix in adjacent rows so SPEC traceability is preserved.
    - **REQ-20.5 dual-source citation:** The light-mode pixel stability gate is uniquely well-evidenced — both source-level byte-equality (each `Color(light:dark:)` light hex equals the pre-Phase-20 literal byte-for-byte, proven in Plan 20-01/02 SUMMARY hex transcripts) AND visual user attestation (`20-VERIFICATION.md` Row group D). The WITHDRAWN reason cites both for completeness.
    - **REQ-20.6 vibrancy boundary:** DictationHUD's `.hudWindow` vibrancy material is system-rendered AppKit chrome, not a SwiftUI surface. Adaptation under appearance toggle is a system invariant; visual attestation in `20-VERIFICATION.md` Row group F is the right Source.
    - **Token-of-token chain (`youBg` / `youFg`):** Defined as `Color.ink` / `Color.paper` in `DesignTokens.swift:103-104`. They auto-flip because the underlying tokens are adaptive; Phase 25's REQ-20.2 test asserts on the 17 directly-defined Chronicle tokens (paper..liveGreen). The "17 Chronicle tokens" SPEC contract is satisfied by the directly-defined entries; youBg/youFg are accessors over those.
    ```

    No additional content below the Audit Notes section. Verify the diff is exactly one new file.

    After writing, run the full test suite to confirm no regressions:
    ```
    cd PSTranscribe && swift test
    ```
    Expected: full suite green; suite count grew by 4 since Plan 25-01 (3 new in PreferredColorSchemeGrepGateTests + 1 new in DesignTokensAdaptivePaletteTests). End-of-Phase-25 suite count = pre-Phase-25 baseline + 11 (Plan 25-01 added 7; Plan 25-02 added 4).
  </action>
  <verify>
    <automated>test -f .planning/milestones/v1.2-phases/20-dark-mode-parity/20-VALIDATION.md && grep -E "^(phase|slug|status|nyquist_compliant|wave_0_complete|created|last_audited):" .planning/milestones/v1.2-phases/20-dark-mode-parity/20-VALIDATION.md && grep -c "Validation Audit 2026-05-07" .planning/milestones/v1.2-phases/20-dark-mode-parity/20-VALIDATION.md && grep -c "WITHDRAWN" .planning/milestones/v1.2-phases/20-dark-mode-parity/20-VALIDATION.md && cd PSTranscribe && swift test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/milestones/v1.2-phases/20-dark-mode-parity/20-VALIDATION.md`
    - `grep -c "^phase: 20$" .planning/milestones/v1.2-phases/20-dark-mode-parity/20-VALIDATION.md` returns 1
    - `grep -c "^slug: dark-mode-parity$" .planning/milestones/v1.2-phases/20-dark-mode-parity/20-VALIDATION.md` returns 1
    - `grep -c "^status: approved$" .planning/milestones/v1.2-phases/20-dark-mode-parity/20-VALIDATION.md` returns 1
    - `grep -c "^nyquist_compliant: true$" .planning/milestones/v1.2-phases/20-dark-mode-parity/20-VALIDATION.md` returns 1
    - `grep -c "^wave_0_complete: true$" .planning/milestones/v1.2-phases/20-dark-mode-parity/20-VALIDATION.md` returns 1
    - `grep -c "^last_audited: 2026-05-07$" .planning/milestones/v1.2-phases/20-dark-mode-parity/20-VALIDATION.md` returns 1
    - `grep -c "Validation Audit 2026-05-07" .planning/milestones/v1.2-phases/20-dark-mode-parity/20-VALIDATION.md` returns 1
    - All 6 SPEC reqs covered (with PARTIAL split): `grep -cE '\| REQ-20\.(1|2|3|4a|4b|5|6) \|' .planning/milestones/v1.2-phases/20-dark-mode-parity/20-VALIDATION.md` returns 7
    - WITHDRAWN row count matches D-01/D-02: `grep -cE '\| (REQ-20\.4b|REQ-20\.5|REQ-20\.6) \| WITHDRAWN \|' .planning/milestones/v1.2-phases/20-dark-mode-parity/20-VALIDATION.md` returns 3
    - `grep -c "Source: 20-VERIFICATION.md" .planning/milestones/v1.2-phases/20-dark-mode-parity/20-VALIDATION.md` returns at least 3 (one per WITHDRAWN row)
    - REQ-20.5 dual-source citation: `grep -c "20-01-SUMMARY.md" .planning/milestones/v1.2-phases/20-dark-mode-parity/20-VALIDATION.md` returns at least 1 AND `grep -c "20-02-SUMMARY.md" .planning/milestones/v1.2-phases/20-dark-mode-parity/20-VALIDATION.md` returns at least 1
    - `grep -cE 'swift test --filter (PreferredColorSchemeGrepGateTests|DesignTokensAdaptivePaletteTests)' .planning/milestones/v1.2-phases/20-dark-mode-parity/20-VALIDATION.md` returns at least 4 (one per UNIT row)
    - `cd PSTranscribe && swift test` exits 0 (full suite green; no regressions; suite count grew by 11 total across Phase 25)
  </acceptance_criteria>
  <done>20-VALIDATION.md created with 7 rows; full Swift Testing suite green locally; ready to commit + close plan + push for CI verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → filesystem | Tests read source files via `URL(fileURLWithPath:)` from `Sources/PSTranscribe/...`; no writes; no network |
| Test process → AppKit NSAppearance | `DesignTokensAdaptivePaletteTests` mutates `NSAppearance.current` via `performAsCurrentDrawingAppearance` (process-global); `.serialized` annotation prevents cross-test races |

## STRIDE Threat Register

ASVS Level 1. Phase 25 ships only Swift Testing test code + Markdown VALIDATION.md files. No new production code, no I/O beyond reading committed source files, no auth, no network calls, no untrusted input. Surface is minimal; all categories evaluated.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02-01 | Spoofing | n/a (no auth, no identity boundary in test code) | accept | N/A — test-only code; no identity checks |
| T-25-02-02 | Tampering | If a future commit re-introduces an `extension Color` block in `TranscriptView.swift` or adds a hex-literal `Color(red:...)` to a view file, the Phase 20 single-source-of-truth contract breaks | mitigate | `transcriptViewDoesNotDefineExtensionColor` and `viewSourcesContainNoHexColorLiterals` tests fail on regression; CI gate enforces. Similarly `noForcedPreferredColorSchemeAnywhere` blocks reintroduction of forced overrides |
| T-25-02-03 | Tampering | If a future commit changes a Chronicle token from `Color(light:dark:)` to a single hex literal, the adaptive palette breaks | mitigate | `allChronicleTokensHaveDistinctLightAndDarkVariants` test asserts every token resolves to distinct RGB triples under `.aqua` vs `.darkAqua`; CI gate fails on a non-adaptive token |
| T-25-02-04 | Repudiation | n/a — Phase 25 is audit-only; no state changes in production code, all evidence in committed VALIDATION.md + test files | accept | N/A — full audit trail in git history (commit message references NYQUIST-06; SUMMARY.md frontmatter carries `requirements-completed: [NYQUIST-06]` per PROCESS-01) |
| T-25-02-05 | Information Disclosure | Tests read source files at `Sources/PSTranscribe/...`; all are committed source — no secrets, no config | accept | Source files contain no secrets (verified by Phase 24 `WorkflowSecretsTests`); test reads do not exfiltrate to network |
| T-25-02-06 | Denial of Service | `NSAppearance.current` mutation is process-global; parallel tests could race and cause indeterminate failures | mitigate | `@Suite("DesignTokensAdaptivePaletteTests", .serialized)` annotation enforces serial execution; race is structurally prevented |
| T-25-02-07 | Elevation of Privilege | `@testable import PSTranscribe` exposes internal symbols (e.g., `Color` extension members); test target only — never compiled into the shipped app | accept | N/A — test bundle is not distributed; `@testable` is the standard Swift Testing convention for exercising internal types |

No high-severity threats. Mitigations are CI-gated via existing Swift Testing assertions; .serialized gate prevents the only structural concurrency hazard.
</threat_model>

<verification>
- `swift test --filter PreferredColorSchemeGrepGateTests` exits 0 with 7 passing tests (Plan 25-01's 3 + Plan 25-02's 4)
- `swift test --filter DesignTokensAdaptivePaletteTests` exits 0 with 1 passing test (17 tokens iterated)
- Full `swift test` exits 0 (no regressions in pre-Phase-25 suite + Plan 25-01 suite)
- `20-VALIDATION.md` frontmatter shows `phase: 20`, `slug: dark-mode-parity`, `status: approved`, `nyquist_compliant: true`, `wave_0_complete: true`, `last_audited: 2026-05-07`
- `20-VALIDATION.md` Per-Task Verification Map contains 8 rows covering REQ-20.1, REQ-20.2, REQ-20.3 (deletion + promotion halves as adjacent rows), REQ-20.4a, REQ-20.4b, REQ-20.5, REQ-20.6
- `20-VALIDATION.md` WITHDRAWN rows (REQ-20.4b, REQ-20.5, REQ-20.6) each cite `20-VERIFICATION.md`; REQ-20.5 also cites `20-01-SUMMARY.md` + `20-02-SUMMARY.md` byte-equality proof
- No production source code changes (per Phase 25 D-01 cite-only — Phase 25 is audit, not fix; if a planner finds REQ-20.x broken in source, escalate)
- `build-check.yml` CI green on the PR (full Phase 25 swift test step exits 0 on macos-26)
</verification>

<success_criteria>
1. `PreferredColorSchemeGrepGateTests.swift` extended with 4 new `@Test` methods (W-02 fix added the 4th — `legacyPromotedTokensAreDefinedAsColorLightDarkInDesignTokens`); `swift test --filter PreferredColorSchemeGrepGateTests` passes with 7 total tests
2. `DesignTokensAdaptivePaletteTests.swift` created with 1 `@Test` method covering all 17 Chronicle tokens via NSColor bridging; `swift test --filter DesignTokensAdaptivePaletteTests` passes
3. `20-VALIDATION.md` created at `status: approved` / `nyquist_compliant: true` with 8 verification map rows (5 UNIT + 3 WITHDRAWN)
4. Full `swift test` suite green locally (no regressions; Phase-25-total suite count grew by 12 — 7 from Plan 25-01 + 5 from Plan 25-02)
5. PROCESS-01 frontmatter (`requirements-completed: [NYQUIST-06]`) on the eventual SUMMARY.md
6. NYQUIST-06 closed: ROADMAP.md success criterion 1 ("`20-VALIDATION.md` exists for v1.2 Phase 20 with assertions covering token color resolution per appearance, removal of `.preferredColorScheme(.light)` overrides, 17 Chronicle tokens + 11 legacy tokens — tests run green") satisfied with explicit coverage of BOTH rosters (W-02 fix): the 17 Chronicle tokens are exercised adaptively by `allChronicleTokensHaveDistinctLightAndDarkVariants` (NSColor bridging, `light != dark` per token); the 11 legacy promoted tokens (bg0, bg1, bg2, fg1, fg2, fg3, accent1, accent2, recordRed, speakerTeal, speakerAmber — distinct names from Chronicle, NOT subsumed) are exercised structurally by `legacyPromotedTokensAreDefinedAsColorLightDarkInDesignTokens` (each appears with `Color(light:dark:)` syntax in DesignTokens.swift). The legacy tokens intentionally have light==dark in Wave 1 (DesignTokens.swift:175-177 explicit comment); the test asserts the structural promotion contract (Phase 20 SPEC line 29 + line 83), not adaptive behavior, since DesignTokens.swift makes adaptive behavior infeasible by design.
7. Phase 25 closes with both NYQUIST-06 and NYQUIST-07 satisfied; v1.3 progress increments to 4/5 phases complete (Phase 26 is the only remaining phase)
</success_criteria>

<output>
After completion, create `.planning/phases/25-nyquist-sweep-v1-2/25-02-SUMMARY.md` with:
- Frontmatter: `requirements-completed: [NYQUIST-06]` (PROCESS-01 in effect)
- What shipped: PreferredColorSchemeGrepGateTests extension (3 @Test) + DesignTokensAdaptivePaletteTests creation (1 @Test) + 20-VALIDATION.md
- D-01 referenced explicitly (cite-only snapshot posture; Phase 23 baselines not extended)
- D-02 referenced explicitly (PARTIAL `a`/`b` split for REQ-20.4)
- D-04 referenced explicitly (smallest-first plan ordering — Plan 25-02 lands second after 25-01)
- Note that PreferredColorSchemeGrepGateTests.swift was created in Plan 25-01 and EXTENDED here (the file spans both plans; mirrors Phase 24 skeleton-then-extend convention)

Commit message style: NO Claude attribution (per user-level CLAUDE.md). Subject: `feat(25-02): backfill Phase 20 Nyquist coverage (NYQUIST-06)` or similar; body explains the cite-only + PARTIAL split + NSColor bridging deltas.
</output>
