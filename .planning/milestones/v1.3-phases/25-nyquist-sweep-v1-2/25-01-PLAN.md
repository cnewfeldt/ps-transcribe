---
phase: 25-nyquist-sweep-v1-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - PSTranscribe/Tests/PSTranscribeTests/AppSettingsTests.swift
  - PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift
  - .planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md
autonomous: true
requirements: [NYQUIST-07]
tags: [nyquist, validation, appearance, phase-21, v1.2]

must_haves:
  truths:
    - "Honors D-01 (cite-only snapshot posture): WITHDRAWN rows for REQ-21.2b/21.3b/21.5 cite 21-VERIFICATION.md (and 21-HUMAN-UAT.md where applicable); no new snapshot tests added"
    - "Honors D-02 (PARTIAL a/b row split): REQ-21.2 split into 21.2a UNIT + 21.2b WITHDRAWN; REQ-21.3 split into 21.3a UNIT + 21.3b WITHDRAWN; both halves preserve SPEC traceability via adjacent rows"
    - "Honors D-03 (cross-ref Phase 26 titlebar bridge): 21-VALIDATION.md contains an `## Out of scope: Post-fix titlebar bridge (CR-01)` section with cross-ref to Phase 26's 26-UAT.md (QA-06..09); no @Test code is added for the bridge"
    - "Honors D-04 (smallest-first plan ordering): Plan 25-01 lands first, smaller scope (~6-7 @Test methods), creates shared PreferredColorSchemeGrepGateTests.swift to be extended by Plan 25-02"
    - "Honors Phase 24 D-04 (flat behavior-named test files): new file at PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift; AppSettingsTests.swift extension stays in-file"
    - "AppSettingsTests.swift extended with appearancePreference key in v12Keys array AND 4 new @Test methods covering REQ-21.1 (default + light round-trip + dark round-trip) and REQ-21.6 (missing-key fallback)"
    - "PreferredColorSchemeGrepGateTests.swift created with 3 new @Test methods covering REQ-21.2a (Scene-root call-sites read from settings), REQ-21.3a (SettingsView Picker exists + binds to $settings.appearancePreference), REQ-21.4 (relaxed location/source grep — same @Test as REQ-21.2a per Phase 21 D-06)"
    - "REQ-21.2a and REQ-21.4 are intentionally satisfied by the SAME @Test method (`preferredColorSchemeOnlyAtAppRootReadingFromSettings`) — Phase 21 D-06 relaxed grep gate combines location AND source contracts; the dual-citation in 21-VALIDATION.md's Per-Task Verification Map is by design (I-02 polish)"
    - "21-VALIDATION.md created from scratch at .planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md with frontmatter status: approved, nyquist_compliant: true, wave_0_complete: true, last_audited: 2026-05-07"
    - "21-VALIDATION.md Per-Task Verification Map contains 8 rows: REQ-21.1 (UNIT), REQ-21.2a (UNIT), REQ-21.2b (WITHDRAWN), REQ-21.3a (UNIT), REQ-21.3b (WITHDRAWN), REQ-21.4 (UNIT), REQ-21.5 (WITHDRAWN), REQ-21.6 (UNIT)"
    - "21-VALIDATION.md WITHDRAWN rows include literal `Source: 21-VERIFICATION.md` pointer (and `21-HUMAN-UAT.md` for REQ-21.5)"
    - "21-VALIDATION.md `## Out of scope: Post-fix titlebar bridge (CR-01)` section names QA-06, QA-07, QA-08, QA-09 and references Phase 26's 26-UAT.md as `(produced when Phase 26 ships)`"
    - "swift test --filter AppSettingsTests passes (exit 0) locally including the 4 new @Test methods"
    - "swift test --filter PreferredColorSchemeGrepGateTests passes (exit 0) locally with 3 passing tests"
    - "Full swift test suite passes (exit 0) locally with no regressions"
  artifacts:
    - path: "PSTranscribe/Tests/PSTranscribeTests/AppSettingsTests.swift"
      provides: "Extended Swift Testing assertions for AppearancePreference UD round-trip and missing-key fallback"
      contains: "appearancePreferenceDefaultsToSystem"
      min_lines: 160
    - path: "PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift"
      provides: "Source-grep guardrails for app-root preferredColorScheme call-sites + SettingsView picker existence"
      contains: "@Suite(\"PreferredColorSchemeGrepGateTests\")"
      min_lines: 80
    - path: ".planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md"
      provides: "Phase 21 Nyquist validation contract — full per-task verification map covering REQ-21.1..21.6 with PARTIAL a/b split + Phase 26 cross-ref"
      contains: "nyquist_compliant: true"
      min_lines: 100
  key_links:
    - from: "PSTranscribe/Tests/PSTranscribeTests/AppSettingsTests.swift"
      to: "PSTranscribe/Sources/PSTranscribe/Settings/AppSettings.swift"
      via: "@testable import PSTranscribe — references AppearancePreference enum + AppSettings.appearancePreference property"
      pattern: "settings\\.appearancePreference|AppearancePreference\\.(system|light|dark)"
    - from: "PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift"
      to: "PSTranscribe/Sources/PSTranscribe/App/PSTranscribeApp.swift"
      via: "URL(fileURLWithPath: \"Sources/PSTranscribe/App/PSTranscribeApp.swift\") + String(contentsOf:) — matches WorkflowSecretsTests.swift readRepoRootFile precedent"
      pattern: "readSource\\(\"App/PSTranscribeApp.swift\"\\)"
    - from: ".planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md"
      to: "PSTranscribe/Tests/PSTranscribeTests/AppSettingsTests.swift + PreferredColorSchemeGrepGateTests.swift"
      via: "Per-Task Verification Map Automated Command column citing swift test --filter <suite>/<method>"
      pattern: "swift test --filter (AppSettingsTests|PreferredColorSchemeGrepGateTests)"
    - from: ".planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md"
      to: ".planning/phases/26-qa-sweep-visual-uat/26-UAT.md (does not yet exist)"
      via: "Out-of-scope section forward cross-reference — names QA-06..09 IDs"
      pattern: "26-UAT\\.md"
---

<objective>
Create `21-VALIDATION.md` from scratch and ship the Swift Testing coverage that backs every TESTABLE requirement row, closing NYQUIST-07.

Purpose: Phase 21 (User-Controlled Appearance Preference) shipped 2026-05-01 without Nyquist validation. Phase 25 D-04 lands this plan FIRST (smallest, ~6-7 tests) so the shared `PreferredColorSchemeGrepGateTests.swift` is created here and extended by Plan 25-02. Honors D-01 (cite-only — no new snapshot tests), D-02 (PARTIAL a/b split for REQ-21.2 / REQ-21.3), D-03 (Phase 26 cross-ref for the post-fix titlebar bridge — no @Test code added for it), and D-04 (smallest-first; one plan per VALIDATION.md).

Output:
- 1 extended test file: `AppSettingsTests.swift` with `appearancePreference` added to `v12Keys` and 4 new `@Test` methods (REQ-21.1 default + 2 round-trips, REQ-21.6 missing-key fallback)
- 1 new test file: `PreferredColorSchemeGrepGateTests.swift` with 3 new `@Test` methods (REQ-21.2a Scene-root grep, REQ-21.3a SettingsView picker grep, REQ-21.4 source contract — REQ-21.4 reuses same @Test as REQ-21.2a per Phase 21 D-06)
- 1 new VALIDATION.md: `21-VALIDATION.md` at status: approved with 8 verification map rows + Phase 26 out-of-scope section
- 1 SUMMARY.md (created at plan close, includes `requirements-completed: [NYQUIST-07]`)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-nyquist-sweep-v1-2/25-CONTEXT.md
@.planning/phases/25-nyquist-sweep-v1-2/25-PATTERNS.md
@.planning/phases/24-nyquist-sweep-v1-0/24-01-PLAN.md
@.planning/milestones/v1.0-phases/01-rebrand/01-VALIDATION.md
@.planning/milestones/v1.2-phases/21-appearance-override/21-SPEC.md
@.planning/milestones/v1.2-phases/21-appearance-override/21-VERIFICATION.md
@.planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md
@PSTranscribe/Tests/PSTranscribeTests/AppSettingsTests.swift
@PSTranscribe/Tests/PSTranscribeTests/WorkflowSecretsTests.swift
@PSTranscribe/Sources/PSTranscribe/Settings/AppSettings.swift
@PSTranscribe/Sources/PSTranscribe/App/PSTranscribeApp.swift
@PSTranscribe/Sources/PSTranscribe/Views/SettingsView.swift

<interfaces>
<!-- Source-of-truth values verified 2026-05-07 via direct grep against Sources/PSTranscribe -->

# AppearancePreference enum (Sources/PSTranscribe/Settings/AppSettings.swift:14-30)
```swift
enum AppearancePreference: String, CaseIterable, Identifiable, Sendable {
    case system
    case light
    case dark
    var id: String { rawValue }
    var colorScheme: ColorScheme? { ... }  // .system -> nil
}
```

# AppSettings.appearancePreference (Sources/PSTranscribe/Settings/AppSettings.swift:119-121)
```swift
var appearancePreference: AppearancePreference {
    didSet { UserDefaults.standard.set(appearancePreference.rawValue, forKey: "appearancePreference") }
}
```

# AppSettings.init (Sources/PSTranscribe/Settings/AppSettings.swift:195-198)
```swift
let appearanceRaw = defaults.string(forKey: "appearancePreference")
    ?? AppearancePreference.system.rawValue
self.appearancePreference = AppearancePreference(rawValue: appearanceRaw) ?? .system
```

# PSTranscribeApp.swift Scene-root call-sites (REQ-21.2a / REQ-21.4)
- Line 173: WindowGroup -> `.preferredColorScheme(settings.appearancePreference.colorScheme)`
- Line 195: Settings    -> `.preferredColorScheme(settings.appearancePreference.colorScheme)`
- Line 207: MenuBarExtra Group -> `.preferredColorScheme(settings.appearancePreference.colorScheme)`
- Plus a single doc-comment hit at AppSettings.swift:9 ("/// `.preferredColorScheme(nil)`") — NOT a SwiftUI modifier; tests must use `\.` regex prefix to filter

# SettingsView.swift Picker (REQ-21.3a) — lines 30-37
```swift
Section("Appearance") {
    Picker("Appearance", selection: $settings.appearancePreference) {
        Text("System").tag(AppearancePreference.system)
        Text("Light").tag(AppearancePreference.light)
        Text("Dark").tag(AppearancePreference.dark)
    }
    .font(.system(size: 12))
}
```

# Test runtime: cwd at `swift test` time is `PSTranscribe/`. Source files at `Sources/PSTranscribe/...`.
# readSource helper (mirrors WorkflowSecretsTests.swift:28-35 readRepoRootFile pattern):
```swift
private func readSource(_ relativePath: String) throws -> String {
    let url = URL(fileURLWithPath: "Sources/PSTranscribe/\(relativePath)")
    let content = try String(contentsOf: url, encoding: .utf8)
    try #require(!content.isEmpty,
                 "Read empty content from \(relativePath); is the test cwd PSTranscribe/?")
    return content
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend AppSettingsTests.swift with AppearancePreference UD round-trip + missing-key fallback (REQ-21.1, REQ-21.6)</name>
  <files>PSTranscribe/Tests/PSTranscribeTests/AppSettingsTests.swift</files>
  <read_first>
    - PSTranscribe/Tests/PSTranscribeTests/AppSettingsTests.swift (existing file — read line 12-22 v12Keys array; lines 26-62 Defaults sub-suite; lines 66-135 round-trip pattern; lines 107-116 missing-key fallback precedent (modelLastCheckedDateNilClearsKey))
    - PSTranscribe/Sources/PSTranscribe/Settings/AppSettings.swift (read lines 14-30 enum; lines 119-121 property+didSet; lines 195-198 init+fallback — these are the symbols under test)
    - .planning/phases/25-nyquist-sweep-v1-2/25-PATTERNS.md (lines 47-128 — Plan 25-01 §AppSettingsTests.swift extension; provides exact extension shape to copy)
    - .planning/phases/25-nyquist-sweep-v1-2/25-CONTEXT.md (D-04 Claude's Discretion — extend AppSettingsTests.swift rather than create new file; D-04 also locks `.serialized` + `@MainActor` annotations carry forward from existing suite)
  </read_first>
  <behavior>
    Test 1 (REQ-21.1 default): `appearancePreferenceDefaultsToSystem()` — instantiate AppSettings with cleared UserDefaults, assert `s.appearancePreference == .system`. Lives in the nested `Defaults` sub-suite (mirrors `dictationHotkeyMode()` shape at line 28-33).
    Test 2 (REQ-21.1 round-trip light): `roundTrip_appearancePreferenceLight()` — set `s1.appearancePreference = .light`, instantiate `s2`, assert `s2.appearancePreference == .light`. Mirrors `roundTrip_dictationHotkeyMode()` at lines 66-73.
    Test 3 (REQ-21.1 round-trip dark): `roundTrip_appearancePreferenceDark()` — set `.dark`, assert it round-trips. Same shape as Test 2.
    Test 4 (REQ-21.6 missing-key fallback): `appearancePreferenceMissingKeyFallsBackToSystem()` — clear keys, assert `UserDefaults.standard.object(forKey: "appearancePreference") == nil`, then instantiate AppSettings, assert `s.appearancePreference == .system`. Mirrors `modelLastCheckedDateNilClearsKey()` at lines 107-116.
    Pre-condition for all 4 tests: `AppSettingsTests.clearV12Keys()` MUST cover `"appearancePreference"` — this requires adding the key to the v12Keys static array (otherwise round-trip tests leak state between runs).
  </behavior>
  <action>
    Use the Edit tool to apply two deltas to `PSTranscribe/Tests/PSTranscribeTests/AppSettingsTests.swift`:

    DELTA 1 — Extend `v12Keys` array (currently lines 12-18):

    Find the exact block:
    ```swift
        private static let v12Keys = [
            "dictationHotkeyMode",
            "clipboardRestoreDelay",
            "installedModelVersion",
            "modelLastCheckedDate",
            "modelAutoUpdateEnabled",
        ]
    ```

    Replace with:
    ```swift
        private static let v12Keys = [
            "dictationHotkeyMode",
            "clipboardRestoreDelay",
            "installedModelVersion",
            "modelLastCheckedDate",
            "modelAutoUpdateEnabled",
            "appearancePreference",  // Phase 25 NYQUIST-07
        ]
    ```

    DELTA 2 — Add a new `@Test` method to the `Defaults` sub-suite (currently lines 26-62). Insert AFTER line 61 (after `modelAutoUpdateEnabled_defaultsTrue`) and BEFORE the closing brace of the `Defaults` struct on line 62. New test:

    ```swift
            @Test @MainActor func appearancePreferenceDefaultsToSystem() {
                AppSettingsTests.clearV12Keys()
                defer { AppSettingsTests.clearV12Keys() }
                let s = AppSettings()
                #expect(s.appearancePreference == .system)
            }
    ```

    DELTA 3 — Add three new top-level `@Test` methods at the end of the outer `AppSettingsTests` struct, AFTER the existing `roundTrip_modelAutoUpdateEnabledRestoresTrue` (line 134) and BEFORE the closing brace of the struct on line 136:

    ```swift

        // MARK: - v1.2 Appearance Override (Phase 25 NYQUIST-07)

        @Test @MainActor func roundTrip_appearancePreferenceLight() {
            Self.clearV12Keys()
            defer { Self.clearV12Keys() }
            let s1 = AppSettings()
            s1.appearancePreference = .light
            let s2 = AppSettings()
            #expect(s2.appearancePreference == .light)
        }

        @Test @MainActor func roundTrip_appearancePreferenceDark() {
            Self.clearV12Keys()
            defer { Self.clearV12Keys() }
            let s1 = AppSettings()
            s1.appearancePreference = .dark
            let s2 = AppSettings()
            #expect(s2.appearancePreference == .dark)
        }

        @Test @MainActor func appearancePreferenceMissingKeyFallsBackToSystem() {
            Self.clearV12Keys()
            defer { Self.clearV12Keys() }
            // Phase 25 NYQUIST-07 / REQ-21.6: Simulate fresh-from-Phase-20 install — no key in UD.
            #expect(UserDefaults.standard.object(forKey: "appearancePreference") == nil)
            let s = AppSettings()
            #expect(s.appearancePreference == .system)
        }
    ```

    No changes to imports, no `.serialized` annotation changes (existing `@Suite("AppSettings v1.2 keys", .serialized)` already covers the new tests). No `@MainActor` changes (each new `@Test` carries its own `@MainActor` matching the existing convention).

    Verify by running:
    ```
    cd PSTranscribe && swift test --filter AppSettingsTests
    ```
    Expected: ALL existing tests still pass + 4 new tests pass. If any existing test starts failing because `clearV12Keys()` now also clears `"appearancePreference"`, STOP — that means an existing test was relying on stale key state, which is a pre-existing bug to escalate, not modify.
  </action>
  <verify>
    <automated>cd PSTranscribe && swift test --filter AppSettingsTests 2>&1 | tee /tmp/25-01-task1.log; tail -25 /tmp/25-01-task1.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"appearancePreference"' PSTranscribe/Tests/PSTranscribeTests/AppSettingsTests.swift` returns at least 4 (1 in v12Keys + 1 in defaults test + 2 in round-trip tests + 1 in missing-key test = expected ≥ 5)
    - `grep -cE '@Test @MainActor func (appearancePreferenceDefaultsToSystem|roundTrip_appearancePreferenceLight|roundTrip_appearancePreferenceDark|appearancePreferenceMissingKeyFallsBackToSystem)\(' PSTranscribe/Tests/PSTranscribeTests/AppSettingsTests.swift` returns 4
    - Per-method existence checks (W-03 — each delta surfaces precisely on partial implementation):
      - `grep -c '@Test @MainActor func appearancePreferenceDefaultsToSystem' PSTranscribe/Tests/PSTranscribeTests/AppSettingsTests.swift` returns 1 (Defaults sub-suite gain)
      - `grep -c '@Test @MainActor func appearancePreferenceMissingKeyFallsBackToSystem' PSTranscribe/Tests/PSTranscribeTests/AppSettingsTests.swift` returns 1 (Defaults sub-suite gain)
      - `grep -c '@Test @MainActor func roundTrip_appearancePreferenceLight' PSTranscribe/Tests/PSTranscribeTests/AppSettingsTests.swift` returns 1 (outer struct gain)
      - `grep -c '@Test @MainActor func roundTrip_appearancePreferenceDark' PSTranscribe/Tests/PSTranscribeTests/AppSettingsTests.swift` returns 1 (outer struct gain)
    - `grep -c '// Phase 25 NYQUIST-07' PSTranscribe/Tests/PSTranscribeTests/AppSettingsTests.swift` returns at least 2 (v12Keys comment + section MARK comment)
    - `cd PSTranscribe && swift test --filter AppSettingsTests` exits 0 with all tests green (existing + 4 new)
    - Pre-existing test count from AppSettingsTests grew by exactly 4 (Defaults: was 5, becomes 6; outer struct round-trips: was 7, becomes 10)
  </acceptance_criteria>
  <done>4 new @Test methods written; v12Keys extended; existing AppSettingsTests still green; new tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create PreferredColorSchemeGrepGateTests.swift with REQ-21.2a, REQ-21.3a, REQ-21.4 source-grep gates</name>
  <files>PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift</files>
  <read_first>
    - PSTranscribe/Tests/PSTranscribeTests/WorkflowSecretsTests.swift (canonical source-grep precedent — copy `readRepoRootFile` shape from lines 28-35; note the `try #require(!content.isEmpty)` smoke check is non-negotiable per Phase 24 D-04)
    - PSTranscribe/Sources/PSTranscribe/App/PSTranscribeApp.swift (lines 160-210 — three Scene-root .preferredColorScheme call-sites confirmed at 173, 195, 207; verify CURRENT state before writing assertions)
    - PSTranscribe/Sources/PSTranscribe/Views/SettingsView.swift (lines 28-37 — Section("Appearance") + Picker bound to $settings.appearancePreference; verify CURRENT state before writing assertions)
    - PSTranscribe/Sources/PSTranscribe/Settings/AppSettings.swift (line 9 — doc-comment with literal `.preferredColorScheme(nil)` string; this is NOT a SwiftUI modifier, tests must filter via `\.preferredColorScheme(` with leading `\.` to avoid false positives)
    - .planning/phases/25-nyquist-sweep-v1-2/25-PATTERNS.md (lines 131-260 — Plan 25-01 §PreferredColorSchemeGrepGateTests.swift; provides exact suite shape, readSource helper, stripPreviewBlocks rationale, and assertion patterns)
    - .planning/phases/25-nyquist-sweep-v1-2/25-CONTEXT.md (D-04 — file is shared with Plan 25-02; D-06 from Phase 21 — relaxed grep gate is location+source, NOT count; planner picks min-floor of 1 not exact 3)
    - .planning/milestones/v1.2-phases/21-appearance-override/21-VERIFICATION.md (re-verification block context — confirms three Scene-root call-sites are the structural contract; `>= 1` floor is correct per D-06)
  </read_first>
  <behavior>
    Test 1 (REQ-21.2a + REQ-21.4): `preferredColorSchemeOnlyAtAppRootReadingFromSettings()` — read `App/PSTranscribeApp.swift`, strip `#Preview` blocks (helper), then for every `.preferredColorScheme(...)` match: assert the argument contains `settings.appearancePreference.colorScheme`. Floor: at least 1 match (per Phase 21 D-06 relaxed grep — location + source, NOT count). This single @Test satisfies both REQ-21.2a (Scene-root location) AND REQ-21.4 (source contract reads from AppSettings).
    Test 2 (REQ-21.2a forbidden-elsewhere): `noPreferredColorSchemeOutsideAppRoot()` — enumerate every `.swift` file under `Sources/PSTranscribe/Views/` at test time via `swiftFilesUnder("Views")` (W-01 fix — see helper definition; superseded original 5-file hardcoded roster), strip `#Preview` blocks, assert each stripped source does NOT contain `.preferredColorScheme(`. Refactor tripwire — if a future PR re-introduces a forced override OR adds a new view file with one, this fails.
    Test 3 (REQ-21.3a): `settingsViewContainsAppearancePicker()` — read `Views/SettingsView.swift`, assert it contains the literal substrings `Section("Appearance")`, `Picker`, AND `selection: $settings.appearancePreference`. Three substring assertions; no preview-strip needed (Settings UI is production code).
    Smoke pre-condition: `readSource(_:)` MUST `try #require(!content.isEmpty)` to fail fast on wrong cwd (Phase 24 idiom locked in 25-PATTERNS.md §Shared Patterns).
  </behavior>
  <action>
    Use the Write tool to create `PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift` (file does not exist yet).

    Header doc-comment (mandatory per 25-PATTERNS.md "No Analog Found" — this file establishes the Sources/PSTranscribe-relative grep gate convention; future plans extend it):
    ```swift
    // PreferredColorSchemeGrepGateTests
    // Phase 25 (NYQUIST-07) — Phase 21 REQ-21.2a / REQ-21.3a / REQ-21.4.
    // (Plan 25-02 extends this file with REQ-20.1 / REQ-20.3 / REQ-20.4a tests.)
    //
    // Static source assertions on Swift source files under Sources/PSTranscribe/.
    // Reads the committed files as Strings and verifies invariants:
    //   - Phase 21 REQ-21.2a / REQ-21.4: every `.preferredColorScheme(...)` call in
    //     PSTranscribeApp.swift OUTSIDE `#Preview { ... }` blocks reads from
    //     `settings.appearancePreference.colorScheme` (relaxed grep per Phase 21
    //     D-06: location + source contract, not exact count).
    //   - Phase 21 REQ-21.2a forbidden-elsewhere: no other view source file under
    //     Sources/PSTranscribe/Views/ contains `.preferredColorScheme(` outside
    //     `#Preview` blocks (refactor tripwire).
    //   - Phase 21 REQ-21.3a: SettingsView.swift contains a `Section("Appearance")`
    //     + `Picker` bound to `$settings.appearancePreference`.
    //
    // Working-directory assumption: `swift test` runs from `PSTranscribe/` (the
    // SwiftPM package root). Source files live at `Sources/PSTranscribe/...`. The
    // helper `readSource(_:)` includes a `try #require(!content.isEmpty)` smoke
    // check so a wrong cwd fails fast with a clear message instead of silently
    // passing absence assertions.
    //
    // The `stripPreviewBlocks(_:)` helper is a Phase-25-introduced utility (no
    // in-house analog). It removes `#Preview { ... }` blocks before grep-style
    // assertions so dev-tool-only preview code does not pollute production-code
    // contracts. Implementation: linear scan with brace counting (not regex —
    // regex with balanced braces is fragile in Swift's NSRegularExpression).

    import Testing
    import Foundation

    @Suite("PreferredColorSchemeGrepGateTests")
    struct PreferredColorSchemeGrepGateTests {

        private func readSource(_ relativePath: String) throws -> String {
            // cwd at `swift test` time is PSTranscribe/; source root is Sources/PSTranscribe/
            let url = URL(fileURLWithPath: "Sources/PSTranscribe/\(relativePath)")
            let content = try String(contentsOf: url, encoding: .utf8)
            try #require(!content.isEmpty,
                         "Read empty content from \(relativePath); is the test cwd PSTranscribe/?")
            return content
        }

        /// Returns `.swift` files under `Sources/PSTranscribe/<subdir>` as relative
        /// paths suitable for `readSource(_:)` (e.g. `["Views/CaptureDock.swift",
        /// "Views/ContentView.swift", ...]`). Used for refactor-tripwire tests so
        /// new view files added by future PRs are automatically in scope (W-01 fix —
        /// hardcoded rosters under-cover the actual surface).
        private func swiftFilesUnder(_ subdir: String) throws -> [String] {
            let path = "Sources/PSTranscribe/\(subdir)"
            let names = try FileManager.default
                .contentsOfDirectory(atPath: path)
                .filter { $0.hasSuffix(".swift") }
                .sorted()
            try #require(!names.isEmpty,
                         "No Swift files found under Sources/PSTranscribe/\(subdir); is the test cwd PSTranscribe/?")
            return names.map { "\(subdir)/\($0)" }
        }

        /// Strips `#Preview { ... }` and `#Preview("name") { ... }` blocks from source
        /// so grep gates inspect only production code. Linear scan with brace counting.
        /// Limitation: does not support nested `#Preview` (codebase does not use this).
        private func stripPreviewBlocks(_ source: String) -> String {
            var result = ""
            var i = source.startIndex
            while i < source.endIndex {
                // Look for `#Preview` token
                if source[i...].hasPrefix("#Preview") {
                    // Skip to first `{` after the macro literal
                    var j = source.index(i, offsetBy: "#Preview".count)
                    while j < source.endIndex && source[j] != "{" { j = source.index(after: j) }
                    if j == source.endIndex { break }  // malformed; bail to safety
                    // Brace-balance from `{`
                    var depth = 0
                    while j < source.endIndex {
                        if source[j] == "{" { depth += 1 }
                        else if source[j] == "}" {
                            depth -= 1
                            if depth == 0 {
                                j = source.index(after: j)
                                break
                            }
                        }
                        j = source.index(after: j)
                    }
                    i = j
                } else {
                    result.append(source[i])
                    i = source.index(after: i)
                }
            }
            return result
        }

        // MARK: - REQ-21.2a + REQ-21.4 (one @Test satisfies both per Phase 21 D-06)

        @Test func preferredColorSchemeOnlyAtAppRootReadingFromSettings() throws {
            let appSrc = try readSource("App/PSTranscribeApp.swift")
            let stripped = stripPreviewBlocks(appSrc)
            // Match `.preferredColorScheme(<arg>)` — leading `\.` so doc-comment hits in
            // other files don't false-positive (here we read PSTranscribeApp.swift only).
            let pattern = try NSRegularExpression(pattern: #"\.preferredColorScheme\(([^)]+)\)"#)
            let nsStripped = stripped as NSString
            let matches = pattern.matches(in: stripped, range: NSRange(location: 0, length: nsStripped.length))
            #expect(matches.count >= 1,
                    "PSTranscribeApp.swift must contain at least one Scene-root .preferredColorScheme call (Phase 21 D-05 / REQ-21.2a)")
            for m in matches {
                let arg = nsStripped.substring(with: m.range(at: 1))
                #expect(arg.contains("settings.appearancePreference.colorScheme"),
                        "Every .preferredColorScheme call outside #Preview must read from AppSettings (Phase 21 REQ-21.4); got: \(arg)")
            }
        }

        // MARK: - REQ-21.2a forbidden-elsewhere (refactor tripwire)

        @Test func noPreferredColorSchemeOutsideAppRoot() throws {
            // W-01 fix: walk Views/ at test time so any future-added view file is
            // automatically in scope. The app-root file (PSTranscribeApp.swift) lives
            // in Sources/PSTranscribe/App/ and is not enumerated here by design.
            let surfaces = try swiftFilesUnder("Views")
            for path in surfaces {
                let stripped = stripPreviewBlocks(try readSource(path))
                #expect(!stripped.contains(".preferredColorScheme("),
                        "\(path) must not call .preferredColorScheme outside #Preview (Phase 21 REQ-21.2a single-app-root invariant)")
            }
        }

        // MARK: - REQ-21.3a

        @Test func settingsViewContainsAppearancePicker() throws {
            let src = try readSource("Views/SettingsView.swift")
            #expect(src.contains(#"Section("Appearance")"#),
                    "SettingsView.swift must contain Section(\"Appearance\") (Phase 21 REQ-21.3a)")
            #expect(src.contains("Picker"),
                    "SettingsView.swift Appearance section must contain a Picker (Phase 21 REQ-21.3a)")
            #expect(src.contains("selection: $settings.appearancePreference"),
                    "SettingsView.swift Picker must bind to $settings.appearancePreference (Phase 21 REQ-21.3a)")
        }
    }
    ```

    No `.serialized` annotation — pure file reads, no shared state mutation. No `@MainActor` — no SwiftUI invocation. No `@testable import PSTranscribe` — file IO + Foundation only.

    After writing the file, verify by running:
    ```
    cd PSTranscribe && swift test --filter PreferredColorSchemeGrepGateTests
    ```
    Expected: 3 tests pass. If `preferredColorSchemeOnlyAtAppRootReadingFromSettings` reports 0 matches, the cwd is wrong OR the source has been refactored — STOP and escalate, do not loosen the assertion. If `noPreferredColorSchemeOutsideAppRoot` finds an unexpected hit, that's a real regression — STOP and escalate.
  </action>
  <verify>
    <automated>cd PSTranscribe && swift test --filter PreferredColorSchemeGrepGateTests 2>&1 | tee /tmp/25-01-task2.log; tail -25 /tmp/25-01-task2.log</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift`
    - Contains exactly 3 `@Test func` declarations: `grep -cE '^\s+@Test func (preferredColorSchemeOnlyAtAppRootReadingFromSettings|noPreferredColorSchemeOutsideAppRoot|settingsViewContainsAppearancePicker)\(' PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift` returns 3
    - Contains header marker: `grep -c "Phase 25 (NYQUIST-07)" PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift` returns 1
    - Contains `readSource` helper with cwd smoke check: `grep -c 'try #require(!content.isEmpty' PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift` returns at least 1
    - Contains `stripPreviewBlocks` helper: `grep -c 'private func stripPreviewBlocks' PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift` returns 1
    - No `.serialized` annotation (pure file reads): `grep -c '\.serialized' PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift` returns 0
    - No `@MainActor` annotation: `grep -c '@MainActor' PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift` returns 0
    - `cd PSTranscribe && swift test --filter PreferredColorSchemeGrepGateTests` exits 0 with 3 passing tests
  </acceptance_criteria>
  <done>3 new @Test methods written; PreferredColorSchemeGrepGateTests.swift suite passes locally; file ready to be extended by Plan 25-02.</done>
</task>

<task type="auto">
  <name>Task 3: Create 21-VALIDATION.md from scratch with 8 verification map rows + Phase 26 cross-ref + run full test suite</name>
  <files>.planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v1.0-phases/01-rebrand/01-VALIDATION.md (canonical post-Phase-24 shape — frontmatter, Test Infrastructure section, Sampling Rate section, Per-Task Verification Map column shape, Validation Sign-Off, Validation Audit block)
    - .planning/milestones/v1.2-phases/21-appearance-override/21-SPEC.md (REQ-21.1..21.6 source — verify req text matches what 21-VALIDATION.md cites)
    - .planning/milestones/v1.2-phases/21-appearance-override/21-VERIFICATION.md (final-gate sign-off — used as `Source:` for WITHDRAWN rows)
    - .planning/milestones/v1.2-phases/21-appearance-override/21-HUMAN-UAT.md (2026-05-01 attestation — used as additional `Source:` for REQ-21.5 default-state pixel stability)
    - .planning/phases/25-nyquist-sweep-v1-2/25-CONTEXT.md (D-01 cite-only; D-02 PARTIAL a/b split; D-03 Phase 26 cross-ref; D-04 smallest-first)
    - .planning/phases/25-nyquist-sweep-v1-2/25-PATTERNS.md (lines 432-465 — §`21-VALIDATION.md`; provides exact 8-row Per-Task Map + Out-of-scope section template)
    - .github/workflows/build-check.yml (CI gate that backs the validation contract)
  </read_first>
  <action>
    Use the Write tool to create `.planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md` (file does not exist).

    Use this EXACT content (do not paraphrase — it mirrors `01-VALIDATION.md` post-Phase-24 shape with Phase 25 amendments):

    ```markdown
    ---
    phase: 21
    slug: appearance-override
    status: approved
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-05-07
    last_audited: 2026-05-07
    ---

    # Phase 21 -- Validation Strategy

    > Per-phase validation contract for feedback sampling during execution.

    Backfilled by Phase 25 (NYQUIST-07) on 2026-05-07. Phase 21 shipped 2026-05-01 without Nyquist coverage; this validation contract certifies the structural half of every REQ-21.x with Swift Testing assertions and cites `21-VERIFICATION.md` for the visual halves per Phase 25 D-01 (cite-only snapshot posture) and D-02 (PARTIAL `a`/`b` split for compound contracts).

    ---

    ## Test Infrastructure

    | Property | Value |
    |----------|-------|
    | **Framework** | Swift Testing (`import Testing`) — `PSTranscribeTests` target in `PSTranscribe/Package.swift` |
    | **Config file** | `PSTranscribe/Package.swift` — `.testTarget(name: "PSTranscribeTests", ...)` |
    | **Quick run command** | `cd PSTranscribe && swift test --filter <suite>` |
    | **Full suite command** | `cd PSTranscribe && swift test` |
    | **Estimated runtime** | ~15-90 seconds (full suite, macos-26) |

    ---

    ## Sampling Rate

    - **After every task commit:** Run focused `swift test --filter <suite>`
    - **After every plan wave:** Run full `swift test` locally
    - **Before merge:** `build-check.yml` CI green on PR (macos-26, Xcode 26)
    - **Max feedback latency:** 90 seconds

    ---

    ## Per-Task Verification Map

    Compound REQs are split into TESTABLE structural halves (`Xa`) and WITHDRAWN visual halves (`Xb`) per Phase 25 D-02. Visual halves cite `21-VERIFICATION.md` and `21-HUMAN-UAT.md` per Phase 25 D-01 cite-only posture (Phase 23's snapshot infrastructure is not extended here — see Audit Notes).

    | Task ID | Plan | Wave | Requirement | Test Type | Automated Command | Status | Notes |
    |---------|------|------|-------------|-----------|-------------------|--------|-------|
    | 25-01-01 | 25-01 | 1 | REQ-21.1 | unit | `cd PSTranscribe && swift test --filter AppSettingsTests/roundTrip_appearancePreferenceLight` | green | AppearancePreference enum + UD round-trip; companion tests cover `.dark` and `.system` default |
    | 25-01-02 | 25-01 | 1 | REQ-21.2a | unit | `cd PSTranscribe && swift test --filter PreferredColorSchemeGrepGateTests/preferredColorSchemeOnlyAtAppRootReadingFromSettings` | green | Structural half (D-02 split): app-root call-sites read from `settings.appearancePreference.colorScheme` (relaxed grep per Phase 21 D-06 — location + source contract, not exact count) |
    | 25-01-03 | 25-01 | 1 | REQ-21.2b | WITHDRAWN | n/a -- WITHDRAWN | withdrawn | Visual half (D-02 split): runtime override re-renders all 10 surfaces without app restart. Manual UAT covers; no CI-runnable harness for runtime preference-toggle re-renders (Phase 25 D-01 cite-only). Source: 21-VERIFICATION.md Manual UAT Scenarios A-H + 21-HUMAN-UAT.md. |
    | 25-01-04 | 25-01 | 1 | REQ-21.3a | unit | `cd PSTranscribe && swift test --filter PreferredColorSchemeGrepGateTests/settingsViewContainsAppearancePicker` | green | Structural half (D-02 split): SettingsView Picker existence + binding source (`Section("Appearance")` + `Picker` + `selection: $settings.appearancePreference`) |
    | 25-01-05 | 25-01 | 1 | REQ-21.3b | WITHDRAWN | n/a -- WITHDRAWN | withdrawn | Visual half (D-02 split): user changes selection in Settings, every visible window re-renders immediately. Manual UAT covers. Source: 21-VERIFICATION.md Manual UAT Scenarios A-H. |
    | 25-01-06 | 25-01 | 1 | REQ-21.4 | unit | `cd PSTranscribe && swift test --filter PreferredColorSchemeGrepGateTests/preferredColorSchemeOnlyAtAppRootReadingFromSettings` | green | Same `@Test` as REQ-21.2a — one assertion satisfies both reqs (location AND source contract per Phase 21 D-06 relaxed grep). Companion `noPreferredColorSchemeOutsideAppRoot` test covers the forbidden-elsewhere refactor tripwire. |
    | 25-01-07 | 25-01 | 1 | REQ-21.5 | WITHDRAWN | n/a -- WITHDRAWN | withdrawn | Default-state pixel stability with preference = `.system`. Pure visual UAT against post-Phase-20 baseline; no CI-runnable visual diff in scope (Phase 25 D-01). Source: 21-VERIFICATION.md + 21-HUMAN-UAT.md. |
    | 25-01-08 | 25-01 | 1 | REQ-21.6 | unit | `cd PSTranscribe && swift test --filter AppSettingsTests/appearancePreferenceMissingKeyFallsBackToSystem` | green | Migration: missing UserDefaults key falls back to `.system` (invisible upgrade for post-Phase-20 installs) |

    *Status: pending / green / red / flaky / withdrawn*

    *withdrawn: requirement is satisfied at the product level (per `21-VERIFICATION.md`) but the specific contract is not automatable within Phase 25's cite-only scope (D-01). Source pointer in Notes column points at where the human-attested evidence lives.*

    ---

    ## Out of scope: Post-fix titlebar bridge (CR-01)

    The `applyChronicleTitlebar(_:)` and `observeChronicleTitlebar(controller:settings:)` helpers in `PSTranscribeApp.swift` (introduced in commits `197043b..ce79965` after `21-SPEC.md` was locked) are scope extensions outside the original Phase 21 SPEC. The bridge is verified at the code level by `21-VERIFICATION.md`'s post-fix re-verification block (NSColor.labelColor dynamic, `window.appearance` set, `bestMatch(from:)` gate). Visual UAT for the titlebar Light / Dark / System / unfocused-window scenarios lives in **Phase 26's `26-UAT.md` (QA-06, QA-07, QA-08, QA-09)** — produced when Phase 26 ships.

    Phase 25 deliberately does not encode the bridge audit as `@Test` methods (per Phase 25 D-03): the bridge code is tightly tied to AppKit invariants that don't regress silently, and the re-verification grep audit in `21-VERIFICATION.md` already certifies the code-level invariants. Encoding it as CI tests would duplicate work without buying coverage; deferring entirely would leave a hole in this VALIDATION.md's narrative — the cross-ref split is the cleanest fit.

    ---

    ## Wave 0 Requirements

    - Existing Swift Testing test target (`PSTranscribe/Tests/PSTranscribeTests/`) already meets infra requirements — no new dependencies, no `Package.swift` edits.
    - `build-check.yml` CI runs `swift test` on macos-26 (inherited from Phase 23 D-06).

    **Wave 0 complete:** No tooling installation needed; sampling rate met via existing Swift Testing target + CI.

    ---

    ## Validation Sign-Off

    - [x] All tasks have automated verify or are explicitly WITHDRAWN per Phase 25 D-01/D-02
    - [x] Sampling continuity: no 3 consecutive tasks without automated verify (8 rows; 5 UNIT + 3 WITHDRAWN; pattern: U-U-W-U-W-U-W-U)
    - [x] Wave 0 covers all MISSING references (no missing references — existing test target suffices)
    - [x] No watch-mode flags
    - [x] Feedback latency < 90s
    - [x] `nyquist_compliant: true` set in frontmatter

    **Approval:** approved 2026-05-07 (Phase 25 NYQUIST-07 — Swift Testing-backed; new VALIDATION.md, no prior audit)

    ---

    ## Validation Audit 2026-05-07 (Phase 25 -- NYQUIST-07)

    | Metric | Count |
    |--------|-------|
    | Gaps found | 0 (no MISSING test coverage; Phase 21 shipped with manual UAT only — Phase 25 backfills automated coverage for the structural half of every REQ) |
    | Resolved | 5 (REQ-21.1 UD round-trip + missing-key fallback; REQ-21.2a Scene-root grep + source contract; REQ-21.3a SettingsView Picker grep; REQ-21.4 — same `@Test` as REQ-21.2a per D-06; REQ-21.6 missing-key fallback) |
    | Withdrawn | 3 (REQ-21.2b visual half + REQ-21.3b visual half + REQ-21.5 default-state pixel stability per D-01 cite-only + D-02 PARTIAL split) |
    | Escalated | 0 |
    | Document updates | New 21-VALIDATION.md created at `status: approved` / `nyquist_compliant: true`; created 2026-05-07. Out-of-scope section added per D-03 cross-referencing Phase 26's 26-UAT.md (QA-06..09). |

    ### Audit Method

    - Extended `PSTranscribe/Tests/PSTranscribeTests/AppSettingsTests.swift`:
        - Added `"appearancePreference"` to `v12Keys` array (cleanup coverage)
        - 4 new `@Test` methods: `appearancePreferenceDefaultsToSystem`, `roundTrip_appearancePreferenceLight`, `roundTrip_appearancePreferenceDark`, `appearancePreferenceMissingKeyFallsBackToSystem`
    - Created `PSTranscribe/Tests/PSTranscribeTests/PreferredColorSchemeGrepGateTests.swift`:
        - 3 new `@Test` methods: `preferredColorSchemeOnlyAtAppRootReadingFromSettings`, `noPreferredColorSchemeOutsideAppRoot`, `settingsViewContainsAppearancePicker`
        - Pattern: cwd-relative file IO via `URL(fileURLWithPath: "Sources/PSTranscribe/...")` + `String(contentsOf:)` (mirrors `WorkflowSecretsTests.swift` Phase 24 precedent)
        - Phase-25-introduced `stripPreviewBlocks(_:)` helper (linear scan + brace counting) so dev-tool `#Preview` blocks don't pollute production-code grep gates
    - Ran `cd PSTranscribe && swift test --filter AppSettingsTests` — exits 0
    - Ran `cd PSTranscribe && swift test --filter PreferredColorSchemeGrepGateTests` — exits 0
    - Ran full `cd PSTranscribe && swift test` — exits 0 (no regressions)

    ### Notes

    - **D-01 cite-only posture:** Phase 23's 15 snapshot baselines (`PSTranscribe/Tests/PSTranscribeTests/__Snapshots__/`) prove static palette correctness across light/dark/system but are NOT extended in Phase 25. WITHDRAWN rows for REQ-21.2b/21.3b/21.5 cite `21-VERIFICATION.md` (and `21-HUMAN-UAT.md` for REQ-21.5).
    - **D-02 PARTIAL `a`/`b` split:** Phase-25-introduced convention. REQ-21.2 cleaved into `21.2a` (testable structural: app-root grep + source contract) and `21.2b` (WITHDRAWN visual: runtime override re-renders surfaces). REQ-21.3 cleaved into `21.3a` (testable structural: SettingsView Picker existence) and `21.3b` (WITHDRAWN visual: live re-render on selection change). Both halves keep the original SPEC ID prefix in adjacent rows so SPEC traceability is preserved.
    - **D-03 cross-ref to Phase 26:** Out-of-scope section above names QA-06/07/08/09 explicitly and links to `26-UAT.md` (file does not yet exist; Phase 26 produces it). The cross-ref is forward — readers landing on `21-VALIDATION.md` before Phase 26 ships see "produced when Phase 26 ships" disambiguator.
    - **REQ-21.4 single-`@Test` policy:** Same `@Test` (`preferredColorSchemeOnlyAtAppRootReadingFromSettings`) satisfies both REQ-21.2a (location) and REQ-21.4 (source contract reads from AppSettings) per Phase 21 D-06 relaxed grep gate. The companion `noPreferredColorSchemeOutsideAppRoot` test enforces the forbidden-elsewhere half — refactor tripwire.
    ```

    No additional content below the Audit Notes section. Verify the diff is exactly one new file (no edits to other files in this task).
  </action>
  <verify>
    <automated>test -f .planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md && grep -E "^(phase|slug|status|nyquist_compliant|wave_0_complete|created|last_audited):" .planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md && grep -c "Validation Audit 2026-05-07" .planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md && grep -c "WITHDRAWN" .planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md && grep -c "Out of scope: Post-fix titlebar bridge" .planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md && cd PSTranscribe && swift test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md`
    - `grep -c "^phase: 21$" .planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md` returns 1
    - `grep -c "^slug: appearance-override$" .planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md` returns 1
    - `grep -c "^status: approved$" .planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md` returns 1
    - `grep -c "^nyquist_compliant: true$" .planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md` returns 1
    - `grep -c "^wave_0_complete: true$" .planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md` returns 1
    - `grep -c "^last_audited: 2026-05-07$" .planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md` returns 1
    - `grep -c "Validation Audit 2026-05-07" .planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md` returns 1
    - `grep -c "Out of scope: Post-fix titlebar bridge (CR-01)" .planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md` returns 1
    - All 6 SPEC reqs covered (with PARTIAL split): `grep -cE '\| REQ-21\.(1|2a|2b|3a|3b|4|5|6) \|' .planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md` returns 8
    - WITHDRAWN row count matches D-01/D-02: `grep -cE '\| (REQ-21\.2b|REQ-21\.3b|REQ-21\.5) \| WITHDRAWN \|' .planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md` returns 3
    - `grep -c "Source: 21-VERIFICATION.md" .planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md` returns at least 3 (one per WITHDRAWN row)
    - `grep -c "26-UAT.md" .planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md` returns at least 1 (D-03 cross-ref)
    - `grep -cE 'QA-0(6|7|8|9)' .planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md` returns 4 (one per Phase 26 QA scenario named)
    - `grep -cE 'swift test --filter (AppSettingsTests|PreferredColorSchemeGrepGateTests)' .planning/milestones/v1.2-phases/21-appearance-override/21-VALIDATION.md` returns at least 5 (one per UNIT row)
    - `cd PSTranscribe && swift test` exits 0 (full suite green; no regressions; new test count grew by 7 — 4 in AppSettingsTests + 3 in PreferredColorSchemeGrepGateTests)
  </acceptance_criteria>
  <done>21-VALIDATION.md created with 8 rows + Phase 26 cross-ref; full Swift Testing suite green locally; ready to commit + close plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → filesystem | Tests read source files via `URL(fileURLWithPath:)` from `Sources/PSTranscribe/...`; no writes; no network |
| Test process → UserDefaults | `AppSettingsTests` clears + writes `appearancePreference` key in `UserDefaults.standard`; `.serialized` annotation prevents cross-test races |

## STRIDE Threat Register

ASVS Level 1. Phase 25 ships only Swift Testing test code + Markdown VALIDATION.md files. No new production code, no I/O beyond reading committed source files, no auth, no network calls, no untrusted input. Surface is minimal; all categories evaluated.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-01 | Spoofing | n/a (no auth, no identity boundary in test code) | accept | N/A — test-only code; no identity checks |
| T-25-01-02 | Tampering | If a future commit silently removes `.preferredColorScheme(settings.appearancePreference.colorScheme)` from `PSTranscribeApp.swift`, the Phase 21 contract breaks | mitigate | `preferredColorSchemeOnlyAtAppRootReadingFromSettings` test asserts at least one matching call-site exists AND each reads from AppSettings; CI gate fails on violation. `noPreferredColorSchemeOutsideAppRoot` test fails if a forced override is reintroduced in any view file |
| T-25-01-03 | Repudiation | n/a — Phase 25 is audit-only; no state changes in production code, all evidence in committed VALIDATION.md + test files | accept | N/A — full audit trail in git history (commit message references NYQUIST-07; SUMMARY.md frontmatter carries `requirements-completed: [NYQUIST-07]` per PROCESS-01) |
| T-25-01-04 | Information Disclosure | Tests read source files at `Sources/PSTranscribe/...`; all are committed source — no secrets, no config | accept | Source files contain no secrets (verified by Phase 24 `WorkflowSecretsTests`); test reads do not exfiltrate to network |
| T-25-01-05 | Denial of Service | n/a — test-only; runs in CI sandbox with macos-26 runner timeouts | accept | N/A — `swift test` has its own runner timeout; new tests are pure file reads + UD round-trips, sub-second runtime |
| T-25-01-06 | Elevation of Privilege | `@testable import PSTranscribe` exposes internal symbols (e.g., `AppearancePreference`); test target only — never compiled into the shipped app | accept | N/A — test bundle is not distributed; `@testable` is the standard Swift Testing convention for exercising internal types |

No high-severity threats. All dispositions are `accept` (minimal surface) or `mitigate` (CI gate via existing Swift Testing assertions).
</threat_model>

<verification>
- `swift test --filter AppSettingsTests` exits 0 with all existing tests + 4 new @Test methods passing
- `swift test --filter PreferredColorSchemeGrepGateTests` exits 0 with 3 passing tests
- Full `swift test` exits 0 (no regressions in pre-Phase-25 suite)
- `21-VALIDATION.md` frontmatter shows `phase: 21`, `slug: appearance-override`, `status: approved`, `nyquist_compliant: true`, `wave_0_complete: true`, `last_audited: 2026-05-07`
- `21-VALIDATION.md` Per-Task Verification Map contains 8 rows covering REQ-21.1, REQ-21.2a, REQ-21.2b, REQ-21.3a, REQ-21.3b, REQ-21.4, REQ-21.5, REQ-21.6
- `21-VALIDATION.md` WITHDRAWN rows (REQ-21.2b, REQ-21.3b, REQ-21.5) each cite `21-VERIFICATION.md` (REQ-21.5 also cites `21-HUMAN-UAT.md`)
- `21-VALIDATION.md` `## Out of scope: Post-fix titlebar bridge (CR-01)` section names QA-06/07/08/09 and references `26-UAT.md` with "produced when Phase 26 ships" disambiguator
- No production source code changes (per Phase 25 D-01 cite-only — Phase 25 is audit, not fix; if a planner finds REQ-21.x broken in source, escalate)
- Both REQ-21.2a and REQ-21.4 cite the SAME `@Test` method (`preferredColorSchemeOnlyAtAppRootReadingFromSettings`) — Phase 21 D-06 single-assertion-for-both-reqs convention
</verification>

<success_criteria>
1. `AppSettingsTests.swift` extended with 4 new `@Test` methods + `"appearancePreference"` added to `v12Keys`; `swift test --filter AppSettingsTests` passes
2. `PreferredColorSchemeGrepGateTests.swift` created with 3 new `@Test` methods; `swift test --filter PreferredColorSchemeGrepGateTests` passes; file is ready to be extended by Plan 25-02
3. `21-VALIDATION.md` created at `status: approved` / `nyquist_compliant: true` with 8 verification map rows (5 UNIT + 3 WITHDRAWN) + Phase 26 cross-ref out-of-scope section
4. Full `swift test` suite green locally (no regressions; suite count grew by 7)
5. PROCESS-01 frontmatter (`requirements-completed: [NYQUIST-07]`) on the eventual SUMMARY.md
6. NYQUIST-07 closed: ROADMAP.md success criterion 2 ("`21-VALIDATION.md` exists for v1.2 Phase 21 with assertions covering `colorScheme: ColorScheme?` bridge, AppSettings persistence, three Scene-root call-sites, `DictationWindowController.NSAppearance(named:)` mirror, Chronicle titlebar bridge — tests run green") satisfied (Chronicle titlebar bridge cross-referenced to Phase 26 per D-03)
</success_criteria>

<output>
After completion, create `.planning/phases/25-nyquist-sweep-v1-2/25-01-SUMMARY.md` with:
- Frontmatter: `requirements-completed: [NYQUIST-07]` (PROCESS-01 in effect)
- What shipped: AppSettingsTests extension (4 @Test) + PreferredColorSchemeGrepGateTests creation (3 @Test) + 21-VALIDATION.md
- D-01 referenced explicitly (cite-only snapshot posture)
- D-02 referenced explicitly (PARTIAL `a`/`b` split convention introduced; REQ-21.2 / REQ-21.3 cleaved)
- D-03 referenced explicitly (Phase 26 cross-ref for titlebar bridge; no @Test code added for the bridge)
- Note that PreferredColorSchemeGrepGateTests.swift is shared with Plan 25-02 — Plan 25-02 extends this file

Commit message style: NO Claude attribution (per user-level CLAUDE.md). Subject: `feat(25-01): backfill Phase 21 Nyquist coverage (NYQUIST-07)` or similar; body explains the cite-only + PARTIAL split + Phase 26 cross-ref deltas.
</output>
