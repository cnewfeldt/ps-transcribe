# Milestone v1.2: Standalone Dictation + Model Auto-Update + Dark Mode Parity

**Status:** SHIPPED 2026-05-01
**Phases:** 16 -- 21 (Phase 19 removed from scope at close)
**Total Plans:** 32 plans across 6 executed phases
**Tag:** `v1.2`
**Released:** v2.2.0 macOS app + ongoing marketing site
**Dates:** 2026-04-27 -> 2026-05-01

## Overview

Make PS Transcribe useful as a standalone dictation tool -- hotkey-triggered capture writing to clipboard and/or a plain OS folder with no Obsidian/Notion required -- let the ASR model update without shipping a new app build, and bring the macOS app to full dark-mode parity so every surface respects the system appearance. Phase 21 added a user-controlled appearance preference override on top of Phase 20's system-following baseline.

## Phases

### Phase 16: Foundation

**Goal:** Internal scaffolding so Phases 17 and 18 can compile and build without conflicts.
**Depends on:** Nothing (first phase of v1.2)
**Plans:** 4/4 complete (2026-04-27)

- [x] 16-01: Models.swift enums (`SessionType.dictation`, `DictationOutputMode`, `DictationHotkeyMode`); 6 switch-site updates
- [x] 16-02: Six v1.2 `AppSettings` keys with `didSet` UserDefaults mirroring + persistence tests
- [x] 16-03: New `DictationLogger` actor (plain markdown, no YAML frontmatter, ms-suffix filenames)
- [x] 16-04: `LibraryStore` lifted to `PSTranscribeApp` scope + new `SessionCoordinator` with computed `anySessionActive` + `ContentView` injection + manual smoke test

### Phase 17: Model Auto-Update

**Goal:** Users can check for and install FluidAudio model updates from Settings without waiting for a Sparkle app release.
**Depends on:** Phase 16
**Requirements:** MODEL-01..MODEL-10
**Plans:** 5/5 complete (2026-04-28)

- [x] 17-01: `ModelUpdateService` core (manifest fetch, version compare, 24h throttle, `min_app_version` gate)
- [x] 17-02: Staging download + SHA-256 verify + atomic swap + cancel cleanup + disk-space preflight
- [x] 17-03: `TranscriptionEngine.reloadModels()` hot-swap + apply-deferred-while-session-active + version persistence + rollback on reload failure
- [x] 17-04: Settings > Speech Model section (4 UI states: upToDate / updateAvailable / downloading / failed) + auto-update toggle + manual "Check for Updates" button
- [x] 17-05: D-14 backfill (existing model files but empty `installedModelVersion`) + manifest generation script

**Manifest publication:** `cnewfeldt/ps-transcribe-releases` repo, file `model-manifest.json`, served via raw GitHub URL. Live in production since v2.2.0.

### Phase 18: Hotkey Dictation + Plain-Folder Output

**Goal:** Users can trigger dictation from any app with a global hotkey, see a live floating HUD, and have the transcript written to clipboard and/or a plain OS folder.
**Depends on:** Phase 16
**Requirements:** DICT-01..DICT-11 + initial FOLDER-01..FOLDER-05
**Plans:** 8/8 complete (2026-04-28)

- [x] 18-01: Wave 0 RED test scaffolding (16 test files, Nyquist gate)
- [x] 18-02: KeyboardShortcuts dependency + `GlobalHotkeyService` (Cmd+Shift+D registration via `RegisterEventHotKey`)
- [x] 18-03: `DictationLogger.discardSession()` + `hasActiveSession` (D-08 atomic cancel support)
- [x] 18-04: `DictationCoordinator` skeleton + `SessionCoordinator.dictation` slot (DICT-11 mutual exclusion)
- [x] 18-05: `DictationWindowController` + `DictationHUD` (NSPanel + SwiftUI body, `sharingType=.none`)
- [x] 18-06: Full begin/end/cancel flow (clipboard with privacy markers, library entry, auto-name, fallback)
- [x] 18-07: Settings > Dictation section (hotkey recorder, mode picker, folder picker, restore-delay stepper)
- [x] 18-08: App-scope wiring (MenuBarExtra `mic.fill`/`book.closed` pulse, Esc-key monitor, eager pre-warm gated on hotkeyAssigned)

**Note:** Phase 18's original plain-folder data flow had a gap (file body empty -- DictationCoordinator never called `dictationLogger.append`). The architectural refactor in Phase 18.1 retired the broken path and routed dictation through `SaveDestinations` + `LocalFileWriter` instead.

### Phase 18.1: Shared save destinations + Local File (INSERTED)

**Goal:** Refactor save destinations into a top-level shared layer (Notion / Obsidian / Local File) so all content producers (meeting recording, voice memo, dictation) emit `(content, sessionType)` to a single `SaveDestinations` fan-out; retire `DictationOutputMode` and the dictation-private folder picker; add Local File destination peer to Notion/Obsidian; collapse Obsidian to a single folder + frontmatter tagging.
**Depends on:** Phase 18
**Requirements:** FOLDER-01 (reframed via Local File), FOLDER-02 (Local File `Dictation/` subfolder), FOLDER-03, FOLDER-05, DICT-05 (always-on clipboard), DICT-09 (privacy markers); FOLDER-04 REMOVED (replaced by always-on clipboard + destination fan-out)
**Plans:** 9/9 complete (2026-04-30; gap-closure plans 07-09 closed UAT-discovered issues)

- [x] 18.1-01: `AppSettings` rip-and-replace (delete legacy keys, add four shared destination keys, delete `DictationOutputMode` enum)
- [x] 18.1-02: `SaveDestinations` + `LocalFileWriter` + `ObsidianWriter` + `SessionType` destination-mapping extension
- [x] 18.1-03: `SettingsView` restructure (Local File / Obsidian / Notion top-level + behavior-only Dictation)
- [x] 18.1-04: `DictationCoordinator` refactor (always-on clipboard, `SaveDestinations` fan-out)
- [x] 18.1-05: `ContentView` meeting/memo refactor (Local File subfolder routing, D-20 zero-destinations guard, post-session fan-out)
- [x] 18.1-06: Phase 18 sibling test migration sweep + full `swift test` green gate
- [x] 18.1-07: NSOpenPanel title + prompt set in `chooseFolder` helper for legible folder picker UX (UAT #1)
- [x] 18.1-08: `DictationHUDBinding` wrapper replaces frozen snapshot-at-attach (UAT #2)
- [x] 18.1-09: `DestinationGuardErrors.noDestinationsConfigured` constant + `.onChange` exact-match auto-clear (UAT #3)

### Phase 19: Integration & Hardening -- REMOVED from scope (2026-05-01)

**Status:** Removed at v1.2 milestone close.

**Original scope absorbed by other phases:**
- Mutual exclusion (meeting / dictation / model-update apply) -> covered by Phase 18 `SessionCoordinatorMutualExclusionTests` + Phase 18.1 destination integration
- Model rollback simulation -> covered by Phase 17 `ModelUpdateServiceTests` (`failedReloadRollsBack`, `checksumMismatchRollsBack`)
- Privacy-mode HUD verification (`NSWindowSharingType.none`) -> covered by Phase 18 DICT-10 at `DictationWindowController.swift:47`
- SettingsView UX audit (three-folder-picker coherence) -> SUPERSEDED by Phase 18.1 (Local File / Obsidian / Notion top-level destinations)

**Deferred to a later milestone:** "Looks Done But Isn't" QA checklist (clipboard history under Maccy/Alfred live test, multi-monitor HUD positioning, security-scoped bookmark survival across relaunch, disk-space preflight warning UAT). Tracked as `QA-FUT-01`.

App shipped v2.2.0 on 2026-05-01 without the QA checklist.

### Phase 20: Dark mode parity

**Goal:** Remove all forced `.preferredColorScheme` overrides and convert the Chronicle design system to a unified light+dark token palette so every macOS surface re-renders correctly when the user toggles System Settings > Appearance, with the light-mode appearance remaining pixel-stable.
**Depends on:** Phase 18.1
**Requirements:** REQ-20.1..REQ-20.6
**Plans:** 3/3 complete (2026-05-01)

- [x] 20-01: Token system foundation -- `Color(light:dark:)` helper + 17 Chronicle tokens adaptive + 11 legacy tokens promoted + TranscriptView legacy block deleted (Wave 1)
- [x] 20-02: Call-site audit across 10 surfaces -- `warningTint`/`warningInk`/`errorTint`/`overlayDim`/`glassRule` added; 6 inline literals tokenized; DictationHUD partial-text adaptive (Wave 2)
- [x] 20-03: Override removal + D-10 UAT -- deleted `.preferredColorScheme` in `ContentView` + `NotionTagSheet`; full Light <-> Dark <-> Auto UAT signed off; screenshot capture waived per user (Wave 3)

### Phase 21: User-controlled appearance preference

**Goal:** Add a three-way `AppearancePreference` (System / Light / Dark) to `SettingsView` that overrides the macOS system color scheme app-wide; default `.system` preserves Phase 20's system-following behavior byte-for-byte; preference persists via `AppSettings` -> `UserDefaults` and is applied at every SwiftUI scene root in `PSTranscribeApp.body`; `DictationWindowController` mirrors the preference onto its NSPanel via `NSAppearance` because the HUD lives outside the SwiftUI scene graph.
**Depends on:** Phase 20
**Requirements:** APPEARANCE-01..APPEARANCE-07 (SPEC-locked via `21-SPEC.md`; 11 SPEC criteria + 7 ROADMAP success criteria)
**Plans:** 3/3 complete (2026-05-01)

- [x] 21-01: `AppearancePreference` enum + `appearancePreference` property in `AppSettings.swift` (Wave 1, autonomous)
- [x] 21-02: Three `.preferredColorScheme` call-sites in `PSTranscribeApp.body` + NSPanel appearance observation in `DictationWindowController` + `Section("Appearance")` Picker at top of `SettingsView` (Wave 2, autonomous)
- [x] 21-03: Manual UAT (live-toggle, persistence, fresh-install, default-state visual parity, D-07 HUD) + relaxed D-06 grep-gate audit (Wave 3, autonomous: false)

**Post-fix Chronicle titlebar bridge:** Code review caught the AppKit-owned main-window titlebar fighting the Dark override (cream `#FAFAF7` background painted unconditionally). Fix landed in commits `197043b` -> `ee28500` -> `4b51319` -> `ce79965`: `window.appearance` now follows preference, cream paint gated on resolved `effectiveAppearance == .aqua`, toolbar title uses dynamic `NSColor.labelColor`, `observeChronicleTitlebar` re-applies on every preference change.

---

## Milestone Summary

**Decimal phases:**
- Phase 18.1: Shared save destinations + Local File (inserted after Phase 18 to absorb architectural feedback during Phase 18 UAT)

**Removed phases:**
- Phase 19: Integration & Hardening (scope absorbed by 17/18/18.1; QA checklist deferred to `QA-FUT-01`)

**Key Decisions:**
- v1.2 phases continue v1.1's numbering (first v1.2 phase = 16; phase 15 was reverted in v1.1)
- Marketing site lives in `/website` subdirectory of this repo (carried from v1.1)
- KeyboardShortcuts via `RegisterEventHotKey` (no Accessibility or Input Monitoring permission required; App Store compatible)
- Dictation uses a separate `TranscriptionEngine` instance owned by `DictationCoordinator` (Option B from architecture research)
- Manifest hosting: project-owned JSON at `cnewfeldt/ps-transcribe-releases` (per-file SHA-256 + `min_app_version` support)
- Model swap: atomic `FileManager.moveItem` (intentional deviation from `replaceItem` -- documented in 17-03-SUMMARY.md as APFS rollback safety fix)
- Save destinations: top-level shared layer (Local File / Obsidian / Notion) -- features fan out, never own a folder picker (Phase 18.1 architectural pivot)
- Default `AppearancePreference` is `.system` (Phase 21) -- preserves Phase 20 byte-for-byte; `colorScheme: nil` is a SwiftUI no-op
- Chronicle titlebar bridge added post-Phase-21 to fix CR-01 hardcoded `#FAFAF7` fighting Dark override

**Issues Resolved:**
- Plain-folder file body empty bug (Phase 18) -> retired via Phase 18.1 SaveDestinations refactor
- DictationHUD frozen snapshot at attach (Phase 18.1 UAT #2) -> `DictationHUDBinding` wrapper
- D-20 error one-way trap (Phase 18.1 UAT #3) -> `.onChange` exact-match auto-clear with mic-permission scope discipline
- Chronicle titlebar fighting Dark override (Phase 21 CR-01) -> `window.appearance` bridge + `bestMatch` gate

**Issues Deferred (`QA-FUT-01` and tech debt):**
- Phase 19 "Looks Done But Isn't" QA checklist (clipboard history vs. Maccy/Alfred, multi-monitor HUD, security-scoped bookmark survival, disk-space preflight warning UAT)
- Nyquist VALIDATION.md missing for Phase 20 + Phase 21
- Phase 21 titlebar bridge visual UAT (4 items, code-verified, deferred to backlog)
- Phase 18.1 transient `settings.localFileEnabled` toggle pattern -> replace with explicit `skipLocalFile` parameter on `SaveDestinations.save()`
- Phase 18.1 path validation `..` substring check -> tighten to component-level
- Phase 18.1 `isAnyDestinationEnabled` doesn't check `localFileRoot.isEmpty` -> mirror Obsidian/Notion non-empty check
- Phase 18 carry-over flake scope (3 tests, 1 listed in deferred-items.md)

**Stats:**
- Phases planned: 7 (16, 17, 18, 18.1, 19, 20, 21)
- Phases executed: 6 (Phase 19 removed)
- Total plans: 32 (4 + 5 + 8 + 9 + 3 + 3)
- Git range: `33d6baa` (2026-04-27 docs(16): research foundation phase domain) -> `cb7a35e` (2026-05-01 chore(planning): commit phase 18 / 18.1 PLAN artifacts and doc updates)
- Commits in range: 191 total (52 feat/fix)
- Files changed: 293
- LOC: 10,165 Swift + 2,687 website TS/MDX
- Timeline: 5 days (2026-04-27 -> 2026-05-01)
- Released: v2.2.0 macOS app on 2026-05-01

---

*For current project status, see `.planning/ROADMAP.md`. Audit report at `milestones/v1.2-MILESTONE-AUDIT.md`. Requirements archive at `milestones/v1.2-REQUIREMENTS.md`.*
