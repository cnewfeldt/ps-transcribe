---
phase: 03-session-management-recording-naming
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified: []
autonomous: false
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - SESS-04
  - SESS-05
  - SESS-06
  - SESS-07
  - SESS-08
  - SESS-09
  - NAME-01
  - NAME-02
  - NAME-03
  - NAME-04
  - NAME-05

must_haves:
  truths:
    - "User sees sidebar with library entries showing name, date, duration, type icon, source tag"
    - "User can start a recording, name it, stop it, and see it in the library"
    - "User can click a library entry and see the transcript"
    - "User can rename from the library sidebar and file renames on disk"
    - "Library persists across app restart"
    - "Missing files show a badge"
    - "Obsidian deep link opens the transcript in Obsidian when vault name is configured"
  artifacts: []
  key_links: []
---

<objective>
Manual verification of the complete session library and recording naming flow. Build the app, run through all user stories, and confirm every requirement is met.

Purpose: Visual and interactive verification that cannot be automated -- layout correctness, interaction flow, and end-to-end behavior.
Output: Verification results. No code changes expected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-session-management-recording-naming/03-CONTEXT.md
@.planning/phases/03-session-management-recording-naming/03-UI-SPEC.md
@.planning/phases/03-session-management-recording-naming/03-01-SUMMARY.md
@.planning/phases/03-session-management-recording-naming/03-02-SUMMARY.md
@.planning/phases/03-session-management-recording-naming/03-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end verification of session library and recording naming</name>
  <files></files>
  <read_first>
    .planning/phases/03-session-management-recording-naming/03-CONTEXT.md,
    .planning/phases/03-session-management-recording-naming/03-UI-SPEC.md
  </read_first>
  <action>
    Build the app with `swift build --package-path PSTranscribe` and run it. Walk through the following 12 test scenarios to verify the complete feature set:

    **Test 1: Empty state (SESS-01, D-04)** -- Sidebar shows "No recordings yet" with waveform icon. Detail shows "Select a recording". Top bar shows "PS TRANSCRIBE" and "Ready".

    **Test 2: Start voice memo (SESS-08, NAME-01, D-05)** -- Click Voice Memo. Top bar switches to editable "Name this recording" field with timer. Type "Test Memo". New entry appears in sidebar. Utterances appear in detail.

    **Test 3: Stop recording (SESS-07, D-08)** -- Click stop. Transcript stays visible (no clear). Entry updates with duration. "Saved" confirmation appears in top bar and fades after 2s.

    **Test 4: Persistence (SESS-09)** -- Quit and relaunch. Recording appears in sidebar.

    **Test 5: Load transcript (SESS-03, D-10)** -- Click library entry. Transcript loads in detail.

    **Test 6: Entry display (SESS-02)** -- Entry shows name, date, duration, type icon (mic), source tag, preview.

    **Test 7: Library rename (NAME-03, D-06)** -- Click name in sidebar, edit, press Enter. Name updates. File renamed on disk.

    **Test 8: Unnamed fallback (NAME-04, D-07)** -- Record without naming. Entry shows "Voice Memo -- {date}" or "Call Recording -- {date}".

    **Test 9: Show in Finder (SESS-04)** -- Right-click entry, "Show in Finder", Finder opens to file.

    **Test 10: Missing file (SESS-05)** -- Move/delete file from Finder. Badge appears with tooltip.

    **Test 11: Obsidian link (SESS-06, D-11, D-12)** -- Set vault name in Settings. Icon appears on entry. Click opens Obsidian.

    **Test 12: No overwrite (D-09)** -- View past transcript, start new recording. Detail switches to live. Past entry remains. Both are distinct.
  </action>
  <verify>
    <automated>cd /Users/cary/Development/ai-development/Tome && swift build --package-path PSTranscribe 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - App compiles and launches without errors
    - All 12 test scenarios pass visual and interactive verification
    - User types "approved" to confirm
  </acceptance_criteria>
  <done>User has verified all session library and recording naming features work correctly end-to-end.</done>
</task>

</tasks>

<verification>
All 12 test scenarios pass. No code changes needed (or issues are filed for gap closure).
</verification>

<success_criteria>
User approves the complete session library and recording naming flow after running through all test scenarios.
</success_criteria>

<output>
After completion, create `.planning/phases/03-session-management-recording-naming/03-04-SUMMARY.md`
</output>
