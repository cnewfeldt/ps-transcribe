---
phase: 09-verification-sweep-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-mic-button-model-onboarding/04-VERIFICATION.md
  - .planning/phases/07-notion-integration/07-VERIFICATION.md
autonomous: true
requirements:
  - MICB-01
  - MICB-02
  - MICB-03
  - MICB-04
  - MICB-05
  - MICB-06
  - ONBR-01
  - ONBR-02
  - ONBR-03
  - ONBR-04
  - ONBR-05
  - NOTN-01
  - NOTN-02
  - NOTN-03
  - NOTN-04
  - NOTN-05
must_haves:
  truths:
    - "Phase 4 has a VERIFICATION.md with pass/fail for all 11 requirements (MICB-01..06, ONBR-01..05)"
    - "Phase 7 has a VERIFICATION.md with pass/fail for all 5 requirements (NOTN-01..05)"
    - "Each requirement verdict includes file path and line number evidence from code inspection"
    - "Verdicts use SATISFIED/BLOCKED/HUMAN_NEEDED vocabulary matching Phases 1-3 format"
  artifacts:
    - path: ".planning/phases/04-mic-button-model-onboarding/04-VERIFICATION.md"
      provides: "Formal verification of MICB-01..06 and ONBR-01..05"
      contains: "MICB-01"
    - path: ".planning/phases/07-notion-integration/07-VERIFICATION.md"
      provides: "Formal verification of NOTN-01..05"
      contains: "NOTN-01"
  key_links:
    - from: "04-VERIFICATION.md"
      to: "ControlBar.swift, OnboardingView.swift, ContentView.swift, TranscriptionEngine.swift"
      via: "grep evidence with line numbers"
      pattern: "SATISFIED or BLOCKED"
    - from: "07-VERIFICATION.md"
      to: "KeychainHelper.swift, NotionService.swift, SettingsView.swift, LibraryEntryRow.swift, NotionTagSheet.swift"
      via: "grep evidence with line numbers"
      pattern: "SATISFIED or BLOCKED"
---

<objective>
Create formal VERIFICATION.md files for Phase 4 (Mic Button + Model Onboarding) and Phase 7 (Notion Integration) by inspecting source code against each requirement.

Purpose: These are the two phases identified by the v1.0 milestone audit as having no VERIFICATION.md. All 16 requirements (11 Phase 4 + 5 Phase 7) are "orphaned" from verification. Code inspection will produce formal verdicts with file/line evidence per D-01.

Output: Two VERIFICATION.md files matching the format established in Phases 1-3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/09-verification-sweep-tracking/09-CONTEXT.md

@.planning/phases/02-security-stability/02-VERIFICATION.md
@.planning/phases/04-mic-button-model-onboarding/04-VALIDATION.md
@.planning/phases/04-mic-button-model-onboarding/04-01-SUMMARY.md
@.planning/phases/04-mic-button-model-onboarding/04-02-SUMMARY.md
@.planning/phases/04-mic-button-model-onboarding/04-03-SUMMARY.md
@.planning/phases/07-notion-integration/07-01-SUMMARY.md
@.planning/phases/07-notion-integration/07-02-SUMMARY.md
@.planning/phases/07-notion-integration/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 4 VERIFICATION.md (MICB-01..06, ONBR-01..05)</name>
  <files>.planning/phases/04-mic-button-model-onboarding/04-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/02-security-stability/02-VERIFICATION.md (format template -- match frontmatter, Observable Truths table, Required Artifacts table, Key Link Verification table structure exactly)
    - .planning/phases/04-mic-button-model-onboarding/04-VALIDATION.md (reference as per D-03 -- note nyquist_compliant: true but perform independent checks)
    - .planning/phases/04-mic-button-model-onboarding/04-CONTEXT.md (original phase decisions and requirement definitions)
    - PSTranscribe/Sources/PSTranscribe/Views/ControlBar.swift (MicButton struct, MicState enum, pulsing ring, error state, tooltip, settings navigation)
    - PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift (hasError/activeErrors wiring, onboarding flow, model download trigger)
    - PSTranscribe/Sources/PSTranscribe/Views/OnboardingView.swift (download prompt, progress indicator, success/failure states, close button)
    - PSTranscribe/Sources/PSTranscribe/Transcription/TranscriptionEngine.swift (activeErrors, hasError, prepareModels, modelsReady, modelStatus)
    - PSTranscribe/Sources/PSTranscribe/Settings/AppSettings.swift (lastUsedSessionType)
  </read_first>
  <action>
Create `.planning/phases/04-mic-button-model-onboarding/04-VERIFICATION.md` per D-01 (code inspection only) and D-02 (match existing format). Per D-03, reference 04-VALIDATION.md but perform independent requirement checks.

**Frontmatter** (match 02-VERIFICATION.md structure -- use phase: 04-mic-button-model-onboarding, verified: 2026-04-07T00:00:00Z, status: passed, score: "11/11 must-haves verified", re_verification: false):

**For each requirement, grep the source files and record file path + line number evidence. Specific checks:**

MICB-01 (Waveform visualizer replaced with mic icon button):
- Confirm WaveformView.swift does NOT exist: `ls PSTranscribe/Sources/PSTranscribe/Views/WaveformView.swift` must fail
- Confirm no WaveformView references: `grep -r "WaveformView" PSTranscribe/Sources/` must return empty
- Confirm ControlBar.swift contains mic button: grep for `mic.fill` or `mic.slash` in ControlBar.swift
- Verdict: SATISFIED if WaveformView gone and mic button present

MICB-02 (Idle state shows static mic icon; clicking starts recording):
- grep ControlBar.swift for `case idle` or idle state handling
- grep for `mic.fill` as idle icon
- grep for the idle tap action routing to session start (look for `onStart` or `startSession` in idle branch)
- Verdict: SATISFIED if idle state renders mic.fill and tap starts recording

MICB-03 (Recording state shows green pulsing ring animation; clicking stops recording):
- grep ControlBar.swift for `PulsingRing` or `repeatForever` or `scaleEffect(ringScale)`
- grep for green color usage (`Color.green` or similar) in recording state
- grep for stop action in recording tap handler (`onStop`)
- Verdict: SATISFIED if pulsing ring animation present and tap stops recording

MICB-04 (Error state shows red mic icon with circle/slash overlay):
- grep ControlBar.swift for `mic.slash` and `recordRed` or red color in error state
- Verdict: SATISFIED if mic.slash with red color found

MICB-05 (Clicking error state opens settings pane):
- grep ControlBar.swift for settings window opening in error tap handler -- look for `NSApp.sendAction` or `showSettingsWindow` or similar in the error branch of micTapped/tap handler
- Verdict: SATISFIED if error tap opens settings

MICB-06 (Hovering error state shows tooltip):
- grep ControlBar.swift for `.help(` with error text in error state
- Verdict: SATISFIED if .help() modifier present with activeErrors/errorTooltip content

ONBR-01 (First launch shows download message):
- grep OnboardingView.swift for download prompt text (e.g., "download" or "transcription model")
- grep ContentView.swift for OnboardingView presentation gated on `!modelsReady` or similar
- Verdict: SATISFIED if onboarding view shown when model not ready

ONBR-02 (Download shows progress indicator):
- grep OnboardingView.swift for `ProgressView` in downloading state
- Verdict: SATISFIED if ProgressView present in download branch

ONBR-03 (Successful download shows success message with close button):
- grep OnboardingView.swift for success state with checkmark or "success" and a close/dismiss button
- Verdict: SATISFIED if success branch with close action found

ONBR-04 (Failed download shows error with close button):
- grep OnboardingView.swift for failure state with error message display and close/dismiss action
- Also check for "Try Again" button (retry from 04-01)
- Verdict: SATISFIED if failure branch with error text and close/retry found

ONBR-05 (Recording disabled until model downloaded):
- grep ControlBar.swift for `.disabled` modifier gated on `!modelsReady` or equivalent
- Verdict: SATISFIED if disabled modifier prevents recording without model

**Structure the document with:**
1. Title: "Phase 04: Mic Button + Model Onboarding Verification Report"
2. Phase Goal (from ROADMAP)
3. Note: "References 04-VALIDATION.md (nyquist_compliant: true) per D-03; this report performs independent requirement-by-requirement verification"
4. Observable Truths table (one row per requirement, columns: #, Requirement ID, Truth, Status, Evidence)
5. Required Artifacts table
6. Key Link Verification table
  </action>
  <verify>
    <automated>grep -c "MICB-01\|MICB-02\|MICB-03\|MICB-04\|MICB-05\|MICB-06\|ONBR-01\|ONBR-02\|ONBR-03\|ONBR-04\|ONBR-05" .planning/phases/04-mic-button-model-onboarding/04-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/04-mic-button-model-onboarding/04-VERIFICATION.md exists
    - File contains all 11 requirement IDs: MICB-01, MICB-02, MICB-03, MICB-04, MICB-05, MICB-06, ONBR-01, ONBR-02, ONBR-03, ONBR-04, ONBR-05
    - Each requirement has a verdict of SATISFIED, BLOCKED, or HUMAN_NEEDED
    - Each verdict includes at least one file path with line number as evidence
    - Frontmatter contains `phase: 04-mic-button-model-onboarding` and `verified:` and `status:` and `score:` fields
    - File contains "Observable Truths" section with a table
    - File contains "Required Artifacts" section
    - File contains "04-VALIDATION.md" reference per D-03
    - No `BLOCKED` verdicts unless code evidence genuinely shows a missing implementation
  </acceptance_criteria>
  <done>Phase 4 has a complete VERIFICATION.md with pass/fail verdicts and code evidence for all 11 requirements</done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 7 VERIFICATION.md (NOTN-01..05)</name>
  <files>.planning/phases/07-notion-integration/07-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/02-security-stability/02-VERIFICATION.md (format template)
    - PSTranscribe/Sources/PSTranscribe/Notion/KeychainHelper.swift (NOTN-01: Keychain storage for API key)
    - PSTranscribe/Sources/PSTranscribe/Notion/NotionService.swift (NOTN-02: validateDatabase, NOTN-03: sendTranscript with properties/blocks, NOTN-04: tag support, NOTN-05: duplicate via notionPageURL)
    - PSTranscribe/Sources/PSTranscribe/Views/SettingsView.swift (NOTN-01: API key input field, NOTN-02: database URL field + validation display)
    - PSTranscribe/Sources/PSTranscribe/Views/NotionTagSheet.swift (NOTN-04: tag workflow UI)
    - PSTranscribe/Sources/PSTranscribe/Views/LibraryEntryRow.swift (NOTN-05: context menu with Send/Open/Resend states)
    - PSTranscribe/Sources/PSTranscribe/Models/Models.swift (NOTN-05: notionPageURL field on LibraryEntry)
    - PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift (send flow handler, notionService wiring)
    - PSTranscribe/Tests/PSTranscribeTests/KeychainHelperTests.swift (test evidence for NOTN-01)
    - PSTranscribe/Tests/PSTranscribeTests/NotionServiceTests.swift (test evidence for NOTN-02..05)
  </read_first>
  <action>
Create `.planning/phases/07-notion-integration/07-VERIFICATION.md` per D-01 (code inspection only) and D-04 (bundle NOTN verification in one pass).

**Frontmatter** (use phase: 07-notion-integration, verified: 2026-04-07T00:00:00Z, status: passed, score: "5/5 must-haves verified", re_verification: false):

**For each requirement, grep source files and record file path + line number evidence:**

NOTN-01 (API key stored in macOS Keychain, not UserDefaults):
- grep KeychainHelper.swift for `SecItemAdd` or `SecItemCopyMatching` or `kSecClass` (Keychain API usage)
- grep SettingsView.swift for `SecureField` or API key input that calls KeychainHelper.save
- grep AppSettings.swift to confirm NO `notionAPIKey` property in UserDefaults (key must NOT be in UserDefaults)
- grep KeychainHelperTests.swift for test functions covering save/read
- Verdict: SATISFIED if Keychain storage confirmed and no UserDefaults leak

NOTN-02 (Database URL/ID validated with title display):
- grep NotionService.swift for `validateDatabase` method
- grep SettingsView.swift for database title display (e.g., `databaseTitle` or `extractDatabaseTitle`)
- grep NotionService.swift for URL parsing (extract database ID from URL)
- grep NotionServiceTests.swift for validation-related tests
- Verdict: SATISFIED if validateDatabase exists and SettingsView shows title

NOTN-03 (Send creates page with properties + transcript as content):
- grep NotionService.swift for `sendTranscript` method
- grep NotionService.swift for property names: "Title", "Date", "Duration", "Source App", "Session Type", "Speakers", "Tags" (in buildProperties or sendTranscript)
- grep NotionService.swift for `transcriptToBlocks` (converts transcript to Notion block format)
- grep NotionServiceTests.swift for transcript/block/property tests
- Verdict: SATISFIED if sendTranscript creates page with all 7 properties and transcript blocks

NOTN-04 (Tag workflow via sheet before send):
- grep NotionTagSheet.swift for tag input field and tag chips
- grep ContentView.swift for `NotionTagSheet` presentation (sheet modifier)
- grep for tags being passed to sendTranscript call
- Verdict: SATISFIED if tag sheet exists and tags flow to send

NOTN-05 (Duplicate prevention via notionPageURL; context menu shows Open/Resend after send):
- grep Models.swift for `notionPageURL` field on LibraryEntry
- grep LibraryEntryRow.swift for context menu items: "Send to Notion", "Open in Notion", "Resend"
- grep LibraryEntryRow.swift for conditional rendering based on `notionPageURL` nil vs present
- grep ContentView.swift for updating notionPageURL after successful send
- Verdict: SATISFIED if notionPageURL stored and context menu adapts

**Structure the document identically to 04-VERIFICATION.md (Task 1):**
1. Title: "Phase 07: Notion Integration Verification Report"
2. Phase Goal (from ROADMAP)
3. Observable Truths table
4. Required Artifacts table
5. Key Link Verification table
  </action>
  <verify>
    <automated>grep -c "NOTN-01\|NOTN-02\|NOTN-03\|NOTN-04\|NOTN-05" .planning/phases/07-notion-integration/07-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/07-notion-integration/07-VERIFICATION.md exists
    - File contains all 5 requirement IDs: NOTN-01, NOTN-02, NOTN-03, NOTN-04, NOTN-05
    - Each requirement has a verdict of SATISFIED, BLOCKED, or HUMAN_NEEDED
    - Each verdict includes at least one file path with line number as evidence
    - Frontmatter contains `phase: 07-notion-integration` and `verified:` and `status:` and `score:` fields
    - File contains "Observable Truths" section with a table
    - File contains "Required Artifacts" section
    - No `BLOCKED` verdicts unless code evidence genuinely shows a missing implementation
  </acceptance_criteria>
  <done>Phase 7 has a complete VERIFICATION.md with pass/fail verdicts and code evidence for all 5 requirements</done>
</task>

</tasks>

<verification>
- Both VERIFICATION.md files exist in their respective phase directories
- Combined 16 requirements all have formal verdicts
- Format matches existing Phases 1-3 VERIFICATION.md files
- `grep -l "SATISFIED\|BLOCKED\|HUMAN_NEEDED" .planning/phases/04-mic-button-model-onboarding/04-VERIFICATION.md .planning/phases/07-notion-integration/07-VERIFICATION.md` returns both files
</verification>

<success_criteria>
- Phase 4 VERIFICATION.md has verdicts for MICB-01..06 and ONBR-01..05 (11 requirements)
- Phase 7 VERIFICATION.md has verdicts for NOTN-01..05 (5 requirements)
- All verdicts backed by file path + line number evidence from code inspection
- Format consistent with existing VERIFICATION.md files in Phases 1-3
</success_criteria>

<output>
After completion, create `.planning/phases/09-verification-sweep-tracking/09-01-SUMMARY.md`
</output>
