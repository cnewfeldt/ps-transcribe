---
phase: 26.1-close-gap-process-03-lint-summaries-failure-on-v1-0-archive
gathered: 2026-05-11
status: ready-for-planning
source: v1.3-MILESTONE-AUDIT.md (PROCESS-03 blocker, closure-path step 1)
---

# Phase 26.1: Close gap — PROCESS-03 lint-summaries failure on v1.0 archive — Context

**Gathered:** 2026-05-11
**Status:** Ready for planning
**Source:** `.planning/v1.3-MILESTONE-AUDIT.md` — PROCESS-03 blocker (priority 1 of closure path)

<domain>
## Phase Boundary

**In scope (this phase):**
- Add `requirements-completed: []` YAML frontmatter key to the 8 v1.0-phase SUMMARY files currently failing `scripts/lint-summaries.sh` C1 (missing canonical key per D-07).
- Verify `bash scripts/lint-summaries.sh` exits 0 against the full SUMMARY corpus (55 files).

**Explicitly out of scope (deferred to backlog / future milestone):**
- Extending `lint-summaries.sh DEFAULT_ROOT` to also walk `.planning/phases/` (active-phase blind spot — warning only, not a current violation).
- Retro-deriving the *true* completed-requirement IDs for each v1.0-phase plan from git history. Per audit + D-04, empty list `[]` is explicitly valid; the original v1.0 phases pre-date the PROCESS-03 frontmatter standard, and a forensic backfill is not the gap closure path the audit prescribes.
- All other gaps from v1.3-MILESTONE-AUDIT.md (QA-05 model-manifest URL, CI build-check Swift 6, REQUIREMENTS.md traceability cleanup, QA-03b disposition). Each routes through its own phase or todo.

</domain>

<decisions>
## Implementation Decisions

### Closure mechanism (LOCKED — from audit)
- **D1:** Add `requirements-completed: []` (empty list literal) to each of the 8 affected SUMMARY files. Empty list is explicitly valid per `lint-summaries.sh` rule D-04.
- **D2:** Do NOT attempt to retro-derive a populated list of REQ-IDs. The 8 files are v1.0-era SUMMARYs that pre-date the Phase 22 PROCESS-03 frontmatter standard; the audit's prescribed closure is the empty-list form, not historical backfill.
- **D3:** Preserve all existing frontmatter fields and ordering. Insert the new key in a stable location (suggested: immediately after the `tags:` line, before `dependency_graph` / `requires` / `provides` blocks). Insertion location is "Claude's Discretion" so long as the key appears inside the `---`-delimited YAML frontmatter block.

### Affected files (LOCKED — from audit + lint output)
The exact 8 files reported by `bash scripts/lint-summaries.sh` as `[FAIL C1]`:

1. `.planning/milestones/v1.0-phases/01-rebrand/01-01-SUMMARY.md`
2. `.planning/milestones/v1.0-phases/02-security-stability/02-01-SUMMARY.md`
3. `.planning/milestones/v1.0-phases/03-session-management-recording-naming/03-01-SUMMARY.md`
4. `.planning/milestones/v1.0-phases/03-session-management-recording-naming/03-02-SUMMARY.md`
5. `.planning/milestones/v1.0-phases/03-session-management-recording-naming/03-03-SUMMARY.md`
6. `.planning/milestones/v1.0-phases/03-session-management-recording-naming/03-04-SUMMARY.md`
7. `.planning/milestones/v1.0-phases/08-code-defect-fixes/08-01-SUMMARY.md`
8. `.planning/milestones/v1.0-phases/08-code-defect-fixes/08-02-SUMMARY.md`

All 8 sibling `*-PLAN.md` files exist on disk (C3 subset check will not fire — empty list is trivially a subset of any set).

### Verification gate (LOCKED — non-negotiable)
- **G1:** `bash scripts/lint-summaries.sh` exits 0 after the migration. Final output line must read `55 SUMMARY files checked, 0 failures, 0 warnings` (or equivalent OK marker per script).
- **G2:** No regressions — the other 47 currently-passing SUMMARY files must remain passing (lint count stays at 55).
- **G3:** No content changes outside YAML frontmatter — body of each SUMMARY untouched (`git diff` for body sections is empty).

### Plan structure (LOCKED)
- **D4:** Single PLAN, single wave — this is the audit-prescribed "Single closure plan (≤30 min)" path. No parallel waves needed. One task per file is acceptable but a single bulk migration task is preferred (atomic commit, single verification).
- **D5:** Commit at the end of the plan — one commit titled `fix(26.1): add requirements-completed: [] to 8 v1.0-archive SUMMARYs (PROCESS-03 closure)` (or similar). NO Claude attribution footer per project CLAUDE.md.

### Claude's Discretion
- Exact YAML-frontmatter insertion line for each file (after `tags:`, before `dependency_graph` — but any location inside the `---`/`---` block is valid).
- Whether to use one bulk task or one task per file (8 files × ~1 min each is well under the single-task wave budget; bulk preferred).
- Format of the empty list — `[]` is canonical; `requirements-completed:` with a blank line and no items is also valid YAML. Either is acceptable; `[]` is shorter and unambiguous.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Audit & gap context
- `.planning/v1.3-MILESTONE-AUDIT.md` — Phase 26.1's reason for existing. PROCESS-03 row at line 13–31, closure path at line 234.
- `.planning/REQUIREMENTS.md` — PROCESS-03 requirement definition.

### Lint script & rules (D-01..D-10)
- `scripts/lint-summaries.sh` — The gate this phase must flip green. Rules D-01 (canonical hyphen key), D-04 (empty list is valid), D-07 (subset check), and checks C1..C4 are all defined in the script header.
- `.github/workflows/lint-summaries.yml` — CI workflow that triggers the script on PR.

### Phase 22 origin
- `.planning/phases/22-process-frontmatter-standard/22-04-PLAN.md`, `22-05-PLAN.md`, `22-06-PLAN.md` — Original PROCESS-03 implementation plans (for context only; do NOT re-execute).

### Affected SUMMARY files (all 8, listed above in Decisions D2)
Plus their sibling PLAN.md files (for C3 reference; no edits to PLAN.md):
- `.planning/milestones/v1.0-phases/{01-rebrand,02-security-stability,03-session-management-recording-naming,08-code-defect-fixes}/*-PLAN.md`

</canonical_refs>

<specifics>
## Specific Ideas

- The audit explicitly characterizes this as "Single closure plan (≤30 min)". Treat that as the upper bound on plan complexity.
- Lint script behavior to verify against:
  - Pre-fix: `55 SUMMARY files checked, 8 failures, 0 warnings` → exits 1
  - Post-fix: `55 SUMMARY files checked, 0 failures, 0 warnings` → exits 0
- Phase 22 commit history can be reviewed if the executor needs reference for the canonical insertion location of `requirements-completed:` in newly-conforming files, but the audit makes clear that location-within-frontmatter is the only constraint.

</specifics>

<deferred>
## Deferred Ideas

- **Lint scope extension to `.planning/phases/`** — surfaced in audit (warning severity, blind spot). Not part of this phase. Route via `/gsd-capture` to a backlog item or future process-hardening milestone.
- **Forensic backfill of v1.0-era REQ-IDs** — would produce more accurate per-plan traceability for the v1.0 archive but is explicitly NOT the closure path the audit prescribes. If desired, file as a separate research-and-backfill phase against the v1.0 milestone.
- **Other v1.3 audit items** (QA-05, ci-build-check, REQUIREMENTS.md traceability, QA-03b) — each has its own routing in the audit's "Recommended Closure Path" section.

</deferred>

---

*Phase: 26.1-close-gap-process-03-lint-summaries-failure-on-v1-0-archive*
*Context gathered: 2026-05-11 from v1.3-MILESTONE-AUDIT.md (skipping /gsd-discuss-phase — locked-decision audit doc is the equivalent input)*
