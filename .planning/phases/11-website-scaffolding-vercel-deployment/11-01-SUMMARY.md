---
phase: 11
plan: 01
subsystem: website-scaffolding
tags:
  - nextjs
  - scaffolding
  - pnpm
  - typescript
  - monorepo
requirements_completed:
  - SITE-01
  - SITE-05
dependency_graph:
  requires: []
  provides:
    - /website Next.js 16 App Router + TypeScript scaffold
    - pnpm-lock.yaml (Vercel pnpm auto-detect anchor)
    - website/.nvmrc + engines.node (Node 22 pin, both layers)
    - .gitignore rules for website/.next, website/node_modules, website/.vercel, website/out
  affects:
    - Plan 11-02 (content + metadata) — rewrites layout.tsx / page.tsx on top of this scaffold
    - Plan 11-03 (Vercel deploy) — needs pnpm-lock.yaml + engines.node to ship
    - Phases 12/13/14/15 — all build on the /website directory this plan creates
tech_stack:
  added:
    - next@16.2.4
    - react@19.2.4
    - react-dom@19.2.4
    - typescript@5.9.3
    - tailwindcss@4.2.4
    - "@tailwindcss/postcss@4.2.4"
    - eslint@9.39.4
    - eslint-config-next@16.2.4
    - "@types/node@20.19.39"
    - "@types/react@19.2.14"
    - "@types/react-dom@19.2.3"
  patterns:
    - App Router (src/app/*)
    - TypeScript strict mode
    - "@/* path alias → src/*"
    - Turbopack dev + build (Next.js 16 default)
    - pnpm as package manager (Vercel auto-detect via pnpm-lock.yaml)
    - Node 22 pinned in BOTH .nvmrc and engines.node
key_files:
  created:
    - path: website/package.json
      purpose: Node package manifest (deps, scripts, engines.node)
    - path: website/pnpm-lock.yaml
      purpose: Committed pnpm lockfile (Vercel pnpm auto-detect anchor)
    - path: website/tsconfig.json
      purpose: TypeScript strict config with @/* → ./src/*
    - path: website/.nvmrc
      purpose: Local Node auto-switch pin (value 22)
    - path: website/next.config.ts
      purpose: Next.js config (empty scaffold default)
    - path: website/postcss.config.mjs
      purpose: PostCSS plumbing for Tailwind 4
    - path: website/eslint.config.mjs
      purpose: ESLint flat config (Next.js 16 default)
    - path: website/src/app/layout.tsx
      purpose: Default root layout (rewritten in plan 11-02)
    - path: website/src/app/page.tsx
      purpose: Default home page (rewritten in plan 11-02)
    - path: website/src/app/globals.css
      purpose: Tailwind directives + default CSS
    - path: website/src/app/favicon.ico
      purpose: Default favicon (replaced by icon.png in plan 11-02)
    - path: website/public/
      purpose: Static asset directory (file.svg, globe.svg, next.svg, vercel.svg, window.svg defaults)
    - path: website/.gitignore
      purpose: Next.js-local gitignore generated by create-next-app
    - path: website/AGENTS.md
      purpose: Auto-generated Next.js 16 agent guidance pointing to versioned docs
    - path: website/CLAUDE.md
      purpose: Claude Code pointer referencing AGENTS.md
    - path: website/README.md
      purpose: Default create-next-app README
    - path: website/pnpm-workspace.yaml
      purpose: pnpm workspace config (ignoredBuiltDependencies for sharp + unrs-resolver)
  modified:
    - path: .gitignore
      purpose: Appended Next.js / website build artifacts block (4 paths)
decisions:
  - Committed create-next-app auto-generated AGENTS.md and CLAUDE.md — not in the plan's files list but produced by --yes
  - Honored create-next-app 16.2.4 default of ignoring next-env.d.ts (file is auto-regenerated on every build); RESEARCH.md Open Question 3 suggested committing it, but the framework's own default is a stronger signal than a pre-execution recommendation
metrics:
  duration: "~2.5m (149s wall clock)"
  completed_date: "2026-04-22"
  tasks_completed: 2
  files_created: 20
  files_modified: 1
---

# Phase 11 Plan 01: Scaffold Summary

Next.js 16.2.4 App Router scaffold landed at `/website` via non-interactive `create-next-app`, with Node 22 pinned in both `.nvmrc` (local auto-switch) and `package.json` engines (`>=22 <23`, Vercel's source of truth). Repo root `.gitignore` extended with the four `website/` build-artifact paths. `pnpm build` passes locally; `swift build` still passes as the guardrail probe.

## What Shipped

### Scaffold (Task 1)
- `pnpm create next-app@latest website` with the exact flag set from the plan — `--typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-pnpm --turbopack --no-react-compiler --disable-git --yes`
- Resolved versions: Next.js **16.2.4**, React 19.2.4, React-DOM 19.2.4, TypeScript 5.9.3, Tailwind 4.2.4, ESLint 9.39.4, eslint-config-next 16.2.4
- TypeScript strict mode enabled (`"strict": true` in `tsconfig.json`)
- `@/*` alias maps to `./src/*`
- `pnpm-lock.yaml` only — no `package-lock.json`, no `yarn.lock`
- No nested `.git/` inside `/website` (the `--disable-git` flag worked)
- 20 files added under `website/` on the first commit

### Node pin + gitignore (Task 2)
- `website/.nvmrc` created with exactly `22\n`
- `website/package.json` gained a top-level `"engines": { "node": ">=22 <23" }` block placed between `"private"` and `"scripts"`
- Repo root `.gitignore` appended with a `# Next.js / website build artifacts` block covering the four paths
- All pre-existing `.gitignore` entries intact (verified: `.DS_Store`, `.env`, `SECURITY-SCAN.md` each appear exactly once)

### Verification Run
| Probe                  | Command                             | Result        |
|------------------------|-------------------------------------|---------------|
| Next.js production build | `cd website && pnpm build`         | Exit 0 in 3.0s total |
| TypeScript strict check  | (part of `pnpm build`)              | "Finished TypeScript in 797ms" |
| Swift guardrail          | `cd PSTranscribe && swift build`    | Exit 0 in 20.85s (`Build complete!`) |
| No tracked artifacts     | `git ls-files website/.next website/node_modules` | 0 entries |

## Commits

| Task | Commit    | Message                                                      |
|------|-----------|--------------------------------------------------------------|
| 1    | `666b75b` | feat(11-01): scaffold Next.js 16 App Router at /website      |
| 2    | `02fe936` | chore(11-01): pin Node 22 and ignore website build artifacts |

## Key Decisions

- **Committed auto-generated `AGENTS.md` + `CLAUDE.md`:** create-next-app 16.2.4 with `--yes` produces both — `AGENTS.md` warns future agents that Next 16 has breaking changes vs training data, and `CLAUDE.md` just `@AGENTS.md`-references it. These weren't in the plan's explicit files list, but they are part of the authoritative scaffold output and will be helpful for every future plan that touches `/website`. Committed as part of Task 1.
- **Committed auto-generated `pnpm-workspace.yaml`:** create-next-app emits this to mark `sharp` and `unrs-resolver` as ignoredBuiltDependencies. Normal scaffold output, committed.
- **Did not commit `next-env.d.ts`:** create-next-app 16.2.4's own `website/.gitignore` includes `next-env.d.ts` on its last line. RESEARCH.md Open Question 3 had recommended committing it, but the framework default is the stronger signal — the file is auto-regenerated on every `next build`, so ignoring it is safe and matches 2026-era community practice. The repo root `.gitignore` does NOT mention `next-env.d.ts` (the plan's acceptance criterion requires this), so this is self-consistent.

## Deviations from Plan

### Auto-fixed Issues

None. Plan executed exactly as written — no bugs discovered, no missing functionality, no blocking issues.

### Adjustments

**1. [Extra scaffold output — not a rule-1/2/3 deviation] Additional auto-generated files committed**
- **Found during:** Task 1 (post-scaffold `git status`)
- **Issue:** `create-next-app --yes` generated `website/AGENTS.md`, `website/CLAUDE.md`, and `website/pnpm-workspace.yaml` — none of these were enumerated in the plan's `<files>` list.
- **Resolution:** Committed them as part of the scaffold. They are authoritative output from the framework's interactive prompt (auto-accepted by `--yes`), not unwanted detritus. Removing them would break the scaffold's self-consistency.
- **Commit:** `666b75b`

## Observations (non-blocking)

- **Next.js workspace-root warning during `pnpm build`:** The build log shows "We detected multiple lockfiles and selected the directory of /Users/cary/bun.lockb as the root directory." This is caused by a stray `bun.lockb` at the user's home directory (`/Users/cary/`) — not in this repo. The build still succeeded and output is correct. Future fix (out of scope for phase 11): set `turbopack.root` in `next.config.ts` to the `/website` directory, or delete the stray `bun.lockb` if it's unused. Logging here so plan 11-02 or 11-03 can address if it becomes noisy.

## Requirements Completed

- **SITE-01** — `/website` subdirectory initialized with Next.js App Router + TypeScript inside the `ps-transcribe` repo, with its own `package.json` independent from the Swift package. Node 22 pinned in both layers. TypeScript strict compiles. `@/*` alias resolves to `src/*`. pnpm is the sole lockfile.
- **SITE-05** — Repo `.gitignore` excludes website build artifacts (`.next/`, `node_modules/`) as well as `.vercel/` and `out/`. No build artifacts tracked by git.

## Known Stubs

None. Plan 11-01 is pure infrastructure. The scaffold's default `layout.tsx` and `page.tsx` are placeholder content (create-next-app's "Welcome to Next.js" default UI) but they are **explicitly scoped to plan 11-02** per the plan's `<done>` criteria and the `src/app/layout.tsx` / `src/app/page.tsx` notes ("Root layout scaffolded by create-next-app — content rewrite in plan 02"). Not a stub — a deferred task with a named owner.

## Handoff Notes

- **Plan 11-02** will rewrite `src/app/layout.tsx` (add `next/font` for Inter/Spectral/JetBrains Mono, full `Metadata` export, metadataBase) and `src/app/page.tsx` (Chronicle-flavored placeholder). Also adds `src/app/sitemap.ts`, `robots.ts`, `manifest.ts`, `opengraph-image.tsx`, and derives `src/app/icon.png` + `src/app/apple-icon.png` from `assets/icon.png`.
- **Plan 11-03** will configure the Vercel project in the dashboard (Root Directory = `website`, Ignored Build Step = `git diff HEAD^ HEAD --quiet -- .`) and verify the production deploy at `ps-transcribe.vercel.app`.
- **Sanity check before 11-02:** confirm `/Users/cary/bun.lockb` workspace-root warning doesn't break `pnpm dev`. Set `turbopack.root` in `next.config.ts` if needed.

## Metrics

| Metric               | Value               |
|----------------------|---------------------|
| Wall-clock duration  | ~2.5 minutes (149s) |
| Tasks completed      | 2 / 2               |
| Files created        | 20                  |
| Files modified       | 1                   |
| Commits              | 2 (666b75b, 02fe936) |
| Completed            | 2026-04-22          |

## Self-Check: PASSED

**Files verified present:** `website/package.json`, `website/pnpm-lock.yaml`, `website/tsconfig.json`, `website/.nvmrc`, `website/next.config.ts`, `website/postcss.config.mjs`, `website/eslint.config.mjs`, `website/src/app/layout.tsx`, `website/src/app/page.tsx`, `website/src/app/globals.css`, `website/src/app/favicon.ico`, `website/AGENTS.md`, `website/CLAUDE.md`, `website/README.md`, `website/pnpm-workspace.yaml`, `website/.gitignore`, repo-root `.gitignore`, this SUMMARY.md.

**Commits verified in git log:** `666b75b` (Task 1), `02fe936` (Task 2).
