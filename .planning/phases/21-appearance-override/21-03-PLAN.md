---
phase: 21-appearance-override
plan: 03
type: execute
wave: 3
depends_on:
  - 21-01
  - 21-02
files_modified:
  - .planning/phases/21-appearance-override/21-VERIFICATION.md
autonomous: false
requirements:
  - "SPEC.Req-2"  # App-root override application — UAT live-toggle scenarios
  - "SPEC.Req-3"  # Settings UI — UAT picker change without restart
  - "SPEC.Req-4"  # Phase 20 invariant preserved (relaxed per D-06) — grep-gate audit
  - "SPEC.Req-5"  # Default-state pixel stability — visual parity with Phase 20
  - "SPEC.Req-6"  # Invisible migration — fresh-install / wiped UserDefaults scenario
  - "D-06"        # Relaxed grep gate documented inline in 21-VERIFICATION.md
  - "D-07"        # DictationHUD live re-render verification
user_setup: []

must_haves:
  truths:
    - "D-05: Manual UAT confirms that changing the Appearance Picker live updates the main window, Settings window, and MenuBarExtra menu content without app restart"
    - "D-07: Manual UAT confirms that the DictationHUD re-renders live with the chosen appearance, including the .system case which inherits NSApp.effectiveAppearance"
    - "Manual UAT confirms that quitting and relaunching with a non-System preference launches the app in the chosen mode"
    - "Manual UAT confirms that wiping the appearancePreference UserDefaults key (`defaults delete com.newfeldt.PSTranscribe appearancePreference` or equivalent) on a Phase 21 build behaves identically to a Phase 20 build — the Picker shows 'System' and the app follows macOS appearance"
    - "Manual UAT confirms that with preference = .system the post-Phase-21 build is visually indistinguishable from the post-Phase-20 build across light/dark/auto macOS toggles"
    - "D-06: 21-VERIFICATION.md documents the relaxed grep gate (N hits in PSTranscribeApp.swift, all reading settings.appearancePreference.colorScheme) explicitly, superseding SPEC #4's 'exactly ONE hit' wording"
    - "D-01: 21-VERIFICATION.md captures the v1.2 milestone state-flip reminder — STATE.md `status` toggles `completed → in_progress` while Phase 21 is open and back to `completed` at sign-off (orchestrator action, not implementation)"
    - "21-VERIFICATION.md is committed and references all SPEC.md acceptance criteria with pass/fail status"
  artifacts:
    - path: ".planning/phases/21-appearance-override/21-VERIFICATION.md"
      provides: "Phase 21 manual UAT log + relaxed D-06 grep gate audit + SPEC.md acceptance criteria status table"
      contains: "21-VERIFICATION"
      contains_2: "D-06"
  key_links:
    - from: "21-VERIFICATION.md"
      to: "SPEC.md acceptance criteria"
      via: "checkbox status table covering all 11 SPEC criteria"
      pattern: "Acceptance Criteria"
    - from: "21-VERIFICATION.md"
      to: "D-06 (CONTEXT.md)"
      via: "explicit deviation note: SPEC #4's 'exactly ONE hit' is superseded; relaxed gate is 'N hits, all in PSTranscribeApp.swift, all reading settings.appearancePreference.colorScheme'"
      pattern: "D-06"
---

<objective>
Run the manual UAT for Phase 21 and record results in `21-VERIFICATION.md`. This plan is non-autonomous (`autonomous: false`) because the verification covers visual rendering across macOS appearance toggles, live-update without restart, persistence across relaunch, fresh-install behavior, and DictationHUD vibrancy — none of which can be deterministically verified by automated tooling alone (Phase 20 deferred snapshot testing per CONTEXT.md `<deferred>`, and Phase 21 inherits that deferral).

Purpose: Convert SPEC.md's 11 acceptance criteria into a checked-off log that proves Phase 21 ships behaviorally correct, with the relaxed D-06 grep gate explicitly documented so future verifiers don't trip on SPEC #4's superseded "exactly ONE hit" wording.

Output: `.planning/phases/21-appearance-override/21-VERIFICATION.md` capturing pass/fail status for every acceptance criterion, the grep-gate audit transcript, the relaxed D-06 deviation note, and any UAT findings (deviations, edge cases, follow-up items).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-appearance-override/21-CONTEXT.md
@.planning/phases/21-appearance-override/21-SPEC.md
@.planning/phases/21-appearance-override/21-01-SUMMARY.md
@.planning/phases/21-appearance-override/21-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run automated grep-gate audit and capture transcript</name>
  <files>.planning/phases/21-appearance-override/21-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/21-appearance-override/21-CONTEXT.md (D-05, D-06, D-07 specifically)
    - .planning/phases/21-appearance-override/21-SPEC.md (Acceptance Criteria section, especially items 3-5 and 9-11)
    - .planning/phases/21-appearance-override/21-02-SUMMARY.md (final line numbers from Plan 21-02)
  </read_first>
  <action>
Run the relaxed D-06 grep gate plus a handful of structural greps that prove Phase 21's plumbing is in place. Capture the literal output verbatim into a fresh `21-VERIFICATION.md` under a `## Automated Audit` heading.

Run each command from the project root (`/Users/cary/Development/ai-development/ps-transcribe`) and capture both the command and its output. Use the literal commands below (do NOT abbreviate; future readers should be able to copy-paste and re-run).

**Commands to run:**

```bash
# 1. Relaxed D-06 grep gate: zero hits outside PSTranscribeApp.swift and #Preview blocks
grep -rn 'preferredColorScheme(' PSTranscribe/Sources --include='*.swift' | grep -v '#Preview' | grep -v 'PSTranscribeApp.swift' || echo "[no matches — relaxed D-06 gate passes]"

# 2. Three call-sites in PSTranscribeApp.swift, all reading settings.appearancePreference.colorScheme
grep -n '\.preferredColorScheme(settings\.appearancePreference\.colorScheme)' PSTranscribe/Sources/PSTranscribe/App/PSTranscribeApp.swift

# 3. No hardcoded .preferredColorScheme(.light/.dark/nil) anywhere
grep -rnE '\.preferredColorScheme\(\s*\.(light|dark)\s*\)' PSTranscribe/Sources --include='*.swift' | grep -v '#Preview' || echo "[no hardcoded overrides outside #Preview — passes]"

# 4. AppearancePreference enum lives in AppSettings.swift
grep -n 'enum AppearancePreference: String' PSTranscribe/Sources/PSTranscribe/Settings/AppSettings.swift

# 5. AppSettings.appearancePreference property + UserDefaults key
grep -n 'appearancePreference' PSTranscribe/Sources/PSTranscribe/Settings/AppSettings.swift

# 6. DictationWindowController coverage path
grep -n 'panel\.appearance\|NSAppearance(named:' PSTranscribe/Sources/PSTranscribe/App/DictationWindowController.swift

# 7. SettingsView Section + Picker
grep -n 'Section("Appearance")\|Picker("Appearance"' PSTranscribe/Sources/PSTranscribe/Views/SettingsView.swift

# 8. Section("Appearance") is the FIRST section (above Audio Input)
APPEARANCE_LINE=$(grep -n 'Section("Appearance")' PSTranscribe/Sources/PSTranscribe/Views/SettingsView.swift | head -1 | cut -d: -f1)
AUDIO_LINE=$(grep -n 'Section("Audio Input")' PSTranscribe/Sources/PSTranscribe/Views/SettingsView.swift | head -1 | cut -d: -f1)
echo "Appearance section at line $APPEARANCE_LINE; Audio Input section at line $AUDIO_LINE"
[ "$APPEARANCE_LINE" -lt "$AUDIO_LINE" ] && echo "PASS: Appearance is above Audio Input" || echo "FAIL: ordering wrong"

# 9. swift build clean
cd PSTranscribe && swift build 2>&1 | tail -5
```

Capture each command's output into the verification document — don't summarize, paste verbatim. The grep transcripts ARE the audit.

**Document structure** (write this to `.planning/phases/21-appearance-override/21-VERIFICATION.md`):

```markdown
# Phase 21: User-Controlled Appearance Preference — Verification

**Date:** {fill in actual date during execution}
**Status:** {PASS | PASS WITH NOTES | FAIL — pending fix}
**Build:** {commit SHA at time of UAT}

## Automated Audit

### Relaxed D-06 Grep Gate

CONTEXT.md D-06 explicitly relaxes SPEC.md acceptance criterion #4 from "exactly ONE hit outside `#Preview` blocks" to **"N hits, all inside `PSTranscribe/Sources/PSTranscribe/App/PSTranscribeApp.swift`, all reading `settings.appearancePreference.colorScheme`."** Verifier audits **location + source**, not count.

This deviation is intentional and locked. Phase 21 ships with three call-sites in `PSTranscribeApp.swift` (one per Scene root: WindowGroup ContentView, Settings scene, MenuBarExtra menu content) — see CONTEXT.md D-05 for the rationale.

#### Audit transcript

{paste the actual command outputs from steps 1, 2, 3 above}

### Structural greps

{paste outputs from steps 4, 5, 6, 7, 8, 9}

### Build status

{paste swift build output}

## Manual UAT

(Filled in by Task 2.)

## SPEC.md Acceptance Criteria Status

(Filled in by Task 3.)
```
  </action>
  <verify>
    <automated>
[ -f /Users/cary/Development/ai-development/ps-transcribe/.planning/phases/21-appearance-override/21-VERIFICATION.md ] && grep -c 'D-06' /Users/cary/Development/ai-development/ps-transcribe/.planning/phases/21-appearance-override/21-VERIFICATION.md
    </automated>
    Plus: file exists, contains the literal section headings `## Automated Audit`, `### Relaxed D-06 Grep Gate`, `#### Audit transcript`, `### Structural greps`, `### Build status`. The relaxed D-06 deviation paragraph is present and explicitly cites both SPEC #4 and CONTEXT.md D-06.
  </verify>
  <acceptance_criteria>
    - `21-VERIFICATION.md` exists at `.planning/phases/21-appearance-override/21-VERIFICATION.md`
    - File contains a section titled `## Automated Audit` with the relaxed D-06 grep gate transcript
    - The relaxed-gate paragraph cites both "SPEC #4" and "D-06" by their reference IDs
    - All nine command outputs from the action block are pasted verbatim — no command output is truncated or summarized
    - The "Section('Appearance') is FIRST" check passes (line-number comparison shows Appearance < Audio Input)
    - `swift build` output shows zero errors and no NEW warnings (vs. the pre-Phase-21 baseline)
    - The grep gate (`grep -rn 'preferredColorScheme(' PSTranscribe/Sources --include='*.swift' | grep -v '#Preview' | grep -v 'PSTranscribeApp.swift'`) returns no output (the `|| echo "[no matches — passes]"` branch fires)
    - Hits in `PSTranscribeApp.swift` for `settings.appearancePreference.colorScheme` total exactly 3 (per D-05's three call-sites)
    - All three hits use `settings.appearancePreference.colorScheme` (not a hardcoded value, not a different property)
  </acceptance_criteria>
  <done>
    The automated audit section of `21-VERIFICATION.md` is complete with verbatim grep + build transcripts. The relaxed D-06 deviation is explicitly documented inline, so any future verifier reading SPEC #4's "exactly ONE hit" wording is immediately redirected to the location+source check.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual UAT — live-toggle, persistence, fresh-install, default-state visual parity</name>
  <what-built>
    A new "Appearance" section at the top of Settings (above Audio Input) with a System / Light / Dark Picker. The preference persists via `UserDefaults` and re-renders every visible surface (main window, Settings, MenuBarExtra menu content, DictationHUD) without app restart.
  </what-built>
  <how-to-verify>
Run each scenario sequentially. After each, record the result in `21-VERIFICATION.md` under `## Manual UAT` as `- [x] {scenario}` (PASS), `- [!] {scenario} — {note}` (PASS WITH NOTES), or `- [ ] {scenario} — FAILED: {description}` (FAIL).

**Pre-flight:**

1. Build the app: `cd /Users/cary/Development/ai-development/ps-transcribe/PSTranscribe && swift build` — must exit 0
2. Launch the app: `swift run PSTranscribe` (or open the built bundle)
3. Open Settings (Cmd+,) — verify "Appearance" is the FIRST section, visually above "Audio Input", with a default-style dropdown showing "System" / "Light" / "Dark"
4. Confirm the Picker is currently set to "System" (default state)

**Scenario A — Live toggle while macOS is in Dark mode:**

1. Open System Settings → Appearance → set to "Dark"
2. Confirm the PS Transcribe main window is rendering Dark (system-following baseline)
3. In PS Transcribe Settings, change Appearance to "Light"
4. Verify within ~1 second:
   - Main window flips to Light rendering (titlebar, sidebar, content area)
   - Settings window itself flips to Light (you're looking at it; the Form should re-render)
   - Trigger the dictation hotkey (default Cmd+Shift+D) — DictationHUD should appear with Light vibrancy material (`.hudWindow` material flips correctly)
   - Stop dictation
   - Click the menu bar icon — the menu content (Text "PS Transcribe", Divider, Quit button) should render in Light
5. Record: PASS / FAIL with notes

**Scenario B — Live toggle while macOS is in Light mode:**

1. Set macOS Appearance to "Light" via System Settings
2. In PS Transcribe Settings, change Appearance to "Dark"
3. Verify the same four surfaces flip to Dark within ~1 second
4. Record: PASS / FAIL

**Scenario C — Revert to System mode:**

1. Set PS Transcribe Appearance back to "System"
2. Verify the app immediately reverts to following macOS appearance
3. Toggle macOS Appearance Light → Dark → Light via System Settings — every surface in PS Transcribe should follow without lag and without restart
4. Record: PASS / FAIL

**Scenario D — Persistence across relaunch:**

1. Set PS Transcribe Appearance to "Dark"
2. Quit PS Transcribe (Cmd+Q)
3. Relaunch — the app should launch in Dark mode immediately, before any user interaction
4. Open Settings — Picker shows "Dark"
5. Set Appearance back to "Light", quit, relaunch — app launches Light, Picker shows "Light"
6. Record: PASS / FAIL

**Scenario E — Fresh-install / wiped UserDefaults (Req 6 invisible migration):**

1. Quit PS Transcribe completely
2. Wipe the `appearancePreference` UserDefaults key:
   ```bash
   defaults delete com.newfeldt.PSTranscribe appearancePreference 2>/dev/null || echo "[key already absent — OK]"
   ```
   (Replace the bundle identifier above if the actual app bundle ID differs; check via `defaults domains | tr ',' '\n' | grep -i transcribe`)
3. Relaunch PS Transcribe
4. Verify:
   - No crash, no error dialog
   - App renders in System mode (follows macOS appearance)
   - Open Settings — Picker shows "System" (the default fallback)
5. Record: PASS / FAIL

**Scenario F — Default-state pixel stability (Req 5):**

1. With preference = "System" (the default), confirm the app renders identically to a checkout of the post-Phase-20 build (commit `f9f2139` per recent commits). This is a sanity check, not a literal pixel-diff (Phase 21 deferred snapshot testing). Compare visually:
   - Main window in macOS Light mode — chrome, sidebar, content area should match
   - Main window in macOS Dark mode — same
   - Settings window in both modes
   - DictationHUD in both modes
2. Record: PASS / PASS WITH NOTES (describe any visual diff) / FAIL

**Scenario G — DictationHUD live re-render (D-07 specifically):**

1. Set Appearance to "System" while macOS is in Dark mode
2. Trigger dictation (hotkey) — HUD appears in Dark vibrancy (`.hudWindow` material renders as a dark translucent panel)
3. Without dismissing the HUD, switch to PS Transcribe Settings (the HUD remains visible because it's `.nonactivatingPanel`)
4. Change Appearance to "Light"
5. The HUD should flip to Light vibrancy WITHOUT requiring HUD dismiss/re-show
6. Change Appearance to "Dark" — HUD flips to Dark
7. Change Appearance to "System" — HUD reverts to following macOS appearance (in this case, still Dark since macOS is Dark)
8. With Appearance = "System", toggle macOS appearance Light ↔ Dark via System Settings; HUD should flip live (this verifies the `panel.appearance = nil` path correctly inherits `NSApp.effectiveAppearance`; if it doesn't, planner falls back to KVO per CONTEXT.md Claude's Discretion — record as PASS WITH NOTES and flag for follow-up)
9. Record: PASS / PASS WITH NOTES / FAIL

**Scenario H — MenuBarExtra menu content (D-05 site 3):**

1. Set Appearance to "Light" while macOS is in Dark mode
2. Click the menu bar icon — confirm the dropdown menu content (Text "PS Transcribe", Divider, Quit button) renders Light
3. Set Appearance to "Dark" while macOS is in Light mode
4. Click the menu bar icon — content renders Dark
5. Note: the status bar icon glyph itself (the mic/book icon) is system-rendered and may not flip — this is the tolerated gap per CONTEXT.md Claude's Discretion. Record glyph behavior as a note, not a failure.
6. Record: PASS / PASS WITH NOTES / FAIL
  </how-to-verify>
  <resume-signal>
Reply with one of:
- **"approved"** — All eight scenarios passed (or passed with notes); ready to record SPEC.md acceptance criteria status (Task 3) and finalize verification
- **"approved with notes: {brief description}"** — Some scenarios passed with notes (e.g., menu bar glyph doesn't flip — tolerated gap); proceed to Task 3 with notes captured
- **"failed: {scenario letter} — {description}"** — Specify which scenario failed and what was observed; planner will route to gap-closure planning before Task 3
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Record SPEC.md acceptance criteria status table and finalize 21-VERIFICATION.md</name>
  <files>.planning/phases/21-appearance-override/21-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/21-appearance-override/21-SPEC.md (Acceptance Criteria section — the 11 checkboxes)
    - .planning/phases/21-appearance-override/21-VERIFICATION.md (the partial doc from Tasks 1 + 2)
    - The user's response to Task 2's resume-signal (approved / approved with notes / failed)
  </read_first>
  <action>
Append the SPEC.md acceptance criteria status table to `21-VERIFICATION.md`. Each of SPEC.md's 11 criteria gets a row with its source, the verifying evidence (automated grep, manual UAT scenario, or build output), and a checkbox status.

**Status table to append** (under the existing `## SPEC.md Acceptance Criteria Status` heading):

```markdown
## SPEC.md Acceptance Criteria Status

| # | Criterion (verbatim from SPEC.md) | Verified by | Status |
|---|------------------------------------|-------------|--------|
| 1 | `AppearancePreference` enum defined with `.system`, `.light`, `.dark` cases and a `colorScheme: ColorScheme?` computed property (returns `nil` for `.system`) | Plan 21-01 grep gate (Task 1 audit transcript: enum + colorScheme greps) | {x or ! or fail} |
| 2 | `AppSettings.appearancePreference` property exists, persists via UserDefaults key `"appearancePreference"`, defaults to `.system` when key absent | Plan 21-01 grep gate + Manual UAT Scenario E (fresh install) | {status} |
| 3 | `grep -rn "preferredColorScheme(" PSTranscribe/Sources` returns exactly ONE hit outside `#Preview` blocks; that hit reads from `settings.appearancePreference.colorScheme` | **SUPERSEDED by D-06** — see Relaxed D-06 Grep Gate above. Audit confirms 3 hits, all in `PSTranscribeApp.swift`, all reading `settings.appearancePreference.colorScheme` | {x with note "see D-06 relaxation"} |
| 4 | `SettingsView` shows an "Appearance" section with a three-option Picker bound to the settings property | Task 1 audit transcript (Section + Picker greps) + Manual UAT pre-flight | {status} |
| 5 | `swift build` clean, zero new warnings | Task 1 audit (build transcript) | {status} |
| 6 | Manual UAT — change picker to Light while macOS is Dark: app renders Light immediately, no restart | Manual UAT Scenario A | {status} |
| 7 | Manual UAT — change picker to Dark while macOS is Light: app renders Dark immediately, no restart | Manual UAT Scenario B | {status} |
| 8 | Manual UAT — change picker back to System: app reverts to following macOS appearance immediately | Manual UAT Scenario C | {status} |
| 9 | Manual UAT — quit + relaunch with non-System preference: app launches in the chosen mode | Manual UAT Scenario D | {status} |
| 10 | Manual UAT — first launch with no `appearancePreference` key (simulate via `defaults delete`): app launches in System mode, picker shows "System" | Manual UAT Scenario E | {status} |
| 11 | Visual parity — with preference = System, post-Phase-21 build matches post-Phase-20 build on all 10 surfaces in both light and dark macOS settings | Manual UAT Scenario F | {status} |

## Phase 21 Sign-Off

- **Build:** {commit SHA}
- **Date:** {date}
- **Outcome:** {ALL PASS | PASS WITH NOTES — see scenarios X, Y | FAILED — see scenario Z}
- **Notes / deferred items:**
  - {Any tolerated gaps: e.g., MenuBarExtra status bar icon glyph does not flip (per Claude's Discretion in CONTEXT.md)}
  - {Any KVO fallback needed for `.system` resolution on the NSPanel — if Scenario G step 8 failed, document fallback decision}
  - {Any other deviation from CONTEXT.md decisions}

## D-01 milestone state update

Per CONTEXT.md D-01, with Phase 21 complete, milestone v1.2's status flips from `in_progress` back to `completed`. Capture the next steps:

- [ ] Update `.planning/STATE.md` frontmatter `status: completed` and increment progress counters
- [ ] Update `.planning/ROADMAP.md` Phase 21 row to `[x]` and mark completion date
- [ ] Tag `v1.2` in git when ready to release

(These are orchestrator/follow-up tasks, not part of this plan's execution.)
```

Fill in:
- Each `{status}` cell with `[x]` (PASS), `[x] (note: ...)` (PASS WITH NOTES), or `[ ] FAIL: ...` (FAIL) based on Task 2's resume-signal and the audit transcripts from Task 1
- The `{commit SHA}` from `git rev-parse HEAD`
- The `{date}` (today)
- Any notes from Task 2's resume-signal under "Notes / deferred items"

If any criterion failed in Task 2, the table reflects it accurately and the orchestrator routes to gap-closure planning. Do NOT mark a criterion PASS that the user didn't confirm.
  </action>
  <verify>
    <automated>
[ -f /Users/cary/Development/ai-development/ps-transcribe/.planning/phases/21-appearance-override/21-VERIFICATION.md ] && grep -c 'SPEC.md Acceptance Criteria Status' /Users/cary/Development/ai-development/ps-transcribe/.planning/phases/21-appearance-override/21-VERIFICATION.md && grep -c 'D-01 milestone state update' /Users/cary/Development/ai-development/ps-transcribe/.planning/phases/21-appearance-override/21-VERIFICATION.md
    </automated>
    Plus: the table has exactly 11 rows (one per SPEC.md acceptance criterion); row #3 cites D-06's relaxation explicitly; the sign-off section captures build SHA + date + outcome; the D-01 milestone state update section is present.
  </verify>
  <acceptance_criteria>
    - `21-VERIFICATION.md` contains a `## SPEC.md Acceptance Criteria Status` table with 11 rows mapping to SPEC.md's 11 acceptance criteria
    - Row #3 (the SPEC #4 grep gate) is annotated with "SUPERSEDED by D-06" and references the Relaxed D-06 Grep Gate section above
    - Every row has a status cell filled in (`[x]`, `[x] (note: ...)`, or `[ ] FAIL: ...`)
    - No row is left as `{status}` — all placeholders replaced with actual values
    - `## Phase 21 Sign-Off` section includes commit SHA (`git rev-parse HEAD`), date, outcome
    - `## D-01 milestone state update` section is present and captures the orchestrator's follow-up tasks (STATE.md flip, ROADMAP.md checkbox, v1.2 git tag)
    - The verification document is committed to git (the orchestrator handles the commit per `runtime_environment_notes`)
    - If any criterion is `[ ] FAIL`, the sign-off outcome is "FAILED — see scenario {X}" and the orchestrator routes to gap-closure planning (not this plan's responsibility, but the failure must be visible in the table for the routing to happen)
  </acceptance_criteria>
  <done>
    `21-VERIFICATION.md` is complete: automated audit transcript (Task 1), manual UAT log (Task 2), SPEC.md acceptance criteria status table (Task 3), sign-off, D-01 milestone state update notes. Phase 21 is verified end-to-end. The relaxed D-06 grep gate is documented inline, superseding SPEC #4's "exactly ONE hit" wording for any future verifier.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan is verification-only — no code changes, no new trust boundaries. The trust boundaries already analyzed in Plans 21-01 and 21-02 are not re-introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-10 | Repudiation | Manual UAT log integrity | mitigate | The UAT log is authored during the verification session and committed to git in the same plan; the commit history provides tamper-evidence. The grep transcripts are reproducible — any future verifier can re-run the commands and compare. |
| T-21-11 | Information Disclosure | Bundle identifier in `defaults delete` command (Scenario E) | accept | The bundle identifier is public (already visible in `Info.plist`, in build output, and in the marketing site). Including it in 21-VERIFICATION.md leaks no secret. The verifier may need to substitute the actual identifier if the placeholder differs from the real one — that's a documentation hygiene issue, not a security issue. |

(No new mitigation surface beyond log integrity, which git provides for free.)
</threat_model>

<verification>
- `21-VERIFICATION.md` exists and contains the four required sections: Automated Audit, Manual UAT, SPEC.md Acceptance Criteria Status, Phase 21 Sign-Off
- D-06 relaxation is documented at least twice (once in the Automated Audit's Relaxed D-06 Grep Gate paragraph, once in row #3 of the criteria table)
- Every acceptance criterion has a definitive status (no placeholders, no TBDs)
- The orchestrator can read the file and determine: (a) phase passes → close v1.2 milestone (D-01); (b) phase passes with notes → log notes for backlog; (c) phase fails → route to gap-closure
</verification>

<success_criteria>
- All eight UAT scenarios from Task 2 are recorded in `21-VERIFICATION.md` with definitive PASS/PASS-WITH-NOTES/FAIL status
- The relaxed D-06 grep gate is explicitly documented (inline in `21-VERIFICATION.md`) so future verifiers reading SPEC.md #4's "exactly ONE hit" wording are immediately redirected to the relaxed location+source check
- The SPEC.md acceptance criteria status table covers all 11 criteria with traceable evidence (audit transcripts, UAT scenario references)
- Phase 21 has a clear sign-off outcome
- The D-01 milestone state update follow-up is captured (STATE.md flip, ROADMAP.md checkbox, v1.2 git tag preparation)
</success_criteria>

<output>
After completion, create `.planning/phases/21-appearance-override/21-03-SUMMARY.md` capturing:
- The final UAT outcome (ALL PASS / PASS WITH NOTES / FAIL)
- Any tolerated gaps surfaced during UAT (especially MenuBarExtra status bar icon glyph behavior, NSPanel `.system` resolution behavior on live macOS toggle)
- Whether the KVO fallback for the NSPanel `.system` resolution was needed (CONTEXT.md Claude's Discretion item)
- Any follow-up items deferred to backlog (e.g., automated visual-regression testing, designer-tuned dark Chronicle pass — both already in CONTEXT.md `<deferred>`)
- A reminder that the orchestrator must execute the D-01 milestone state update (STATE.md status flip, ROADMAP.md checkbox tick, v1.2 readiness for git tag)
</output>
