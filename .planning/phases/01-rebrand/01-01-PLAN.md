---
phase: 01-rebrand
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - PSTranscribe/Package.swift
  - PSTranscribe/Sources/PSTranscribe/App/PSTranscribeApp.swift
  - PSTranscribe/Sources/PSTranscribe/App/AppUpdaterController.swift
  - PSTranscribe/Sources/PSTranscribe/Transcription/StreamingTranscriber.swift
  - PSTranscribe/Sources/PSTranscribe/Transcription/TranscriptionEngine.swift
  - PSTranscribe/Sources/PSTranscribe/Storage/SessionStore.swift
  - PSTranscribe/Sources/PSTranscribe/Settings/AppSettings.swift
  - PSTranscribe/Sources/PSTranscribe/Views/OnboardingView.swift
  - PSTranscribe/Sources/PSTranscribe/PSTranscribe.entitlements
autonomous: true
requirements:
  - REBR-01
  - REBR-02
  - REBR-03
  - REBR-04

must_haves:
  truths:
    - "Source directory is PSTranscribe/Sources/PSTranscribe/ (not Tome/Sources/Tome/)"
    - "Package.swift compiles with target name PSTranscribe"
    - "All Swift type names use PSTranscribe prefix (not Tome prefix)"
    - "All user-visible strings read PS Transcribe (not Tome)"
    - "Logger subsystem is com.pstranscribe.app"
    - "Bundle identifier in source is com.pstranscribe.app"
  artifacts:
    - path: "PSTranscribe/Package.swift"
      provides: "SwiftPM manifest with PSTranscribe target"
      contains: "name: \"PSTranscribe\""
    - path: "PSTranscribe/Sources/PSTranscribe/App/PSTranscribeApp.swift"
      provides: "Renamed app entry point"
      contains: "struct PSTranscribeApp"
    - path: "PSTranscribe/Sources/PSTranscribe/PSTranscribe.entitlements"
      provides: "Renamed entitlements file"
  key_links:
    - from: "PSTranscribe/Package.swift"
      to: "PSTranscribe/Sources/PSTranscribe/"
      via: "path field in executableTarget"
      pattern: "path: \"Sources/PSTranscribe\""
    - from: "PSTranscribe/Package.swift"
      to: "PSTranscribe/Sources/PSTranscribe/PSTranscribe.entitlements"
      via: "exclude list"
      pattern: "PSTranscribe.entitlements"
---

<objective>
Rename directory structure from Tome to PSTranscribe and update all Swift source content to use the new names.

Purpose: Establishes the new identity at the filesystem and source code level -- every subsequent plan depends on these paths and names being correct.
Output: Compilable PSTranscribe project with all source files renamed and content updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rebrand/01-CONTEXT.md
@.planning/phases/01-rebrand/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename directories and files via git mv</name>
  <files>
    PSTranscribe/ (entire directory, renamed from Tome/)
    PSTranscribe/Sources/PSTranscribe/ (renamed from Sources/Tome/)
    PSTranscribe/Sources/PSTranscribe/PSTranscribe.entitlements (renamed from Tome.entitlements)
    PSTranscribe/Sources/PSTranscribe/App/PSTranscribeApp.swift (renamed from TomeApp.swift)
  </files>
  <read_first>
    Tome/Package.swift
    Tome/Sources/Tome/App/TomeApp.swift
    Tome/Sources/Tome/Tome.entitlements
  </read_first>
  <action>
    Rename directories and files using git mv to preserve history. Per D-05, D-06, D-07, D-08:

    Step 1 -- Rename inner source directory:
    ```
    git mv Tome/Sources/Tome Tome/Sources/PSTranscribe
    ```

    Step 2 -- Rename entitlements file:
    ```
    git mv Tome/Sources/PSTranscribe/Tome.entitlements Tome/Sources/PSTranscribe/PSTranscribe.entitlements
    ```

    Step 3 -- Rename app entry point file:
    ```
    git mv Tome/Sources/PSTranscribe/App/TomeApp.swift Tome/Sources/PSTranscribe/App/PSTranscribeApp.swift
    ```

    Step 4 -- Rename outer project directory:
    ```
    git mv Tome PSTranscribe
    ```

    Step 5 -- Clean SwiftPM build cache (old .build references will be stale):
    ```
    cd PSTranscribe && swift package clean && cd ..
    ```

    IMPORTANT: Do NOT rename the git repo root directory. Only the inner `Tome/` directory (containing Package.swift) becomes `PSTranscribe/`.
  </action>
  <verify>
    <automated>test -d PSTranscribe/Sources/PSTranscribe/App && test -f PSTranscribe/Sources/PSTranscribe/PSTranscribe.entitlements && test -f PSTranscribe/Sources/PSTranscribe/App/PSTranscribeApp.swift && test ! -d Tome && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `PSTranscribe/Sources/PSTranscribe/` directory exists
    - `PSTranscribe/Sources/PSTranscribe/PSTranscribe.entitlements` file exists
    - `PSTranscribe/Sources/PSTranscribe/App/PSTranscribeApp.swift` file exists
    - `Tome/` directory does NOT exist
    - `git status` shows renames (not deletes + adds)
  </acceptance_criteria>
  <done>All directories and files renamed; git tracks them as renames; old Tome/ path no longer exists</done>
</task>

<task type="auto">
  <name>Task 2: Update Package.swift and all Swift source content</name>
  <files>
    PSTranscribe/Package.swift
    PSTranscribe/Sources/PSTranscribe/App/PSTranscribeApp.swift
    PSTranscribe/Sources/PSTranscribe/App/AppUpdaterController.swift
    PSTranscribe/Sources/PSTranscribe/Transcription/StreamingTranscriber.swift
    PSTranscribe/Sources/PSTranscribe/Transcription/TranscriptionEngine.swift
    PSTranscribe/Sources/PSTranscribe/Storage/SessionStore.swift
    PSTranscribe/Sources/PSTranscribe/Settings/AppSettings.swift
    PSTranscribe/Sources/PSTranscribe/Views/OnboardingView.swift
  </files>
  <read_first>
    PSTranscribe/Package.swift
    PSTranscribe/Sources/PSTranscribe/App/PSTranscribeApp.swift
    PSTranscribe/Sources/PSTranscribe/App/AppUpdaterController.swift
    PSTranscribe/Sources/PSTranscribe/Transcription/StreamingTranscriber.swift
    PSTranscribe/Sources/PSTranscribe/Transcription/TranscriptionEngine.swift
    PSTranscribe/Sources/PSTranscribe/Storage/SessionStore.swift
    PSTranscribe/Sources/PSTranscribe/Settings/AppSettings.swift
    PSTranscribe/Sources/PSTranscribe/Views/OnboardingView.swift
  </read_first>
  <action>
    Update source content in three passes per Research pitfall 6 guidance. Per D-01, D-02, D-05:

    **Pass 1 -- Bundle ID and subsystem strings:**
    - StreamingTranscriber.swift: `"io.gremble.tome"` -> `"com.pstranscribe.app"` (per D-02)
    - Any other occurrence of `io.gremble.tome` in Swift files -> `com.pstranscribe.app` (per D-01)

    **Pass 2 -- Swift type names (PascalCase, no space):**
    - Package.swift: `name: "Tome"` -> `name: "PSTranscribe"` (package level)
    - Package.swift: target `name: "Tome"` -> `name: "PSTranscribe"`
    - Package.swift: `path: "Sources/Tome"` -> `path: "Sources/PSTranscribe"`
    - Package.swift: `exclude: [..., "Tome.entitlements", ...]` -> `exclude: [..., "PSTranscribe.entitlements", ...]`
    - PSTranscribeApp.swift: `struct TomeApp: App` -> `struct PSTranscribeApp: App`
    - PSTranscribeApp.swift: `@main` annotation stays on the struct
    - AppUpdaterController.swift: `class TomeUserDriver` -> `class PSTranscribeUserDriver`
    - AppUpdaterController.swift: all references to `TomeUserDriver` -> `PSTranscribeUserDriver`

    **Pass 3 -- User-visible strings (spaced: "PS Transcribe"):**
    - PSTranscribeApp.swift: `Text("Tome")` -> `Text("PS Transcribe")`
    - PSTranscribeApp.swift: `Button("Quit Tome")` -> `Button("Quit PS Transcribe")`
    - AppUpdaterController.swift: `"...latest version of Tome..."` -> `"...latest version of PS Transcribe..."`
    - OnboardingView.swift: `"Welcome to Tome"` -> `"Welcome to PS Transcribe"`

    **Pass 4 -- Path strings:**
    - TranscriptionEngine.swift: `"/tmp/tome.log"` (both occurrences) -> `"/tmp/pstranscribe.log"`
    - SessionStore.swift: `"Tome/sessions"` -> `"PSTranscribe/sessions"` in the app support path
    - AppSettings.swift: `"~/Documents/Tome/Meetings"` -> `"~/Documents/PSTranscribe/Meetings"`
    - AppSettings.swift: `"~/Documents/Tome/Voice"` -> `"~/Documents/PSTranscribe/Voice"`

    After all edits, verify the project builds:
    ```
    cd PSTranscribe && swift build 2>&1
    ```
  </action>
  <verify>
    <automated>cd PSTranscribe && swift build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `swift build` succeeds with exit code 0
    - `grep -r "io.gremble.tome" PSTranscribe/Sources/` returns no matches
    - `grep -r '"Tome"' PSTranscribe/Sources/ PSTranscribe/Package.swift` returns no matches (string literal "Tome" eliminated)
    - `grep -c "PSTranscribeApp" PSTranscribe/Sources/PSTranscribe/App/PSTranscribeApp.swift` returns at least 1
    - `grep -c "PSTranscribeUserDriver" PSTranscribe/Sources/PSTranscribe/App/AppUpdaterController.swift` returns at least 1
    - `grep "com.pstranscribe.app" PSTranscribe/Sources/PSTranscribe/Transcription/StreamingTranscriber.swift` returns a match
    - `grep "PSTranscribe/sessions" PSTranscribe/Sources/PSTranscribe/Storage/SessionStore.swift` returns a match
    - `grep "PSTranscribe/Meetings" PSTranscribe/Sources/PSTranscribe/Settings/AppSettings.swift` returns a match
    - `grep "pstranscribe.log" PSTranscribe/Sources/PSTranscribe/Transcription/TranscriptionEngine.swift` returns a match
    - Package.swift contains `name: "PSTranscribe"` and `path: "Sources/PSTranscribe"` and `"PSTranscribe.entitlements"`
  </acceptance_criteria>
  <done>All Swift source files updated with new names; project compiles successfully; zero residual "Tome" string literals in source</done>
</task>

</tasks>

<verification>
1. `cd PSTranscribe && swift build` completes with exit code 0
2. `grep -r "io.gremble.tome" PSTranscribe/Sources/` returns nothing
3. `grep -rn '"Tome"' PSTranscribe/Sources/ PSTranscribe/Package.swift` returns nothing
4. `test -d PSTranscribe/Sources/PSTranscribe/App` passes
5. `test ! -d Tome` passes
</verification>

<success_criteria>
- PSTranscribe/ directory exists with correct internal structure
- All Swift source references updated (type names, user-visible strings, paths, subsystem IDs)
- Project builds successfully with `swift build`
- No residual "Tome" string literals in Swift source or Package.swift
</success_criteria>

<output>
After completion, create `.planning/phases/01-rebrand/01-01-SUMMARY.md`
</output>
