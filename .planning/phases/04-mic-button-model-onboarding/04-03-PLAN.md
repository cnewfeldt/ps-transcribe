---
phase: 04-mic-button-model-onboarding
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified: []
autonomous: false
requirements:
  - MICB-01
  - MICB-02
  - MICB-03
  - MICB-05
  - MICB-06
  - ONBR-01
  - ONBR-02

must_haves:
  truths:
    - "All mic button states are visually correct and interactive"
    - "Onboarding flow works end-to-end including failure/retry"
    - "No waveform visualizer appears anywhere in the app"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of all Phase 4 changes -- mic button states, onboarding flow, and WaveformView removal.

Purpose: Several requirements (MICB-01, MICB-03, MICB-05, MICB-06, ONBR-01, ONBR-02) can only be verified by running the app and observing behavior.
Output: Confirmed working UI or identified issues for correction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-mic-button-model-onboarding/04-CONTEXT.md
@.planning/phases/04-mic-button-model-onboarding/04-UI-SPEC.md
@.planning/phases/04-mic-button-model-onboarding/04-01-SUMMARY.md
@.planning/phases/04-mic-button-model-onboarding/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and run full test suite</name>
  <files></files>
  <read_first>
    PSTranscribe/Sources/PSTranscribe/Views/ControlBar.swift,
    PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift,
    PSTranscribe/Sources/PSTranscribe/Views/OnboardingView.swift
  </read_first>
  <action>
Run the full test suite and build to confirm all code changes compile and pass:

```bash
cd /Users/cary/Development/ai-development/Tome/PSTranscribe && swift test 2>&1
```

Also verify the WaveformView file is gone:
```bash
test ! -f PSTranscribe/Sources/PSTranscribe/Views/WaveformView.swift && echo "DELETED" || echo "STILL EXISTS"
```

And verify no remaining WaveformView references:
```bash
grep -r "WaveformView" PSTranscribe/Sources/ 2>/dev/null || echo "NO REFERENCES"
```

If any test failures or build errors, fix them before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/cary/Development/ai-development/Tome/PSTranscribe && swift test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `swift test` exits with code 0
    - WaveformView.swift does not exist
    - No grep matches for WaveformView in source files
  </acceptance_criteria>
  <done>Full test suite passes, build succeeds, WaveformView fully removed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of mic button and onboarding</name>
  <what-built>
    Three-state mic button (idle/recording/error) in the control bar, replacing the waveform visualizer. OnboardingView with success/failure/retry states for model download.
  </what-built>
  <how-to-verify>
    1. **Build and launch the app:**
       ```
       cd /Users/cary/Development/ai-development/Tome/PSTranscribe && swift build && open .build/debug/PSTranscribe.app
       ```

    2. **Verify idle state (MICB-01, MICB-02):**
       - Confirm waveform visualizer is gone
       - Confirm mic icon button is centered between Call Capture and Voice Memo buttons
       - Confirm mic icon is ~24pt, muted color (fg2)
       - Hover mic icon -- tooltip should show "Start Recording"

    3. **Verify recording state (MICB-03):**
       - Click the mic icon or Call Capture to start recording
       - Confirm green pulsing ring radiates outward around mic icon (radar ping, not back-and-forth)
       - Confirm mic icon turns green
       - Click the mic icon to stop recording -- should stop the session

    4. **Verify error state (MICB-04, MICB-05, MICB-06):**
       - Revoke mic permission in System Settings > Privacy > Microphone (or disconnect mic if possible)
       - Confirm mic icon turns red with mic.slash
       - Hover the red mic -- tooltip should show error message
       - Click the red mic -- Settings pane should open

    5. **Verify onboarding (ONBR-01 through ONBR-05):**
       - Delete the hasCompletedOnboarding UserDefaults key:
         ```
         defaults delete com.pstranscribe.app hasCompletedOnboarding
         ```
       - Relaunch app -- onboarding wizard should appear
       - Verify 2 info slides with "Next" button
       - Verify model download step shows progress indicator
       - Verify "Get Started" button is disabled during download
       - Verify "Get Started" becomes enabled when download completes
       - If you can simulate a failure (disconnect internet during download), verify "Download Failed" message and "Try Again" button appear

    6. **Verify layout stability:**
       - Start and stop a recording -- control bar height should not jerk/jump
       - Window should maintain its minHeight:400 constraint
  </how-to-verify>
  <resume-signal>Type "approved" to complete phase, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Full test suite passes
- User confirms all visual/interactive behaviors match UI spec
</verification>

<success_criteria>
- All automated tests pass
- User has visually confirmed mic button idle/recording/error states
- User has verified onboarding failure/retry flow
- No WaveformView visible in the app
</success_criteria>

<output>
After completion, create `.planning/phases/04-mic-button-model-onboarding/04-03-SUMMARY.md`
</output>
