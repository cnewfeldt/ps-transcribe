---
phase: 13
plan: 05
type: execute
wave: 4
depends_on: [13-01, 13-02, 13-03, 13-04]
files_modified:
  - website/src/app/page.tsx
autonomous: false
requirements: [LAND-01, LAND-02, LAND-03, LAND-04, LAND-05, LAND-06, LAND-07]
must_haves:
  truths:
    - "`src/app/page.tsx` composes Hero, ThreeThingsStrip, four FeatureBlocks (one per mini-mock), ShortcutGrid, and FinalCTA in exactly that order"
    - "Each FeatureBlock receives its correct `index` (0-3), `tint`, `metaTone`, `metaLabel`, `headline`, `body`, `bullets`, `mock` per CONTEXT D-10/D-12"
    - "A production build (`pnpm build`) exits 0"
    - "`node website/scripts/verify-landing.mjs` exits 0 (every LAND-01..LAND-07 assertion passes, no forbidden strings ship)"
    - "A human UAT checkpoint confirms the five visual-only criteria from VALIDATION.md §'Manual-Only Verifications' (reveal motion-safety, nav scroll state, hero LCP, Spectral italic fidelity, responsive breakpoints)"
  artifacts:
    - path: "website/src/app/page.tsx"
      provides: "Landing-page composition"
      exports: ["default Home"]
      contains: "Hero, ThreeThingsStrip, FeatureBlock x4, ShortcutGrid, FinalCTA"
      min_lines: 80
  key_links:
    - from: "website/src/app/page.tsx"
      to: "website/src/components/sections/*"
      via: "import { Hero } from '@/components/sections/Hero' (+ siblings)"
      pattern: "from '@/components/sections/"
    - from: "website/src/app/page.tsx"
      to: "website/src/components/mocks/*"
      via: "import { DualStreamMock } from '@/components/mocks/DualStreamMock' (+ siblings)"
      pattern: "from '@/components/mocks/"
    - from: "website/scripts/verify-landing.mjs"
      to: "website/.next/server/app/*.html"
      via: "post-build grep suite"
      pattern: "verify-landing.mjs"
---

<objective>
Wire every section created in Plans 03/04 into the landing page, run the full production build, execute `verify-landing.mjs` green, and gate the phase on a human UAT checkpoint for the visual-only criteria.

Purpose: This is the assembly + acceptance plan. Plans 01-04 built every piece in isolation; Plan 05 composes them and proves the whole system works. The verify-landing script flips from MISS-heavy to all-OK here, and the UAT checkpoint confirms what grep can't see (reveal motion, scroll nav, italic fidelity, responsive breakpoints).

Output: A rewritten `page.tsx` + a production-green build + a green verify-landing run + a human-approved visual UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-landing-page/13-CONTEXT.md
@.planning/phases/13-landing-page/13-RESEARCH.md
@.planning/phases/13-landing-page/13-VALIDATION.md
@.planning/phases/13-landing-page/13-01-PLAN.md
@.planning/phases/13-landing-page/13-02-PLAN.md
@.planning/phases/13-landing-page/13-03-PLAN.md
@.planning/phases/13-landing-page/13-04-PLAN.md
@website/src/app/page.tsx
@website/src/components/sections/Hero.tsx
@website/src/components/sections/ThreeThingsStrip.tsx
@website/src/components/sections/FeatureBlock.tsx
@website/src/components/sections/ShortcutGrid.tsx
@website/src/components/sections/FinalCTA.tsx
@website/src/components/mocks/DualStreamMock.tsx
@website/src/components/mocks/ChatBubbleMock.tsx
@website/src/components/mocks/ObsidianVaultMock.tsx
@website/src/components/mocks/NotionTableMock.tsx
@website/scripts/verify-landing.mjs

<interfaces>
All interfaces required are listed in Plans 02-04 (Hero, ThreeThingsStrip, FeatureBlock, ShortcutGrid, FinalCTA, DualStreamMock, ChatBubbleMock, ObsidianVaultMock, NotionTableMock). This plan only CONSUMES them.
</interfaces>

<feature_specifications>
Feature block copy and configuration (all four feature assignments; Plan 05 passes these exact values to FeatureBlock):

| Index | tint | metaTone | metaLabel | headline | body (React node) | bullets[] | mock |
|-------|------|----------|-----------|----------|-------------------|-----------|------|
| 0 | `tint` | `default` | `Dual-stream capture` | `Microphone and system audio, recorded in parallel.` | `Uses ScreenCaptureKit to pull the other side of the call cleanly, while your mic is captured separately. After the session ends, Silero VAD and diarization resolve who said what.` | `["You on mic, them via system audio — two distinct streams", "Voice-activity detection runs locally; silences are trimmed", "Post-session speaker diarization clusters unknown voices"]` | `<DualStreamMock />` |
| 1 | `sage` | `sage` | `Transcript view` | `Chat bubbles. Not a wall of text.` | `Your side sits right; Speaker 2 sits left with a sage rail. Rename any speaker inline, and every subsequent bubble updates. Click any timestamp to scrub the session.` | `["10pt mono timestamps, quietly recessed", "Inline speaker rename with ⌘E", "Full-text search with ⌘F, scoped to the session"]` | `<ChatBubbleMock />` |
| 2 | `default` | `navy` | `Obsidian vault` | `Every session lands where your notes already live.` | JSX: `Markdown file, YAML frontmatter, saved to the folder you configure. No proprietary format, no export step — the transcript <em className="italic">is</em> a note in your vault, instantly linkable.` | `["Template frontmatter: date, duration, participants, tags", "Configurable path templates per recording type", "Works with Obsidian sync, git, iCloud — whatever you already use"]` | `<ObsidianVaultMock />` |
| 3 | `tint` | `default` | `Notion, on send` | `Push finished sessions to a database, one key away.` | `Configure a Notion database once. When you stop recording, PS Transcribe can send the transcript as a new page — with the same frontmatter mapped into properties. Leave it off and nothing syncs.` | `["Property mapping for participants, tags, duration", "Opt-in per recording, or default on", "Integration token stays in Keychain"]` | `<NotionTableMock />` |

(Index 2 body is JSX because of the `<em>` for "is" — preserves the real Spectral italic cut on "is". Index 2 bullet #3 has "whatever you already use" — do not drop the closing word.)
</feature_specifications>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compose page.tsx and run the full build + verify suite</name>
  <files>website/src/app/page.tsx</files>
  <read_first>
    - website/src/app/page.tsx (current — placeholder; you REPLACE wholesale)
    - .planning/phases/13-landing-page/13-RESEARCH.md §"Example 8: Top-level page assembly" (exact composition order + feature wiring — port verbatim with the corrections noted below)
    - .planning/phases/13-landing-page/13-CONTEXT.md (feature block ordering, Reveal opt-in, deferred items that MUST NOT appear)
    - .planning/phases/13-landing-page/13-VALIDATION.md (the sign-off criteria this plan must satisfy; update its Per-Task Verification Map via the planner's step after this task ships)
  </read_first>
  <action>
    Step 1 — Replace the contents of `website/src/app/page.tsx` ENTIRELY with the composition below. Do not preserve any of the placeholder inline styling from the Phase-11 page. The current placeholder's `<main style={...}>` block is completely replaced.

    ```tsx
    import { Hero } from '@/components/sections/Hero'
    import { ThreeThingsStrip } from '@/components/sections/ThreeThingsStrip'
    import { FeatureBlock } from '@/components/sections/FeatureBlock'
    import { ShortcutGrid } from '@/components/sections/ShortcutGrid'
    import { FinalCTA } from '@/components/sections/FinalCTA'
    import { DualStreamMock } from '@/components/mocks/DualStreamMock'
    import { ChatBubbleMock } from '@/components/mocks/ChatBubbleMock'
    import { ObsidianVaultMock } from '@/components/mocks/ObsidianVaultMock'
    import { NotionTableMock } from '@/components/mocks/NotionTableMock'

    export default function Home() {
      return (
        <main className="bg-paper text-ink">
          <Hero />
          <ThreeThingsStrip />

          <section className="py-10 md:py-14">
            <div className="mx-auto max-w-[1200px] px-6 md:px-10">
              <FeatureBlock
                index={0}
                tint="tint"
                metaLabel="Dual-stream capture"
                headline="Microphone and system audio, recorded in parallel."
                body="Uses ScreenCaptureKit to pull the other side of the call cleanly, while your mic is captured separately. After the session ends, Silero VAD and diarization resolve who said what."
                bullets={[
                  'You on mic, them via system audio — two distinct streams',
                  'Voice-activity detection runs locally; silences are trimmed',
                  'Post-session speaker diarization clusters unknown voices',
                ]}
                mock={<DualStreamMock />}
              />

              <hr className="border-0 h-[0.5px] bg-rule" />

              <FeatureBlock
                index={1}
                tint="sage"
                metaTone="sage"
                metaLabel="Transcript view"
                headline="Chat bubbles. Not a wall of text."
                body="Your side sits right; Speaker 2 sits left with a sage rail. Rename any speaker inline, and every subsequent bubble updates. Click any timestamp to scrub the session."
                bullets={[
                  '10pt mono timestamps, quietly recessed',
                  'Inline speaker rename with ⌘E',
                  'Full-text search with ⌘F, scoped to the session',
                ]}
                mock={<ChatBubbleMock />}
              />

              <hr className="border-0 h-[0.5px] bg-rule" />

              <FeatureBlock
                index={2}
                tint="default"
                metaTone="navy"
                metaLabel="Obsidian vault"
                headline="Every session lands where your notes already live."
                body={
                  <>
                    Markdown file, YAML frontmatter, saved to the folder you configure. No proprietary format, no export step — the transcript <em className="italic">is</em> a note in your vault, instantly linkable.
                  </>
                }
                bullets={[
                  'Template frontmatter: date, duration, participants, tags',
                  'Configurable path templates per recording type',
                  'Works with Obsidian sync, git, iCloud — whatever you already use',
                ]}
                mock={<ObsidianVaultMock />}
              />

              <hr className="border-0 h-[0.5px] bg-rule" />

              <FeatureBlock
                index={3}
                tint="tint"
                metaLabel="Notion, on send"
                headline="Push finished sessions to a database, one key away."
                body="Configure a Notion database once. When you stop recording, PS Transcribe can send the transcript as a new page — with the same frontmatter mapped into properties. Leave it off and nothing syncs."
                bullets={[
                  'Property mapping for participants, tags, duration',
                  'Opt-in per recording, or default on',
                  'Integration token stays in Keychain',
                ]}
                mock={<NotionTableMock />}
              />
            </div>
          </section>

          <ShortcutGrid />
          <FinalCTA />
        </main>
      )
    }
    ```

    Step 2 — Run the full production build:
    ```
    pnpm --filter ps-transcribe-website typecheck
    pnpm --filter ps-transcribe-website build
    ```
    Both MUST exit 0. If typecheck fails, fix before building. If build fails, read the error; common failures:
    - `Module not found` → an import path is wrong; check `@/components/...` paths match the files.
    - `fs is not a function` → `changelog.ts` ended up inside a `'use client'` module (should not happen; reread changelog.ts and verify no `'use client'` at top).
    - `Only plain objects can be passed to Client Components from Server Components` → a function/non-serializable thing leaked through `<Reveal>`; inspect the offending FeatureBlock `body`/`mock` prop.

    Step 3 — Run `verify-landing.mjs`:
    ```
    node website/scripts/verify-landing.mjs
    ```
    Expected: exit code 0, every `OK   LAND-0X` line printed, every `OK   forbidden-absent: ...` printed, no `MISS` or `BAD` lines. If ANY assertion fails, investigate — do NOT loosen the grep strings. The forbidden-string checks (`macOS 14+`, `PS-Transcribe.dmg`, `Apple Silicon & Intel`, `content="noindex"`) are load-bearing; if any trip, the fix is in the section component that leaked the wrong copy, not in the verifier.

    Step 4 — Commit the page.tsx change. The verify + build artifacts are not committed (.next/ is in .gitignore); only source files.
  </action>
  <verify>
    <automated>grep -q "import { Hero }" website/src/app/page.tsx &amp;&amp; grep -q "import { ThreeThingsStrip }" website/src/app/page.tsx &amp;&amp; grep -q "import { FeatureBlock }" website/src/app/page.tsx &amp;&amp; grep -q "import { ShortcutGrid }" website/src/app/page.tsx &amp;&amp; grep -q "import { FinalCTA }" website/src/app/page.tsx &amp;&amp; grep -q "DualStreamMock" website/src/app/page.tsx &amp;&amp; grep -q "ChatBubbleMock" website/src/app/page.tsx &amp;&amp; grep -q "ObsidianVaultMock" website/src/app/page.tsx &amp;&amp; grep -q "NotionTableMock" website/src/app/page.tsx &amp;&amp; grep -q "index={0}" website/src/app/page.tsx &amp;&amp; grep -q "index={1}" website/src/app/page.tsx &amp;&amp; grep -q "index={2}" website/src/app/page.tsx &amp;&amp; grep -q "index={3}" website/src/app/page.tsx &amp;&amp; ! grep -q "Site coming soon" website/src/app/page.tsx &amp;&amp; pnpm --filter ps-transcribe-website typecheck &amp;&amp; pnpm --filter ps-transcribe-website build &amp;&amp; node website/scripts/verify-landing.mjs</automated>
  </verify>
  <done>
    page.tsx composes all nine components in the right order with the right feature props; typecheck + build green; verify-landing.mjs exits 0 with every OK line printed.
  </done>
  <acceptance_criteria>
    - `! grep -q "Site coming soon" website/src/app/page.tsx` (placeholder gone)
    - `! grep -qE "style=\\{\\{" website/src/app/page.tsx` (no inline style blocks left over from the placeholder)
    - `grep -c "FeatureBlock" website/src/app/page.tsx` returns at least 4 (one import + 4 usages = 5; accept ≥4)
    - `grep -c "index={" website/src/app/page.tsx` returns 4 (indexes 0..3)
    - `grep -q "DualStreamMock" website/src/app/page.tsx`
    - `grep -q "ChatBubbleMock" website/src/app/page.tsx`
    - `grep -q "ObsidianVaultMock" website/src/app/page.tsx`
    - `grep -q "NotionTableMock" website/src/app/page.tsx`
    - `grep -q "the transcript <em" website/src/app/page.tsx` (Feature 3 preserves the italic `is`)
    - `pnpm --filter ps-transcribe-website typecheck` exits 0
    - `pnpm --filter ps-transcribe-website build` exits 0
    - `node website/scripts/verify-landing.mjs` exits 0
    - `node website/scripts/verify-landing.mjs 2>&1 | grep -c "^OK"` returns at least 28 (≥24 must-have assertions + 4 forbidden-absent)
    - `node website/scripts/verify-landing.mjs 2>&1 | grep -c "^MISS"` returns 0
    - `node website/scripts/verify-landing.mjs 2>&1 | grep -c "^BAD"` returns 0
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — visual-only criteria from VALIDATION.md</name>
  <what-built>
    The complete landing page at `/` composed in page.tsx, covering hero variant C with the app screenshot, three-things intro strip, four feature blocks with mini-mockups, keyboard shortcuts callout, final CTA card, shared Nav, and shared Footer. All the grep-verifiable criteria (LAND-01..LAND-07 content, DMG URL encoding, macOS version string, no forbidden strings) already proven green by `verify-landing.mjs` in Task 1. This checkpoint covers what automated greps cannot see.
  </what-built>
  <how-to-verify>
    Run the site locally:
    ```
    pnpm --filter ps-transcribe-website dev
    ```
    Open http://localhost:3000/ in Chrome (or your preferred browser).

    Check each of the five manual criteria from `.planning/phases/13-landing-page/13-VALIDATION.md` §"Manual-Only Verifications":

    1. **Spectral italic fidelity (D-specifics)**
       - Zoom the hero headline. The word `never leaves your Mac.` on line 2 is italic.
       - Visit https://fonts.google.com/specimen/Spectral and compare the 'a', 'e', 'y' glyphs in the italic specimen against what you see on the page.
       - A real italic has distinct stroke terminals (e.g., the `a` has a rounded bowl shape that differs from the upright cut). A synthesized italic just skews the upright letters.
       - PASS: glyphs match the Google Fonts italic specimen (different letterforms, not skewed uprights).

    2. **Nav scroll state transition (D-17)**
       - Scroll position = 0: nav is transparent/paper background with no shadow.
       - Scroll down 10px+: nav gains a subtle `shadow-btn` shadow and the background shifts to `paper-warm/92` (slightly warmer tone).
       - Scroll back to top: transitions back to transparent. No jitter, no flash.
       - PASS: transition is smooth, both states visible.

    3. **prefers-reduced-motion fallback (D-19)**
       - Chrome DevTools → Rendering panel → Emulate CSS media feature → set `prefers-reduced-motion` to `reduce`.
       - Hard-reload `/`.
       - Scroll through the page. Every feature block, three-things card, and the final CTA should be fully opaque and positioned BEFORE any scroll — no fade-in transition visible.
       - PASS: no fade animations observed anywhere; hero is visible on load (it's always visible regardless).

    4. **Responsive breakpoint collapse (CONTEXT "Responsive breakpoints")**
       - DevTools → responsive mode.
       - At 1440px: hero centered, four feature blocks alternate copy-left / mock-right / copy-left / mock-right, three-things strip is a 3-col grid, shortcuts grid is 4 columns.
       - At 980px: hero still centered; feature blocks may still be two-column (the `lg` breakpoint is 1024px by default in Tailwind v4).
       - At 899px: feature blocks collapse to single column with COPY FIRST, then mock (no reading-order inversion).
       - At 819px: three-things strip collapses to single column.
       - At 390px (iPhone width): no horizontal scrollbar, copy still reads left-aligned, CTAs stack if needed.
       - PASS: no horizontal scroll at any width ≥360px; feature blocks read heading-first on mobile.

    5. **Hero LCP (D-07)**
       - DevTools → Lighthouse → Performance audit on `/` (localhost or Vercel preview). Requires `pnpm build && pnpm start` to get production build numbers.
       - LCP ≤ 2.5s on emulated desktop (cable throughput).
       - CLS = 0 (hero image has explicit width/height; no layout shift).
       - PASS: LCP and CLS within thresholds.

    Bonus sanity check:
    - Click the primary "Download for macOS" CTA. The browser should download (or offer to download) a DMG file named `PS Transcribe.dmg` from GitHub Releases (you can cancel the download; you just need to confirm the URL resolves to a real asset, not a 404).
    - Click "View on GitHub →" — should open the repo.
    - Click nav "GitHub" — same target.
    - Click nav "Docs" — 404 (expected; Phase 14 lands this route).
    - Click nav "Changelog" — 404 (expected; Phase 15 lands this route).
    - Click footer "Sparkle appcast" — should return an Atom feed from GitHub.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 5 criteria pass AND the sanity checks succeed.
    Type "reject: &lt;reason&gt;" if any criterion fails (e.g., "reject: italic looks synthesized — letterforms are identical to upright" or "reject: feature 2 mock shows before heading at 390px").
    If you flag a failure, a gap-closure plan will be scheduled to address it.
  </resume-signal>
  <files>website/src/app/page.tsx (no modification in this task — already written in Task 1; this is a manual-verify checkpoint against the running site)</files>
  <action>Pause for human UAT. Do NOT proceed to commit, mark-complete, or close the phase until the human returns "approved". See `&lt;how-to-verify&gt;` above for the step-by-step test script; treat the five criteria as independent pass/fail gates. If any criterion fails, stop execution and schedule a gap-closure plan — do NOT attempt an in-place fix in this plan.</action>
  <verify>
    <automated>echo "MANUAL UAT — awaiting human approval per how-to-verify steps 1-5"; test -n "${UAT_APPROVED:-}"</automated>
  </verify>
  <done>Human typed "approved". All five VALIDATION.md manual criteria passed. The three bonus sanity checks (DMG URL resolves, GitHub link works, Sparkle appcast returns Atom) also passed.</done>
  <acceptance_criteria>
    - Human returned resume-signal = "approved"
    - All five VALIDATION.md manual criteria pass visual inspection
    - Bonus sanity: `curl -sI "https://github.com/cnewfeldt/ps-transcribe/releases/latest/download/PS%20Transcribe.dmg" | head -1` returns `HTTP/2 302` or `HTTP/2 200` (not 404)
    - Bonus sanity: `curl -sI https://github.com/cnewfeldt/ps-transcribe/releases.atom | head -1` returns 200
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| integration | First time all sections touch each other. Serialization boundary between `<Reveal>` (client) and each section's JSX (plain) is the load-bearing compile-time check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05-01 | Tampering | Feature block body JSX (Feature 3's `<em>`) fails serialization across the `<Reveal>` client boundary | mitigate | FeatureBlock wraps with `<Reveal>` internally, children are plain JSX — no render props, no functions. Verified by `pnpm build` succeeding (the error "Only plain objects can be passed to Client Components from Server Components" is compile-time). |
| T-13-05-02 | Integrity | Forbidden strings (`macOS 14+`, `PS-Transcribe.dmg`, `Apple Silicon & Intel`, `noindex`) leak into the production build | mitigate | `verify-landing.mjs`'s forbidden-string list is asserted AFTER the build; the script exits 1 if any are present. Task 1's acceptance criteria gate on `verify-landing.mjs` exit code 0. |
| T-13-05-03 | Denial of Service | Build succeeds but prerendered HTML is missing the landing route | mitigate | `verify-landing.mjs` walks `.next/server/app/` and fails with a clear error if the prerendered HTML isn't produced. Exit code 2 distinguishes this from assertion failures. |
| T-13-05-04 | Information Disclosure | `noindex` accidentally ships on `/` because a previous-phase robots config was inherited | mitigate | `verify-landing.mjs` forbidden-string list contains `content="noindex"`; `layout.tsx` (Plan 01 / Phase 11) sets `robots: { index: true, follow: true }`. |
| T-13-05-05 | Elevation of Privilege | External link opens via `target="_blank"` without `rel="noopener"` (tabnabbing) | accept | No `target="_blank"` anywhere in Phase 13 (verified: Nav, Footer, Hero, FinalCTA all use default same-tab). |
</threat_model>

<verification>
Phase 13 complete when:
- `pnpm --filter ps-transcribe-website typecheck` exits 0
- `pnpm --filter ps-transcribe-website build` exits 0
- `node website/scripts/verify-landing.mjs` exits 0 (every LAND-01..LAND-07 assertion + forbidden-string check green)
- Human UAT returns "approved" on all 5 visual-only criteria
- Vercel preview deploy (auto-generated from the PR) renders correctly at the shareable URL (implicit — Vercel reruns the same build commands)
</verification>

<success_criteria>
- `website/src/app/page.tsx` ≥ 80 lines, imports all 9 components, passes typecheck + build.
- `verify-landing.mjs` exits 0.
- UAT approved.
- Every LAND-01..LAND-07 requirement has at least one rendered-HTML artifact proving it's live.
</success_criteria>

<output>
After completion, create `.planning/phases/13-landing-page/13-05-SUMMARY.md` describing:
- Final page.tsx composition + line count
- `verify-landing.mjs` full output (copy/paste the OK lines)
- UAT outcome (pass/fail per criterion) with screenshots if the human attaches any
- Build bundle-size delta vs Phase 12's placeholder page (informational — not a gate)
- Any deferred follow-ups identified during UAT (e.g., "copy tweak for final CTA" if the human noticed one) to add to pending todos
</output>
