---
phase: 18.1
plan: 06
type: execute
wave: 3
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - PSTranscribe/Tests/PSTranscribeTests/Phase18/DictationCancelFlowTests.swift
  - PSTranscribe/Tests/PSTranscribeTests/Phase18/DictationCoordinatorStateTests.swift
  - PSTranscribe/Tests/PSTranscribeTests/Phase18/ClipboardPrivacyMarkersTests.swift
  - PSTranscribe/Tests/PSTranscribeTests/Phase18/ClipboardRestoreTests.swift
  - PSTranscribe/Tests/PSTranscribeTests/Phase18/SessionCoordinatorMutualExclusionTests.swift
  - PSTranscribe/Tests/PSTranscribeTests/Phase18/MenuBarIndicatorTests.swift
  - PSTranscribe/Tests/PSTranscribeTests/Phase18/DictationHUDLiveTranscriptTests.swift
  - PSTranscribe/Tests/PSTranscribeTests/Phase18/LibraryEntryRowDictationIconTests.swift
  - PSTranscribe/Tests/PSTranscribeTests/Phase18/DictationLoggerDiscardTests.swift
  - PSTranscribe/Tests/PSTranscribeTests/Phase18/AutoNameTests.swift
autonomous: true
requirements:
  - DICT-05
  - DICT-09
  - FOLDER-03
must_haves:
  truths:
    - "Every Phase 18 test that constructs a DictationCoordinator passes the new saveDestinations: parameter"
    - "Every Phase 18 test that referenced DictationOutputMode / dictationOutputMode / dictationFolderPath has been migrated to AppSettings flags (localFileEnabled / localFileRoot / obsidianEnabled / etc.) or had those assertions deleted"
    - "Full Phase 18 test suite is green: cd PSTranscribe && swift test --filter Phase18 exits 0"
    - "Full Phase 18.1 test suite is green: cd PSTranscribe && swift test --filter Phase18_1 OR equivalent suite-name match exits 0"
    - "Full project test suite is green: cd PSTranscribe && swift test exits 0"
  artifacts:
    - path: "PSTranscribe/Tests/PSTranscribeTests/Phase18/"
      provides: "All Phase 18 tests that survive 18.1's API changes are migrated and green"
  key_links:
    - from: "Phase 18 DictationCoordinator construction sites"
      to: "Plan 04 new init signature (saveDestinations: SaveDestinations)"
      via: "all Phase 18 init call sites updated"
      pattern: "DictationCoordinator\\(\\s*settings:"
    - from: "Phase 18 obsolete tests"
      to: "Plan 02/03/04 replacements"
      via: "deleted in Plans 01-05; this plan catches stragglers"
      pattern: "dictationOutputMode|dictationFolderPath|DictationOutputMode\\."
---

<objective>
Migrate ALL remaining Phase 18 test files that reference deleted AppSettings keys or the old `DictationCoordinator` init signature. Plans 04 (Task 2 Step C) already migrated some sibling tests; this plan systematically completes the migration across all eight remaining Phase 18 test files and verifies the full test suite passes.

Two categories of changes per file:

1. **Init signature update**: `DictationCoordinator(settings:..., sessionCoordinator:..., libraryStore:...)` → add `saveDestinations: <newly constructed SaveDestinations>` parameter.

2. **Property rename / removal**:
   - `settings.dictationOutputMode = .clipboard` → `settings.localFileEnabled = false`
   - `settings.dictationOutputMode = .plainFolder` → `settings.localFileEnabled = true; settings.localFileRoot = <tempPath>`
   - `settings.dictationOutputMode = .both` → `settings.localFileEnabled = true; settings.localFileRoot = <tempPath>`
   - `settings.dictationFolderPath = X` → `settings.localFileRoot = X`
   - References to file existing at `<dictationFolderPath>/<filename>` → check `<localFileRoot>/Dictation/<filename>` (the new hardcoded subfolder per D-04)
   - `DictationOutputMode.clipboard` / `.plainFolder` / `.both` enum references → DELETE (enum doesn't exist anymore; assertion likely needs rewording)

After completion: `cd PSTranscribe && swift test` is green for the entire suite. This is the phase's final acceptance gate.

Purpose: catch every Phase 18 test that depends on the deleted API and migrate it. This is execution-mechanical work; no new logic.
Output: green `swift test`, ready for `/gsd-verify-work`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18.1-shared-save-destinations-local-file/18.1-CONTEXT.md
@.planning/phases/18.1-shared-save-destinations-local-file/18.1-RESEARCH.md
@PSTranscribe/Sources/PSTranscribe/App/DictationCoordinator.swift
@PSTranscribe/Sources/PSTranscribe/Services/SaveDestinations.swift

<interfaces>
<!-- The new DictationCoordinator init signature (after Plan 04 Task 1) -->

```swift
init(settings: AppSettings,
     sessionCoordinator: SessionCoordinator,
     libraryStore: LibraryStore,
     saveDestinations: SaveDestinations)
```

The MainActor wrapper required by SaveDestinations:
```swift
@MainActor
final class SaveDestinations {
    init(settings: AppSettings, notionService: NotionService, ...)
}
```

So every test that constructs a DictationCoordinator must:
1. Be `@MainActor` annotated (or wrap construction in `await MainActor.run { ... }`).
2. Construct a SaveDestinations:
   ```swift
   let saveDest = SaveDestinations(settings: settings, notionService: NotionService())
   ```
3. Pass it into the coordinator init.

Plan 04 Task 2 Step C already lists these eight files as targets. This plan REQUIRES that work to be complete and runs the verification. If Plan 04 Task 2 missed any file or any reference, this plan catches it.

Remaining files to inspect (from `ls Tests/PSTranscribeTests/Phase18/` minus those already deleted in Plans 01/02 and minus those already migrated in Plan 04 Task 2 Step C):
- AutoNameTests.swift -- uses coordinator's `autoNameFromTranscript` directly; may not construct full coordinator
- ClipboardPrivacyMarkersTests.swift -- uses coordinator
- ClipboardRestoreTests.swift -- uses coordinator
- DictationCancelFlowTests.swift -- uses coordinator
- DictationCommitFlowTests.swift -- migrated by Plan 04
- DictationCoordinatorStateTests.swift -- uses coordinator
- DictationHUDLiveTranscriptTests.swift -- may use coordinator
- DictationLoggerDiscardTests.swift -- uses DictationLogger directly, NOT coordinator -- likely no change
- DictationWindowControllerTests.swift -- uses window controller, NOT coordinator -- likely no change
- GlobalHotkeyServiceTests.swift -- isolated to hotkey -- no change
- LibraryEntryRowDictationIconTests.swift -- may use coordinator
- MenuBarIndicatorTests.swift -- may use coordinator
- PasteboardTestLock.swift -- helper, no change
- SessionCoordinatorMutualExclusionTests.swift -- uses coordinator
- SettingsViewDictationSectionTests.swift -- migrated by Plan 03
- AppSettingsDictationPersistenceTests.swift -- DELETED by Plan 01
- PlainFolderFallbackTests.swift -- DELETED by Plan 02
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sweep + migrate remaining Phase 18 test files; run full test suite</name>
  <files>
    PSTranscribe/Tests/PSTranscribeTests/Phase18/AutoNameTests.swift
    PSTranscribe/Tests/PSTranscribeTests/Phase18/ClipboardPrivacyMarkersTests.swift
    PSTranscribe/Tests/PSTranscribeTests/Phase18/ClipboardRestoreTests.swift
    PSTranscribe/Tests/PSTranscribeTests/Phase18/DictationCancelFlowTests.swift
    PSTranscribe/Tests/PSTranscribeTests/Phase18/DictationCoordinatorStateTests.swift
    PSTranscribe/Tests/PSTranscribeTests/Phase18/DictationHUDLiveTranscriptTests.swift
    PSTranscribe/Tests/PSTranscribeTests/Phase18/DictationLoggerDiscardTests.swift
    PSTranscribe/Tests/PSTranscribeTests/Phase18/LibraryEntryRowDictationIconTests.swift
    PSTranscribe/Tests/PSTranscribeTests/Phase18/MenuBarIndicatorTests.swift
    PSTranscribe/Tests/PSTranscribeTests/Phase18/SessionCoordinatorMutualExclusionTests.swift
  </files>
  <read_first>
    PSTranscribe/Tests/PSTranscribeTests/Phase18/AutoNameTests.swift
    PSTranscribe/Tests/PSTranscribeTests/Phase18/ClipboardPrivacyMarkersTests.swift
    PSTranscribe/Tests/PSTranscribeTests/Phase18/ClipboardRestoreTests.swift
    PSTranscribe/Tests/PSTranscribeTests/Phase18/DictationCancelFlowTests.swift
    PSTranscribe/Tests/PSTranscribeTests/Phase18/DictationCoordinatorStateTests.swift
    PSTranscribe/Tests/PSTranscribeTests/Phase18/DictationHUDLiveTranscriptTests.swift
    PSTranscribe/Tests/PSTranscribeTests/Phase18/DictationLoggerDiscardTests.swift
    PSTranscribe/Tests/PSTranscribeTests/Phase18/LibraryEntryRowDictationIconTests.swift
    PSTranscribe/Tests/PSTranscribeTests/Phase18/MenuBarIndicatorTests.swift
    PSTranscribe/Tests/PSTranscribeTests/Phase18/SessionCoordinatorMutualExclusionTests.swift
    PSTranscribe/Sources/PSTranscribe/App/DictationCoordinator.swift
    PSTranscribe/Sources/PSTranscribe/Services/SaveDestinations.swift
  </read_first>
  <behavior>
    - Test 1: After this task, `grep -rE "dictationOutputMode|dictationFolderPath|DictationOutputMode\\." PSTranscribe/Tests/PSTranscribeTests/` returns nothing.
    - Test 2: After this task, every `DictationCoordinator(` construction in `PSTranscribe/Tests/` includes a `saveDestinations:` argument.
    - Test 3: `cd PSTranscribe && swift test --filter Phase18` exits 0.
    - Test 4: `cd PSTranscribe && swift test --filter Phase18_1` (or test-suite filter that catches Phase 18.1 tests) exits 0.
    - Test 5: `cd PSTranscribe && swift test` (full suite) exits 0.
  </behavior>
  <action>
**Step A — sweep all test files for legacy API references.**

Run a discovery pass first:
```bash
cd PSTranscribe
grep -rln "DictationOutputMode\|dictationOutputMode\|dictationFolderPath" Tests/PSTranscribeTests/
grep -rln "DictationCoordinator(" Tests/PSTranscribeTests/
```

The first command lists every test file that references a deleted property/enum. The second lists every test file that constructs a DictationCoordinator (which needs the new init signature).

For each file in the union of those two lists (excluding files already migrated by Plans 01-04 — verify by re-greping after edits), apply the migration rules below.

**Step B — migration rules (apply per-file).**

For each test file in the per-file list:

1. **`@MainActor` annotation**: if the test file's `@Suite` struct constructs a `DictationCoordinator` (which is `@MainActor`), the suite struct or each test method MUST be `@MainActor`. If not already annotated, add `@MainActor` to the `@Suite` struct or to each `@Test` function as appropriate (mirror the existing Phase 18 pattern that already works for some files like DictationCommitFlowTests).

2. **Construct `SaveDestinations`** before constructing the coordinator. Add this line right after the `let settings = ...` and before the `DictationCoordinator(` call:
   ```swift
   let saveDest = SaveDestinations(settings: settings, notionService: NotionService())
   ```

3. **Update the coordinator init**: append `, saveDestinations: saveDest` as the last argument:
   ```swift
   let coordinator = DictationCoordinator(
       settings: settings,
       sessionCoordinator: sessionCoord,
       libraryStore: library,
       saveDestinations: saveDest    // <-- ADD THIS LINE
   )
   ```

4. **Replace AppSettings legacy assignments**:
   - `settings.dictationOutputMode = .clipboard` → `settings.localFileEnabled = false; settings.obsidianEnabled = false; settings.notionAutoSendEnabled = false`
   - `settings.dictationOutputMode = .plainFolder` → `settings.localFileEnabled = true; settings.localFileRoot = <tempPath>; settings.obsidianEnabled = false; settings.notionAutoSendEnabled = false` (use whatever tempPath the test already declared, or generate one with `FileManager.default.temporaryDirectory.appendingPathComponent("test-\(UUID()).path")`)
   - `settings.dictationOutputMode = .both` → same as `.plainFolder` (clipboard is now unconditional, so "both" semantics are now "Local File on")
   - `settings.dictationFolderPath = X` → `settings.localFileRoot = X`
   - `settings.dictationFolderPath` (read) → `settings.localFileRoot` (read)

5. **Update file-existence assertions**:
   - If a test asserts a file at `<dictationFolderPath>/<filename>`, change the path to `<localFileRoot>/Dictation/<filename>` per D-04 hardcoded subfolder.

6. **Delete `DictationOutputMode` enum references**:
   - Remove any `import` or `@testable` access pattern that requires the enum to exist.
   - If a test asserts `coordinator.<some property> == DictationOutputMode.clipboard`, that test was about the deleted mode-branching behavior — the test should be DELETED entirely (the behavior it covered no longer exists).

7. **Keep clipboard tests UNCHANGED conceptually** (they're now MORE relevant, not less, since clipboard is unconditional in 18.1):
   - `ClipboardPrivacyMarkersTests` — clipboard write with privacy markers is now D-15 unconditional, no settings flag gates it.
   - `ClipboardRestoreTests` — same: restore is unconditional.
   - These tests should still pass after the init-signature update with NO behavioral assertion changes.

**Step C — special-case files.**

These files have unique migration considerations:

- **AutoNameTests.swift**: tests `coordinator.autoNameFromTranscript(_:)` directly. The function is unchanged in 18.1. The only edit is updating the coordinator init to include `saveDestinations:`.

- **DictationLoggerDiscardTests.swift**: tests `DictationLogger` actor directly, NOT through DictationCoordinator. NO changes needed unless the file imports the coordinator (unlikely; it tests `dictationLogger.discardSession()` D-08 atomicity). Verify by reading the file; if it has zero `DictationCoordinator(` references, leave it alone.

- **DictationWindowControllerTests.swift**: tests the NSPanel/window controller. NO changes needed.

- **GlobalHotkeyServiceTests.swift**: tests the hotkey service. NO changes needed.

- **PasteboardTestLock.swift**: helper actor, NOT a test file. NO changes.

- **DictationCoordinatorStateTests.swift**: tests the state machine via `_test_setState` / `_test_setElapsed` debug helpers. The init must be updated; the state machine assertions remain unchanged.

- **SessionCoordinatorMutualExclusionTests.swift**: tests that SessionCoordinator.anySessionActive ORs in dictationCoordinator.isActive. Init must be updated; assertions remain.

- **MenuBarIndicatorTests.swift**: tests that MenuBarExtra label flips on `dictationCoordinator.isActive`. Init must be updated.

- **DictationHUDLiveTranscriptTests.swift**: tests the HUD live partial transcript binding. Init must be updated.

- **LibraryEntryRowDictationIconTests.swift**: tests the dictation row icon. May or may not construct a coordinator; if it does, init must be updated.

**Step D — verify and run.**

After all per-file edits:

1. `cd PSTranscribe && grep -rE "dictationOutputMode|dictationFolderPath|DictationOutputMode\\." Tests/PSTranscribeTests/` — expected: no output.
2. `cd PSTranscribe && grep -rl "DictationCoordinator(" Tests/PSTranscribeTests/` — for each file listed, verify that `grep -A 5 "DictationCoordinator(" <file>` shows a `saveDestinations:` parameter on the call.
3. `cd PSTranscribe && swift build` — exits 0.
4. `cd PSTranscribe && swift test --filter Phase18` — runs all Phase 18 tests; exits 0.
5. `cd PSTranscribe && swift test` — full suite; exits 0.

If any test fails after migration:
- If it's a SOURCE-LEVEL failure (compile error referencing a deleted symbol) → the migration missed a reference; find and fix.
- If it's a BEHAVIORAL failure (assertion fail with valid Swift compile) → the test's expectations may need recalibration:
  - Tests that asserted "clipboard mode → no file written" now must assert "localFileEnabled=false → no file written" (semantically equivalent).
  - Tests that asserted "DictationOutputMode is .clipboard" had their entire scope removed; the test should be DELETED, not adjusted.
  - Tests that exercise positive paths (clipboard contents present, library entry created, etc.) should pass unchanged after the init update.

Document any deleted tests + their replacement in 18.1-06-SUMMARY.md.
  </action>
  <verify>
    <automated>cd PSTranscribe && (! grep -rE "dictationOutputMode|dictationFolderPath|DictationOutputMode\\." Tests/PSTranscribeTests/) && swift build 2>&1 | tail -5 && swift test --filter Phase18 2>&1 | tail -10 && swift test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE "dictationOutputMode|dictationFolderPath|DictationOutputMode\\." PSTranscribe/Tests/PSTranscribeTests/` returns no output (no remaining legacy references in any test file).
    - Every test file that contains the substring `DictationCoordinator(` ALSO contains `saveDestinations:` on a line within 10 lines of the `DictationCoordinator(` line (verifiable with `awk` or visual grep). Every coordinator construction passes the new arg.
    - `cd PSTranscribe && swift build` exits 0.
    - `cd PSTranscribe && swift test --filter Phase18` exits 0 (Phase 18 regression suite green).
    - `cd PSTranscribe && swift test` exits 0 (entire test suite green — Phase 18, Phase 18.1, and all pre-existing tests).
  </acceptance_criteria>
  <done>
    All Phase 18 tests are migrated to the new DictationCoordinator init and the new AppSettings flags. Full test suite is green. Phase 18.1 is functionally complete; ready for `/gsd-verify-work`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (test-only) | This plan modifies test files; no production trust boundaries change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.1-06-01 | Tampering | Test files modified to add `saveDestinations:` parameter | accept | Test-only changes; no production code modified. The new parameter exposes the SaveDestinations API to tests, which is exactly what's needed for the new D-15/D-18/D-20 tests landing in Plans 02/04/05. `accepted_risk: test surface expansion is intentional.` |
| T-18.1-06-02 | Information Disclosure | Tests that touch NSPasteboard.general | mitigate | All clipboard-touching tests already use `PasteboardTestLock.shared` (Phase 18-06 cross-suite mutex). The `.serialized` trait + global lock ensures parallel test runs don't trample one another's clipboard state. No new clipboard surface introduced. |
| T-18.1-06-03 | Denial of Service | Stale tests that test deleted behavior remaining green | mitigate | Plan acceptance criterion REQUIRES `swift test --filter Phase18` to be green AND `grep -rE "dictationOutputMode|..."` to return nothing. Either the test exercises real, live code OR it's deleted. |
</threat_model>

<verification>
- `cd PSTranscribe && swift build` exits 0.
- `cd PSTranscribe && swift test --filter Phase18` exits 0.
- `cd PSTranscribe && swift test` exits 0.
- `grep -rE "dictationOutputMode|dictationFolderPath|DictationOutputMode\\." PSTranscribe/Tests/PSTranscribeTests/` returns nothing.
- For each file modified: `grep -B 2 -A 2 "DictationCoordinator(" <file>` shows the `saveDestinations:` argument.
</verification>

<success_criteria>
- All Phase 18 sibling tests are migrated to the new init signature (saveDestinations: parameter).
- All references to deleted AppSettings keys (dictationOutputMode, dictationFolderPath) and deleted enum (DictationOutputMode) are gone from the test directory.
- Full project test suite passes green.
- Phase 18.1 acceptance gate met: ready for `/gsd-verify-work`.
</success_criteria>

<output>
After completion, create `.planning/phases/18.1-shared-save-destinations-local-file/18.1-06-SUMMARY.md` with: list of files migrated, list of tests deleted (with rationale), full `swift test` summary, and a "ready for /gsd-verify-work" sign-off line.
</output>
