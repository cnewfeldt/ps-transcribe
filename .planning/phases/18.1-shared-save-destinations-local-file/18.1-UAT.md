---
status: diagnosed
phase: 18.1-shared-save-destinations-local-file
source:
  - 18.1-01-SUMMARY.md
  - 18.1-02-SUMMARY.md
  - 18.1-03-SUMMARY.md
  - 18.1-04-SUMMARY.md
  - 18.1-05-SUMMARY.md
  - 18.1-06-SUMMARY.md
started: 2026-04-29T23:45:00Z
updated: 2026-04-30T00:45:00Z
---

## Current Test

[testing paused — 1 item outstanding (test 7 blocked on test 6 dictation bug)]

## Tests

### 1. Cold Launch and Settings Open
expected: App launches clean from quit state. Settings window opens and renders without missing controls.
result: issue
reported: "(a) Choose-Folder button icon is not rendering correctly in the Obsidian section. (b) The NSOpenPanel that opens from that button has dialog/title text rendered with low contrast — barely legible against the panel background, making it hard to identify which folder picker is open. Both issues observed in the same Obsidian > Choose Folder flow."
severity: major

### 2. Settings — Three Top-Level Destination Sections Present
expected: Settings window shows, in order: Audio Input, Local File, Obsidian, Notion, Privacy, Updates, Speech Model, Dictation. The three destination sections (Local File, Obsidian, Notion) each have their own enable toggle and folder/database picker.
result: pass

### 3. Settings — Local File Section Defaults
expected: Local File section has an enable Toggle defaulting to ON, a root-folder picker defaulting to ~/Documents/PSTranscribe (tilde expanded), and helper text mentioning Meeting / Memo / Dictation subfolders that are created on first write.
result: pass

### 4. Settings — Obsidian Section is Single Folder
expected: Obsidian section has one enable Toggle, one folder picker (NOT separate "meetings folder" and "voice folder" pickers as in v1.0), and a vault-detection status dot. Selecting an Obsidian vault folder lights the dot green.
result: pass

### 5. Settings — Dictation Section is Behavior-Only
expected: Dictation section has hotkey recorder, hotkey mode picker, and clipboard-restore-delay stepper. There is NO output-mode picker (clipboard / plainFolder / both) and NO dictation folder picker. Helper text points users to Local File / Obsidian / Notion sections for storage configuration.
result: pass

### 6. Dictation Writes to Clipboard Even With Zero Destinations (D-15)
expected: In Settings, turn OFF all three destinations (Local File, Obsidian, Notion). Trigger a dictation session, speak a short phrase, end dictation. The transcribed text is on the clipboard (Cmd-V pastes it) regardless of destination state.
result: issue
reported: "nothing appears to be recording."
severity: major

### 7. Dictation D-19 Zero-Destinations Inline Transcript
expected: With all three destinations still off, after the dictation in test 6 completes: the Library shows a new entry whose row displays (or its detail pane reveals) the transcript text inline. No file was written to ~/Documents/PSTranscribe/Dictation/ for that session.
result: blocked
blocked_by: prior-phase
reason: "Depends on test 6 (dictation recording) succeeding; test 6 is open as a major issue ('nothing appears to be recording')."

### 8. Meeting/Voice Memo D-20 Guard
expected: With all three destinations still off, attempt to start a meeting or voice-memo session from the main window. The session is blocked — an error/warning surfaces ("no destination configured" or similar), and no recording begins. Re-enable Local File and the same start action now works.
result: pass

### 9. Local File Destination — Subfolder Routing
expected: Enable only Local File (Obsidian and Notion off). Set localFileRoot to a known empty folder (e.g., ~/Desktop/PSTranscribeTest). Record one of each session type: meeting, voice memo, dictation. Inspect the folder. Files appear under {root}/Meeting/, {root}/Memo/, and {root}/Dictation/ with appropriate filenames; subfolders were created lazily on first write.
result: issue
reported: "fail, added the local folder, but the error message remains. Assuming it wasn't cleared and the buttons won't work due to the last error"
severity: major

### 10. Obsidian Destination — Single Folder With Session-Type Frontmatter
expected: Enable only Obsidian (Local File and Notion off). Point obsidianFolderPath at an empty test folder. Record one meeting, one voice memo, one dictation. All three files land in the single configured Obsidian folder (no Meeting/Memo/Dictation subfolders). Each file's YAML frontmatter contains a `session-type:` field identifying which kind it was. Filenames include a label that distinguishes the three.
result: pass
note: "User confirmed test passes after restarting app to clear the test 9 lingering-error state."

### 11. Multi-Destination Fan-Out
expected: Enable BOTH Local File and Obsidian (Notion optional). Record a single meeting. After session ends, the meeting transcript exists at BOTH {localFileRoot}/Meeting/ AND the Obsidian folder. Both files contain the same transcript content. No errors surface in the UI.
result: pass

### 12. Per-Destination Failure is Non-Fatal
expected: Enable Local File (valid path) AND Obsidian, but set obsidianFolderPath to an invalid/non-existent path that should fail to write (e.g., /System/protected/path). Record one session. Local File write succeeds (file exists on disk), the session completes normally, and any Obsidian failure is surfaced as a non-blocking error (not a crash, not a session abort, not a lost transcript).
result: pass
note: "Variant exercised: user enabled Obsidian without setting a path. UI showed 'transcripts won't be saved to Obsidian until a folder is configured below'; recording proceeded with no error. The empty-path case is filtered by isAnyDestinationEnabled (WR-04 fix) and SaveDestinations.save's empty-path gate, so the Obsidian branch is skipped entirely. The non-fatal contract is satisfied for this variant. The strict invalid-path-write-fails scenario was not exercised; if needed for full coverage, retest with an obsidianFolderPath that exists at config time but fails to write at runtime (e.g., revoked permission)."

## Summary

total: 12
passed: 8
issues: 3
pending: 0
skipped: 0
blocked: 1

## Gaps

- truth: "Settings > Obsidian > Choose Folder button opens an NSOpenPanel whose title/prompt text is legible against the panel background"
  status: failed
  reason: "User reported: (a) Choose-Folder button icon is not rendering correctly in the Obsidian section. (b) The NSOpenPanel opened from that button has dialog/title text rendered with low contrast — barely legible against the panel background, making it hard to choose a folder. Both issues observed in the same Obsidian > Choose Folder flow."
  severity: major
  test: 1
  root_cause: "(b) chooseFolder(message:onSelect:) helper at SettingsView.swift:645-657 sets only panel.message; panel.title and panel.prompt are left as system defaults, so the descriptive text falls into NSOpenPanel's accessory-message slot rendered against translucent chrome with poor contrast. (a) is a misdiagnosis — there is NO icon on the Choose button (lines 178-184 are pure text 'Choose...' / 'Change...'). User likely reacted to the dim gray vault-detection Circle at line 144-146 (its color is intentional, lights green when a valid Obsidian vault is selected)."
  artifacts:
    - path: "PSTranscribe/Sources/PSTranscribe/Views/SettingsView.swift"
      issue: "chooseFolder helper at lines 645-657 sets only panel.message, leaving panel.title and panel.prompt as system defaults"
  missing:
    - "Set panel.title = message in chooseFolder helper"
    - "Set panel.prompt = \"Choose\" in chooseFolder helper"
  debug_session: ".planning/debug/18.1-uat-issue-1-choose-folder-ui.md"

- truth: "Triggering dictation captures audio AND the DictationHUD reflects the live recording state (gray idle dot → red listening indicator, elapsed timer, partial transcript caption, Stop button visible). Without a visible HUD, the user cannot tell that recording is happening."
  status: failed
  reason: "User reported: 'nothing appears to be recording.' Dictation appears not to record / nothing observable to the user — no clipboard write either, because the user never presses the hotkey again to end the session."
  severity: major
  test: 6
  root_cause: "DictationHUD is wired into the floating panel ONCE at app launch via DictationCoordinator.attach(windowController:) (DictationCoordinator.swift:117-129) using windowController.setContent(AnyView(DictationHUD(state: state, elapsed: elapsed, partialText: dictationStore.volatileYouText, onStop: ...))). The state/elapsed/partialText values are evaluated as plain Swift values at init time and frozen into DictationHUD's non-bindable `let` properties. setContent has only one caller in the codebase; show() only does orderFrontRegardless() — neither rebuilds the SwiftUI subtree. So when the hotkey fires, beginDictation runs, the audio engine actually starts, the coordinator's @Observable state transitions to .listening — but the visible HUD remains frozen at .idle (gray dot, '0:00', empty caption, Stop button hidden). User reasonably concludes nothing is recording, never presses hotkey again, endDictation never runs, no clipboard/library entry materializes. 208/208 tests pass because no test exercises the begin→live-HUD path. Pre-existing structural bug from plan 18-05 (commit c88998f), surfaced by 18.1 UAT but not introduced by it."
  artifacts:
    - path: "PSTranscribe/Sources/PSTranscribe/App/DictationCoordinator.swift"
      issue: "attach(windowController:) at lines 117-129 calls setContent(AnyView(DictationHUD(...))) once with snapshotted values, breaking Observation tracking"
    - path: "PSTranscribe/Sources/PSTranscribe/Views/DictationHUD.swift"
      issue: "Struct uses non-bindable `let` properties for state/elapsed/partialText, so SwiftUI cannot re-render when coordinator state changes"
  missing:
    - "Replace AnyView snapshot with a DictationHUDBinding wrapper view that takes the @Observable DictationCoordinator and re-reads coordinator.state / coordinator.elapsed / coordinator.dictationStore.volatileYouText inside its body so Observation tracks the mutations"
    - "Regression test that mounts the HUD wrapper and asserts it reflects state mutations after beginDictation"
  debug_session: ".planning/debug/18.1-uat-issue-2-dictation-no-recording.md"

- truth: "When the user enables a destination after seeing the D-20 zero-destinations error, the error message clears and start buttons re-engage. Recovery from D-20 error state is automatic on settings change."
  status: failed
  reason: "User reported: 'fail, added the local folder, but the error message remains. Assuming it wasn't cleared and the buttons won't work due to the last error.' Confirmed in test 10: 'I had to refresh the app, once there is an error, the application won't allow the user to record.' D-20 error (transcriptionEngine.lastError) is not cleared when settings change to make a destination valid; user must quit and relaunch the app to recover. Inert start buttons + stale error message persist until app restart."
  severity: major
  test: 9
  root_cause: "D-20 guard in ContentView.startSession (ContentView.swift:826-830) sets transcriptionEngine.lastError to a 'no destinations' string and returns early. The two start buttons in CaptureDock (capturePrimaryButton at CaptureDock.swift:147; voiceMemoButton guard at CaptureDock.swift:184) silently no-op when !hasError is false; hasError is true whenever lastError != nil (TranscriptionEngine.swift:40, 46). NO observer in the project listens for a destination toggle and clears lastError — existing .onChange(of:) calls in ContentView (lines 289, 410, 432, 437, 440, 515) cover unrelated state, and @Observable settings don't trigger side effects on their own. Every existing `lastError = nil` assignment (TranscriptionEngine 148/419, ContentView 869) is bound to a session-start or permission flow, all of which the user can't reach because the buttons are inert. This is a missing wire in plan 05's D-20 implementation, not a regression."
  artifacts:
    - path: "PSTranscribe/Sources/PSTranscribe/Views/ContentView.swift"
      issue: "D-20 guard at lines 826-830 sets lastError without registering any observer to clear it when isAnyDestinationEnabled flips to true"
    - path: "PSTranscribe/Sources/PSTranscribe/Audio/TranscriptionEngine.swift"
      issue: "hasError computed property (lines 40, 46) exposes lastError != nil; no auto-clear when destinations become valid"
    - path: "PSTranscribe/Sources/PSTranscribe/Views/CaptureDock.swift"
      issue: "capturePrimaryButton (line 147) and voiceMemoButton (line 184) silently no-op on hasError, so user has no way to invoke the existing lastError = nil reset paths"
  missing:
    - "Extract the D-20 error message into a named constant (e.g., DestinationGuardErrors.noDestinationsConfigured) so the auto-clear can match it precisely without false positives"
    - "Add .onChange(of: saveDestinations.isAnyDestinationEnabled) modifier in ContentView's modifier stack near line 432 that, when the new value is true, clears transcriptionEngine.lastError ONLY IF lastError currently equals (or starts with) the D-20 message constant — must NOT broaden the button-disable predicate or unconditionally clear lastError, which would mask unrelated errors like microphone permission failures"
    - "Regression test asserting that toggling a destination on after D-20 fires clears lastError and re-enables the start buttons without an app restart"
  debug_session: ".planning/debug/18.1-uat-issue-3-stale-d20-error.md"
